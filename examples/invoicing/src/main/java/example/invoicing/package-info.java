/**
 * 別プロジェクトが公開した Souther モジュールを import する側。中身は
 * {@code src/main/souther/invoicing.sou} が宣言している。
 *
 * <p>このファイルがあるのは sharedmoney と同じ理由で、javac はコンパイルする Java ソースが1つも
 * 無いとアノテーションプロセッサを動かさない。
 */
package example.invoicing;
