/**
 * The types ({@code Issue} / {@code Label} / {@code LabelSet} / {@code LabelCounts} / …) and behaviors
 * ({@code normalizeLabels} / {@code addLabel} / {@code removeLabel} / {@code sharedLabels} /
 * {@code assigneeOf} / {@code countByLabel}) in this package are generated at compile time by
 * {@code SoutherProcessor} from {@code src/main/souther/tags.sou}. This {@code package-info} is the one
 * minimal source that makes javac run annotation processing (it will not without at least one source).
 */
package example.tagging;
