# In-memory H2. DB_CLOSE_DELAY=-1 keeps the database while the JVM lives.
spring.datasource.url=jdbc:h2:mem:issuetracker;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=

# Run schema.sql (DDL) and data.sql (the seeded issues) at startup.
spring.sql.init.mode=always
