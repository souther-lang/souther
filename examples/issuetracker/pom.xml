<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>example.souther</groupId>
        <artifactId>souther-examples</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>issuetracker</artifactId>

    <description>
        A small issue tracker, and the Kotlin case: the domain is .sou, and the boundary around it —
        REST and the H2 connection — is Spring Boot + Kotlin. issues.sou shows Set as a data field, Map
        as a behavior output, Some(Assignee(name)) destructuring, and three injected database behaviors
        whose sequencing is checked at compile time with `fake` + `example`. The Kotlin boundary drives
        the generated types: an exhaustive `when` over a generated sealed output union, raoh's Result
        folded by an extension function, and a bound behavior called through `operator invoke`. Like
        ordering it actually starts Boot and connects to H2.
    </description>

    <properties>
        <!-- Declared here rather than taken from spring-boot-dependencies: a property from an imported
             BOM does not reach a plugin version, and the kotlin-maven-plugin needs one. The value is
             what Boot 4.0.7 pins, and setting it also overrides the BOM, so the compiler, the stdlib
             and the compiler plugins stay on one version (this is what a start.spring.io Kotlin pom
             does). -->
        <kotlin.version>2.2.21</kotlin.version>
        <kotlin.code.style>official</kotlin.code.style>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>4.0.7</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- The Souther generated code's runtime (decoder/encoder). -->
        <dependency><groupId>org.souther-lang</groupId><artifactId>souther-runtime</artifactId></dependency>
        <dependency><groupId>net.unit8.raoh</groupId><artifactId>raoh</artifactId></dependency>

        <!-- Kotlin. reflect is what Spring reads constructor parameter names and nullability through.
             There is no jackson-module-kotlin: no request or response shape is a Kotlin data class —
             every one of them is a Souther data, decoded and encoded by the derived codec. -->
        <dependency><groupId>org.jetbrains.kotlin</groupId><artifactId>kotlin-stdlib</artifactId></dependency>
        <dependency><groupId>org.jetbrains.kotlin</groupId><artifactId>kotlin-reflect</artifactId></dependency>

        <!-- The boundary that actually runs: Boot's standard starters. DataSource / DSLContext /
             TransactionManager and schema.sql execution are all left to autoconfig. -->
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-jooq</artifactId></dependency>
        <dependency><groupId>com.h2database</groupId><artifactId>h2</artifactId><scope>runtime</scope></dependency>

        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Declared only so `mvn spring-boot:run` resolves a pinned version. No repackage
                 execution, so the module's artifact stays a plain jar like the other examples'. -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>4.0.7</version>
            </plugin>

            <!-- The build order is what makes a Kotlin module work with a javac annotation processor.
                 The parent declares maven-compiler-plugin, so its default-compile (javac +
                 SoutherProcessor over the one package-info.java) runs first within the `compile`
                 phase and lands the generated classes in target/classes. kotlin-maven-plugin is
                 declared here, so it runs after it in the same phase, and target/classes is on its
                 compile classpath — which is how Kotlin gets to reference the generated types. This
                 is the reverse of the usual mixed Kotlin/Java setup (where kotlinc is pulled forward
                 to process-sources): here nothing on the Java side depends on Kotlin, and everything
                 on the Kotlin side depends on generated bytecode. -->
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <configuration>
                    <!-- kotlinc still defaults to a 1.8 target, which cannot inline the stdlib's own
                         17-targeted bytecode. 21 is Souther's runtime floor (the generated classes and
                         souther-runtime target it), so the Kotlin boundary asks no more of a JVM than
                         the generated code it drives. -->
                    <jvmTarget>21</jvmTarget>
                    <args>
                        <!-- Read the JSR-305 nullability annotations Spring carries as strict Kotlin
                             types rather than platform types. -->
                        <arg>-Xjsr305=strict</arg>
                    </args>
                    <compilerPlugins>
                        <!-- Kotlin classes are final by default, and Spring needs @Configuration and
                             @Transactional classes open to proxy them. The spring plugin opens exactly
                             those. -->
                        <plugin>spring</plugin>
                    </compilerPlugins>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-allopen</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>compile</id>
                        <phase>compile</phase>
                        <goals><goal>compile</goal></goals>
                        <configuration>
                            <sourceDirs>
                                <sourceDir>${project.basedir}/src/main/kotlin</sourceDir>
                            </sourceDirs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>test-compile</id>
                        <phase>test-compile</phase>
                        <goals><goal>test-compile</goal></goals>
                        <configuration>
                            <sourceDirs>
                                <sourceDir>${project.basedir}/src/test/kotlin</sourceDir>
                            </sourceDirs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
