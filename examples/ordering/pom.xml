<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>example.souther</groupId>
        <artifactId>souther-examples</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>ordering</artifactId>

    <description>
        The checkout example, and the multi-module case: two Souther modules (example.cart and
        example.ordering) live under src/main/souther and are compiled together in one build, with
        ordering importing cart's PricedCart. cart.sou prices a cart purely with list combinators
        (quote). ordering.sou records and reserves over the DB (place), and shows `fake` + `example`:
        place's record-then-reserve sequencing is checked at compile time with the injected behaviors
        faked, no database involved. The two modules are composed at the Java boundary (quote, then
        place for a priced cart) — each keeps its own single-module sealed output. The generated types
        are used from Spring Boot + jOOQ boundary code: it actually starts Boot, connects to H2, and
        shows transaction control. When a later line of the order is out of stock, the recorded rows
        and the earlier lines' stock decrements all roll back (programmatic setRollbackOnly). A failure
        is a case; the rollback is the boundary's decision, not an exception. Unlike the other examples
        this one is not `provided` — it runs.
    </description>

    <!-- この例は「実行する」ので、Spring Boot 4 系（Spring 7）を実バージョンで解決する。
         初回ビルドは Boot のスターターを取りに行くため、他の例と違いネットワークが要る
         （2 回目以降は ~/.m2 に載るので -o でも回る）。Spring のバージョンは
         spring-boot-dependencies の BOM に一任する。core/beans/web/webmvc など全モジュールが
         この BOM 一枚で 7.0.8 に揃うので、モジュール間でパッチが混在しない。 -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>4.0.7</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Souther 生成物のランタイム（decoder/encoder。jOOQ Record 源は raoh-jooq） -->
        <dependency><groupId>org.souther-lang</groupId><artifactId>souther-runtime</artifactId></dependency>
        <dependency><groupId>net.unit8.raoh</groupId><artifactId>raoh</artifactId></dependency>
        <dependency><groupId>net.unit8.raoh</groupId><artifactId>raoh-jooq</artifactId></dependency>

        <!-- 実行する境界：Spring Boot の標準スターター。DataSource / DSLContext / TransactionManager /
             schema.sql 実行はすべて autoconfig に任せる（member と違い実際に起動・接続するため）。 -->
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-jooq</artifactId></dependency>
        <dependency><groupId>com.h2database</groupId><artifactId>h2</artifactId><scope>runtime</scope></dependency>

        <!-- テスト：@SpringBootTest で実際に Boot を起動し、MockMvc で本物の HTTP を通す -->
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    </dependencies>
</project>
