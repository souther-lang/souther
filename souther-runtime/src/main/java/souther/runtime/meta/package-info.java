/**
 * What a compiled module carries about itself, for a reader that has the jar and no {@code .sou}.
 * Every element of these annotations is written by the compiler and is always present, so nothing
 * here is null; the package says so with {@code @NullMarked}.
 */
@NullMarked
package souther.runtime.meta;

import org.jspecify.annotations.NullMarked;
