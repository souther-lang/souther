# Architecture Decision Records

This directory records the decisions behind Souther's design — the reasoning, the
alternatives weighed, and the prior art consulted. The language specification
([`../../specification.adoc`](../../specification.adoc)) states *what* the language does;
these ADRs state *why*.

An ADR records a decision that is architecturally **significant**: one that is expensive
to reverse, or that constrains the decisions that come after it. A rule the specification
can simply state, a standard-library signature, or an implementation caught up with a rule
already stated is not one — those live in the specification and in the pull request that
made the change.

Referencing is one-directional: each ADR points back to the spec sections whose
rationale it holds, but the specification itself does not link to ADRs — it states
the rules and examples, nothing more.

Format: [Nygard-style](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions)
(Status / Context / Decision / Consequences). ADRs are written in English; the
specification is in Japanese.

| ADR | Decision | Spec |
| --- | --- | --- |
| [0001](0001-one-to-one-with-spec-dsl.md) | The implementation model maps one-to-one onto the spec DSL | `[#purpose]`, `[#design-principles]`, `[#behavior]`, `[#non-functional]`, `[#core-semantics]` |
| [0002](0002-closed-construction-paths.md) | Close data construction paths; permission lives on the behavior | `[#closed-construction]`, `[#constructs]`, `[#java-base-class]` |
| [0003](0003-invariant-violations-abort-in-domain.md) | Invariant violations abort; preconditions are business cases, not contracts | `[#invariant-on-construct]`, `[#algebraic-types]`, `[#violation-destination]`, `[#out-of-scope]`, `[#stdlib-int]`, `[#jvm-abort]` |
| [0004](0004-derive-codecs-delegate-parsing-to-raoh.md) | Derive decoders/encoders from data shape; delegate external representation to Raoh | `[#separate-representation]`, `[#external-representation]`, `[#decoder]`, `[#encoder]` |
| [0005](0005-behavior-as-membership-free-composition-unit.md) | behavior is a membership-free composition unit; spec and implementation are separate | `[#behavior-composition-unit]`, `[#behavior]` |
| [0006](0006-outside-world-via-missing-implementation.md) | Outside-world dependencies are behaviors with no implementation, injected from Java | `[#no-impl-for-outside]`, `[#injected-behavior]` |
| [0007](0007-unmarked-sum-output-and-railway.md) | Business results are an unmarked sum; the off-ramp is decided by composition | `[#unmarked-sum]`, `[#unmarked-output]`, `[#composition]` |
| [0008](0008-asymmetric-java-interop.md) | Java interop is asymmetric | `[#asymmetric-interop]`, `[#out-of-scope]` |
| [0009](0009-decimal-ignores-scale.md) | Decimal does not include scale in identity | `[#primitives]` |
| [0010](0010-polymorphism-limited-to-stdlib.md) | Polymorphism is limited to stdlib types; no user-written type parameters, no generic `data` or `behavior`, no polymorphic values (narrowed by ADR-0092) | `[#algebraic-types]` |
| [0011](0011-option-never-unit-not-surface-types.md) | Option, Never, and Unit are not surface-writable types. Revised for `Option` by ADR-0078 — the `Never` and `Unit` decisions stand | `[#algebraic-types]`, `[#optional]` |
| [0012](0012-nominal-include-no-intersection-types.md) | Field composition is nominal `include`; no structural intersection types | `[#field-spread]`, `[#union-intersection]` |
| [0013](0013-sum-cases-are-named-data-references.md) | Sum-data cases are references to already-declared named data | `[#sum-data]` |
| [0014](0014-explicit-newtype-syntax.md) | newtype is declared explicitly by `data X = Y`, not inferred from shape | `[#newtype]` |
| [0015](0015-cross-module-field-read-construction-closed.md) | Field reads may cross module boundaries; only construction is closed | `[#field-visibility]`, `[#jvm-product]` |
| [0016](0016-requirements-as-arguments-not-effects.md) | The requirement set is injected constructor arguments, not an effect type | `[#blocks]`, `[#depends-on]`, `[#requirement-propagation]`, `[#core-semantics]` |
| [0017](0017-declare-dont-infer-permissions-and-requirements.md) | Declare, don't infer: requirements and composed output; construction permission is optional | `[#constructs]`, `[#depends-on]`, `[#declared-composition-output]` |
| [0018](0018-no-dedicated-update-syntax.md) | No dedicated update syntax; use a named record literal with spread | `[#record-literal]` |
| [0019](0019-one-arrow-for-types-and-terms.md) | One arrow `->` for types and terms (lambdas, match); `=` binds names | `[#delimiters]`, `[#fn-declaration]`, `[#match]` |
| [0020](0020-guard-desugars-to-if.md) | `guard ... else` is sugar for `if` | `[#guard]` |
| [0021](0021-compiler-layers-core-ir.md) | Compiler layers — a Core IR between type-checking and code generation | `[#compiler-pipeline]`, `[#ast-tracking]` |
| [0022](0022-pin-classfile-version-to-java-21.md) | Pin the generated class-file version rather than let it follow the build JDK (set at Java 21; moved to 25 by ADR-0062) | `[#target-jdk]` |
| [0023](0023-capitalize-behavior-class-names.md) | Capitalize generated behavior class names; collisions with data are errors | `[#jvm-behavior]` |
| [0024](0024-exposed-composition-output-in-exposing.md) | An exposed composition declares its output signature in the exposing list | `[#modules]`, `[#declared-composition-output]`, `[#jvm-anonymous-union]` |
| [0025](0025-first-class-functions.md) | First-class functions: inline when they can't escape, close over the runtime `Fn` when they must | `[#blocks]`, `[#fn-declaration]`, `[#stdlib-list]` |
| [0026](0026-signatures-colon-definitions-equals.md) | Signatures use `:`, definitions use `=`; a function is defined with `let` | `[#delimiters]`, `[#behavior]`, `[#fn]` |
| [0027](0027-match-with-pipe-cases.md) | match reads `match e with \| case -> ...` (F# form) | `[#match]` |
| [0028](0028-stdlib-privileged-core-over-intrinsic-kernel.md) | The stdlib is a privileged core written in Souther over an intrinsic kernel | `[#stdlib]`, `[#primitives]` |
| [0029](0029-platform-failures-are-exceptions-not-cases.md) | Platform failures propagate as exceptions; only domain outcomes are cases | `[#java-impl-rules]`, `[#out-of-scope]` |
| [0030](0030-spread-not-include-keyword.md) | Record composition and update use a `...` spread, not an `include` keyword | `[#product-data]`, `[#field-spread]`, `[#record-literal]` |
| [0031](0031-invariant-follows-the-type-body.md) | A data's `invariant` clause follows the type body, not the record's braces | `[#invariant]`, `[#product-data]` |
| [0032](0032-newtype-constructor-parenthesized.md) | A newtype is constructed by applying its name, parenthesized: `Amount(500)` | `[#newtype]`, `[#violation-destination]` |
| [0033](0033-numeric-literals-and-arithmetic-operators.md) | Decimal literals carry an `m` suffix; `+ - * /` work on Int and Decimal | `[#primitives]`, `[#stdlib-int]`, `[#stdlib-decimal]` |
| [0034](0034-value-pipe-and-argument-order.md) | The value pipe `\|>` threads a value through calls; the stdlib takes its main object last | `[#pipe]`, `[#stdlib]`, `[#delimiters]` |
| [0035](0035-match-destructures-fields-as-binds-the-whole.md) | A match case destructures a case's fields; `as` binds the whole; Option binds positionally (`\| Some v`) | `[#match]`, `[#algebraic-types]` |
| [0036](0036-tuples-and-exposing-parentheses.md) | Tuples `(a, b)` are expression-level first-class values; a module's `exposing`/`import` lists use parentheses | `[#tuple]`, `[#modules]`, `[#delimiters]` |
| [0037](0037-tuple-types-in-signatures.md) | Tuple types are writable in helper/stdlib signatures, not at a codec boundary | `[#tuple]`, `[#stdlib-map]` |
| [0038](0038-user-recursion-over-self-referential-data.md) | User helpers may recurse over self-referential data; a recursive helper is lowered to a method | `[#fn-declaration]`, `[#algebraic-types]` |
| [0039](0039-set-collection-type.md) | Set is a collection type; its external representation is a deduplicated array | `[#collections]`, `[#stdlib-set]` |
| [0040](0040-typed-map-keys.md) | A Map key may be a String-backed newtype, not only String | `[#collections]`, `[#stdlib-map]` |
| [0041](0041-map-higher-order-operations.md) | Map's fold/map/update self-host over List.fold; update is a value step, not an Option step | `[#stdlib-map]` |
| [0042](0042-defer-rrb-and-transient-fold-accumulation.md) | Defer RRB concatenation and a transient fold accumulator; the persistent-collection performance backlog is closed | — |
| [0043](0043-optional-module-header-for-single-file.md) | A single-file compilation unit may omit the `module` header (named after the file, or `Main`); an imported/multi-file module must declare it | `[#modules]` |
| [0044](0044-diagnostics-as-data-with-renderer-layer.md) | Diagnostics are data rendered Elm-style or as JSON by a locale-aware layer (Japanese default); the code and source region are the stable identity | `[#compile-errors]`, `[#non-functional]` |
| [0045](0045-string-append-and-concat-elm-aligned.md) | `++` is Elm's appendable (two lists or two strings); `String.append(a, b)` joins two strings and `String.concat(xs)` flattens a `List<String>` | `[#stdlib-string]`, `[#stdlib-list]` |
| [0046](0046-example-compile-time-checked-in-language-tests.md) | `example` rows attached to a behavior are checked at compile time (a mismatch is `E1905`); inline or in an attached `examples for` file; the pure/total model is what lets the compiler evaluate them | `[#examples]`, `[#compile-errors]` |
| [0047](0047-newtype-compared-by-its-wrapped-value.md) | A single-value newtype is compared by its wrapped value (no `.value`): same newtype or a bare literal, never a different newtype or a non-literal; ordering needs an ordered base; arithmetic deferred | `[#newtype-comparison]`, `[#newtype]` |
| [0048](0048-fake-test-doubles-for-injected-dependencies.md) | An example fakes a behavior's `depends on`: `with dep = value` (value dependency) or a `fake dep \| table` (function dependency, value-equality lookup); a `Behavior` proxy at evaluation, no class shipped; local-only | `[#example-fakes]`, `[#compile-errors]` |
| [0049](0049-behavior-interface-hides-result-union.md) | A fn/`>->` behavior is a public interface (`$Impl` holds the body, `of()`/`bind()` build it) so Java declares it by name and never spells the result union; the union suffix is `Result`; an injected behavior stays an abstract class | `[#jvm-behavior]`, `[#jvm-anonymous-union]` |
| [0050](0050-infer-non-recursive-helper-value-parameters.md) | (Superseded by 0066) A non-recursive helper's value parameter types are inferred from its call sites; a function parameter and a recursive helper still declare theirs | `[#fn-declaration]` |
| [0051](0051-fold-is-a-recursive-helper.md) | `fold` is a recursive helper (`foldFrom`) over `List.get`, not a privileged loop: its step is a first-class closure, its tail self-call is compiled to a loop, and a recursive helper may take a pure function parameter | `[#stdlib]`, `[#blocks]`, `[#fn-declaration]` |
| [0052](0052-recursion-total-by-default.md) | Recursion is total by default, checked by size-change termination over the structural sub-term order (covers self, mutual, and lexicographic recursion); `partial` opts out (a `partial` member opts its whole mutual group out); numeric/index recursion must be `partial` (no inductive `Nat`); stdlib `foldFrom` is trusted total; example evaluation is time-bounded | `[#fn-declaration]`, `[#examples]` |
| [0053](0053-stdlib-implementation-location-policy.md) | Every stdlib function is declared in a core module (self-hosted or `intrinsic` kernel), not a hardcoded compiler table; the compiler-integrated set is closed to operators, ordered-constrained generics, and primitive-headed-union returns; `Int`/`Decimal` `add`/`subtract`/`multiply`/`compare` move to `souther.int`/`souther.decimal`, and `Int.modBy` (Elm floored modulo) is added | `[#intrinsics]`, `[#stdlib-int]`, `[#stdlib-decimal]` |
| [0054](0054-newtype-constructor-destructuring-pattern.md) | A newtype case is opened in `match` with the constructor form `X(v)`, nestable as `X(Y(s))` to reach through a newtype-over-newtype in one pattern (F#/Elm parity, name-checked per layer); `.value` stays as the single-layer accessor (Kotlin value-class positioning); pure sugar lowered to the existing `LetIn`+`FieldAccess` chain, no new Core node | `[#match]`, `[#newtype]` |
| [0055](0055-bare-field-getter.md) | A bare `.field` is sugar for the getter `(x) -> x.field` (Elm notation, Scala `_.field` meaning), lowered at parse time to a second-class block; single-layer, works for any field, not a first-class value | `[#blocks]`, `[#pipe]` |
| [0056](0056-injected-behaviors-are-multi-argument.md) | Injected behaviors may take several inputs (injection is orthogonal to composition): 0/1-input stays the unary `Behavior<I,O>` `>->` stage, 2+ inputs is a standalone base with a typed `apply(A,B,…)` called via `invokevirtual`; collection params keep their `java.util` type, not `Object` | `[#java-base-class]`, `[#jvm-behavior]`, `[#example-fakes]` |
| [0057](0057-behavior-output-unions-are-module-local.md) | A behavior's output union is built from its own module's cases (E1606): a case class's union memberships are settled when its own module is generated, so an imported case cannot join a union declared here — the JVM's same-package `permits` rule was not the reason; translate the imported case into one of this module | `[#jvm-anonymous-union]`, `[#e1606]` |
| [0058](0058-types-are-reachable-through-their-module.md) | A type is identified by its declaring module plus its name, and may be named through it (`example.pricing.Amount`, or an `import ... as` alias) with no `import` required — the standard library's rule extended to user modules; a name written in a declaration resolves in the module that wrote it; a cycle counts however it is written; `exposing` does not re-export an imported name (Java re-exports readability, not names; F#/OCaml/Scala re-export via a transparent alias, which Souther has no form for) | `[#qualified-reference]`, `[#imports]` |
| [0059](0059-construction-is-not-closed-to-the-declaring-module.md) | Construction is closed to declared paths, not to the declaring module: `constructs T` may name another module's exposed type, built through the checked entry (public for an exposed data) so the invariant still runs; Souther guards construction by declaration rather than by constructor visibility, and borrowing the visibility mechanism had added a rule no ADR stated | `[#closed-construction]`, `[#jvm-construction-privacy]`, `[#newtype-arithmetic]` |
| [0060](0060-runtime-claimed-tail-append.md) | A tail array is 32 wide and a slot belongs to whoever claims it, so `append` extends in place instead of copying the tail (144 → 66 B/element for `acc ++ [x]`); the claim is confined to one thread, which keeps the JLS final-field guarantee a compare-and-set would have given up; amends ADR-0042 with the workload it asked for, without touching the compiler or ADR-0051's rule that nothing keys on `fold` | `[#stdlib-list]` |
| [0061](0061-champ-bulk-builder-not-a-transient.md) | CHAMP gets a bulk builder, not a transient: an insert rewrites the bitmaps older versions read, so unlike the vector's tail there is no region a claim could protect and no runtime check can make in-place writing safe; mutation is confined to a single-use from-empty builder behind `from`/`fromList`/`mapKeys`/the set algebra (406 → 167 B/entry), ownership is a flag threaded down the call rather than a mark on each node so the persistent `assoc` path costs nothing, and the fold-accumulated combinators stay ADR-0042's deferred compiler-side problem | `[#stdlib-map]`, `[#stdlib-set]` |
| [0062](0062-target-java-25.md) | The floor is Java 25 for generated classes, souther-runtime and every module: Raoh is a class-file 69 artifact that every derived decoder references, so the Java 21 the spec promised was never reachable and nothing tested it; the explicit pin from ADR-0022 stands, only its value moves, and souther-runtime stops being held a language version behind the compiler | `[#target-jdk]`, `[#non-functional]` |
| [0063](0063-a-compiled-module-carries-its-declarations.md) | A compiled module carries what it declares — its `data`, its behavior signatures, its `exposing` line — as the source that declared it, on the classes it generated, so another project imports it from a jar with no `.sou` of its own; the declarations travel as text because the surface is recursive and an annotation type may not have an element of its own type, and no statically typed language on the JVM or CLR ships source; one class path serves both the declarations an import resolves against and the classes an example runs, and a `BOUNDARY_VERSION` independent of the release records what an importing module reaches | `[#published-modules]`, `[#imports]` |
| [0064](0064-irrefutable-patterns-bind-anywhere.md) | An irrefutable pattern — a name, a tuple, a newtype opened by its constructor, a record's fields — binds wherever a name binds (a block's `let`, a helper parameter, a lambda parameter); a refutable one (a sum's case, `Some`) stays in `match`, the arm that catches what fails. Refutability is read off the resolved name, and outside `match` the name a constructor pattern writes is held against the value's own type, which no exhaustiveness pass establishes there. Transparent access to the wrapped value was rejected: it is one rule only until `List.map` changes the element type. Supersedes ADR-0036's lambda-parameter consequence | `[#blocks]`, `[#tuple]`, `[#match]`, `[#fn-declaration]` |
| [0065](0065-a-data-is-a-java-record.md) | A data and a unit class are Java records on the JVM (`java.lang.Record`, a `Record` attribute, a record-form `toString`, an accessor on every data, a non-public canonical constructor): Kotlin then reads `issue.id.value` as properties instead of resolving the backing field and reporting that it is package-private, and javac deconstructs a value — including a sum's case in an exhaustive `switch` — in a record pattern. Making the field private only changes the message to `it is private`; measured, along with the cost accepted: a record's canonical constructor is a creator Jackson finds, so a framework can build a value without the invariant `__construct` runs and serialize under component names rather than the declared `encoder`. Nullness moves to `@NonNull` per accessor return, the one place Kotlin does not apply the class's `@NullMarked`; a field may not be named after a no-argument method of `Object` | `[#jvm-product]`, `[#jvm-sum]`, `[#jvm-nullness]`, `[#field-visibility]` |
| [0066](0066-a-helper-is-typed-by-its-body.md) | A helper's parameter types come from the helper — written beside the parameter, or determined by what the body asks of it at any position it stands in, with a signature's type variables solved at the call they are written in and a closure read in a scope its own parameters are in force over — and never from its call sites; a parameter the body leaves open is annotated, because a type variable is written only in core and call sites are not read, so no type is left for it to be given (amended by ADR-0092) (Elm and F# both type the body and generalise; call-site inference stood in for that). A function parameter and a recursive helper still declare their types. Reading a helper is enough to know what it means, which is what a per-definition question needs; measured migration cost across the prelude and every example: 0 lines | `[#fn-declaration]`, `[#binding-patterns]` |
| [0067](0067-a-name-is-resolved-once.md) | A written name is resolved once, into the tree: `Ast` carries the resolved name and its home rather than text, so no check compares spellings and `resolveIn`/`home` disappear. Retires the #101 / #113 / #124 / #132 / #154 class by construction; one grammar production for a qualified name replaces six | `[#imports]`, `[#qualified-reference]` |
| [0068](0068-a-behavior-is-called-when-it-requires-nothing.md) | What a name may be used for is decided by what it is, and is the same in the module that declares it and in one that imports it. For a behavior the deciding question is the requirement set, not whether an implementation is written: one that requires nothing is called by name, one that requires something is named in `depends on` and called through it. E1607 retires "an implemented behavior is composed, not injected"; behaviors gain a no-recursion rule (E1608). Nothing is added to inference or to what an edit costs | `[#modules]`, `[#depends-on]`, `[#calling-a-behavior]`, `[#requirement-propagation]` |
| [0069](0069-a-property-of-every-case-is-a-property-of-the-sum.md) | What holds of every case is a property of the sum. A field every case spreads is read on the sum itself, and later (issue #237) spread from it into a construction (the sharing is nominal — a name two cases happen to agree on is the structural reading ADR-0012 declined — and the sealed interface declares the accessor each case record already has); a sum every one of whose cases is a unit data is an enumeration, travelling as its case's name rather than as an object holding a discriminator alone, which is the form serde/Jackson/OpenAPI give a fieldless enum and what lets it key a boundary `Map` without excepting ADR-0040; and it is ordered by its declaration (F#, Haskell, Rust, Java all give this; Elm alone withholds it), with the order on the sum rather than on the case values, since one unit data may be a case of two sums. Map iteration order is untouched | `[#sum-data]`, `[#sum-discrimination]`, `[#collections]`, `[#primitives]` |
| [0070](0070-a-construction-may-be-attempted.md) | A construction may be attempted — `guard T(v) as x else <case>` and the `if T(v) as x then … else …` it desugars to — and the type's own invariant decides the branch, so the rule is stated once instead of restated in a guard beside it. Nothing carries the failure: no `Option`, no `Result`, no reason, and the name a business failure gets is the one written in `else`. An attempt needs `constructs T` and a type that carries an invariant | `[#attempted-construction]`, `[#guard]`, `[#invariant-discharge]` |
| [0071](0071-guard-and-depends-on.md) | The departure statement is `guard` and the dependency clause is `depends on`. `require` and `requires` differed by one character while naming a branch and a wiring, and both are the precondition-contract word of Eiffel and Dafny, which ADR-0003 decided Souther does not do. `guard … else` is Swift's, down to the mandatory `else` and the binder scoped to the rest of the body; `depends on` names what the section is titled after. `needs` was dropped because a real model uses it as a field name, `using` because it becomes a synonym of the `with` that supplies these dependencies, `uses` because the clause may only name a behavior whose own requirement set is not empty. `on` is not reserved — it is read after `depends` as `for` is after `examples` | `[#reserved-words]`, `[#guard]`, `[#depends-on]` |
| [0072](0072-a-let-with-no-parameter-list-is-a-value.md) | A `let` written with a parameter list defines a function; one written without defines a value, which is substituted at each reference rather than held as module state. This is the split F# and Elm draw, and nothing had decided against it — the parentheses were simply made mandatory. A lambda on the right of `=` is the parameter-list form, so the two spellings define one thing. A module's `let` may not be named like a data it declares: a data is written where a value goes, and Souther cannot use capitalization to tell them apart. An `example` row may name a value and spread one; what a row may name is a property of the value graph, not of one definition's text | `[#fn-declaration]`, `[#fn-value-semantics]`, `[#fn-rules]`, `[#example-evaluable]` |
| [0074](0074-a-value-is-part-of-a-modules-published-surface.md) | `exposing` may name a value. A limit a rule is written against, and the representative record its examples are stated with, are part of what a module offers; a helper is not, because it takes arguments and a function does not cross into another module as a value. A published value is substituted where it is named, so publishing one adds no class, field or method — what travels is the declaration, as it already does for a data invariant. Its names are resolved where it was written, so the reader imports the value and not what it is made of. It may not build a type its module keeps to itself | `[#exposed-values]`, `[#exposed-surface]` |
| [0075](0075-a-module-publishes-its-helpers.md) | `exposing` may name a helper, so a rule is written once and shared instead of restated or declared a behavior the specification does not name. A published helper crosses closed and is expanded at the reader's call sites; a recursive one cannot be closed over, so it travels with the body that calls it — with everything it reaches in turn, a mutually-recursive group whole — under the name of the module that declares it, and the importer emits it on its own package-private `$Fns`, as it already does for a recursive standard-library helper. The declaring module is the helper's identity and not where its method goes, so no Java-reachable construction entry appears and two modules may declare a recursive helper of one name. What a published body builds is not the reader's to declare: publishing states that origination, and a construction records the module that carried it — preservation, not a grant, so a third module's type stays the reader's to declare. A published helper's argument and return types join the exposed surface. ADR-0074's refusal of a value reaching a recursive helper is withdrawn. A carried body is under the boundary version, since the importing compiler reads it | `[#exposed-values]`, `[#exposed-surface]`, `[#fn-rules]` |
| [0076](0076-a-function-type-is-a-type.md) | A function type is an ordinary type term, writable wherever a type may be written, with `->` right-associative so a function may take one and return one. Being writable in a position is not being usable in it: what a position admits is asked of the type rather than of the syntax that spelled it, through three separate predicates — boundary-representable, where a codec is derived or crossed; equality, asked of what elaboration decided rather than only of what was written; and ordering. Asking the type is what finds a function nested inside `List<(Int) -> Bool>`, which no syntactic rule reaches. That a function is the only type failing all three is a fact about the types there are now and not a claim that they are one question | `[#delimiters]`, `[#blocks]`, `[#fn-declaration]`, `[#equality]` |
| [0077](0077-an-example-row-may-call-a-helper.md) | An `example` row may apply a helper — the rule the row is about — wherever a fixture value is written, so a row states the rule instead of restating what it computes as a literal. The helper is run and the value it returns is re-materialised into the form a fixture is written in, field by field through the declared types: one path for an application at the top of a position and one nested inside a record, and no runtime value reaching the fixture around it. Re-materialising through the derived encoder was measured and refused — a `Date`'s neutral form is the parsed temporal while its encoder writes ISO text. Emitting a method for a non-recursive helper is what a row needs and its only reason; the closure is free, because a helper is emitted from its inlined body and a recursive one it reaches is a method already. A published helper's method is the reader's, as ADR-0075 already decided, since `$Fns` is package-private everywhere. The row carries one time budget for everything it runs. ADR-0072's refusal of a value whose body computes is withdrawn | `[#example-evaluable]`, `[#example-fakes]`, `[#e1903]`, `[#e1908]` |
| [0078](0078-an-optional-is-made-in-one-place-and-named-anywhere-it-is-read.md) | `Option<T>` may be written wherever a model reads an optional — and for a recursive helper's return type and a function-typed parameter it is the only way to say it, since those must be annotated, so refusing the name would remove programs rather than a spelling. What is refused is enforced on the type instead of on a spelling: making one is the `?` field being given its value and nothing else, which the check carries rather than the expected type — ADR-0011's own reasoning rested on a field being the only thing that could ask for an optional, and naming the type is another way to ask, which is how `let p: Option<Int> = None` and `| None -> None` inside an optional-returning helper slipped through. A behavior still does not answer one; the `>->` question ADR-0011 declined turned out to be answered already (an optional output composes and travels as one case), so that rule stands on the business vocabulary and on keeping the runtime's `Option` out of the Java-facing signature. Measured migration cost: none | `[#algebraic-types]`, `[#optional]`, `[#e1303]`, `[#e1701]` |
| [0079](0079-a-composition-carries-an-example.md) | An `example` may target a `>->` composition, evaluated by applying the class its module emits — the same class normal execution applies — so the routing is the routing and not a second implementation of it. E1902 refused it on the grounds that a composition has no implementation to evaluate, which was not true; what the refusal cost is that a case leaving the main line is not any stage's rule, so no stage's own example could state it, and the two units the crm model names were pinned by a hand-written test instead. The fake rule is unchanged: a composition's dependencies are its stages', each is faked, and a row departing at the first stage still fakes the second's — what a row supplies is a property of the composition, not of its input. What a behavior takes injected becomes one question both the emitter and the verifier read, since two walks that agreed today would bind a fake to the wrong parameter the first time one changed, with nothing reported; a requirement carries its requesters, so E1908 names the stage that wants the missing fake | `[#example-evaluable]`, `[#example-fakes]`, `[#e1902]`, `[#e1908]` |
| [0080](0080-an-invariant-clause-may-be-named.md) | An invariant clause may be named (`invariant nonEmpty = List.length(value) >= 1`), and a failure is one clause: the first that does not hold, in declaration order. An attempted construction may then depart per clause (`| nonEmpty -> NoLines | uniqueProducts -> DuplicateProduct`) instead of restating the rules as guards, and a decode failure reports which rule broke rather than only that the value was rejected — which is where a product data's issue gains the metadata it never carried. ADR-0070's "nothing carries the failure" stands: what crosses is the identifier of a rule the type declares, and the name a business failure gets is still the one written in the arm. A clause's name is never the issue's `code`, so a resolver keyed on Raoh's vocabulary keeps working, and the rejecting type travels as its module and its name rather than as a simple name two modules could share. `_` cannot name a clause, being what an attempt writes for the clauses that carry none. Declaration order becomes observable, so reordering a published type's clauses is a breaking change; the arms are total over what can fail, with `| _ ->` answering the clauses carrying no name. The constraint mapping gains Raoh's collection constraints and reads the representation where a prelude helper is still that helper; Raoh answers a plain `Decoder` after a `refine`, so a mapped clause declared behind an unmapped one gives up its code to keep its place in the order | `[#invariant-clause-name]`, `[#invariant-mvp]`, `[#attempt-departures]`, `[#decoder-error]` |
| [0081](0081-a-union-member-a-module-cannot-give-an-interface-joins-through-a-bridge-case.md) | A behavior's output union takes a primitive or an imported type as a member, so "the amount, or the reason there is none" is written without declaring a type to hold the value. A member must be nominal, tell itself apart at run time, and have a `match` arm form, which admits the primitives and named data types and keeps collections, `Option` and function types out — a collection's type argument is erased, so no arm could choose between `List<Order>` and `List<Item>`. Effective members are the written ones with named sums expanded to their leaves, since a leaf is what an arm names and what the `"type"` discriminator carries, and two members cannot go by one name. A member the module declares implements the generated result union itself; a primitive or an imported type joins through a bridge case the module emits, which follows the rule a local case class already does and leaves the imported module untouched, so jar-level imports stay available where ADR-0057's whole-program membership would have closed them. The bridge case is a JVM form only — not a Souther type, not written in a `match` arm or an `example` row, and not in an external representation, so belonging to a union does not change a member's codec. A Souther body holds only Souther values: the JVM form exists across `apply`, injected at a behavior's return and projected at a call, and projected at the call rather than at the arm because a body may answer with a call's result without matching it. E1606 narrows to a declared sum's cases, refused because a named sum introduces a type name into this module's vocabulary and an anonymous union introduces none | `[#union-member]`, `[#unmarked-output]`, `[#primitive-arm]`, `[#bridge-case]`, `[#e1606]` |
| [0082](0082-a-numeric-fold-takes-its-element-from-the-position-it-is-written-in.md) | `List.sum` / `List.product` are primitives over `Int` or `Decimal`, so a domain whose quantity has a fraction stops writing the fold out; the constraint is checked in the compiler as `sort`'s ordered-element one is, Souther having no type classes. They could not be generalised where they were written: `member` generalises because its seed is `false`, while `sum`'s seed is `0` or `0.0m` — one of the two types and never both, with no implicit widening to let a written `0` follow the element. Over the empty list the answer is that seed, and which of the two comes from the position the call is written in (the field, the annotated binding, the declared output, a `?` field asking for the value it wraps); written where nothing states a type it is a compile error asking for the annotation, because defaulting to `Int` would buy a language-wide numeric default rule for one call shape, and written where the position states some other type it is a type mismatch, since nothing about the element is unknown there and asking for an annotation would send the reader after what the position already carries. A newtype over `Int` or `Decimal` is not a numeric element — it declares no addition and no zero, and unlike ADR-0047's ordering an addition is not a total function of the wrapped value, since the seed is a construction an invariant may refuse. `Decimal.sum` was rejected as a collection operation in the element's namespace, `List.sumDecimal` as the same overload hand-encoded in a name | `[#stdlib-list]` |
| [0083](0083-an-arm-may-declare-its-combination-cannot-arise.md) | An arm may answer `unreachable "reason"`, typed `Never` and fitting any position, so a table with a blank cell states that the cell is blank instead of answering the neighbouring one. What rules such a pair out — a fact about the world, or a check the caller has already made — is not written in the types the rule sees, so the compiler cannot prove the arm away, and reshaping the data to make the pair unrepresentable would trade one table shaped like the published one for a sum per row. It is a statement, not a proof: exhaustiveness still counts the arm, there is still no `_`, reaching it aborts with `UnreachableReached` (separate from `ConstraintViolation` because a premise failing is not a value failing its type), and an `example` row that reaches it is E1911. The reason is a required literal, which is what keeps the form from being a way past exhaustiveness — it is readable without running the model. It is barred from an `invariant`, which answers on every path through itself, and it is E1307 where the position states no type and no sibling branch supplies one at the join — the abort answers no value, but the code that would read one is still emitted and reads a shape, which is the rule `[]` already follows. The misuse it admits — an arm standing for a case the model can produce — has the same shape as the legitimate one and is left to the author | `[#unreachable]`, `[#algebraic-types]`, `[#invariant-expressions]`, `[#e1307]`, `[#e1911]` |
| [0084](0084-a-tuple-is-a-value-that-compares-by-its-elements.md) | A tuple compares by its elements, so two built separately from the same values are one value, and a tuple is an operand of `==`, a `Set`'s element and a `Map`'s key. It was an `Object[]`, whose equality is identity, and the equality rule never listed tuples while the checker admitted anything that was not a function — so a composite uniqueness rule (`allUniqueBy(t -> (t.from, t.event), ...)`, the shape a model reaches for) compiled and then held of every input, including the ones it was written to exclude. It gains no ordering, so `sort` and a `sortBy` key still refuse one, and no external form. Carried as a value of its own class rather than an array: giving the shared equality an arm for arrays would have made the language's own answers right at no cost, but a tuple's `equals` would have stayed identity and the next reader written without that knowledge would be silently wrong in the way this ends. The cost is a construction where an array was an instruction, and the inlined call spends the budget `PersistentVector.append` was being inlined on — the six combinators that fold a tuple accumulator cost about 11% more per element, measured, and nothing else moved | `[#tuple]`, `[#equality]`, `[#stdlib-list]` |
| [0085](0085-a-collection-keeps-the-contract-it-owes-java.md) | Sameness is defined once in the runtime and every reader asks it there — the generated `==`, a data's `equals`/`hashCode`, a `fake`'s rows, an `example`'s expected value, the collections. A collection implementing a JDK interface keeps that interface's `equals`/`hashCode` and carries the language's semantics beside it, because a List that compared elements by value equality would be equal to a JDK list that was not equal back. A `Set` and a `Map` cannot: their trie is indexed by one hash, not two, so `contains`/`get` answer the language's question and `equals`/`hashCode` follow membership — against a foreign collection holding the same `Decimal` at another scale that is not symmetric, which the spec states rather than leaving to be found. Clojure's `Util/equiv` and `IHashEq` are the same shape reached from the same starting point. A container Souther did not build is walked by the interface it implements, so which route a value arrived by is not something a model can see | `[#equality]`, `[#collections]`, `[#jvm-product]`, `[#jvm-collection-equality]` |
| [0086](0086-a-function-name-is-a-value-and-application-is-an-operation-on-one.md) | A name that names a function is a value of function type, and applying something is an operation on the value it evaluates to. What is applied was a string beside the node rather than a subexpression, so "what does this name mean" was asked by two ladders that disagreed — the parser chose between them by looking three tokens ahead for a `(`, which is why `Map.empty` had no path to the library and `Map.empty()` did. One ladder now answers a name wherever it is written, an import contributes a table resolution consults last rather than a tree rewrite that ran before the bindings were known, and a named function — the standard library's included — is η-expanded where a value goes, so past the type check a named function, an anonymous block and a runtime choice among them are one value. Application is postfix and iterated (`f(x)(y)`, `(if flag then f else g)(x)`), with an argument list tied to the line its callee ends on: reading a next-line `(` as an application was the plan and stopped the standard library loading. Every library signature is a declaration and `Prelude.BUILTINS` is gone; an ordering constraint and "this argument is a rounding mode" are side conditions beside a declared signature, not replacements for one. A function type is refused only where the position requires an external representation, and a behavior's name handed over is the behavior rather than the `let` behind it — binding one to a local `let` is the single position still unimplemented | `[#blocks]`, `[#fn-declaration]`, `[#fn-rules]`, `[#stdlib]`, `[#reserved-namespace]`, `[#let]` |
| [0087](0087-a-rounding-mode-is-an-ordinary-value-of-a-core-declared-data.md) | A rounding mode is an ordinary value of a `RoundingMode` data the prelude declares, evaluated like any other argument, rather than a case name the compiler recognises at the call site. A prelude module resolves against its own declarations through the two-phase collection every module gets, so a core signature may name a data the same module declares; the JVM side is written by hand in the runtime rather than generated, registered once, and everything — namespace anchoring, symbol lookup, the descriptor a call links to — follows from that one registration. The initialisation cycle it would otherwise create is broken by the direction of the dependency, not by a special case | `[#stdlib-decimal]`, `[#stdlib]` |
| [0088](0088-an-injected-behaviors-example-is-recorded-until-it-can-be-run.md) | A row naming a behavior with no `let` is recorded rather than refused: a row is both an assertion the compiler checks and a written record of what a behavior owes, and E1902 was throwing away the second to protect the first. Everything the row can be held to without a body it is held to — its arity, each input built through the derived decoder, the case its expectation names — so values harvested from the system being migrated meet the model's own invariants on day one, in the model rather than in a spreadsheet beside it. Waiting is the normal state of a model being written, so it is a status the report carries and not a warning | `[#example-pending]`, `[#example-evaluable]`, `[#e1902]` |
| [0089](0089-adequacy-evidence-accumulates-and-the-measures-do-not-switch.md) | A behavior is measured at the same axes whether or not it has a body, and implementing it adds axes rather than replacing them — no column empties when the `let` arrives, so writing the body cannot look like progress on a scale nobody moved. Evidence is graded and the grades are kept apart: a case a row expects, a case the behavior was seen to answer with, and a case a row expected and the behavior produced are three claims, and a row that disagreed is still evidence for the second. A measure that could not be made is a third value beside met and unmet, because a row whose value could not be read leaves a position undecided rather than uncovered. Nothing is reduced to a score, at any level: adding a case to a signature, writing a body, and drawing a new boundary all specify the model further and all move a score down | `[#example-adequacy]`, `[#example-pending]`, `[#example-branch-coverage]` |
| [0090](0090-a-types-partition-is-the-equivalence-class-and-a-threshold-gives-the-boundary.md) | The equivalence classes an input is measured at are only the ones the model states — a type's cases, an optional, a `Bool`, and the intervals a threshold cuts — and a position the model draws no line through has none and is named as not derivable rather than divided. Splitting an unbounded `Int` at zero would measure a rule nobody wrote and report a gap for failing to test it; 398 positions across the example models come back not derivable, which is why that answer is reported and never warned about. An invariant's bound gives a boundary and not a partition, everything outside it being refused at construction, while a `guard`'s line has values on both sides and gives both. Only a comparison that is the whole of a `guard`'s condition is read, since an arm of `a && b` is reached without `b` having been evaluated. A cut keeps every rule that drew it, so one value can be owed several times | `[#example-partition]`, `[#example-adequacy]` |
| [0091](0091-an-unbuilt-candidate-does-not-prove-a-pair-infeasible.md) | Combination coverage is reported as counts and never as one ratio, because the denominator is not known: a combination a row sits in is proven reachable and the row is the proof, while one no row sits in has not been shown to be anything. A candidate refused at construction settles nothing — two classes covering wide ranges can offer representatives that break a rule relating them while other values of the same two classes build — so it is reported as its own reason and the combination stays untried, and only an exhausted finite domain or a contradiction may claim impossibility. `provenInfeasible` is in the shape from the start and stays zero, since adding a state a build reads would be a schema change for something that was always there. Single-position class coverage is measured on its own rather than derived, or a behavior with one divided input would read as fully covered while a class of it goes untouched | `[#example-adequacy]`, `[#example-partition]` |
| [0092](0092-a-helper-carries-the-variables-its-body-leaves-open.md) | A non-recursive helper may retain the type variables that remain inside a body-determined outer type constructor, scoped to the definition and instantiated afresh at each expansion, which is how a core helper carrying one already reaches its callers. A bare unconstrained variable determines nothing, and a stated answer always wins over one that leaves something open; what the body gets wrong is reported at the disagreement rather than blamed on the parameter. The variables are the helper's own: a library signature is shared by every call site, so two calls spelling `'a` link nothing, and what links two parameters is a position that reads them together. A user module still writes no type variable, a recursive helper still writes all of its types, and `data` and `behavior` still take none | `[#fn-rules]`, `[#stdlib]`, `[#type-variables]` |
