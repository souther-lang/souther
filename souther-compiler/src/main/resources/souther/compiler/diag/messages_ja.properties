# 診断メッセージカタログ — 日本語（既定ロケール）。
# ここに無いキーは messages.properties（英語）へフォールバックする。コードは locale 非依存の
# 安定 ID、タイトル/本文/ヒントは訳文。{0} などのプレースホルダは各ロケールで対応を揃える。

# --- 枠 ---
diag.error.title=エラー
diag.warning.title=警告
diag.hint.label=ヒント:
diag.diff.found=実際:
diag.diff.expected=期待:
diag.suggestion=`{0}` の間違いではありませんか？

# --- エラー見出し（仕様 22 章） ---
e1001.title=データの直接構築
e1002.title=構築権限の不足
e1004.title=スプレッドのフィールド名衝突
e1005.title=スプレッドでのフィールド欠落
e1006.title=構築権限の過剰宣言
e1101.title=不変条件の型不一致
e1201.title=網羅されていない match
e1301.title=null の使用
e1302.title=例外の使用
e1303.title=Option のケース名の使用
e1305.title=生成できない注入データ
e1401.title=任意の Java 呼び出し
e1501.title=循環 import
e1601.title=未束縛の依存
e1602.title=depends on 宣言の不足
e1603.title=depends on の過剰宣言
e1604.title=合成出力の不一致
e1605.title=出力署名の欠落
e1607.title=depends on が依存を指していない
e1608.title=behavior が自分に到達
e1701.title=behavior 合成の型不一致

# --- 書き直した高価値メッセージ（Elm 流: found/expected・二次リージョン・ヒント） ---

# 型不一致（値がその位置に必要な型を持っていない）
check.type.mismatch.title=型の不一致
check.type.mismatch.msg=この式の型が、ここで必要な型と合っていません。
check.type.mismatch.hint=値の型を合わせるか、この位置が期待する型を変えてください。

# if の2つの分岐が食い違う
check.if.title=型の不一致
check.if.msg=この `if` の2つの分岐は異なる型を返しているため、`if` 全体の型が定まりません。
check.if.then=`then` 分岐は {0} を返します
check.if.else=`else` 分岐は {0} を返します
check.if.hint=両方の分岐が同じ型を返すようにしてください。片方を意図したケースで包むか、上流で処理します。

# 網羅されていない match（E1201）
e1201.msg=この {0} に対する match が、すべてのケースを網羅していません。
e1201.hint=次のケースを追加してください: {0}

# 見つからない名前（did-you-mean 付き）
check.unknown.title=名前が見つかりません
check.notavalue=`{0}` は{1}なので、ここで値として保持できません。
check.unknown.name.msg=`{0}` という名前の値がスコープに見つかりません。
check.unknown.type.msg=`{0}` という名前の型が見つかりません。
check.unknown.behavior.msg=このパイプラインに `{0}` という名前の behavior が見つかりません。

# リスト要素の型が揃っていない
check.list.title=型の不一致
check.list.msg=このリストの要素の型が揃っていません。
check.list.hint=ある要素は {0}、別の要素は {1} です。すべての要素を同じ型にしてください。

# 局所束縛の型注釈（`let x: T = e`）
check.let.annotation=束縛 `{0}` は {1} と宣言していますが、値は {2} です。

# 空コレクションを seed にした fold で、アキュムレータの型が文脈から推論できない
check.fold.seed.title=型を推論できません
check.fold.seed.untyped=この空コレクションの要素型をここで推論できません。流れ込む先の束縛に型注釈を付ける（`let x: <型> = ...`）か、fold を型のある位置（フィールドや behavior の出力）に直接置くか、seed を非空の値にしてください。
check.fold.seed.here=アキュムレータの型が未確定のままなので、この演算には対象がありません

# 二項/単項オペランドの型
check.concat.title=型の不一致
check.concat.msg=`++` は2つのリストまたは2つの文字列を連結しますが、オペランドは {0} と {1} です。
check.compare.title=型の不一致
check.compare.msg=この2つの値は比較できません: {0} と {1}。
check.neg.title=型の不一致
check.neg.msg=単項マイナスには Int か Decimal が必要ですが、これは {0} です。
check.operand=これは {0} です

# behavior 合成の型不一致（E1701）
e1701.msg=この behavior 同士は合成できません。左の behavior の出力が、右の behavior の入力に受け取られません。
e1701.hint=左の出力を右の behavior が受け取れるケースにするか、余分なケースをこの段の前で処理してください。

# --- コード付き宣言エラー ---

# 構築権限（E1002 不足、E1006 過剰）
e1002.msg=behavior `{0}` は `{1}` を構築していますが、`constructs {1}` を宣言していません。
e1002.hint=`{0}` に `constructs {1}` を追加するか、ここで {1} を構築するのをやめてください。
e1006.msg=behavior `{0}` は `constructs {1}` を宣言していますが、{1} を構築していません（既存の値を素通ししています）。
e1006.hint=`constructs` 句から `{1}` を外してください。

# depends on（E1602 不足、E1603 過剰）
e1602.msg=`let {0}` は実装のない `{1}` を呼んでいますが、`behavior {2}` は `depends on {1}` を宣言していません。
e1602.hint=`behavior {2}` に `depends on {1}` を追加してください。
e1603.msg=`behavior {0}` は `depends on {1}` を宣言していますが、`let {2}` はそれを呼んでいません。
e1603.hint=`depends on` 句から `{1}` を外してください。

# `?` フィールドに値を与える場所以外で書かれた `Some` / `None`（E1303）
e1301.msg=`null` はこの言語にありません。`?` を付けたオプショナルのフィールドを使ってください。

e1501.msg=モジュールの依存が循環しています。

e1303.some=`Some(...)` は呼び出しではありません。`?` フィールドに書いた値は自動で包まれ、そこがオプショナルの作られる唯一の場所です。
e1303.some.hint=`?` フィールドに値を与える場所なら、値だけを書いてください。それ以外の場所では、オプショナル（`?` フィールド・`Map.get`・`List.find`）は読むだけで作りません。
e1303.none=`None` は `?` フィールドの空の値ですが、ここではそれを求めているものがありません。
e1303.none.hint=不在がモデル自身のものなら、自分の sum のケースにしてください。その不在は step がオプショナルを返す `List.filterMap` には渡せないので、0個か1個のリストを返す step を `List.flatMap` に渡します。

# 位置が型を述べていないところに書かれた `unreachable`（E1307）
check.unreachable.untyped=この位置が何を持つかを述べているものが何もなく、`unreachable` は値を答えないので自分では述べられません。
check.unreachable.untyped.hint=型が述べられている位置に書いてください。値を答える腕の隣の腕、フィールド、宣言された戻り値、型注釈のある束縛などです。

# 注入 behavior が生成できないデータを構築（E1305）
e1305.msg=注入 behavior `{0}` は `constructs {1}` を宣言していますが、{1} は unit data でも exposed でもないため、Java から生成できません。
e1305.hint={1} を exposed にするか、unit data にしてください。

# 合成出力の宣言と推論（E1604）・出力署名（E1605）
e1604.msg=behavior {0} は出力を {1} と宣言していますが、パイプラインは {2} を生成します。
e1604.hint=宣言した出力を更新するか、余分なケースを出力の前で処理してください。
e1605.notcomposition=`exposing` が `{0}` に出力署名を与えていますが、`{0}` は合成（`>->`）behavior ではありません。署名が要るのは合成だけです。
e1605.missing=exposed な合成 `{0}` は `exposing` で出力を宣言する必要があります。
e1605.missing.hint=`exposing ( {0} : {1} )` と書いてください。

# depends on が注入対象を指していない（E1607）
e1607.nothing=`behavior {0}` は `depends on {1}` を宣言していますが、`{1}` は何にも依存していないので注入するものがありません。
e1607.nothing.hint=`depends on` から `{1}` を外して直接呼んでください。何にも依存しない behavior は名前で届きます。
e1607.unknown=`behavior {0}` は `depends on {1}` を宣言していますが、`{1}` は自身の要求を持つ behavior としてスコープにありません。
e1607.unknown.hint=`let` のない `behavior {1}` をこのモジュールで宣言するか、宣言しているモジュールから import してください。
e1607.composition=`behavior {0}` は `depends on {1}` を宣言していますが、`{1}` は `>->` 合成です。
e1607.composition.hint=`depends on` から `{1}` を外してください。合成の要求は stage から決まり書かれないので、呼ぶ側がそれに寄りかかることはできません。合成するか、stage が依存するものに depends on してください。

# スプレッド/フィールド衝突（E1004）・スプレッドのフィールド欠落（E1005）
e1004.msg=`...{1}` 由来のフィールド `{0}` が、`{2}` のフィールドと衝突しています。
e1004.dup=`{1}` にフィールド `{0}` が重複しています。
e1005.msg=`{0}` の構築でフィールド `{1}` が欠けています。
e1005.hint=`{1}` を明示的に与えてください。スプレッドはそれを提供しません。
e1005.hint.sum=`{1}` は `{2}` の全ケースがスプレッドしている共通部分にないので、スプレッドはそれを提供しません。明示的に与えるか、`{2}` を `match` で開いて、それを持つケースから読んでください。
e1005.hint.sums=直和のスプレッドは、全ケースがスプレッドしている共通部分を提供します。`{1}` は次のいずれの共通部分にもありません: {2}。明示的に与えるか、それを持つケースのある直和を `match` で開いてください。

# 不変条件の型（E1101）
e1101.msg=不変条件の式は Bool 型でなければなりませんが、これは {0} です。

# 任意の Java 呼び出し（E1401）・stdlib 修飾
e1401.msg=`{0}` は behavior でも組み込みでもありません。
e1401.hint=`let` を持たない behavior を宣言し Java 側で実装してください。任意の JVM 呼び出しは許可されていません。
e1401.behavior=`{0}` は behavior ですが、ここからは呼べません。
e1401.behavior.hint=ヘルパ `let` は behavior に届かず、`>->` 合成は呼ぶものではなく合成するものです。`{0}` は behavior 自身の `let` から呼んで、結果を渡してください。
check.construct.position.title=ここに構築は書けません
check.construct.position=`{0}` は型なので `{0}(...)` はその構築ですが、ここに構築は書けません。
check.apply.notfunction.title=関数ではありません
check.apply.notfunction=`{0}` はここでは関数ではないので、引数に適用できません。
check.stdlib.value.hint=`{0}` とだけ書いてください。
check.builtin.notfunction=`{0}` は言語が与える名前であって関数ではないので、引数に適用できません。
check.stdlib.qualified.msg=`{0}` は標準ライブラリの関数です。`{1}` のように修飾して呼んでください。

# stdlib 引数型チェック（期待するコンテナ種別は訳文フレーズ）
kind.list=List
kind.map=Map
kind.set=Set
kind.option=Option
kind.ordered.list=順序を持つ値（Int, String, Decimal, Date, DateTime と、それらの newtype、および全ケースが単位データの直和＝宣言順で並ぶ列挙）のリスト
kind.numeric.list=Int または Decimal のリスト
check.expects=`{0}` は {1} を必要としますが、{2} が渡されました。
check.ordered=`{0}` は {1} を必要としますが、要素は {2} です。
check.ordered.hint=先に順序を持つフィールドに map してから、それを並べ替え／比較してください。
check.ordered.key=`{0}` のキーは順序を持つ値でなければなりませんが、{1} を返しています。
check.numeric=`{0}` は {1} を必要としますが、要素は {2} です。
check.numeric.hint=先に数値のフィールドに map してから、それに `{0}` を適用してください。Int や Decimal の newtype は数値の要素ではありません。演算も、始まりの値も、自分では宣言していないからです。
check.numeric.empty=空リストの `{0}` が答えるのは初期値だけですが、それが Int か Decimal かは要素で決まり、空リストにはその要素がありません。
check.numeric.empty.hint=呼び出しが流れ込む位置に型注釈を付けてください。束縛（`let total: Decimal = ...`）、フィールド、宣言された出力のいずれかです。
check.numeric.result=`{0}` が答えるのは Int か Decimal ですが、この位置が必要としているのは {1} です。
check.numeric.result.hint=Int や Decimal の newtype は結果そのものではなく、結果から構築するものです（`Hours({0}(xs))`）。それ以外の型では、この位置に `{0}` は置けません。

# 重複
check.duplicate.title=重複した定義
check.dup.let=`let {0}` は既に定義されています。
check.dup.data=data `{0}` は既に定義されています。
check.data.qualifier=data `{0}` は標準ライブラリの修飾子 `{0}` に予約された名前です（`{0}.\u2026` のように使います）。修飾子はライブラリに到達する唯一の綴りなので、同名の data はそれを覆い隠します。別の名前にしてください。
check.dup.valuename=`let {0}` と data `{0}` は、値を書く位置では同じ1つの名前です。data は値として書かれます（単位 data はそれ自体が値、newtype は包む値に適用、record は名前で構築）。どちらかを改名してください。
check.dup.field=フィールド `{0}` が複数回定義されています。
check.dup.name=`{0}` が {1} に複数回書かれています。

# match
check.match.title=match のエラー
check.match.notsum=match には直和型の値が必要ですが、{0} が渡されました。
check.match.notcase=`{0}` は {1} のケースではありません。
# `'{'` / `'}'` は MessageFormat のクォート。描画時は `-> { match ... }` になる。
check.match.notcase.other=`{0}` は `{1}` のケースです。このアームは `{1}` を対象とする match のものです。
check.match.notcase.online=アームの本体に書いた `match` は、同じ行でそれに続く `|` をすべて取り込みます。内側の match をブロックに入れる（`-> '{' match ... '}'`）か、アームを行ごとに分けて外側より深く字下げしてください。
check.match.overlap=`{0}` が複数のケースで扱われています。
check.match.nocases=この match にはケースがありません。
check.match.option.orpattern=Option の match では or パターンを使えません。Some と None を別々のケースにしてください。
check.match.option.notcase=`{0}` は Option のケースではありません。Some か None を使ってください。
check.match.option.nopayload=`{0}` は値を持たないので、`{0}(...)` で分解できません。
check.match.newtype.notnewtype=`{0}` は newtype ではないので、match のケースで `{0}(...)` と開くことはできません。
check.match.newtype.notnewtype.hint=一致した値そのものを束縛するなら `| {0} as v` と書きます。
check.match.newtype.mismatch=`{0}` が包むのは `{1}` であって `{2}` ではありません。
check.match.newtype.orpattern=or パターンは直和型を束縛するため、ケースの値を `(...)` で開くことはできません。

# タプルパターン／フィールドアクセス
check.tuple.pattern=タプルパターンにはタプルが必要ですが、{0} が渡されました。
check.tuple.arity=このパターンは {0} 個の名前を束縛しますが、タプルの要素は {1} 個です。
check.access=この値のフィールド `{0}` は読み取れません。
check.access.sum=`{1}` のフィールド `{0}` は読み取れません。直和自身はフィールドを持たず、持っているのは各ケースで、どのケースかは開くまで分かりません。`match` で開いて、ケースごとに `{0}` を読んでください。
check.access.sum.missing=次のケースは `{0}` フィールドを持ちません: {1}。
check.access.sum.unshared=どのケースにも `{0}` フィールドがありますが、各ケースが別々に宣言しています。直和がフィールドを公開するのは、全ケースがそれを持つデータをスプレッドしているときだけです。

# 引数の数・ブロック・構築
check.arity.title=引数の数が違います
check.arity=`{0}` は引数を {1} 個取りますが、{2} 個渡されました。
check.block.title=ブロックは値ではありません
check.block.notvalue=ブロックは値ではありません。引数として渡すか `let` に束縛して適用できますが、返したり data に格納したりはできません。
check.construct.title=構築できません
check.construct.self=data `{0}` は構築できません。必須フィールドを通じて自分自身の値を必要とし、底に達しません。自己参照フィールドを任意（`?`）か `List` にしてください。
check.construct.no=ここで `{0}` を構築することはできません。

# `behavior` を実装する `let` は契約に合わせる
check.impl.title=実装の不一致
check.impl.noreturn=`let {0}` は `behavior {1}` の実装なので、戻り値の型は behavior から取られます。宣言しないでください。
check.impl.arity=`let {0}` は引数を {1} 個取りますが、`behavior {2}` の入力は {3} 個、注入される引数は {4} 個です。
check.impl.noannotate=`let {0}` は `behavior {1}` の実装なので、引数の型は behavior から取られます。`{2}` に型注釈を付けないでください。
check.impl.reqorder=`let {0}` の引数 `{1}` は `{2}` であるべきです。`depends on` が挙げた名前は宣言順に末尾の引数になります。

# ヘルパ `let` の宣言規則
check.helper.title=ヘルパの宣言
check.helper.annotate=ヘルパ `let {0}` は引数 `{1}` に型注釈を付ける必要があります。
check.helper.infer=ヘルパ `let {0}` の引数 `{1}` は本体から型が決まりません。型注釈を付けてください。
check.helper.infer.use=この使い方は型を名指していません
check.helper.infer.field=ヘルパ `let {0}` の引数 `{1}` はフィールド越しにしか読まれておらず、フィールドは型を名指しません。型注釈を付けてください。
check.helper.infer.field.use=フィールドを読んでいますが、それは型を名指しません
check.helper.fnparam=ヘルパ `let {0}` の引数 `{1}` は関数として使われています。関数型引数には型注釈が必要です。
check.helper.return=ヘルパ `let {0}` は {1} を返すと宣言していますが、本体は {2} です。
check.rechelper.return=再帰ヘルパ `let {0}` は戻り値の型を宣言する必要があります（`let {0} (...) : <型> = ...`）。再帰を通じて結果を推論できないためです。
check.rechelper.fnparam=再帰ヘルパ `let {0}` は関数引数 `{1}` を取れません。再帰ヘルパはメソッドであり、関数を渡せません。
check.rechelper.pure=再帰ヘルパ `let {0}` は純粋であり、注入 behavior `{1}` を呼べません。作用はこのヘルパを呼ぶ behavior に置いてください。
check.invariant.invalid.title=不正な不変条件
check.invariant.partial=`{0}` の不変条件が `partial` ヘルパ `{1}` に到達しています（{2}）。`partial` は停止性を保証しません。不変条件は構築時に検査され、停止する必要があります。
check.invariant.construct=`{0}` の不変条件が `{1}` を構築していますが、不変条件はデータを構築できません。構築される値を観測するだけで、別の値を作ってはいけません。
check.invariant.construct.named=`{0}` の不変条件 `{2}` が `{1}` を構築していますが、不変条件はデータを構築できません。構築される値を観測するだけで、別の値を作ってはいけません。
check.invariant.construct.here=不変条件 `{0}` からその構築に到達しています。
check.invariant.construct.here.unnamed=この不変条件からその構築に到達しています。
check.invariant.onunit=`{0}` は単位データなので、この不変条件が観測するフィールドがなく、拒める値もありません。この句を削除するか、制約の対象となるフィールドを `{0}` に持たせてください。
check.invariant.underscore=`_` は `{0}` の invariant の句の名前にできません。名前のない句には attempt が `| _ -> ...` で答えるので、`_` という名前の句は名前で答えられません。
check.invariant.underscore.hint=別の名前を付けるか、名前を付けずに書いてください。名前のない句も検査され、`| _ -> ...` が答えます。
check.invariant.unreachable=`{0}` の invariant が `unreachable` を答えていますが、invariant は構築される値が条件を満たすかどうかを、通るすべての経路で答えるものです。起こり得ない経路は、モデルが作る値についての規則であって、この値についての規則ではありません。
check.invariant.duplicate=`{1}` が `{0}` という名前の invariant の句を二つ宣言しています。名前は試みる構築と境界のエラーが規則を呼ぶためのものなので、二つの規則が共有できません。
check.data.invalid.title=不正なデータ定義
check.data.emptybody=データ `{0}` の本体が空です。これは値がひとつだけの型、つまり単位データであり、単位データは本体を書かずに宣言します。
check.data.emptybody.hint=`data {0}` と書いて名前だけで構築するか、`{0}` が持つフィールドを与えてください。

# モジュール／exposing
check.module.title=モジュールのエラー
check.exposing.granular=`exposing` は型単位です。data の `decoder`/`encoder` は data を公開すれば常に public です。`{1}` ではなく `{0}` と書いてください。
check.exposing.imported=`exposing` が `{0}` を挙げていますが、これはこのモジュールに import されたもので、ここで定義されていません。`exposing` はモジュール自身の定義を挙げるもので、import 名を再公開しません。
check.exposing.notdefined=`exposing` が `{0}` を挙げていますが、これはこのモジュールの data でも behavior でもありません。
check.inject.depends=behavior `{0}` は `let` を持たない注入対象なので、`depends on` を宣言できません。呼び出すか合成する behavior が要求を持ちます。

# decoder/encoder 境界を越えてはならない型
check.boundary.title=境界の型
check.param.union=引数 `{0}` は無名の直和型 `{1}` です。引数の型は単一の名前付き型でなければなりません。`data ... = {1}` を宣言してその名前を取ってください。
check.equality.function=関数は比較する値を持たないので、{0} は `==` / `/=` の被演算子になれません。
check.set.function=Set は重複を持たない集合で、そのために要素どうしが等しいかを問います。関数は比較する値を持ちません: {0}。
check.map.key.function=Map はキーで値を引くので、キーどうしが等しいかを問います。関数は比較する値を持ちません: {0}。
check.param.function=引数 `{0}` は関数を含みます（{1}）。関数は外部表現を持たないので、behavior の入力として境界を越えられません。
check.output.function=`{0}` は関数を出力します（{1}）。関数は外部表現を持たないので、behavior の出力として境界を越えられません。
check.output.optional=behavior `{0}` は optional ({1}) を出力しています。
check.output.optional.hint=behavior が答える不在はモデル自身の和型のケースです。`-> {0} | <不在のケース>` と書いて、それが何であるかを名付けてください。
check.param.tuple=引数 `{0}` はタプルです。タプルは外部表現を持たず境界を越えられないため、behavior の入力は名前付き data でなければなりません。
check.output.tuple=behavior `{0}` はタプルを出力します。タプルは境界を越えられないため、behavior の出力は名前付き data かその直和でなければなりません。

# コレクション型に型引数がない
check.typearg.title=型引数がありません
check.typearg.list=`List` には型引数が必要です（例: `List<Int>`）。
check.typearg.set=`Set` には型引数が必要です（例: `Set<String>`）。
check.typearg.option=`Option` には型引数が必要です。
check.typearg.map=`Map` には値の型が必要です（例: `Map<String, Int>`）。
check.map.key.field=`{0}` は境界を越えるので、持っている Map のキーは String・String ベースの newtype（`data X = String`）・Date・DateTime・全ケースが単位データの直和 のいずれかでなければなりませんが、{1} です。
check.map.key.field.hint=Map の外部表現は JSON オブジェクトで、そのキーは文字列です。behavior の本体内であれば Map のキーは任意の値でかまいません。
check.map.key.param=引数 `{0}` が持つ Map のキーが {1} で、境界を越えられません。
check.map.key.param.hint=境界を越える Map のキーは String・String ベースの newtype（`data X = String`）・Date・DateTime・全ケースが単位データの直和（ケース名として渡ります）のいずれかです。
check.map.key.output=`{0}` が返す Map のキーが {1} で、境界を越えられません。
check.map.key.output.hint=境界を越える Map のキーは String・String ベースの newtype（`data X = String`）・Date・DateTime・全ケースが単位データの直和（ケース名として渡ります）のいずれかです。

# 比較・算術のオペランド
check.compare.ordered=比較の2つのオペランドは同じ順序型（Int, String, Decimal, Date, DateTime、それらの newtype、または1つの列挙）の値でなければなりません。ケース同士が比較できるのは、両方を挙げている直和が1つに定まるときだけです。ここでは {0} と {1} です。
check.arith.operand=算術のオペランドは Int か Decimal でなければなりませんが、{0} です。
check.arith.newtype.incompatible={0} と {1} は別の newtype なので、算術では組み合わせられません。
check.arith.newtype.incompatible.hint=同じものを測っているなら、一方をもう一方の newtype に変換してから組み合わせてください。違うものなら、組み合わせられるものがありません。
check.arith.newtype.product={0} と {1} の積はどちらの量でもなく、その次元をこの言語は表現しません。
check.arith.newtype.product.hint=`.value` で計算し、積が属する newtype を構築してください。
check.arith.newtype.quotient={0} と {1} の商はどちらの値でもなく、その次元をこの言語は表現しません。
check.arith.newtype.quotient.hint=`.value` で計算してください。商が newtype なら、その結果から構築してください。
check.arith.newtype.reciprocal={0} を {1} で割ると逆数になり、そこで変わる次元をこの言語は表現しません。
check.arith.newtype.reciprocal.hint=`.value` で計算してください。結果が newtype なら、そこから構築してください。
check.arith.newtype.scalar={0} として読まれるのは隣に書かれたリテラルだけで、この {1} は書き下されていません。
check.arith.newtype.scalar.hint=値の出どころで {0} を構築するか、`.value` で計算してください。
check.arith.newtype.base={0} が包むのは {1} で、この {2} はそれではありません。
check.arith.newtype.base.hint=`*` はどちら側でも {1} でスケールします。`/` がスケールになるのは `{0} / {1}` の向きだけで、`{1} / {0}` は逆数です。`+` と `-` が {1} を読むのは書き下したリテラルのときだけです。それ以外は {0} を構築するか、`.value` で計算してください。
check.arith.newtype.nested=算術が届くのは value が直接 Int か Decimal の newtype で、{0} が包むのは {1} です。
check.arith.newtype.nested.hint=算術が {0} の value を自動で取り出すことはありません。`.value` と書いて {1} にしてから、{1} の規則に従ってください。

# 直和の定義と導出 decoder/encoder（codec）
check.sum.title=直和の定義
check.codec.title=codec
check.sum.unknowncase=直和 `{1}` に未知のケース `{0}` があります。
check.sum.foreigncase=`{0}` は `{2}` で宣言されているので、直和 `{1}` のケースにはできません。
check.sum.foreigncase.hint=直和のケースはその直和と一緒に宣言します。生成される直和は sealed interface で、それを実装できるのは自分のモジュールだけです。`{0}` は境界で消費するか、このモジュールのケースに再表現してください。
check.expr.toodeep=このソースの式が深く入れ子になりすぎていて、コンパイラが辿れません。`let` で名前を付けて分割してください。
check.sum.cycle=直和 `{0}` が {1} を経由して自分自身を含んでいます。直和のケースはその型が取りうる値なので、ケースが直和自身になることはできません。
check.sum.optioncase=`{0}` は組み込みの Option ケースなので data 型として宣言できません。
check.codec.notcase=`{0}` は `{1}` のケースではありません。
check.codec.needdecoder=ケース `{0}` には decoder が必要です。
check.codec.needencoder=ケース `{0}` には encoder が必要です。
check.codec.missingcase=`{0}` の encoder にケース `{1}` がありません。
check.spread.notproduct=`...{0}` はスプレッドできません（直積 data ではありません）。
check.codec.iso=ISO テキスト encoder は Date か DateTime を必要としますが、{0} です。
check.codec.option=optional encoder は Option を必要としますが、{0} です。
check.codec.noencoder=`{0}` には encoder がないため、`{0}.encode` を呼べません。
check.codec.listsource=`list(...)` encoder の source は List でなければなりませんが、{0} です。
check.codec.setsource=set encoder の source は Set でなければなりませんが、{0} です。
check.codec.mapsource=map encoder の source は Map でなければなりませんが、{0} です。
check.codec.nodecoder=`{0}` には decoder がないため、`{0}.decoder` を呼べません。
check.codec.mustconstruct=`{0}` の decoder は `{0}` を構築しなければなりませんが、`{1}` を構築しています。
check.construct.nofield=`{0}` は `{1}` のフィールドではありません。
check.field.type=フィールド `{0}` は {1} を必要としますが、{2} です。
check.spread.notdata=スプレッド `..{0}` は data の値か、全ケースが同じ data をスプレッドしている直和の値でなければなりません。
check.spread.union.hint=`{0}` はその場に書かれたユニオンなので、ケースが何を共有しているかを宣言しているものがありません。`data` 宣言でユニオンに名前を付ければ、全ケースがスプレッドしている部分をそこからスプレッドできます。
check.spread.sum.unshared=スプレッド `..{0}` は `{1}` のケースが共有するフィールドを写しますが、共有しているものがありません。全ケースがスプレッドしている data がないためです。`{0}` を `match` で開いて、ケースをスプレッドしてください。

# パイプライン／予約名／数値
check.pipe.title=パイプライン
check.pipe.multiinput=`{0}` は入力を {1} 個取るため、`{2}` の `>->` で後続の段にできません。最初以外の段は入力を1つ取ります。インラインで呼ぶか `match` で分岐を開いてください。複数入力を取れるのは最初の段だけです。
check.pipe.boundary=decode/encode は境界の縁であり、パイプラインの段ではありません。`>->` は behavior のみを合成します。
check.pipe.selfcompose=パイプライン `{0}` が自分自身と合成しています（循環）。
check.reserved.title=予約された名前
check.builtin.shadow=`{0}` は組み込みの値なので束縛名に使えません。組み込みを隠してしまいます。別の名前を選んでください。
check.matches.constant=`String.matches` のパターンはコンパイル時に文字列として評価できなければなりません。そこで検証するためです。文字列リテラルを書くか、リテラルとトップレベルの `let` で名付けた値を `++` でつないでください。
check.matches.regex=`String.matches` のパターンが正規表現として不正です: {0}
check.union.members=`{0}` は直和のメンバーになれません。メンバーになれるのは `match` の腕が名前で指せる型 —— プリミティブか、このモジュールで宣言したか import した data 型です。
check.union.samename=1つの直和に `{0}` と書かれる型が2つあります。`{1}` が宣言したものと `{2}` が宣言したものです。
check.case.discriminatorfield=`{0}` は `{2}` のケースで、フィールド `{1}` を宣言しています。これはその直和がフィールドの隣に書く判別子と同じ名前です。
check.member.discriminatorfield=`{0}` は `{2}` の出力のメンバーで、フィールド `{1}` を宣言しています。これはそのユニオンがフィールドの隣に書く判別子と同じ名前です。
check.case.discriminatorfield.hint=タグとフィールドが同じキー `{0}` を取り合い、後に書かれた方だけが残ります。フィールドを改名してください。
check.union.samename.hint=match の腕はメンバーを書かれた名前で指し、外部表現の `"type"` も同じ名前を使うので、1つの名前が2つのメンバーを指すことはできません。どちらかをこのモジュールの型として表し直してください。
check.bridge.collision.data=`{0}` の出力にメンバー `{1}` があります。これはこのモジュールが宣言した型ではないので、生成されるケースクラス `{2}` を介して直和に加わりますが、その名前は `data {3}` が既に使っています。
check.bridge.collision.behavior=`{0}` の出力にメンバー `{1}` があります。これはこのモジュールが宣言した型ではないので、生成されるケースクラス `{2}` を介して直和に加わりますが、`behavior {3}` を大文字化した名前と同じです。
check.bridge.collision.hint=このモジュールが宣言していないメンバーは、自分の名前に `Case` を付けた `{0}` を介して直和に加わります。その名前を持っている方を改名してください。
check.bridge.collision.member=`{0}` と `{1}` はどちらもこのモジュールの直和のメンバーで、どちらも生成されるケースクラス `{2}` を介して加わります。
check.bridge.collision.member.hint=メンバーは自分の名前に `Case` を付けた名前で加わるので、同じ綴りの2つの型が、別々の behavior であってもここで両方メンバーになることはできません。どちらかをこのモジュールの型として表し直してください。

# 第一級関数と関数型のヘルパ引数
check.fn.title=関数
check.fn.blockparam.arity=`let {1}` の `{0}` に渡されたブロックは引数を {2} 個取りますが、{3} 個で書かれています。
check.fn.blockparam.return=`let {1}` の `{0}` に渡されたブロックは {2} を返さなければなりませんが、{3} を返します。
check.fn.notfunction=`let {1}` の `{0}` は関数を必要としますが、`{2}` は値であって関数ではありません。
check.fn.argorder=`{0}` の引数 {1} は `{2}` で、関数を受け取りません。関数は引数 {3}（`{4}`）に渡します。
check.fn.argorder.hint=`{0}({5})` の順で書いてください。
check.fn.argnotvalue=`{0}` の引数 {1} は `{2}` で、関数を受け取ります。名前付き関数かラムダを渡してください。
check.fn.argnotvalue.hint=`{0}({3})` の順で書いてください。
check.fn.argnotfn=`{0}` の引数 {1} は `{2}` で、関数を受け取りません。ラムダやフィールドゲッターはここに書けません。
check.fn.callarity=`{0}` は関数を {1} 個の引数で呼びますが、関数は {2} 個取ります。
check.fn.argtype=`{0}` の要素型 {1} は、{2} を取る関数に渡せません。
check.fn.expectsblock=`{0}` はブロックを必要とします（例: `{0}((acc, x) -> ..., seed, xs)`）。
check.fn.blockarity=このブロックは引数を {0} 個取りますが、{1} 個です。
check.fn.noinfer=関数 `{0}` の型を推論できません。型を書く（`let {0}: (Int) -> Bool = ...`）か、このスコープで適用して引数の型を読めるようにしてください。ラムダの中の適用はそのラムダのスコープなので、ここでは何も言いません。
check.fn.difftypes=関数 `{0}` が異なる引数型で適用されています: {1} と {2}。
check.fn.lambdaarity=このラムダは引数を {0} 個取りますが、{1} 個で適用されています。
check.fn.branchtypes=2つの分岐が異なる関数型を生成します: {0} と {1}。
check.fn.predicatebool=`{0}` の述語は Bool を返さなければなりませんが、{1} を返します。

# 残り
check.impl.compose=`let {0}` は合成 `behavior {0}` を実装できません。合成は既に自身の実装です。
check.behavior.return=behavior `{0}` は {1} を返しますが、`let` の本体は {2} です。
check.field.tuple=タプルは data のフィールドにできません（`{0}.{1}`）。タプルは外部表現を持たず decoder/encoder 境界を越えられません。名前付き data を使ってください。
check.field.objectname=`{1}` は `{0}` のフィールドにできません。生成クラスは同名のアクセサでフィールドを読みますが、`{1}()` はすべての JVM の値が既に持つメソッドです。別の名前にしてください。
check.spread.provides=スプレッドは `{0}` を {1} として提供しますが、`{2}` は {3} を必要とします。
check.codec.elemenc=要素 encoder {0} は要素型 {1} と一致しません。
check.match.branchtypes=match の分岐の型が食い違います: {0} と {1}。
check.fold.shape=空のコレクションで初期化した fold は、そのコレクションの形の値を育てる必要がありますが、ブロックは {0} を返します。
check.fold.acctype=fold のブロックはアキュムレータの型 {0} を返さなければなりませんが、{1} です。
check.stdlib.notfunction=`{0}` は標準ライブラリの関数ではありません。
check.generic.arg={0}: {1} を期待しましたが、{2} でした。

# モジュールのリンク・import・定数の不変条件
check.const.invariant=`{0}` は不変条件に違反しています。
check.const.invariant.clause=`{0}` は invariant の句 `{1}` に違反しています。
check.module.reserved=module `{0}` は予約された `souther` 名前空間にあります。コンパイラは souther.string / souther.list / souther.map / souther.bool を同梱しており、ユーザーのモジュールは予約名を使えません。
check.module.qualifier=module `{0}` は標準ライブラリの修飾子 `{0}` に予約された名前を使っています（`{0}. …` / `import {0} ( … )` のように）。別のモジュール名を選んでください。
check.module.duplicate=モジュール `{0}` が重複しています。
check.module.pathincomplete=モジュール `{1}` は `{0}` を必要としていますが、`{0}` もパス上にありません。
check.module.pathincomplete.hint=`{0}` は、依存しているモジュールがさらに依存しているものです。このプロジェクトの依存に足してください。
check.module.shadowspath=モジュール `{0}` はここでコンパイルされ、同時にパス上にもあります。
check.module.shadowspath.hint=1つの名前が2つのモジュールを指すことはできません。こちらをリネームするか、`{0}` を提供している依存を外してください。
check.module.incompatible=モジュール `{0}` は Souther {1} でコンパイルされていて、import する側が何に触れてよいかについて、このコンパイラと一致していません。
check.module.incompatible.hint=`{0}` をこのコンパイラでビルドし直すか、それをビルドしたバージョンでコンパイルしてください。
check.module.publishedincomplete=モジュール `{1}` は `{0}` を宣言していると言っていますが、その宣言を載せたクラスが classpath にありません。
check.module.publishedincomplete.hint=`{0}` の jar が不完全です。ビルドし直してください。
check.import.unknownmodule=未知のモジュール `{0}` です。
check.import.notexposed=`{0}` は `{1}` から公開されていません。
check.import.notdefined=`{0}` は `{1}` に定義されていません。
check.import.conflict=import した `{0}` が、このモジュールの同名の定義と衝突しています。
check.import.conflict.hint=1つの名前が2つのものを指すことはできません。ローカルの定義をリネームするか、import をやめて import 先をモジュール名で修飾して参照するか、ローカルの定義を削除して import した方を使ってください。
check.import.duplicate=`{0}` を `{1}` と `{2}` の両方から import しています。
check.import.duplicate.first=`{0}` はここで `{1}` から import されています。
check.import.duplicate.hint=1つの名前が2つの型を指すことはできません。import するのはどちらか一方だけにして、もう一方はモジュール名で修飾して（`{1}.{0}`）、または `import ... as` の別名で参照してください。
check.import.duplicate.same.hint=同じモジュールから同じ名前を2回 import しています。2つ目を削除してください。
check.import.notstdfn=`{0}` は標準ライブラリのモジュール `{1}` の関数ではありません。
check.import.ambiguous=`{0}` は `{1}` と `{2}` の両方から公開されています。両方を import せず、修飾して呼んでください。
check.import.behaviordup=behavior `{0}` を `{1}` と `{2}` の両方から参照しています。
check.import.behaviordup.hint=behavior の名前は生成クラスの注入フィールド名でもあるので、1つのモジュールが `{0}` という behavior を2つ受け取ることはできません（修飾してもフィールドは分かれません）。両方が必要なら、このモジュールで区別できる名前の behavior を2つ宣言してください（`let` を書かない behavior が注入対象になります）。どちらにどの上流の behavior を渡すかは Java 側が決めます。
check.inject.hiddeninput=`behavior {0}` に `let` がないので実装はモジュールの外に置かれますが、入力の `{1}` が `exposing` にありません。
check.inject.hiddenoutput=`behavior {0}` に `let` がないので実装はモジュールの外に置かれますが、出力の `{1}` が `exposing` にありません。
check.inject.hidden.hint=実装が継承する基底クラスは `exposing` に関わらず public で、実装は `apply` を override するときにこの型を書きます。`{0}` を `exposing` に加えてください。
check.surface.field=`{0}` は公開されていますが、そのフィールド `{1}` の型 `{2}` は公開されていません。
check.surface.input=`{0}` は公開されていますが、入力の `{1}` は公開されていません。
check.surface.output=`{0}` は公開されていますが、出力の `{1}` は公開されていません。
check.surface.hint=モジュールが外に出すものは、自分の中に留めているものに依存できません。読み手には `{0}` を指す名前がなく、それを載せたクラスにも触れられません。`{0}` を `exposing` に加えるか、`{1}` を `exposing` から外してください。
check.import.aliastaken=別名 `{0}` は、このモジュールではすでに `{1}` を指しています。
check.import.aliastaken.hint=別名はこのモジュールの中だけでモジュールを呼び替えるものなので、他の何も名乗っていない名前にしてください。
check.import.title=インポート
check.import.unused=`{0}` は import されていますが、この名前では一度も使われていません。
check.import.unused.hint=import リストから外してください。他のモジュールが宣言したものは import しなくても修飾して参照できるので、このリストに名前が載る理由は、下のどこかで修飾せずに書かれていることだけです。
check.qualified.unknownmodule=`{0}` というモジュールはないので、`{0}.{1}` は何も指しません。
check.qualified.notexposed=`{0}` は `{1}` に定義されていますが、公開されていません。
check.qualified.notdefined=`{1}` に `{0}` は定義されていません。
check.temporal.literal=`{0}(...)` は書かれた文字列を取ります（例: `Date("2026-07-01")` / `DateTime("2026-07-01T09:00")`）。
check.temporal.malformed=`{1}` は {0} として読めません。`Date("2026-07-01")` / `DateTime("2026-07-01T09:00")` の形で書いてください。
check.newtype.arity=`{0}` は値を1つ包みますが、{1} 個の引数で適用されています。
check.helper.arity=ヘルパ `let {0}` は引数を {1} 個取りますが、{2} 個で呼ばれています。
check.fn.annotation=束縛 `{0}` は関数です。関数型はヘルパの引数位置にしか書けません。
check.fn.annotation.hint=型注釈を外してください。
check.behavior.collision.data=behavior `{0}` は先頭が大文字化され JVM クラス `{1}` になり（spec 19.5）、data `{1}` と衝突します。クラス名が異なるよう、どちらかをリネームしてください。
check.behavior.collision.behavior=behavior `{0}` と `{1}` はどちらも先頭大文字化で同じ JVM クラス `{2}` になります（spec 19.5）。クラス名が異なるよう、どちらかをリネームしてください。
check.result.collision.data=`{0}` の出力はユニオンなので、sealed interface `{1}` として生成されます（spec 19.8）が、そのクラス名は `data {1}` が既に使っています。
check.result.collision.behavior=`{0}` の出力はユニオンなので、sealed interface `{1}` として生成されます（spec 19.8）が、そのクラス名は `behavior {2}` を大文字化した名前と同じです。
check.result.collision.hint=ユニオンを返す behavior は、自分を大文字化した名前に `Result` を付けた `{0}` で Java に渡ります。その名前を持っている方を改名してください。
check.derive.tuplefield=`{0}` は {1} です。タプルは外部表現を持たず codec を導出できません。名前付き data を使ってください。
check.derive.nocodec=`{0}` の codec を導出できません。{1} は外部表現を持ちません。
check.newtype.optional=`{0}` は `{1}?` を包んでいますが、newtype が包むのは1つの値でその外部表現をそのまま受け取るため、値が無いときに何を表すかがありません。
check.newtype.optional.hint=値そのものを包み（`data {0} = ...`）、`?` は使う側のフィールドに書いてください。

# --- 束縛パターンで値を開く（match の外） ---
check.open.title=開けません
check.open.notnewtype=`{0}` は newtype ではないので、束縛で `{0}(...)` と開くことはできません。
check.open.notnewtype.hint=束縛はアームが1つなので、その型のどの値も必ず持っているもの（newtype が包む値・タプルの要素・レコードのフィールド）しか開けません。形を持つかどうかが分かれる値は `match` で開いてください。
check.open.mismatch=このパターンは `{0}` を開きますが、値は `{1}` です。

# --- 構文エラー（レキサ／パーサ） ---
parse.title=構文エラー
parse.expected=ここでは {0} を期待しましたが、{1} が見つかりました。
tok.name=名前
tok.integer=整数
tok.decimal=小数
tok.string=文字列
tok.typevar=型変数
tok.eof=入力の終わり
lex.typevar=型変数には `'` の後ろに名前が必要です（例: `'a`）。
lex.decimal.m=小数リテラルは Decimal です。`m` を付けてください（例: `{0}m`）。Souther に浮動小数点型はありません。
lex.string.unterminated=この文字列リテラルが行末までに閉じられていません。
lex.unexpected=予期しない文字 `{0}` です。
parse.module=ソースファイルは `module` 宣言から始めます（単一ファイルなら省略できます）。
parse.topdef=トップレベルの定義は `data`・`behavior`・`let` のいずれかで始めます。
parse.behavior.colon=behavior のシグネチャは引数の前に `:` を書きます: `behavior {0} : (...) -> ...`
parse.intrinsic.core=intrinsic はコアの特権です。予約された `souther` 名前空間のモジュールだけが宣言できます。
parse.private.core=private はコアの特権です。予約された `souther` 名前空間のモジュールだけが宣言できます。
parse.typevar.core=型変数 `{0}` は core（予約された `souther` 名前空間）でのみ使えます。ユーザーのモデルは有界のままです。
parse.optional.core=オプショナル型 `T?` は data のフィールド、または core（予約された `souther` 名前空間）でのみ書けます。ユーザーのモデルはそれ以外の場所でオプショナルを名前で呼びません。
parse.optional.core.hint=ヘルパーの戻り型なら、型を書かなければオプショナルのまま推論されます。不在がモデル自身のものなら、0個か1個のリストを返して `List.flatMap` を使ってください。
parse.optional.sum=`?` は単一の型をオプショナルにしますが、{0} ケースの sum に付いています。
parse.vpipe.right=`|>` の右辺は関数呼び出しか関数名でなければなりません。
parse.expr=ここには式が必要です。
parse.toodeep=ここは読み取れる深さを超えて入れ子になっています。検査も整形もコンパイルも同じように再帰で下るため、ここで読むのをやめます。名前の付いた部分に分け、それぞれを単独で読めるようにしてください。値なら束縛に、型なら宣言に切り出します。
parse.unreachable.reason=`unreachable` は到達しない理由を文字列で書きます。`unreachable "twenty insured years cannot precede the age of thirty"` のように書いてください。
parse.block.noresult=ブロックは最後に値となる式を1つ置きますが、このブロックには `let`／`guard` の行しかありません。改行は文の区切りではないので、独立した行に書いた結果式が上の行の続きとして読まれることがあります。
parse.option.positional=Option の中身は位置で束縛します。`| Some as v` ではなく `| Some v` と書きます。
parse.case.positional=ケースの値は `as` で束縛します。`| {0} {1}` ではなく `| {0} as {1}` と書いてください。位置で束縛できる中身を持つのは `Some` だけです。
parse.case.record.direct=レコードのフィールドは直接分解します。`| Some(型 { field })` ではなく `| Some { field }` と書いてください。括弧は newtype を開く形です。
parse.newtype.tuple=newtype は包んだ型の外部表現をそのまま受け取りますが、タプルには外部表現がありません。名前付きの data を包んでください。
parse.newtype.fntype=newtype は包んだ型の外部表現をそのまま受け取りますが、関数には外部表現がありません。
parse.sum.case.generic=sum のケースは宣言済みの名前付き data への参照なので、総称型がケースになることはありません。data を宣言してその名前を書いてください。
parse.examples.for=example だけのファイルは、その example が属するモジュールから始めます: `examples for <モジュール>`
parse.depends.on=behavior の依存句は2語です: `depends on <名前>`
parse.fn.emptyparams=`let {0}` は引数を取らないので `()` は書きません。引数リストのある `let` は関数を、無い `let` は値を定義します。
parse.example.row=example には `|` で始まる行が少なくとも1つ必要です。
parse.fake.row=fake には `|` で始まる行が少なくとも1つ必要です。

# --- examples (E1901-E1920) ---
check.example.title=example
check.example.unknown=`{0}` はこのモジュールの behavior ではないため、この example には対象がありません。
check.example.notrunnable=`{0}` は example として評価できません。
check.example.notrunnable.hint=example は `let` 本体を持つ behavior、またはそれらの `>->` 合成を実行します。ここでは{0}。
check.example.arity=`{0}` は入力を {1} 個とりますが、この行は入力を {2} 個書いています。
check.example.input=`{0}` の入力 #{1} を構築できませんでした: {2}
check.example.expected=`{0}` の期待値を構築できませんでした: {1}
check.example.arm=`{0}` は `{1}` の結果ケースではありません。
check.example.arm.hint=次のいずれかを期待します: {0}
check.example.mismatch=この example は成り立ちません。
check.example.mismatch.hint={0}
check.example.file.only=`examples for` ファイルに書けるのは example と、それが使う fake と、example が名指す値だけです。data・behavior・import・引数を持つ `let` はモジュール本体に書いてください。
check.example.file.declared=`{0}` は `{1}` が既に宣言しているので、ここの行はそちらを読みます。この値を改名するか、これを消してモジュールの宣言を名指してください。
check.example.notarget=`examples for {0}` はコンパイル対象にないモジュールを指しています。
check.example.unreachable=この行は、モデルが起こり得ないと宣言した地点に到達しました: {0}
check.example.unreachable.hint=行がモデルの除外している組み合わせを書いているか、その `unreachable` に書かれた理由が成り立っていないかのどちらかです。
check.example.witness.out=`{0}` を期待する行がないので、`{1}` がその答えを返すことを何も述べていません。
check.example.witness.out.hint=期待値が `{0}` の行を書くか、シグネチャからそのケースを外してください。
check.example.witness.in=`{0}` を `{2}` に渡す行がないので、入力 #{1} に一度も試していないケースがあります。
check.example.boundary={0} = {1} を書いた行がありません。{2} が線を引いているのはそこです。
check.example.boundary.hint=線上の行があって初めて、`<=` で書かれた規則と `<` で書かれた規則が区別できます。
check.example.unreachedarm=`{1}` の `{0}` の腕を、どの行も通りません。
check.example.unreachedarm.hint=この腕が返すもののための行が無いか、そもそも到達できないかのどちらかです。

# --- totality (E2001, サイズ変化停止性) ---
check.totality.title=TOTALITY
check.totality.notstructural=再帰ヘルパ `let {0}` は構造的再帰ではありません。パラメータの真に小さい部分を引数に渡していません。`match`（フィールドやケース）で取り出した部分に再帰するか、`fold` で数え上げるか、`partial` を付けてください。
check.totality.sizechange=再帰ヘルパ {0} は相互再帰ですが、サイズ変化停止性を満たしません。どの再帰サイクルでも真に減少する引数がありません。`match`（フィールドやケース）で取り出した真に小さい部分に再帰するか、全員に `partial` を付けてください。互いに到達するので、一人だけでは足りません。
check.totality.toocomplex=再帰 {0} はサイズ変化解析で全域性を証明するには複雑すぎます。全員に `partial` を付けてください。
check.totality.reachespartial=`let {0}` は主張する停止性を保証できません。`partial` ヘルパ `{1}` に到達しています（{2}）。`{0}` にも `partial` を付けるか、到達しないようにしてください。
check.totality.partialasvalue=`partial` ヘルパ `{0}` が値の位置に書かれています。関数型は停止性を保証しないので、`partial` ヘルパは適用はできますが受け渡しはできません。

# --- invariant discharge (E2010 error, E2011 warning) ---
check.invariant.title=INVARIANT
check.invariant.violation.alone=ここでの `{0}` の構築は不変条件に違反します。組み立てている値を不変条件が拒みます。値を直してください。
check.invariant.violation.assumed=ここでの `{0}` の構築は、到達可能な経路で不変条件に違反します。ここで仮定されていることの下では、組み立てている値を不変条件が拒みます。値を直すか、違反ケースがここに届かないよう経路をガードしてください。
check.invariant.unproven=ここでの `{0}` の構築は不変条件に違反する可能性があります。ガードが不変条件を確立していません。`guard` でガードするか、関係を `{0}`（または入力）の `invariant` に宣言してここで仮定させてください。
check.invariant.reify=入力間の関係がこれを保証するなら、入力データの `invariant` として宣言してください。境界で一度検査され、ここでは仮定されます。

# --- fake (E1908-E1909, E1921, 注入依存のテストダブル) ---
check.fake.missing=`{0}` は `{1}` に依存しますが、その fake が与えられていません。
check.fake.missing.through=`{0}` は {2} を通じて `{1}` に依存しますが、その fake が与えられていません。
check.fake.missing.hint={0}
check.fake.value=依存 `{0}` に与えた fake の値を構築できませんでした: {1}
check.fake.unbuildable=依存 `{0}` に与えた fake を構築できませんでした: {1}
check.fake.miss=fake が入力に対する出力を持っていません: {0}
check.example.disagreement=この行と `fake {0}` の行が、同じ入力に対して違う答えを述べています。
check.example.disagreement.hint=行は {0}、fake は {1} と述べています。
check.example.disagreement.here=`fake {0}` の行
check.example.disagreement.with=この行と `with {0}` が、同じ入力に対して違う答えを述べています。
check.example.disagreement.with.hint=行は {0}、`with` は {1} と述べています。
check.example.disagreement.with.here=`with {0}`

# --- souther run（コマンドラインが behavior を1つ駆動する。コンパイルエラーではない） ---
run.usage.line=使い方: souther run <file.sou> [--behavior <名前>] [--input <json>]
run.usage.unknownoption=不明なオプション `{0}` です。
run.usage.singlefile=run が取る .sou ファイルは1つです。
run.usage.nofile=ソースファイルが指定されていません。
run.usage.optionvalue=`{0}` には値が必要です。
run.behavior.none=このモジュールには `run` で動かせる behavior がありません。動かせるのは、Souther 側に実装があって注入を必要とせず（`>->` なら全ステージがそうであり）、かつモジュールが exposing で公開している behavior です。
run.behavior.several=`run` で動かせる behavior が複数あります。--behavior で1つ選んでください: {0}
run.behavior.runnable=`{0}` は実行できます。`run` で動かせる behavior: {1}
run.behavior.noimpl=`{0}` には実装がありません（Java から注入されます）。`run` で動かせる behavior: {1}
run.behavior.depends=`{0}` は注入依存（{1}）を必要とし、`run` はそれを供給できません。`run` で動かせる behavior: {2}
run.behavior.unknown=`{0}` という behavior はありません。`run` で動かせる behavior: {1}
run.behavior.notexposed=`{0}` は公開されていません。`run` が動かすのはモジュールが公開している behavior です。`exposing` に追加してください。`run` で動かせる behavior: {1}
run.behavior.failed=`{0}` が失敗しました: {1}
run.behavior.unreachable=`{0}` を起動できませんでした。生成された `{1}` に到達できません: {2}。これはモジュールではなくコンパイラ側の欠陥です。
run.pipeline.noimpl=`{0}` はパイプラインで、ステージ `{1}` に実装がありません（Java から注入されます）。`run` はそれを供給できません。`run` で動かせる behavior: {2}
run.pipeline.depends=`{0}` はパイプラインで、ステージ `{1}` が注入依存（{2}）を必要とします。`run` はそれを供給できません。`run` で動かせる behavior: {3}
run.input.missing=`{0}` は入力を {1} 個取ります。--input を渡してください。
run.input.notarray=`{0}` は入力を {1} 個取ります。--input はその長さの JSON 配列にしてください。
run.input.count=`{0}` は入力を {1} 個取りますが、--input には {2} 個あります。
run.input.wrapped=この behavior のパラメータは1つです。--input には値を直接指定し、外側の配列を取り除いてください。
run.input.badjson=--input が正しい JSON ではありません: {0}
run.input.toodeep=--input はこの境界が受け取れる深さを超えています: {0} 以下（JSON の入れ子が {1} レベルを超えました）。data の1レベルが JSON の複数レベルを使うことがあります。
run.decode.failed=入力 #{0} をデコードできませんでした — {1}
run.decode.nodecoder=`{0}` のデコーダを取得できません: {1}
run.decode.unsupported=入力 #{0} の型は `{1}` で、`run` はまだデコードできません（data・プリミティブ・それらのコレクションのみ）。
run.decode.raw=入力 #{0} は予約型 Raw で、`run` はデコードできません。
run.encode.noencoder=出力 `{0}` のエンコーダを取得できません: {1}
run.encode.unsupported=出力の型は `{0}` で、`run` はまだエンコードできません（data・プリミティブ・それらのコレクションのみ）。
run.encode.raw=出力が予約型 Raw で、`run` はエンコードできません。
run.encode.toodeep=出力が深く入れ子になりすぎていて、この境界ではエンコードできません。
run.read.failed={0} を読めません: {1}
run.output.json=出力を JSON にできませんでした: {0}
run.output.toodeep=出力はこの境界が書き出せる深さを超えています: {0} 以下（JSON の入れ子が {1} レベルを超えました）。data の1レベルが JSON の複数レベルを使うことがあります。
run.decode.malformed=入力 #{0} は `{1}` の形になっていません。--input を確認してください。

# behavior が自分に到達する（E1608）
e1608.msg=`behavior {0}` が自分に到達しています: {1}。
e1608.hint=behavior は呼び出し・`depends on`・`>->` の stage のいずれでも再帰しません。再帰はヘルパ `let` として書いてください。そこでは全域性が証明されます。


# --- attempted construction (E2012, `if T(v) as x` / `guard T(v) as x`) ---
check.attempt.title=試みられない構築
check.attempt.notconstruction=`as` は構築した値に名前を付けるものですが、これは構築ではありません。
check.attempt.notconstruction.hint=分岐を決める invariant を持つ構築を書くか、`as` をやめて条件を書いてください。
check.attempt.noinvariant=`{0}` は invariant を宣言していないため、試みる構築は必ず成功し、`else` の値には到達しません。
check.attempt.noinvariant.hint=`{0}` をそのまま構築するか、分岐を決める invariant を与えてください。
check.attempt.armswithoutattempt=これらのアームは invariant の句ひとつずつに答えるものですが、答える対象となる試みる構築がここにありません。
check.attempt.armswithoutattempt.hint=構築を試みる形（`guard T(v) as x else ...`）にするか、`else` に値をひとつ書いてください。
check.attempt.armtwice=句 `{0}` に二度答えています。アームは失敗した句で選ばれるので、二つ目には到達しません。
check.attempt.unknownclause=`{1}` に `{0}` という名前の invariant の句はありません。
check.attempt.unknownclause.hint=名前で答えられる句は {0} です。句の名前は宣言側で付けます（`invariant <名前> = ...`）。名前のない句には `| _ -> ...` で答えます。
check.attempt.clauseunanswered=`{1}` の次の句にアームがありません: {0}。失敗はどれかひとつのアームに届くので、失敗しうる句すべてにアームが必要です。
check.attempt.clauseunanswered.hint=それぞれに答えるか、`else` に値をひとつ書いてください。値ひとつはどの失敗にも答えます。
check.attempt.unnamedunanswered=`{0}` には名前のない invariant の句があり、それに答えるものがありません。
check.attempt.unnamedunanswered.hint=`| _ -> ...` を足すか、宣言側で名前を付けてください。
check.attempt.nothingunnamed=`{0}` の invariant の句はすべて名前付きで答えられているので、`| _ -> ...` は何にも答えません。
check.attempt.nothingunnamed.hint=削除してください。`{0}` が名前のない句を宣言した時点で再び必要になります。

# 値の循環
check.value.cycle.title=値の循環
check.value.cycle=`let {0}` が自分自身を通じて定義されています（{1}）。値は名前を書いた位置に置換されるので、自分に戻る値は置換するものがありません。helper が再帰できるのはメソッドに落ちるからで、値にはその形がありません。
check.surface.value=`{0}` は公開されていますが、それが表す `{1}` は公開されていません。
check.surface.param=`{0}` は公開されていますが、引数 `{1}` の型 `{2}` は公開されていません。

# --- JVM が保持できる大きさ (E2101, E2102, E2103) ---
e2101.title=生成メソッドが JVM の引数スロット上限を超える
e2101.data=`data {0}` は JVM の引数スロットを {1} 必要としますが、生成されるコンストラクタが取れるのは最大 {2} です。`Int` のフィールドは `long` として運ばれるので2スロット、それ以外のフィールドは1スロットです。
e2101.data.hint=`{0}` を小さな data に分割し、フィールドとして持たせてください。
e2101.behavior.parameters=`behavior {0}` は引数を {1} 取りますが、生成される `apply` メソッドが取れるのは最大 {2} です。
e2101.behavior.parameters.hint=引数を data にまとめ、それを取るようにしてください。
e2101.behavior.dependencies=`behavior {0}` は依存を {1} 受け取って組み立てられますが、生成されるコンストラクタが取れるのは最大 {2} です。
e2101.behavior.dependencies.hint=依存をまとめる behavior を用意し、そちらに依存してください。
e2101.helper=`let {0}` は引数を {1} 取りますが、生成されるメソッドが取れるのは最大 {2} です。
e2101.helper.hint=引数を data にまとめ、それを取るようにしてください。
e2102.title=生成メソッドが JVM のコードサイズ上限を超える
e2102.msg=`{0}` に対して生成される `{1}` メソッドはコードが {2} バイトですが、JVM のメソッドが保持できるのは最大 {3} バイトです。
e2102.hint=処理を別の behavior に分けてください。別クラスとして生成されます。あるいは表データをソースの外に移してください。`let` で名前を付けても分割にはなりません。非再帰の helper は生成前に inline されます。
e2103.title=生成クラスが JVM の定数プール上限を超える
e2104.title=ソースが深すぎる
e2103.index=`{0}` に対して生成されるクラスは定数プールのエントリ {1} を参照しますが、クラスファイルが指せるのは最大 {2} です。プールにはクラスが参照する名前・型・リテラルがすべて入ります。
e2103.size=`{0}` に対して生成されるクラスは定数プールのエントリを {1} 必要としますが、クラスファイルが保持できるのは最大 {2} です。プールにはクラスが参照する名前・型・リテラルがすべて入ります。
e2103.hint=表データをソースの外に移すか、処理を別の behavior に分けてください。定数は別クラスのものになります。

check.fake.unchecked.steps=`fake {0}` のテーブルの構築が {1} ステップの予算を使い切ったので、これが述べていることは何も検査されていません。
check.fake.unchecked.steps.hint=テーブルの行が、example に許された回数より多く回るヘルパを適用しています。回数を押さえるか、行が構築するものを小さくしてください。それまで、`fake {0}` のテーブルが構築できるかどうかは不明です。
check.fake.unchecked.deep=`fake {0}` のテーブルの構築が再帰深度の上限 {1} に達したので、これが述べていることは何も検査されていません。
check.fake.unchecked.deep.hint=テーブルが適用するヘルパが、example に許された深さを超えて再帰しています。構造的にするか、行が構築するものを小さくしてください。それまで、`fake {0}` のテーブルが構築できるかどうかは不明です。
check.fake.unchecked.unanswered=`fake {0}` のテーブルの構築が {1}ms 以内に応答しなかったので、これが述べていることは何も検査されていません。
check.fake.unchecked.unanswered.hint=これはテーブルが構築できなかったのではなく、評価が応答しなかったということです。戻ってこなかったのはこのコンパイルが生成したコードではないので、`fake {0}` のテーブルが間違っているとは何も言えません。
check.example.disagreement.unread.steps=この fake と `{0}` に記録された行を照合できませんでした。テーブルの構築が {1} ステップの予算を使い切りました。
check.example.disagreement.unread.steps.hint=テーブルの行が、example に許された回数より多く回るヘルパを適用しています。回数を押さえるか、行が構築するものを小さくしてください。それまで、この fake と `example {0}` の行が一致するかは不明です。
check.example.disagreement.unread.deep=この fake と `{0}` に記録された行を照合できませんでした。テーブルの構築が再帰深度の上限 {1} に達しました。
check.example.disagreement.unread.deep.hint=テーブルが適用するヘルパが、example に許された深さを超えて再帰しています。構造的にするか、行が構築するものを小さくしてください。それまで、この fake と `example {0}` の行が一致するかは不明です。
check.example.disagreement.unread.unanswered=この fake と `{0}` に記録された行を照合できませんでした。テーブルの構築が {1}ms 以内に応答しませんでした。
check.example.disagreement.unread.unanswered.hint=これはモデルが二つのことを言っているのではなく、評価が応答しなかったということです。戻ってこなかったのはこのコンパイルが生成したコードではないので、fake と `example {0}` の行が矛盾しているとは何も言えません。

check.example.unanswered=この行の評価が {0}ms 以内に応答しなかったので、打ち切りました。
check.example.unanswered.helper=この行の評価が {0}ms 以内に応答しなかったので、打ち切りました（`{1}` の呼び出し中）。
check.example.unanswered.hint=これはモデルが停止しないのではなく、コンパイラが判定できなかったということです。戻ってこなかったものは計数されていないので、この行が成り立たないとは何も言えません。よくあるのは、このコンパイルが生成していないコード（jar から読んだモジュール）です。
check.example.stack=この行の評価が、再帰深度の上限 {1} に達する前に JVM のスタックを使い切りました（{0}）。
check.example.stack.hint=再帰を止めるのは深度の上限のはずが、スタックが先に尽きました。深度の上限を下げるか、評価に与えるスタックを大きくするか、ヘルパを末尾再帰に書き換えてフレームを積まないようにしてください。

check.fake.unchecked.stack=`fake {0}` のテーブルの構築が、再帰深度の上限 {1} に達する前に JVM のスタックを使い切ったので、これが述べていることは何も検査されていません。
check.fake.unchecked.stack.hint=再帰を止めるのは深度の上限のはずが、スタックが先に尽きました。深度の上限を下げるか、評価に与えるスタックを大きくするか、`fake {0}` のテーブルが適用するヘルパを末尾再帰に書き換えてください。
check.example.disagreement.unread.stack=この fake と `{0}` に記録された行を照合できませんでした。テーブルの構築が、再帰深度の上限 {1} に達する前に JVM のスタックを使い切りました。
check.example.disagreement.unread.stack.hint=再帰を止めるのは深度の上限のはずが、スタックが先に尽きました。深度の上限を下げるか、評価に与えるスタックを大きくするか、テーブルが適用するヘルパを末尾再帰に書き換えてください。それまで、この fake と `example {0}` の行が一致するかは不明です。

check.example.overbudget.steps=この行の評価が、{0} ステップの予算内で終わりませんでした。
check.example.overbudget.steps.hint=計数が示しているのは評価が終わらなかったことで、モデルが停止しないことではありません。止まらない `partial` な再帰もここに来ますし、あと少しで止まったものも来ます。モデルが実際に停止していてもっと必要なら `souther.example.step.limit` を上げてください。そうでなければループを押さえるか、再帰を構造的にしてください。
check.example.overbudget.depth=この行の評価が、再帰深度の上限 {0} に達しました。
check.example.overbudget.depth.hint=計数が示しているのは再帰が上限より深く進んだことで、底に到達しないことではありません。データが本当にそれだけ深いなら `souther.example.recursion.depth` を上げてください。そうでなければ引数の一部で再帰して底に到達するようにしてください。
