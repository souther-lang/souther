package souther.compiler.partition;

/**
 * One line, as the reading of a comparison found it and the rule that drew it.
 *
 * <p>Held before any border is built, because what a border owes away from its line is a run of the
 * arrangement every rule about that quantity makes together. Turned into a border where the
 * comparison was read, each one knew only its own line: two rules over one arithmetic form left the
 * first one's {@code IN} point running to the end of the order, past the second one, and a row well
 * beyond both answered for it.
 *
 * <p>For the quantities that are not one position's own values. A line on a position is collected
 * the same way and arrives already collected, since the cuts of a position are what an axis carries.
 */
public record LineDrawn(Cutting cuts, LineOrigin by) {}
