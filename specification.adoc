= Souther Language Specification
:toc:
:sectnums:
:sectnumlevels: 3
:xrefstyle: short
:section-refsig: §

[#purpose]
== Purpose

Souther is a JVM-targeted language for transcribing the *specification DSL* of Specification Model-Driven Development (SMDD) into an implementation model with minimal translation.

The spec DSL expresses business rules using only `+data+` (AND / OR / List / `+?+`) and `+behavior+` (`+->+` / `+>->+`). Constraints on the values themselves ("amount is non-negative", "the approver is the applicant's manager"), the confinement of construction paths, and dependency on the outside world are not written there. The spec DSL deliberately leaves these three in comments.

Souther's role is to make those three executable.

[cols="1,1",options="header"]
|===
| What the spec DSL leaves in comments | How Souther makes it executable

| Value constraints (`+// at least 0+`, `+// the approver is the applicant's manager+`) | `+invariant+`

| The invariant "this value has been validated" | Confinement of construction paths (`+decoder+` / `+constructs+`)

| `+// depends: productCatalogue+` / `+// side effect: sendingMail+` | A `+behavior+` with no `+let+` at a leaf; `+depends on+` (an injected Java implementation) at the call site

|===

The structure of the spec DSL (distinction, requiredness, multiplicity, state transition, totality) maps directly onto Souther's types. What the spec DSL cannot guard it divides up: structure by types, value constraints by tests, part of requiredness by the types an implementation is written against. Souther takes that remainder into the implementation model — a value constraint becomes an `+invariant+`, and an outside-world dependency an injected behavior.

[#mapping-principle]
=== Mapping principle

Each construct of the spec DSL maps to Souther as follows.

[cols="1,1",options="header"]
|===
| Spec DSL | Souther

| `+data Amount = Integer // at least 0+` | `+data Amount = Int invariant value >= 0+` (newtype; <<newtype>>)

| `+data X = A OR B+` (A, B are named data) | `+data X = A \| B+` (sum; cases are existing named data)

| `+data Reimbursement = Unit+` | `+data Reimbursement+` (data with no fields; a case named in a sum or a behavior output needs no declaration, <<unit-data>>)

| `+data X = A AND b+` (A is a record, b is an added item) | `+data X = { ...A, b: B }+` (field composition)

| `+List<T>+` / `+T?+` | `+List<T>+` / `+T?+`

| `+behavior f = In -> Out+` (Out has multiple success states) | `+behavior f : (...) -> Out1 \| Out2+` (bare sum)

| `+behavior f = In -> Out OR Failed+` | `+behavior f : (...) -> Out \| Rejected+` (a failure is just data; departure from the mainline is decided by composition)

| `+f >-> g+` | `+f >-> g+`

| `+// depends+` / `+// side effect+` | A `+behavior+` with no `+let+`

| `+// precondition+` | `+guard ... else+` (a business branch, not a contract; <<violation-destination>>)

|===

[#boundary]
=== The boundary with the outside world

Souther does not handle the outside world directly. The boundary is expressed with primitive values, collections, and Decoders / Encoders.

[,text]
----
outside world
  ↓
primitive values / collections
  ↓ Decoder
internal data
  ↓ behavior
internal data
  ↓ Encoder
primitive values / collections
  ↓
outside world
----

The JVM is used as the distribution platform that makes the generated types and behaviors available to Java programs.

[#design-principles]
== Design principles

[#closed-construction]
=== Construction paths of data are closed

A value of a given `+data T+` MAY be constructed only through one of the following.

* the Decoder of `+T+`
* the implementation of a behavior declaring `+constructs T+` (a Souther `+let+`, or an injected Java implementation)
* compiler-generated code

A behavior that merely writes `+T+` as its return type MUST NOT be able to construct `+T+`. The authority to construct belongs to the behavior, not to its implementation. Therefore `+constructs+` is written on the behavior (<<constructs>>); the `+let+` and the Java implementation receive the authority from the same declaration.

A helper `+let+` with no corresponding behavior (<<fn-declaration>>) MAY also construct data. In that case the constructed data MUST appear in the `+constructs+` of the behavior that calls that `+let+`.

The type need not be declared in the same module. A behavior constructs what its `+constructs+` names, and a type another module exposes is built through the same checked entry as any other, so its invariant runs either way. A type a module does not expose cannot be built elsewhere — there is no name for it, and no entry — which is also why arithmetic that would build one is a compile error (<<newtype-arithmetic>>).

[#invariant-on-construct]
=== Invariants are checked on every construction

Every invariant declared on a `+data+` MUST be checked on every construction path. A value that violates an invariant MUST NOT be returned.

The destination of a violation differs inside and outside the boundary: in a Decoder it becomes a failure of Raoh's `+Result+`; inside the domain it aborts (<<violation-destination>>).

[#separate-representation]
=== External representation and internal data are separated

A Decoder constructs internal data from an external representation; an Encoder converts internal data to an external representation. An external representation MUST NOT be cast directly to internal data.

[#behavior-composition-unit]
=== behavior is the unit of composition

Internal computation, outside-world dependency, validation, and conversion are all expressed as behaviors and composed sequentially with `+>->+`. A behavior is a top-level input/output relation with no owning class.

As a type, a behavior states *only the specification*. A simple behavior has no implementation; the implementation is written as a `+let+` (<<fn>>). If none is written, it is injected from Java (<<injected-behavior>>). The exception is `+>->+` composition, which is itself the implementation.

Only behaviors that appear in the spec DSL are behaviors; implementation helpers remain `+let+`. This keeps the list of behaviors aligned with the spec DSL.

[#no-impl-for-outside]
=== No implementation is written for outside-world dependencies

Outside-world dependencies — DB, HTTP, files, clock, ID generation — MUST NOT be implemented in the language. These correspond to the `+// depends:+` and `+// side effect:+` notes in the spec DSL. Only the type is declared in the language.

[,souther]
----
behavior findMember : (id: MemberId) -> Member | NoSuchMember // no let
----

The implementation is injected from Java. Writing no implementation (`+let+`) is what "provided from outside" means (<<injected-behavior>>). A caller writes that name in its `+depends on+` (<<depends-on>>). The distinction between dependency (only reads the outside) and side effect (changes the outside) is documentation of intent; it does not affect the rules of value composition.

[#unmarked-sum]
=== Business results are expressed as bare sums

Every business result is returned as a sum of domain data. A *business* "failure" is not a first-class concept in the language. A behavior's output is a bare sum. The case that a downstream stage does not consume in a `+>->+` composition is what departs from the mainline (<<composition>>). *Unexpected* failures such as out-of-memory or a network drop MUST NOT be listed in the type.

An invariant violation MUST NOT appear here. It is not a business result but a model bug, and its destination is an abort (<<algebraic-types>>).

Multiple independent validation errors are accumulated inside the Decoder (<<case-propagation>>).

[#asymmetric-interop]
=== Java interop is asymmetric

Java MAY use the generated artifacts. Souther MUST NOT call arbitrary Java APIs directly.

[,text]
----
Java   -> Souther artifacts     allowed
Souther -> arbitrary Java API    forbidden (touch the outside only via a behavior with no implementation)
----

[#out-of-scope]
== Out of scope

Souther does not have the following. These are design decisions, not features to be added later.

* [[no-arbitrary-jvm-calls]]arbitrary Java API calls
* mutable state
* exceptions (see below)
* [[no-null]]null
* threads
* asynchronous processing in the language
* inheritance
* structural intersection types (`+A & B+`; see <<union-intersection>>)
* a function stored in a data field (it has no external representation, so it would break decoder / encoder derivation; <<decoder>>, <<encoder>>, <<blocks>>)
* reflection
* macros
* type classes
* higher kinds
* dependent types
* proof by SMT
* a termination prover a model writes measures for — recursion is proven total by size-change analysis, and what that cannot show is opted out with `+partial+` (<<fn-rules>>)
* a custom VM
* a package manager
* a REPL

Because the implementation of outside-world dependencies is delegated to Java, Souther itself has no DB, HTTP, or file APIs.

*On exceptions.* What is forbidden is *using exceptions to control business flow*. Business results are returned as cases of a sum (<<unmarked-sum>>); neither `+throw+` nor `+catch+` can be written.

The reporting path for model bugs, however, is not blocked. An invariant violation *aborts* (<<algebraic-types>>). An abort is not a Souther expression and has no catching syntax, so it cannot be used to control business flow.

A Java Binding at the boundary (the Java implementation of a behavior with no implementation; <<injected-behavior>>) is different. Against a *platform failure* (DB down, network drop, host not responding — anything that is not a domain outcome) it MAY throw an exception; Souther does not fold it into a case but passes it through to the boundary. The language (`+.sou+`) has no exceptions, but the Java side that touches the outside world may throw. The distinction is *domain outcome vs. platform failure*: a business-meaningful result such as "not found" or "insufficient stock" is a case; "infrastructure unavailable" or "not responding" is an exception (<<java-impl-rules>>).

[#modules]
== Source files and modules

The extension is `+.sou+`. Each file defines one module.

Two hold of the names inside it:

* [[a-declaration-is-made-once]]a name is written once where one is expected — a `+let+`, a data, a field of one data, a field of one construction, an entry of one clause list;
* [[a-spelling-answers-one-thing-where-a-value-is-written]]a spelling has one answer where a value is written. A data is written as a value — a unit data is one, a newtype is applied to what it wraps — so a `+let+` and a data of one name would be one spelling with two answers.

[[a-binding-does-not-shadow-a-builtin]]A binding MUST NOT take the name of a built-in value. A binding is in force over a region and everything outside it is unaffected (<<blocks>>), which is what makes shadowing a model's own names harmless; a built-in has no declaration to read instead, so a binding over one takes it away with nothing to point at.

[[a-name-is-in-scope]]A name written in an expression, a type position or a pipeline MUST be in scope there. The nearest name that is gets suggested, since a name out of scope and a name misspelled read the same.

[[a-name-held-as-a-value-is-a-value]]A name held as a value MUST be one. A behavior, a module qualifier and a type are names of other things, and writing one where a value goes is naming the thing rather than a value of it.

[[a-standard-library-function-is-called-qualified]]A standard-library function is called through its qualifier — `+List.map+`, not `+map+`. The qualifier is the only spelling that reaches the library (<<stdlib>>).

Three things hold of a module before anything in it is read:

* [[a-module-is-declared-in-one-place]]a module is declared once. Two files declaring one name, or a file declaring a module that is also on the path, leave two answers to what that name means;
* [[a-core-name-is-not-taken]]a module MUST NOT take a name the core has — the reserved `+souther+` namespace, or a standard-library qualifier such as `+List+` (<<reserved-namespace>>). A data MUST NOT take a qualifier's name either: the qualifier is the only spelling that reaches the library, so a declaration of that name hides it;
* [[every-module-reached-is-on-the-path]]every module a compile reaches — imported, named in a qualified reference, or needed by one that is — MUST be among the modules being compiled or on the path.

[,souther]
----
module example.businesstrip
----

A single file compiled on its own MAY omit the `+module+` header and start directly with a `+data+`
/ `+behavior+` / `+let+`; the module is then named after the file (the file-name stem), or `+Main+`
when compiled from a source string with no file name. A module compiled together with others and reached by
`+import+` MUST write the header — an unnamed module cannot be the target of an `+import+`, so
omitting it there is a compile error.

Keywords and the standard library are in English, but user-defined identifiers (types, fields, behaviors) MAY use Unicode. Because the spec DSL is written in Japanese, business vocabulary can be used as identifiers directly. Identifiers SHALL be *NFC-normalized* before binding and comparison; in Japanese a precomposed character and a combining sequence can look identical, so without normalization names that look the same would diverge in binding. Source is UTF-8.

[#identifier]
=== What a name is made of

An identifier is UAX #31's default form:

....
identifier = XID_Start XID_Continue*
....

The properties are Unicode's, read from `+DerivedCoreProperties.txt+` at the version below and not
re-derived from character categories. Every script's letters begin a name, so `+数量+` and
`+髙島屋+` are names, and so is one written with a character outside the basic multilingual plane —
the language reads a source file by code point, and `+𠮷+` is one character to it as it is one to
`+String.length+` (<<stdlib-string>>).

`+_+` continues a name and begins none: `+foo_bar+` is a name and `+_foo+` is not. On its own `+_+`
is not an identifier at all but a form of its own, written where a value arrives under no name:
inside a binding pattern (<<binding-patterns>>), in the departure arm answering the clauses that
carry no name (<<attempt-departures>>), and as a `+fake+` table's default row (<<example-fakes>>).

`+$+` is not written in a name. It is how the compiler spells the names it generates beside the ones
a model declares — a data's codecs, a behavior's implementation, the locals a desugaring introduces
— and the two sets have to be disjoint: a model able to declare the name a generated class already
has would take that class's place rather than collide with it, and the compile would succeed
(<<no-two-declarations-become-one-class>>).

The Unicode version is *17.0.0*, fixed by the language rather than taken from whatever platform a
compile runs on. Which characters may spell a name decides which sources read at all, and a
published module carries its helpers' bodies as source for an importing compiler to read again
(<<exposed-values>>) — so a compiler answering from its host's tables would make one artifact mean
different things on two machines. Moving to a later Unicode version admits names that were not names
before, and moves the boundary version with it (<<exposed-values>>).

[,souther]
----
data TravelRequest = Drafting | Submitted | AwaitingPreApproval | PreApproved
----

Public elements are listed with `+exposing+`.

[,souther]
----
module example.businesstrip exposing ( TravelRequest, submitTravelRequest, preApprove )
----

`+exposing+` exposes at type granularity. A public data's `+decoder()+` / `+encoder()+` is always public API (<<jvm-codec>>), so member-level narrowing such as `+TravelRequest.decoder+` is not written — there is nothing to narrow.


When an exposed behavior is a `+>->+` composition, its output SHALL be written in `+exposing+` in the form `+name : cases+` (<<declared-composition-output>>). A composition's output is inferred from its stages (<<composition-with-requirements>>), but at the public boundary a signature is required rather than inference, so that the contract used by another module or by Java does not change silently when an upstream stage changes. Omitting it is a compile error (E1605).

[,souther]
----
module example.order exposing ( placeOrder : PlacedOrder | OutOfStock )
----

`+exposing+` is the module's interface description: implementations are inferred, only the boundary is stated explicitly.

[#exposed-values]
==== A value and a helper are published

[[exposing-lists-this-modules-own-definitions]]`+exposing+` lists what this module declares. A name it does not declare is not published by writing it here, and neither is an imported one: a module re-exporting another's vocabulary would put one declaration behind two surfaces.

[[exposing-is-type-granular]]`+exposing+` is granular to the type. A data's derived `+decoder+` and `+encoder+` are published with it and are not named separately — they are how the type crosses a boundary, and a type that crosses one carries them (<<decoder>>, <<encoder>>).

`+exposing+` MAY name a *value* — a `+let+` with no parameter list (<<fn-declaration>>) — and a *helper* — a `+let+` with them — alongside the module's data and behaviors. A limit a rule is written against, the representative record its examples are stated with, and the rule itself are part of what the module offers, and a reader names them instead of writing them out again.

[,text]
----
module example.pipeline exposing ( negotiatingDeal, taxIncluded, Deal, ... )

let negotiatingDeal = Negotiating { ... }
let taxIncluded (amount: Amount) = Amount(amount.value * 110 / 100)
----

An importer names the definition and nothing else: what it is built out of stays the declaring module's business, and does not join the reader's import list. What arrives is *closed* — the body with its own module's definitions already substituted into it — so a definition the importer happens to spell the same way is a different definition and changes nothing (<<qualified-reference>>).

A behavior's own `+let+` is not published, whatever its shape. What a reader of the module reaches is the behavior, which it calls; the `+let+` that satisfies it is an implementation, and the module publishes its specification.

A published definition MAY reach a *recursive* helper. Closing cannot remove one — it is lowered to a method rather than expanded (<<fn-rules>>) — so the helper travels with the body that calls it, under the name of the module that declares it, and so does every recursive helper it reaches in turn. The importing module emits them as its own methods, exactly as it does for a recursive standard-library helper it reaches. Which module declared a helper is its identity and not where its method goes: two modules may declare a recursive helper of one name, and one importer may end up emitting both.

A published value is substituted where it is named and a published helper expanded where it is called, exactly as one of the reader's own is (<<fn-value-semantics>>, <<fn-rules>>). What such a body builds is *not* the reader's to declare in `+constructs+`: publishing the definition is what states that origination, and the reader may have no name for the type. A type of some *third* module that the body happens to build is neither the reader's nor the publisher's to hand over, so that one stays the reader's to declare — which it can, since the module declaring it exposed it (<<closed-construction>>).

A published *value* rests on none of that. What a value builds belongs to its definition whatever module declares the type, so reading one across a boundary answers the same as reading one within a module (<<fn-value-semantics>>). Crossing the boundary is what the paragraph above is about, and a value never needed it.

A published body's source travels in the jar and is read back by the importing compiler (<<published-modules>>). What that source means is therefore part of what a compiled module promises, and is recorded under the same boundary version as the rest of it.

[#exposed-surface]
==== What reaches out may not rest on what is kept

[[an-exposed-signature-names-only-exposed-types]]What a module reaches out with SHALL NOT rest on a type it keeps to itself. An exposed data's field types, and an exposed behavior's input and output, are exposed by the module that declares them. A type carried inside a collection counts — `+List<UserId>+` names `+UserId+` as surely as a bare `+UserId+` does.

An exposed value SHALL NOT stand for a type the module keeps to itself, and an exposed helper SHALL NOT return one or take one — a reader would hold a value it has no name for, or be unable to write the argument it has to pass. What a definition stands for is its *type*, settled from its body (<<fn-rules>>), and not the constructions the body makes on the way: a definition may name a type it never builds (`+let published (n: Int) : List<Hidden> = []+`), and a type its body reached for on the way is its own workings — requiring those would put every inner type back in the reader's import list, which is what publishing the definition was for.

The same holds for an injected behavior — one with no `+let+` — whether or not it is in `+exposing+`, because the abstract base it generates is public regardless (<<java-base-class>>) and the implementation writes both types where it overrides `+apply+`.

Otherwise a reader is handed a value it has no name for, and the generated classes carry a public surface over classes it may not touch. It is reported where the module decides it, not at each reader that runs into it.

This does not reach the *cases* of a sum, or of an output written as more than one case. Those are generated as a union interface that is public regardless (<<jvm-anonymous-union>>), and a case reaches a reader through the decoder or through the `+protected+` factory rather than by being named — which is what E1305's unit-data allowance rests on. A module keeping a case to itself therefore stays a decision about the union, not about this rule.

[#imports]
Imports SHALL be listed explicitly. Wildcard imports are forbidden.

[[module-dependencies-are-acyclic]]A module depends on every module it imports and on every module it names in a qualified reference (<<qualified-reference>>). Those dependencies MUST have no cycle.

[[imports-are-used]]An entry on an import list MUST be written bare somewhere below it. What another module declares is reachable qualified whether or not it is imported, so an entry earns its place only by being used under the name it brings in.

[,souther]
----
import example.employee ( Employee, EmployeeId )
----

The standard library is imported in the same form: the qualifier — `+List+`, `+Map+`, `+Set+`, `+String+`, `+Bool+`, `+Int+`, `+Decimal+`, `+Date+`, `+DateTime+`, `+Option+` (<<reserved-namespace>>) — as the name, and the functions used listed. Once imported, they MAY be written unqualified (<<stdlib>>).

[,souther]
----
import List ( map, filter, fold )
import String ( trim, split )
----

Qualified access (`+List.map+`) always works without importing, so `+import+` only elides the qualifier. Importing the same bare name from two modules is a compile error, reported at the second import line: writing both `+import String (length )+` and `+import List (length )+` says `+length+` is exposed from both. Import one and qualify the other call (`+String.length(...)+`). As with the wildcard prohibition, no name is delivered implicitly. A name the library does not have is a compile error at the import line, not at the first use.

An import says which names MAY be written without their qualifier. It does not say what one of them means where it is written: a binding in force means what it binds. So `+import List ( map )+` and a parameter named `+map+` may sit in one module, and inside that parameter's scope `+map+` is the parameter. What a name means is settled once, wherever it is written — in a body, in an `+invariant+`, in an `+example+` row, in a `+fake+` — and an import is the last thing consulted.

A declaration is not a binding in force and does not shadow an import. Importing a name the module declares itself — a `+data+`, a `+let+`, or a behavior written with that name — is a compile error reported at the import line, not a shadowing: one spelling would stand for two things, and nothing at a call site could choose between them. Rename the declaration, drop it, or drop the import and write the library name qualified. Qualified access is always available, so a module MAY declare `+let not+` and still call `+Bool.not(...)+`; it is `+import Bool ( not )+` beside that declaration that is refused.

[#published-modules]
=== Importing a module another project compiled

[[a-published-module-agrees-with-this-compiler]]A module read from compiled output MUST have been compiled by a Souther that agrees with this one about what an importing module may reach. What it published is read back as declarations, so a disagreement about the surface is a disagreement about the text of this compile.

[[a-published-module-carries-what-it-says-it-declares]]A published module MUST carry every declaration it says it publishes. What it publishes is read back as declarations, and a declaration with no class behind it is not one.

[[a-published-module-can-be-read-back]]A module used from compiled output MUST be readable back into the published surface this compiler understands. This is the boundary condition an importing project depends on, and the rules above are among the ways an artifact fails to meet it — a class carrying metadata this compiler cannot read, a boundary revision that does not agree, declarations of a module other than the one they were published under, a declaration whose class is absent, published text this compiler does not parse, an `+import+` line the published surface needs and this compiler cannot read. An artifact that fails any of them is reported as one that cannot be read (<<e1509>>), with the reason said under that report; the rules themselves belong to the project that published it, and are not obligations of the project reading it.

Which import lines those are is settled by what the published surface needs, and not by what the artifact carries. A compiled module carries every `+import+` line it was written under. Reading one back takes only the lines the declarations it publishes name: a module's implementation does not cross, so an import only its bodies used names nothing an importing project reads, and taking it would make that project put a module on its path to read declarations that never mention it. A line left out for that reason is a line this compiler never reads, and so is never one it cannot read.

A module MAY be imported from the compiled output of another project, with no `+.sou+` of it in this
one. A compiled module carries what it declares — its `+data+` definitions, its behavior
signatures, its `+exposing+` line and the imports it was written under — on the classes it generated,
as the source that declared it. An `+import+` naming no module among the sources being compiled is
resolved against the compiled modules on the class path.

Nothing is declared beyond the dependency itself: under the annotation processor the class path is
the compile classpath, and the CLI takes `+-cp+` / `+--class-path+`.

[,text]
----
souther compile app/order.sou -d out-app -cp out-lib
----

What crosses that boundary is what crosses a boundary within one compilation: `+exposing+` is the
gate, and an imported type may not be a case of a sum declared here (<<e1606>>). An imported type in
a behavior's output union is unaffected — it joins through a bridge case this module emits
(<<bridge-case>>), so nothing of the imported module has to be regenerated for it. A module read this
way is not generated again — its classes and its examples belong to the build that produced them.

A compiled module records the version of what an importing module reaches. A module whose version is
not this compiler's is refused, naming the compiler that built it, rather than read and
misunderstood. A module compiled here SHALL NOT also be on the class path, and a module on the class
path that needs one absent from it is reported against the path.

A module on the class path needs another when what it declares names it — in a field's type, a
spread, an invariant, a helper an invariant calls, or an exposed composition's output. A `+let+` body
is not published, so an `+import+` only a `+let+` used is not one the importing project has to
satisfy: a dependency's implementation-only dependencies need not be on the class path.

[#qualified-reference]
=== Qualified reference

A type is also reachable through the module that declares it, written as the module name and the type name.

[[a-reached-name-is-declared-by-its-module]]A name reached across a module boundary MUST be one that module declares, and [[a-reached-name-is-exposed-by-its-module]]it MUST be one that module exposes. The two are separate: a name the module never declared is a mistake about what is there, and a name it declared and kept is a mistake about what it publishes, which is the module's own decision to make (<<exposed-surface>>). Both hold whether the name arrives by `+import+` or is written qualified.

[[an-imported-name-denotes-one-thing]]A name an `+import+` brings in MUST denote one thing here. Two modules exposing it, an alias already in use, or this module's own declaration of that spelling each leave the reader with a name and no single answer; reaching one of them qualified is what says which. Like the standard library's `+List.map+`, this needs no `+import+`: the `+import+` line only lets the name be written bare.

[,souther]
----
data Line =
    { quantity: example.counting.Amount
    , price: example.pricing.Amount
    }
----

This is what a module does when two it depends on declare the same type name — `+Amount+` in each of two bounded contexts, which is normal and not the integrator's to rename. A qualified reference MAY be written wherever a type is written or named (<<a-type-is-written-in-a-type-position>>, <<binding-patterns>>, <<match>>). A sum's case list is the exception: its cases are declared with it (<<sum-data>>), so there is no other module to name.

Which spelling names a type is not something a check has an opinion about. A bare `+Amount+` an `+import+` brings in and a qualified `+example.pricing.Amount+` are one type, so a clause and a body that spell it differently agree, and a pattern opens the type it names however the field that holds it was written.

`+import ... as+` names a module locally, and the alias may then carry the reference. The alias holds only in the module that writes it, and it MUST be a name nothing else answers to there — not another alias, not a module of this compilation, not a standard-library qualifier.

[,souther]
----
import example.counting as Counting
import example.pricing as Pricing ( taxRate )

data Line =
    { quantity: Counting.Amount
    , price: Pricing.Amount
    }
----

Both parts of an `+import+` are optional: `+import example.pricing as Pricing+` only renames, and `+import example.pricing+` only writes the dependency down. What a qualified reference reaches is still what the declaring module exposes — qualifying a name that module keeps to itself is a compile error, as is qualifying with a name that is no module of this compilation.

A behavior of another module is named the same way, as a `+>->+` stage and as a `+depends on+`. What the qualifier says is which module the behavior comes from, which is what an `+import+` says, so a qualified stage or requirement needs no import line either.

[,souther]
----
behavior ship = inventory.allocate >-> makeInvoice
behavior notify : (order: PlacedOrder) -> Notified
    constructs Notified
    depends on messaging.sendMail
----

A behavior is the module that declares it and its name, so two modules declaring one name declare two behaviors. A qualified reference says which of them is meant, and a module may reach both — as a type of one name from two modules is reached, and for the same reason. A behavior this module declares is one of the two: `+inventory.allocate+` and an `+allocate+` written here are different stages, and each is held to its own declaration.

[,souther]
----
behavior fromPrimary = primary.allocate >-> confirm
behavior fromSecondary = secondary.allocate >-> confirm
----

What cannot be told apart is a bare spelling two `+import+` lines both claim: written here, the name has two answers and neither line is the one to prefer, which is a compile error (<<e1508>>). Qualifying one of them is what says which is meant, and is the fix that error names.

A behavior injected into a construction is held in a field of the generated class (<<jvm-behavior>>), and what that field is called is the class's own — not the behavior's name. So a composition may name two behaviors of one name as stages and require both, and the two are two fields.

A `+depends on+` clause is not written that way. What it names becomes the implementation's trailing parameter, under that name, so two entries of one name have one name to be written under and the second is a name listed twice (<<e1011>>). Naming the two apart is the module's to do — declaring a behavior of its own for each, which is what an injection target is for (<<injected-behavior>>) — or reaching one of them through a composition, where nothing is named again.

A dependency is a dependency however it is written, so a cycle closed by a qualified reference is the same compile error (<<e1501>>) as one closed by an `+import+`.

`+exposing+` lists what a module declares and does not re-export a name it imported. A module downstream of an integration module names an upstream type through the module that declares it, so the integration module translates a type it consumes rather than republishing it.

[#grammar]
== Grammar

Where each of the language's written forms is given — its concrete syntax, and where to look each part of it up.

A form is stated with the construct it belongs to, once, where a reader of that construct meets it. This is the index that reaches those statements and not a second copy of them: a form written here as well would be a second place to keep in step, and the two would disagree the first time one of them moved.

A row is a form, or a rule about one, together with what kind of thing it is and the section that gives it. The kinds divide in two. A token, a delimiter and an operator are written with what the compiler holds a set of, so what is listed here is checked against it: the reserved words against the words the lexer reserves, and the symbols of the three rows together against the spellings the syntax kinds carry. A production, a constraint and an association are the specification's own: no set the compiler enumerates says which `+then+` an `+else+` closes, and those rows are held by whatever states them.

Which of the three a symbol is listed as is the specification's own too. The compiler holds no set that says `+->+` joins two types where `{plus}` computes from two numbers, so nothing checks the column, and reading a row for what a symbol does is reading this document. An editor asks something else — which symbols a reader wants told apart from the names and the brackets around them — and answers it for itself, so what it paints as an operator is neither this column nor derived from it.

The full formal grammar (BNF) is deferred (<<future-work>>).

[cols="3,1,2",separator=!,role="syntax-inventory"]
!===
!Form or rule !Kind !Given in

!a name !lexical form !<<identifier>>
!`+_+` !token !<<identifier>>
!a string literal !lexical form !<<string-literal>>
!an integer and a `+Decimal+` literal !lexical form !<<numeric-literal>>
!a `+Date+` and a `+DateTime+` literal !lexical form !<<temporal-literal>>
!a type variable (`+'a+`) !lexical form !<<type-variables>>
!a comment !lexical form !<<comment>>
!the reserved words !token !<<reserved-words>>
!`+==+` `+/=+` `+<+` `+<=+` `+>+` `+>=+` !operator !<<reserved-words>>
!`+&&+` `+||+` !operator !<<reserved-words>>
!`{plus}` `+-+` `+*+` `+/+` !operator !<<reserved-words>>
!`{plus}{plus}` !operator !<<reserved-words>>
!`+{+` `+}+` `+(+` `+)+` `+[+` `+]+` !delimiter !<<delimiters>>
!`+:+` `+,+` !delimiter !<<delimiters>>
!`+=+` !delimiter !<<delimiters>>
!`+|+` !delimiter !<<delimiters>>
!`+->+` !delimiter !<<delimiters>>
!`+.+` and `+...+` !delimiter !<<record-literal>>
!`+?+` !delimiter !<<optional>>
!`+>->+` !delimiter !<<sequential-composition>>
!`+|>+` !delimiter !<<pipe>>
!a module header !production !<<modules>>
!an `+import+` !production !<<imports>>
!an `+exposing+` clause !production !<<exposed-values>>
!a qualified name !production !<<qualified-reference>>
!a product `+data+` and its fields !production !<<product-data>>
!a field spread !production !<<field-spread>>
!a sum `+data+` !production !<<sum-data>>
!a unit `+data+` !production !<<unit-data>>
!a newtype !production !<<newtype>>
!an `+invariant+` clause !production !<<invariant-declaration>>
!a behavior's signature !production !<<behavior-io>>
!a `+constructs+` clause !production !<<constructs>>
!a `+depends on+` clause !production !<<depends-on>>
!an `+ensures+` clause !production !<<ensures>>
!a definition (`+let+` at the top level) !production !<<fn-declaration>>
!a binding (`+let+` in a block) !production !<<let>>
!a lambda and a block !production !<<blocks>>
!a record literal !production !<<record-literal>>
!a list literal and a guard comprehension !production !<<stdlib-list>>
!a tuple !production !<<tuple>>
!a type in a type position, a function type among them !production !<<a-type-is-written-in-a-type-position>>
!an `+if+` !production !<<if>>
!a `+match+` and its arms !production !<<match>>
!a `+guard+` !production !<<guard>>
!an attempted construction !production !<<attempted-construction>>
!a departure per clause !production !<<attempt-departures>>
!an `+unreachable+` !production !<<unreachable>>
!a binding pattern !production !<<binding-patterns>>
!a `+>->+` composition !production !<<sequential-composition>>
!an `+example+` and its rows !production !<<examples>>
!a `+fake+` !production !<<example-fakes>>
!an `+examples for+` file !production !<<example-placement>>
!an `+if+` is written with both of its branches !constraint !<<if>>
!a string literal ends on the line it began on !constraint !<<string-literal>>
!a `+,+`-separated list MAY carry a trailing `+,+` !constraint !<<delimiters>>
!an anonymous union is not written in a narrow type position !constraint !<<an-anonymous-union-is-not-written-in-a-narrow-type-position>>
!a postfix `+?+` is not written inside another type !constraint !<<an-optional-is-not-written-inside-another-type>>
!`+|+` binds more loosely than `+->+` !association !<<delimiters>>
!`+->+` is right-associative !association !<<delimiters>>
!`+|>+` binds most loosely, and to the left !association !<<pipe>>
!`+>->+` is associative !association !<<type-routing>>
!which `+match+` an arm belongs to !association !<<match>>
!===

[#reserved-words]
== Reserved words

[,text]
----
module   import    exposing
data     invariant
behavior constructs depends  ensures
match    with      as
let      if        then     else
guard    unreachable
true     false
----

Operators and symbols: `+=+` `+|+` `+>->+` `+|>+` `+?+` `+->+` `+.+` `+...+` `+[+` `+]+`

Comparison, logic, arithmetic: `+==+` `+/=+` `+<+` `+<=+` `+>+` `+>=+` `+&&+` `+||+` `{plus}` `+-+` `+*+` `+/+` (`+/+` is division; `+/=+` is inequality; negation is not a prefix operator but `+not+` in `+souther.bool+`; <<stdlib-bool>>).

Lists: `+[+` `+]+` (literal and guard comprehension), `{plus}{plus}` (concatenation, of two lists or two strings; <<stdlib-list>>).

`+>->+` is associative (<<type-routing>>). `+|>+` is the value pipe, threading a value through functions — `+e |> f(a)+` is `+f(a, e)+` (<<pipe>>).

`+->+` is the arrow. It joins types — in a behavior declaration (<<behavior>>) and in a function type, wherever a type is written (<<delimiters>>) — and it introduces an anonymous body — in a `+match+` case (<<match>>) and a lambda (<<blocks>>). There is one kind of arrow. `+=+` binds a name to a right-hand side (whether the right-hand side is a type or a term is decided by the rules of <<behavior>>).

`+depends on+` is two words, and the `+on+` is not a reserved word. It is read as the second word of the keyword only where it follows `+depends+`, as the `+for+` of `+examples for+` is read after `+examples+` (<<example-placement>>), so a field, a parameter or a behavior MAY still be named `+on+`.

`+decoder+` / `+encoder+` / `+discriminate+` are not reserved words: the mapping to external representation is not syntax but is derived from a data's shape (<<decoder>>, <<encoder>>). `+error+` / `+success+` / `+failure+` are not reserved words either: a *business* failure is neither a special type nor syntax but ordinary domain data, and whether it departs from the mainline is decided by composition (<<unmarked-sum>>, <<behavior>>, <<composition>>).

There is no type name that carries an invariant violation. A violation inside the domain aborts, so no value needs to be carried (<<algebraic-types>>, <<violation-destination>>).

[#comment]
=== Comments

A comment begins at `+//+` and runs to the end of the line. The line ends it, and nothing else does.

[,text]
----
let a = 1   // the rest of this line is a comment
----

There is no block comment. `+/*+` is not a form: the characters read as the operators they are, and a text written that way does not compile. A `+//+` inside a string literal is part of the literal (<<string-literal>>), the literal having begun first.

[#delimiters]
=== Delimiters and disambiguation

The full formal grammar (BNF) is deferred (<<future-work>>). The delimiters and precedence needed for reading and writing are fixed here.

What a text MUST be for the compiler to read it is stated with each construct, and a text that is not is refused where the reading stops. Where the reading can stop is kept apart, because a reader looking one up is asking about a different part of the language, and each place names where the forms it is about are given (<<grammar>> indexes them all):

[role="reading-stops"]
* [[declaration-syntax]]a declaration MUST be written in the form given for it: the module header (<<modules>>), a top-level `+data+` (<<product-data>>, <<sum-data>>, <<unit-data>>, <<newtype>>), a behavior's signature (<<behavior-io>>), a top-level `+let+` (<<fn-declaration>>), a `+constructs+` clause (<<constructs>>), a `+depends on+` clause (<<depends-on>>), an `+ensures+` clause (<<ensures>>);
* [[expression-syntax]]an expression MUST be written in the form given for it, and a block MUST end in one (<<blocks>>, <<block-expression>>): a literal below, a record literal (<<record-literal>>), a list (<<stdlib-list>>), a tuple (<<tuple>>), an `+if+` (<<if>>), a `+match+` (<<match>>), a `+guard+` (<<guard>>), an `+unreachable+` (<<unreachable>>), an application (<<fn>>), a pipe (<<pipe>>);
* [[pattern-syntax]]a pattern MUST be written in the form given for the thing it destructures (<<binding-patterns>>, <<match>>);
* [[examples-syntax]]an `+example+`, a `+fake+` and an `+examples for+` file MUST be written in the form given for them (<<examples>>, <<example-fakes>>, <<example-placement>>);
* [[literal-syntax]]a literal MUST be written in the form given for its type: an integer and a `+Decimal+` (<<numeric-literal>>), a `+String+` (<<string-literal>>), a `+Date+` and a `+DateTime+` (<<temporal-literal>>);
* [[an-anonymous-union-is-not-written-in-a-narrow-type-position]]an anonymous union MUST NOT be written as a data field's type, a type argument, or a tuple's member. Those three read the narrower type production, so a `+|+` following the type is recognized in order to be refused and is not built as part of the type standing there — which is what lets the refusal name the form rather than the delimiter the form displaced. Where an anonymous union *is* read, and the one position that reads one and refuses it, are <<union-intersection>>;
* [[an-optional-is-not-written-inside-another-type]]a postfix `+?+` MUST NOT be written inside another type — as a type argument or a tuple's member. `+?+` marks where an optional is *made*, which is a data field being given its value (<<optional>>), and no position inside a type is one. This is not a privilege the core keeps (<<core-privileges-stay-in-the-core>>): `+List<T?>+` is written nowhere, and an optional a collection holds is named `+List<Option<T>>+`;
* [[the-source-is-made-of-tokens]]the text MUST be made of tokens the language has (<<reserved-words>>, <<type-variables>>). This one is answered before anything is parsed, so it says nothing about where in the language the text was — a character that begins nothing, and a `+'+` with no name after it, are the two ways to reach it.

* Both `+data+` declaration fields and record-literal `+TypeName { ... }+` fields are `+,+`-separated (<<product-data>>, <<record-literal>>). A declaration gives a type, so `+name: type+`; a literal binds a value, so `+name = value+` (`+:+` = has this type, `+=+` = is defined as this). A `+...+` spread is an element that composes types but appears in the same comma sequence as fields (<<field-spread>>). An `+invariant+` is a clause placed after the type body (`+{ ... }+` or `+= Y+`) and is not part of the field list (<<invariant>>). A clause's name follows the same rule as everything else named with `+=+` — it is defined as that rule, so `+invariant nonEmpty = List.length(value) >= 1+` (<<invariant-clause-name>>).
* Argument lists and the enumerations in `+constructs+` / `+depends on+` are `+,+`-separated (<<behavior-io>>, <<constructs>>, <<depends-on>>).
* Every `+,+`-separated list — declaration and record-literal fields, parameter and argument lists, tuple elements, list literals, and the `+constructs+` / `+depends on+` name lists — MAY carry a single trailing `+,+` before its closing delimiter. It is optional and has no meaning. Where this spec breaks such a list across lines, a `+data+` product body opens each line after the first with its `+,+` (<<product-data>>) and every other list closes each line but the last with its own; neither writes the trailing `+,+`. The convention is the construct's, not the document's.
* `+|+` is the case separator of a sum and binds more loosely than `+->+`. `+(x: T) -> A | B+` reads as `+(x: T) -> (A | B)+` (the whole output is a sum). Where an anonymous `+|+` is read as a type is the type position's answer (<<a-type-is-written-in-a-type-position>>), and a behavior's parameter refuses one it reads (<<a-parameter-names-one-type>>).
* `+>->+` is the stage separator of a pipeline: not a binary operator but a flat n-ary sequence. It is associative (<<type-routing>>). Only a behavior name MAY appear in a stage position; parentheses such as `+(f >-> g)+` MUST NOT be written.
* `+|>+` is the value pipe, binding most loosely and left-associatively. `+a |> f |> g+` reads as `+g(f(a))+`. The left side enters the last argument of the right-hand call and is expanded to a call at parse time (<<pipe>>). This is distinct from the `+>->+` that composes behaviors; it threads the value of an expression.
* `+?+` is a type postfix (`+EmployeeId?+`) and desugars to `+Option<EmployeeId>+` (<<optional>>).
* `+.+` is field reference; `+...+` is spread (<<record-literal>>).
* [[a-type-is-written-in-a-type-position]]A type is written in a *type position*, and these are the positions: a data field and a newtype's base; a behavior's parameter and its output, and the output declared for a `+>->+` composition, written either on the composition or in `+exposing+` (<<declared-composition-output>>); a helper's parameter and its declared return; a local binding's annotation; and, inside another type, a type argument, a tuple's member, and a function type's parameter and its result. A lambda's parameter is a pattern and takes no type (<<blocks>>). A type is also *named* in three places that are not type positions — a `+match+` arm's case, a `+constructs+` entry, and a constructor pattern — and each of those takes the name of a declared type rather than a type form.
* What a type position reads is not the same in every position. Where a *whole* type is read — a behavior's parameter and its output, a composition's declared output, a helper's parameter and its declared return, a local binding's annotation, and a function type's parameter and its result — the forms are a named type, a stdlib generic type (such as `+List<T>+`), a tuple type, a function type `+(A) -> B+`, an anonymous output sum (`+A | B+`), and a postfix `+?+`. A data field reads all of those but the anonymous sum (<<an-anonymous-union-is-not-written-in-a-narrow-type-position>>). A type argument and a tuple's member read neither the anonymous sum nor the postfix `+?+`, so `+List<A | B>+` and `+List<T?>+` are not written at all (<<an-optional-is-not-written-inside-another-type>>). A newtype's base is narrower again, below. What a position *reads* is what it builds as the type standing there; a form it does not read is still recognized where it is written, and named, so that what the author reached for is what they are answered about. Recognizing a form in order to refuse it is neither admitting it nor building it. A function type's parameters and result are themselves whole types, so `+->+` is right-associative — `+(A) -> (B) -> C+` returns a function and `+((A) -> B) -> C+` takes one — and `+(A) -> B | C+` still reads as `+(A) -> (B | C)+` by the `+|+` rule above. `+(A, B) -> C+` takes two parameters and `+((A, B)) -> C+` takes one tuple, as a lambda's parameter list does. A parenthesised single type is grouping.
* Writing a type in a position is not being admitted by it. What a position requires of a type is asked of the type: a data field, a newtype base and a behavior's input and output require an *external representation*, so none of them takes a type that carries at any depth something with none (`+List<(Int) -> Bool>+` included; <<what-has-no-external-representation>>); `+==+` / `+/=+`, a `+Set+`'s element and a `+Map+`'s key require *equality*; `+sort+` and a `+sortBy+` key require an *ordering* (<<equality>>).
* A newtype's base (`+data X = Y+`) is a type position narrowed to what `+X+` can take as its own external representation: no tuple, no function type, and no `+?+` (<<newtype>>). A sum's cases are names of declared data, so a case is never generic: `+data X = List<A> | B+` is not a sum (<<sum-data>>).

[#external-representation]
== External representation

What crosses a decoder/encoder boundary is decided by representation, and these obligations fall out of it:

* [[what-crosses-the-boundary-has-an-external-representation]]a value that crosses MUST have an external representation (<<what-has-no-external-representation>>). What has none cannot be a behavior's parameter or output, a data's field, or anything a codec would be derived from;
* [[a-parameter-names-one-type]]a *behavior's* parameter MUST name a single type. An anonymous union is the shape of one behavior's answer and nobody names it (<<union-member>>), so a caller has no name to write and the input takes a declared one. This is a rule about what crosses, and not about where the form is read. A helper's parameter is not a boundary and takes one (<<a-type-is-written-in-a-type-position>>);
* [[an-optional-does-not-stand-in-a-boundary]]an optional MUST NOT stand anywhere in a behavior's boundary shape — neither as a parameter nor as the output, and at any depth inside a collection either. A boundary carries the model's own vocabulary, and absence there is owned by the data that holds it, on a `+?+` field, or by a sum the model names (<<optional>>). The shape is read down to a named type and no further: a data carrying a `+?+` field crosses as that data;
* [[a-map-that-crosses-is-keyed-by-a-text-key]]a `+Map+` that crosses MUST be keyed by something a key can be written as — `+String+`, a temporal (`+Date+` / `+Time+` / `+DateTime+` / `+Instant+`), an enumeration (a sum every one of whose cases is a unit data), or a newtype over any of those, at any depth (<<collections>>). The boundary writes a key as text, and a key with no text is a key nothing can address;
* [[a-boundary-carries-the-models-own-vocabulary]]a named type that crosses MUST be one a model declares. The language declares vocabulary of its own — the case a division by zero answers with, the rounding mode a `+Decimal+` operation takes, the reserved `+Raw+` — and those name what an operation can answer or take, not what a behavior publishes. A model that has such a case to report declares its own and names it (`+-> Int | Undivided+`), which is what keeps the behavior's protocol independent of how the language accounts for itself;
* [[a-collection-member-supports-equality]]a `+Set+`'s element and a `+Map+`'s key MUST support equality. Both ask whether two of them are the same, and a function has no value to compare (<<equality>>).

The external representation handled by Decoders and Encoders is not Souther's own types but the neutral value model of the boundary library Raoh (a tree of scalar / List / Map). Souther owns the domain's types, invariants, and behaviors; the external representation and its parsing and generation are delegated to the peripheral library Raoh (<<separate-representation>>, <<decoder>>).

From a data's shape, Souther derives the decode side per input source (bare values / Map = raoh-core, JSON `+JsonNode+` = raoh-json, jOOQ `+Record+` = raoh-jooq) and the encode side to a neutral Map (raoh-core). The decode skeleton (fields, keys, discriminator) is common across sources; only the terminal field access varies per source. JSON output serializes the Map at the boundary (`+objectMapper.valueToTree+`, etc.; <<codec-generation>>).

An external representation may contain null, but internal data forbids null. null exists only at the boundary (Raoh's input) and MUST NOT pass through a Decoder into the domain.

[[absence-is-written-as-null]]`+null+` is what a boundary writes for an absent value, wherever the value would have stood: `+None+` is written `+null+` and read back from `+null+`, and `+Some(v)+` is written the way `+v+` is written. An object's field has a second form for holding nothing, which is not being there at all, and a field takes that one — `+None+` omits the key, and a missing key reads back as `+None+` the same way `+null+` does (<<decoder-derivation>>, <<encoder-derivation>>). A collection's element and a `+Map+`'s value take `+null+`. A map's value does not omit, because a boundary map's keys are data rather than a declaration: dropping a key drops the entry and changes which keys there are, where omitting a field's key changes nothing about which fields the data has.

[[what-has-no-external-representation]]Three things have no external representation, and a position that requires one refuses each of them at any depth — inside a `+List+` or a `+Map+` written there as much as standing on its own. A *tuple*, whose members have no names to be written under (<<tuple>>). A *function*, which is code and has no value on the wire (<<fn>>). And an *optional whose own value is an optional*: absence has one form wherever it stands, so `+None+`, `+Some(None)+` and `+Some(Some(v))+` are three values facing two forms, and a field's omitted key is not a third — it is what absence looks like at a field and says what `+null+` says (<<optional>>). This is the list the boundary and the derived codec both read, and it is where a rule about one of them is written down.

[#builtin-types]
== Built-in types


[[a-value-has-the-type-its-position-requires]]A value MUST have the type its position requires (<<a-type-is-written-in-a-type-position>>). This is the rule most others here are a special case of, and it is what a diagnostic falls back to where no narrower one applies.

[[a-lists-elements-have-one-type]]A list literal's elements MUST all have one type. A list is `+List<T>+` for a single `+T+`; a sequence of several types is a list of a sum they are cases of.

[[an-operator-takes-the-types-it-is-defined-for]]An operator MUST be given the types it is defined for: `+++++` joins two lists or two strings, unary minus takes an `+Int+` or a `+Decimal+`, a comparison takes two ordered values of one type, and `+==+` takes values that have something to compare — a function does not (<<equality>>).

[[a-condition-stops-when-its-answer-is-settled]]`+&&+` and `+||+` evaluate their operands left to right and stop as soon as the answer is settled: a false left operand is the answer of a `+&&+` and a true one is the answer of a `+||+`, and in neither case is the right operand evaluated. Which operands run is part of what the operators mean and not a choice a backend makes — `+/+` aborts on a zero divisor and `+Int+` overflows (<<stdlib-int>>), so `+x /= 0 && 100 / x > 1+` is how a condition narrows what its right side may compute, and evaluated eagerly it would abort at the value it exists to exclude.

[[a-tuple-pattern-matches-its-tuple]]A tuple pattern MUST be written on a tuple, and MUST bind as many names as the tuple has elements.

[[a-field-read-is-declared-by-the-value]]A field read MUST be a field the value has. A sum has no fields of its own — its cases do — and it exposes one only where every case spreads the data declaring it (<<sum-data>>); which case a value is is not known until it is opened.

[[a-temporal-value-is-written-as-a-literal]]A temporal literal MUST have its text spelled out (<<temporal-literal>>), so the text can be read at compile time and refused there rather than at run time. This says how a temporal is *written*; it does not say how one is *reached*. A temporal built from values a model holds comes from `+Date.fromParts+` / `+Time.fromParts+`, which answer a case where the parts name no such moment (<<a-temporal-built-from-parts-is-refused-where-the-parts-name-none>>).

[[a-temporal-built-from-parts-is-refused-where-the-parts-name-none]]`+Date.fromParts+` and `+Time.fromParts+` MUST answer `+NotADate+` / `+NotATime+` where the Ints they are given name no day and no time of day, and MUST NOT normalise. February the 30th is not the 2nd of March here, and the 25th hour is not one o'clock the next day. They are the inverses of the part readers, so `+Date.fromParts(Date.year(d), Date.month(d), Date.day(d)) == d+` holds of every `+Date+` and `+Time.fromParts(Time.hour(t), Time.minute(t), Time.second(t)) == t+` of every `+Time+`. This is where a refusal belongs once the parts are values rather than text: the arithmetic normalises, because shifting a date by a month has to land somewhere, so a model that reaches an impossible day by adding gets a real one back and no sign that it asked for a day the calendar does not have.

[[a-matches-pattern-is-a-literal-regular-expression]]`+String.matches+` takes a pattern that is a string literal and a valid regular expression. Both are read at compile time, which is the point of writing it out: a pattern computed at run time could not be checked at all.

[#primitives]
=== Primitive types

[,text]
----
Bool
Int          // signed 64-bit integer
Decimal      // java.math.BigDecimal
String
Date         // date. ISO 8601. java.time.LocalDate
Time         // time of day, to the second. ISO 8601. java.time.LocalTime
DateTime     // date-time, to the second. ISO 8601. java.time.LocalDateTime
Instant      // a moment on the timeline, to the nanosecond. ISO 8601 in UTC. java.time.Instant
----

`+Date+` / `+DateTime+` correspond to the spec DSL's `+Date+` / `+DateTime+`. The current time is not obtained in the language; it is declared as a behavior with no implementation (<<injected-behavior>>).

[[a-date-time-is-a-date-and-a-time]]A `+DateTime+` is a `+Date+` and a `+Time+`, and MUST be readable and writable as the two: `+DateTime.toDate+` / `+DateTime.toTime+` take it apart and `+DateTime.fromDateAndTime+` puts it back (<<stdlib-datetime>>). The last of those cannot fail — whatever could have been wrong about the parts was settled when the `+Date+` and the `+Time+` were built.

[[a-local-temporal-is-held-to-the-second]]`+Date+`, `+Time+` and `+DateTime+` are *local*: they say what a calendar and a clock read, and carry no zone. `+Time+` and `+DateTime+` are held to the second and MUST NOT carry a fraction of one — written text that has one is <<e1322>>, and text arriving at a boundary that has one is a decode failure at that path (<<decoder-error>>). Neither drops it: a value that cannot be read back is a value nothing downstream can tell from the one that was sent.

[[an-instant-carries-what-a-timestamp-said]]`+Instant+` is a moment on the timeline rather than a reading of a clock, and is held to the nanosecond, which is what an outside timestamp says. It has no year, no month and no hour: naming one needs a zone, and the language names none. So `+Instant+` has *no calendar decomposition* (<<stdlib-instant>>), and this is a rule rather than a gap — there is nothing to read a part with and nothing to build one from parts either, so the two directions are absent together. That its module is empty is the smaller fact: what the missing zone rules out is the calendar, and an operation that stays on the timeline would need none. What a model does with one it does without a library: two compare (<<primitives>>), one keys a `+Map+` (<<collections>>), one is written as a literal (<<temporal-literal>>), and turning one into a `+DateTime+` is a behavior with no implementation, the zone being supplied from outside as the current time is (<<injected-behavior>>). A boundary takes an `+Instant+` written with a numeric offset and answers the moment it names, which is the same moment the `+Z+` form of it names; source takes the `+Z+` form only (<<temporal-literal>>). Neither is a zone.

[#numeric-literal]
==== Numeric literals

An integer literal is written as `+500+` and is `+Int+`. A `+Decimal+` literal takes an `+m+` suffix — `+500m+`, `+1.5m+`, `+0.5m+`. A fractional literal without `+m+`, such as `+1.5+`, is an error. Souther has no binary floating-point type and defaults fractions to `+Decimal+` (exact decimal), so `+Int+` and `+Decimal+` MUST be made explicit at the literal.

[#string-literal]
==== String literals

A `+String+` literal is written between `+"+` and `+"+`, and it ends on the line it began on. A literal not closed before the end of the line is refused there (<<e2305>>). A newline in a value is written `+\n+`; the language has no multi-line literal form.

A backslash is read before these characters, and before nothing else:

[,text]
----
\n   a newline
\t   a tab
\r   a carriage return
\"   a quote, which does not close the literal
\\   a backslash
----

A backslash written before anything else is refused (<<e2305>>). It is not read as the character alone: doing that would take a character the author wrote out of the value and say nothing about it, so `+"\d"+` would be the text `+d+`. A regular expression's class is therefore written with the backslash doubled — `+"\\d{3}"+` is the text `+\d{3}+` (<<stdlib-string>>).

A literal's value is normalized to Unicode NFC. A source file is text from an editor, which is the other place text arrives from outside — the first being a decoder, which canonicalizes for the same reason (<<decoder>>). Two canonically equivalent forms are the same text by Unicode's definition and different values to a comparison by code units, so a literal left as written would compare unequal to the same text that came in through a boundary. Which form an editor writes is not something the author chose. The folding is NFC and not NFKC: compatibility folding turns ① into 1 and a half-width kana into a full-width one, which is a different claim about the text than "these are the same characters".

[#equality]
==== Equality

`+==+` and `+/=+` are *value equality*, not reference identity. Souther data are immutable values, so whether two are the same is decided by whether their fields are the same.

[,text]
----
guard approverId == request.applicant.managerId      // two EmployeeId built separately are equal if their values are equal
----

* primitives compare values
* data compare *fields recursively*, including fields taken in by a `+...+` spread
* sums are equal when they are the same case and their contents are equal
* `+List<T>+` compares elements in order; `+Map<String,T>+` compares keys and values
* `+T?+` is equal when both are `+None+`, or both are `+Some+` with equal contents
* a tuple compares its elements left to right (<<tuple>>)

Only *values of the same type* may be compared. Comparing different types is a compile error. A sum may also be compared with one of its *cases* (`+Rank == Staff+`): a case value is a value of its sum (case→sum is transparent, <<sum-data>>), so this is a sum-vs-sum comparison — true when the sum value is that case. Pure case membership is written this way for a case with no fields (a unit data, the usual state / role enum); a field-bearing case compared with `+==+` compares its fields too.

Ordering (`+<+` `+<=+` `+>+` `+>=+`) is defined for `+Int+` (numeric), `+String+` (lexicographic), `+Decimal+` (by value, ignoring scale), and `+Date+` / `+Time+` / `+DateTime+` / `+Instant+` (chronological — `+Instant+` to the nanosecond, the other three to the second, which is all they hold). A product data has no ordering, and neither does a sum with a field-bearing case. An *enumeration* — a sum every one of whose cases is a unit data (<<sum-discrimination>>) — is ordered too, by the order its cases are declared in, which is the order a state moves through (`+Prospecting < Won+`). A single-value newtype (`+data X = Y+`; <<newtype>>) over anything ordered *is* ordered — by the value it wraps — and that includes an enumeration: `+data StageN = Stage+` compares on `+Stage+`'s declaration order, and `+sort+` takes a `+List<StageN>+`. The newtype rule is about the value underneath and not about which kind it is, so excepting one kind from it would be a rule of its own.

The order belongs to the sum, not to the case value: the same unit data may be a case of two sums, which place it differently. A case value therefore takes its order from the sum it is compared with or held in (`+stage < Won+`, a `+List<Stage>+`), and a bare case value that two enumerations both list has no order of its own. Nor does a newtype over such a case: a name wrapped round a value carries the order that value has, and there is none to carry. Where one enumeration lists the case the value does have an order, and a newtype over it is ordered by that enumeration, as one over the *sum* is — what the name asks is whether the value it wraps is ordered, and it asks nothing else.

[#newtype-comparison]
===== Comparing a newtype

A single-value newtype is compared by the value it wraps, for both equality and ordering, so `+.value+` is not written — `+value+` is the newtype's implicit inner name, not something a caller spells out. The nominal boundary is kept:

* Two of the *same* newtype compare their wrapped values: `+Amount <= Amount+`, `+Amount == Amount+`.
* A *bare literal* of the wrapped type takes that newtype from the other operand: `+Amount <= 100+`, `+Amount == 0+` (the `+100+` is an `+Amount+`). This mirrors how `+[]+` takes its element type from context (<<algebraic-types>>).
* Two *different* newtypes over the same base do not compare: `+Amount <= Quantity+` is a compile error, even though both wrap `+Int+`.
* A non-literal value of the wrapped type is not taken implicitly: `+Amount <= n+` (where `+n+` is an `+Int+`) is a compile error — write `+Amount <= Amount(n)+`.

The wrapped type must itself be ordered for `+<+` / `+<=+` / `+>+` / `+>=+`; equality works over any wrapped type. Arithmetic on a newtype is `+{plus}+` / `+-+` only (<<newtype-arithmetic>>).

`+Decimal+` ignores scale. `+1.0+` and `+1.00+` are equal.

[,souther]
----
data Amount = Decimal
// read as 1.00 from a DB column, 1.0 from JSON. Treated as the same amount.
----

Scale is not lost from the value: it is carried, and Java reading the value sees it (<<jvm-product>>). What an Encoder writes is the *amount* — `+1.50+` and `+1.5+` are written `+1.5+`, and `+100.00+` is written `+100+` — because scale records how a number was written and not how much it is, and a boundary that wrote it would write two equal values two ways (<<encode-law>>). An exponent is spelt out into at most a thousand digits and is otherwise written as it stands (`+1E+1000000+` stays `+1E+1000000+`): what is bounded is the *spelling out*, not the number — an amount that already carries a thousand digits is written with all of them. Without a bound, `+1E+1000000+` would be eleven characters asking for a million and one. Which side of the bound an amount falls is decided by the amount, so two writings of one amount are still one number. The round trip (<<round-trip>>) is unaffected: identity ignores scale, so what is read back is the same value. If the number of rounding digits is a domain concern, write it as an invariant or a separate field, not as `+Decimal+` scale — a rounding call decides an amount, and the digits it rounded to are not what crossing the boundary shows.

The `+hashCode+` of a data containing `+Decimal+` is computed after dropping scale (`+stripTrailingZeros+`). If equality ignores scale, the hash MUST ignore it too, or using the value as a `+Map+` key breaks. Java sees the same value equality: a generated data class implements `+equals+` / `+hashCode+` from all fields and can be used as a `+Map+` key directly (<<jvm-product>>). This is distinct from Java's `+==+` (reference) and `+BigDecimal.equals+` (scale-sensitive).

[#newtype-arithmetic]
===== Arithmetic on a newtype


[[newtype-arithmetic-follows-the-units]]Arithmetic on newtypes MUST follow the units. Two newtypes over one base add and subtract only where they are the same newtype; a product and a quotient answer with what the units make, and where that is not a type the model declares there is nothing for the result to be. The refusal names the rule it is: which of them was broken is what an author needs, and it is not readable from the operand types alone.

A single-value newtype over `+Int+` or `+Decimal+` supports `+{plus}+` and `+-+`, and the result stays in the newtype: `+Money {plus} Money+` and `+Money - Money+` are `+Money+`. The operator opens each operand to its wrapped value, computes on the base, and re-wraps the result — so the result's invariant is re-checked at construction, exactly as any other construction is (<<invariant-mvp>>). A subtraction that leaves the invariant (a non-negative `+Money+` going below zero) *aborts* inside the domain (<<violation-destination>>). To make that a business result, guard it with `+guard ... else+` (<<guard>>); the compiler then confirms the guard discharges the construction (<<invariant-discharge>>).

Operand pairing is the same as comparison (<<newtype-comparison>>): the *same* newtype, or a newtype with a bare literal of its base (`+Money {plus} 100+`, the `+100+` taken as `+Money+`). Two *different* newtypes do not combine — `+Money - Quantity+` is a compile error even though both wrap `+Int+` — and a non-literal value of the wrapped type is not taken implicitly.

Arithmetic builds a value, so it is subject to <<closed-construction>>: a newtype of another module is built through the entry that module opens by exposing it, and a value of a type its module does *not* expose does not reach here at all: a module may not expose a data whose field would carry one out (<<exposed-surface>>). So the arithmetic and the entry it needs are always in the same place.

`+*+` and `+/+` *scale* a newtype by a plain number of its base and stay in the newtype (`+Money * 2+`, `+2 * Money+`, `+Money / 2+`) — the dimension is unchanged. The scalar is a bare `+Int+` / `+Decimal+` (literal or variable) of the newtype's base, not itself coerced to the newtype. A product or quotient of *two* newtypes *does* change dimension (`+UnitPrice * Quantity+` is a `+Money+`, not a `+UnitPrice+`), which the language does not model, so `+newtype * newtype+` (and `+Money * Money+`) is a compile error; compute those on `+.value+` and wrap the result explicitly. Only a newtype whose value is *directly* `+Int+` or `+Decimal+` has arithmetic — a newtype over another newtype does not.

Re-wrapping is a computation, not a fresh construction from raw data, so it needs no `+constructs+` (<<constructs>>) — as `+Int+` arithmetic does not "construct" an `+Int+`. A `+Money(...)+` literal construction (<<newtype>>) still does.

[#collections]
=== Collection types


[[a-generic-type-is-given-its-arguments]]A collection type is written with its arguments — `+List<Int>+`, `+Map<String, Int>+`, `+Set<String>+`. The bare qualifier names the module its functions are reached through (<<stdlib>>) and is not a type.

[,text]
----
List<T>
Map<K, V>
Set<T>
----

Collections are immutable. A `+List+` is ordered and may repeat; a `+Set+` is unordered with no duplicate elements, compared by value equality (<<primitives>>); a `+Map+` is keyed by `+K+`, so a key carries its domain type (`+Map<ProductId, Stock>+`). Inside a behavior body `+K+` is any value type — `+List.groupBy+` keys by whatever the projection returns. A map that *crosses the boundary* — a data field, a behavior's input or output, at any depth — is a JSON object, whose keys are strings, so there `+K+` MUST be `+String+`, a temporal (`+Date+` / `+Time+` / `+DateTime+` / `+Instant+`), an enumeration — a sum every one of whose cases is a unit data (<<sum-discrimination>>) — or a newtype over any of those. A newtype is written as what it wraps is written, so it is a key exactly when what it wraps is a key: `+data ProductId = String+` and `+data LoanDate = Date+` both key a boundary map, and so does a newtype over either of them, however many are written round it. A newtype key is decoded through its own decoder (its invariant enforced, and the invariant of anything it wraps, a bad key failing at that key's path) and encoded back through its own encoder; a temporal key is parsed from and written as its ISO 8601 form, the same representation a `+Date+` field has (`+{"2026-01-01": 300}+`); an enumeration key is the case's name, the same representation it has in a field (`+{"Won": 300}+`). An `+Int+`-backed newtype is not a key, because `+Int+` is not: a number is written as a number in a field, and a type's external form does not change with where it stands. A `+Set+`'s external representation is a JSON array, deduplicated on decode. The array is where the boundary has to put a value the language calls unordered, so no meaning is carried by the position an element ends up in: the order of the input array has no semantic significance and is not guaranteed to be preserved. What *is* written is fixed, and by the members rather than by the collection holding them — the array's members are written in ascending order of their own external representation, and a boundary map's members in ascending order of their rendered keys. So the order is decided by the members the collection turned out to hold: not by which of them share a hash, and not by whether the collection was decoded or built (<<stdlib-set>>, <<stdlib-map>>). Two collections that are equal therefore write the same JSON (<<encode-law>>). This is not the language's ordering (<<equality>>), which a `+Set+`'s element is not required to have; it is an order over what is written:

[,text]
----
null < false < true < number < string < array < object
  number : by amount — an amount is written one way (<<primitives>>), so two of one amount are one
  string : by UTF-16 code unit
  array  : element by element, the shorter one first where one runs out
  object : its members read in ascending key order, each key before its value
----

An element may itself be a collection — `+Map<String, List<ProductId>>+` is what bucketing rows by a key produces (`+List.groupBy+`, <<stdlib-list>>) — and it crosses the boundary as the nested JSON its shape implies, decoded and encoded the same way at every depth.

Only `+List+` is written as a literal. `+[ … ]+` is a `+List+` whatever the position it fills declares, so a `+Set+` field takes `+Set.fromList([ … ])+` and a `+Map+` field takes `+Map.fromList([ … ])+` or `+Map.empty+` — the conversion is written rather than implied, which is where a reader sees that duplicate elements collapse. The one thing context supplies is the element type of an empty collection, which `+[ ]+`, `+Map.empty+` and `+Set.empty+` have no argument to learn (<<stdlib-map>>). An `+example+` row is evaluated as this module's code like anything else, and its brackets are the one thing it writes that a body does not: there `+[ … ]+` is the collection the position it stands at declares (<<example-evaluable>>).

A collection MAY be what a newtype wraps (`+data TagSet = Set<Tag>+`, <<newtype>>), which names the collection itself and gives an invariant something to hold about it — that its elements are unique, that it is not empty. The newtype crosses the boundary as the collection's own representation, a JSON array or object, not as `+{"value": [...]}+`.

Collections are *covariant* in their element. A case `+A+` may be used as its sum `+S+` (<<sum-data>>), so `+List<A>+` may be passed where `+List<S>+` is expected, `+Set<A>+` where `+Set<S>+` is expected, `+Map<String, A>+` where `+Map<String, S>+` is expected, and `+A?+` where `+S?+` is expected. This is sound because collections are immutable and there is no room to write a sibling case later. Covariance of mutable arrays (Java) is unsound because writes are a hole; that is the difference. Covariance applies only in the case→sum direction; sibling cases (`+A+` and `+B+`) are unrelated.

[#algebraic-types]
=== Built-in algebraic and auxiliary types

[,text]
----
Option<T>    = Some(T) | None
Never                      // a type with no value: an impossible case (empty sum)
Unit                       // a type with exactly one value
----

Souther has no `+Result+` / `+Either+` type. A behavior's output is a bare domain sum with no two-slot type separating success and failure (<<behavior>>, <<composition>>).

`+Never+` and `+Unit+` are not type names writable at the surface. `+Never+` denotes an impossible case: a single-case output (`+-> A+`) can be read as having an empty failure row, and it is the type of `+unreachable+` (<<unreachable>>) — the one expression that answers no value, and so fits every position. `+Unit+` is a reading aid only; it corresponds to the spec DSL's `+Unit+`, but it is never provided as a way to write a field, and is always expressed as a "data with no fields" (<<sum-data>>).

The only polymorphic types are the stdlib ones (`+Option+`, `+List+`, `+Map+`, `+Set+`). There are no user-defined generics. Neither `+data+` nor `+behavior+` takes a type parameter.

[#invariant-abort]
==== An invariant violation aborts

When the construction of a data with an invariant violates it inside the domain (<<invariant-mvp>>), the computation *aborts*. It returns no value and no case.

[,souther]
----
behavior discount : (d: Quote) -> Discounted
    constructs Discounted
----

Writing a computation that breaks the invariant of `+Discounted+` does not change the output type; breaking it aborts. An abort is not a Souther expression and has no catching syntax, so it cannot control business flow (<<out-of-scope>>). At the boundary (<<java-example>>), Java catches it and maps it to 500, distinct from a business failure (400).

If the author wants to return the violation, that is a business judgment and MUST be written explicitly with `+guard ... else+` (<<guard>>). An invariant is a declaration that "a value passing here is always so", not a branch designed to be broken.

That judgment MAY be written by *attempting* the construction (<<attempted-construction>>), which states the rule once instead of restating it in the guard. What the attempt answers is a branch, not a value: no failure value reaches the domain, and the name a business failure gets is still the one the author writes in `+else+`.

A decode-side violation does not abort; Raoh's `+Result+` failure carries it (<<decoder-error>>). The same invariant is handled differently inside and outside the boundary.

==== How Option appears

`+Option<T>+` is the name of the type a `+?+` field has. It is written in a type position like any other name (<<a-type-is-written-in-a-type-position>>), and the positions that refuse it are these: the boundary, which is a behavior's parameter and its output (<<an-optional-does-not-stand-in-a-boundary>>); a newtype's base, which wraps a value that is always there (<<newtype>>); and, where an external representation is required, inside another optional, which leaves absence no way to say which of the two it is (<<what-has-no-external-representation>>). A helper's signature requires no representation and takes any of them (<<a-type-is-written-in-a-type-position>>). On a data field `+T?+` means the same thing and is the shorter spelling. A recursive helper and a function-typed parameter MUST declare their types (<<fn-declaration>>), so for those the name is the only way to say it at all.

The mark `+?+` outside a field is written only in the shipped core, as a type variable is (<<stdlib>>): `+?+` marks where an optional is *made*, and the name says what the type is.

An optional is *made* in one place: a `+?+` field being given its value (<<optional>>). Writing the type does not make one — `+let p: Option<Int> = None+` is <<e1303>> — and neither does declaring it as a helper's return: a helper answers the optional it read. Where a helper's absence is its own to name, that is a case of the model's own sum.

[,souther]
----
data OrgMember =
    { manager: OrgMember?
    , level: Int
    }

let firstOver (n: Int, m: OrgMember): Option<OrgMember> =
    match m.manager with
        | Some b -> if b.level > n then m.manager else firstOver(n, b)
        | None -> m.manager // the absence it read, passed on
----

Where the boundary refuses it, what replaces it is a name the model owns. To state "might not be found" as a business result, that is a domain sum (<<unmarked-sum>>); an optional output is reported as itself rather than as a composition that does not fit (<<e1701>>).

[,souther]
----
behavior findMember : (id: MemberId) -> Member | NoSuchMember // write this
----

`+Some(T)+` is an exception to the rules of <<sum-data>> (a case is a reference to a named data and cannot carry a payload). Because `+Option+` is built in, users cannot imitate this form. Its contents are taken out with `+match+`; the `+v+` in `+| Some v+` is positionally bound to the wrapped value, not to `+Some+` (<<match>>).

[#optional]
=== Optional type

Optionality is expressed with `+?+`.

[,souther]
----
data TravelCompleted =
    { preApprover: EmployeeId?
    }
----

This desugars to:

[,text]
----
preApprover: Option<EmployeeId>
----

[[optional-marks-one-type]]`+?+` marks one type optional. It is written on a single type and not on a sum: what a sum of several cases is missing is a case of its own, not a mark on the whole.

[#option-cases-are-not-written]
To put a value in a `+?+` field, write an `+EmployeeId+` value directly and it is wrapped in `+Some+`; write `+None+` for empty. This is the only place either name may be written as a value: what puts it there is the field, not the type of the position — a model may name `+Option<T>+` where it reads one (<<algebraic-types>>), and an annotation saying so does not license making one. Users never write `+Some(...)+` to construct a value (as in <<algebraic-types>>, this form is built-in only); `+Some(...)+` appears only as a destructuring pattern in `+match+` (<<match>>).

[,text]
----
TravelCompleted { preApprover = approverId }   // wrapped in Some(approverId)
TravelCompleted { preApprover = None }         // empty
----

Wrapping in `+Some+` and `+None+` are not data construction, so `+constructs+` is not required (<<constructs>>). Extraction is done with `+match+`'s `+| Some v+` / `+| None+` (<<match>>).

Outside a field, `+?+` is written only in the shipped core (<<algebraic-types>>); a user model marks nothing else with it, and names the type where it reads one.

The Decoder's external type is (Raoh's Decoder):

[,text]
----
Decoder<T> = external representation -> Result<T>    // failure carries Raoh's Issues
----

[#data]
== data

`+data+` defines typed data of the internal world. All fields are immutable.

A declaration takes the form `+data <Name> = <right-hand side>+`. If the right-hand side is `+{ ... }+` it is a product; `+A | B+` a sum; a single type name `+Y+` a newtype wrapping `+Y+` (<<newtype>>); if the right-hand side is omitted, a unit. This mirrors the spec DSL's `+data <Name> = ...+`.

[#product-data]
=== Product data (AND)

The spec DSL's `+AND+` maps to a product. A field name is the business role; the type may be specified separately.

[,souther]
----
// spec DSL: data Employee = EmployeeId AND Rank AND ManagerId
data Employee =
    { id: EmployeeId
    , rank: Rank
    , managerId: EmployeeId // role name (managerId) differs from type (EmployeeId)
    }
----

Role and type may be written separately. In the spec DSL the same concept has a different name per layer: `+approverId+` (operation input) becomes `+preApproverId+` (stored state) becomes `+pre_approver_id+` (DB column). A field such as `+preApprover: EmployeeId+` writes the role in the field name and the concept in the type.

[#self-referential-data]
==== Self-referential data

Data MAY reference itself (directly or via another data). Structures such as an org chart or a category tree appear routinely in business.

[,souther]
----
data OrgMember =
    { manager: OrgMember?
    , name: String
    } // the manager is again an OrgMember (the chain ends at None)
data Department =
    { children: List<Department>
    , name: String
    } // a tree (a leaf has an empty children list)
----

The derived codecs follow this self-reference. A nested `+{"name":"a","manager":{"name":"b"}}+` decodes to an `+OrgMember+` whose `+manager+` is again an `+OrgMember+`. Traversing a self-reference *by computation* (climbing the manager chain to the root, counting tree depth) is written with a recursive helper `+let+` (<<fn-declaration>>). `+fold+` folds only a fixed-structure `+List+`, so a self-reference of unbounded depth can be traversed only with helper recursion.

A self-reference requires a *base case*: an occurrence on the cycle that has a value holding no further one. A cycle with none needs its own value to construct itself, which continues indefinitely — it is not constructible. Therefore *a self-referential cycle with no base case is a compile error*.

* `+manager: OrgMember+` (no `+?+`) is a cycle with no base case. It is a compile error, not a runtime stack overflow.
* `+manager: OrgMember?+` is a base case. `+None+` is a value of every optional and no rule can remove it: a newtype MUST NOT wrap an optional (<<e1009>>), so there is nowhere to write one.
* `+children: List<Department>+` is a base case where the empty list is a value the rules admit. The empty collection is not held by the language the way `+None+` is, and a rule on how much the collection holds removes it:
+
[,souther]
----
data Children = List<Department>
    invariant nonEmpty = List.length(value) >= 1
data Department =
    { children: Children
    , name: String
    } // no first value: the empty list is gone, so no Department bottoms out
----
+
Whether the collection may be empty is decided by whether any value satisfying the rules holds nothing, over every rule that reaches the occurrence — those written on its own type, through each name it wears, and those the record holding it writes about that field, including rules that reach it through another field. No spelling is privileged: a floor, an equality, a disequality from none and a ceiling below none all remove the empty collection, because each leaves nothing satisfying the rules that holds none. A rule stating only how many at most leaves it where it was.

Which occurrence a cycle stops at is a property of the occurrence and not of the type standing there. The same `+Children+` is a base case at `+children: Children?+` and is not at `+children: Children+`. A count read at an occurrence carries through the names that value wears, and stops at what a collection holds, which is a value of its own.

This decides a cycle and not inhabitance in general. A sum is an OR composition and is taken as a base case, because a cycle through one may bottom out in a case the cycle did not come through; a sum whose every case is on the cycle therefore has no value and is not reported.

[#field-spread]
=== Field composition (spread)

In the spec DSL, a state sharing common items is written as `+data Submitted = TravelRequestCommon AND submittedAt+`, meaning "has all fields of `+TravelRequestCommon+`, plus `+submittedAt+`". Souther expresses this with the `+...+` spread.

[,souther]
----
data TravelRequestCommon =
    { applicant: Employee
    , estimatedCost: Amount
    , destination: String
    , purpose: String
    , startsOn: Date
    , endsOn: Date
    }

// spec DSL: data Submitted = TravelRequestCommon AND SubmittedAt
data Submitted =
    { ...TravelRequestCommon // take in the common item's fields flatly
    , submittedAt: DateTime
    }
----

The `+...+` spread is defined as follows.

* The taken-in fields are laid out flatly. A `+Submitted+` value is referenced directly as `+x.applicant+` (not nested as `+x.common.applicant+`).
* The taken-in data's invariants are inherited too. The invariants of `+TravelRequestCommon+` are checked when `+Submitted+` is constructed. The spread carries not only fields but invariants.
* [[spread-fields-do-not-collide]]A field-name collision is a compile error.
* A spread is not inheritance. `+Submitted+` is not a subtype of `+TravelRequestCommon+`; the two are not assignment-compatible. They only share fields.
* A spread is an element of the same comma sequence as fields. It appears inside `+{+` separated by `+,+`, and multiple may be written. By convention it is placed first. It uses the same `+...+` as the value-level record-literal spread (<<record-literal>>).

To hold the common items as a single value rather than flattened, nest it as a normal field (`+common: TravelRequestCommon+`). The spec DSL's `+AND+` is a spread; explicit nesting is a normal field.

[#sum-data]
=== Sum data (OR)

The spec DSL's `+OR+` maps to a sum. *A case MAY reference an already-defined named data.*

[[a-sum-case-is-a-declared-data]]A case MUST be a data that is declared. [[a-sum-does-not-contain-itself]]A sum MUST NOT contain itself, through a case or through a chain of them: a sum's cases are the values it can be, and one of them being the sum is not a value. As in the business-trip model, where each state has its own fields, invariants, and transition behaviors and is bound together by `+OR+`, this structure is represented directly.

[,souther]
----
// spec DSL: data TravelRequest = Drafting OR Submitted OR AwaitingPreApproval OR PreApproved
data TravelRequest = Drafting | Submitted | AwaitingPreApproval | PreApproved
----

Nested sums MAY be written directly.

[,souther]
----
data CostBearer = OwnCost | ClientCost
data OwnCost = Reimbursement | Advance | CorporateCard
----

A case names a data; an inline record MUST NOT be written in a case position. A case that carries fields is declared separately, as the spec DSL declares it (for `+data Rank = Manager OR Staff+`, `+data Manager = { level: Level }+`). A case declared nowhere carries no fields, and the case list is its declaration (<<unit-data>>).

*A case value is a value of its sum.* As in functional languages, a value of a case type (e.g. `+Submitted+`) may be used transparently wherever its sum type (`+TravelRequest+`) is expected — field assignment, argument, or return value. Only the upward direction (case→sum) is allowed; the downward direction (sum→specific case) requires `+match+`. A nested sum (`+data OwnCost = Reimbursement | Advance+` being a case of `+CostBearer+`) is decided the same way, folding to the leaf case.

[[sum-cases-are-declared-with-the-sum]]A sum's cases are declared with it. A case a module imports MUST NOT be one of them: the sum is published as a sealed interface its own module closes, so another module's type cannot be a member of it. What crosses a boundary is matched and restated as a case of this module.

*What holds of every case is a property of the sum.* A sum has no fields of its own, so reading one is an error and the value must be opened with `+match+` — except for a field that *every* case spreads (<<field-spread>>): the sum exposes the fields of a data all of its cases include, and `+d.id+` reads it without opening the value. The same part is spread from the sum into a construction, so `+Filed { ...d, filedOn = on }+` writes at once what a `+match+` arm per case would write alike.

[,souther]
----
data DocumentCommon =
    { id: String
    }
data Draft =
    { ...DocumentCommon
    }
data Sent =
    { ...DocumentCommon
    , at: DateTime
    }
data Document = Draft | Sent
data Filed =
    { ...DocumentCommon
    , filedOn: Date
    }

let idOf (d: Document): String = d.id
let file (d: Document, on: Date): Filed = Filed { ...d, filedOn = on }
----

The shared part is what the author wrote: a spread is nominal (<<field-spread>>), so two cases that happen to declare a field of the same name have not shared it, and reading it stays an error. A spread reached through another spread counts, and a case that is itself a sum contributes its own leaves. When a case stops spreading the common data the field is no longer exposed, and the error appears at the read.

The read and the spread reach the same part and no more. What a single case carries of its own is not spread from the sum, so a construction wanting it is the missing-field error it would be from any other source (<<e1005>>), and a sum whose cases share no spread is not a spread source at all. A sum is still not assignment-compatible with the data its cases spread, and constructing one of its cases — deciding which case a value is — is still `+match+`.

[#unit-data]
=== Unit data

A data with no fields is declared with no body. It corresponds to the spec DSL's `+= Unit+`.

[[unit-data-is-written-without-a-body]]A unit data is written without a body. An empty body names a type with one value, which is what a unit data is, so the two spellings would be one declaration written two ways.

[,souther]
----
// spec DSL: data Reimbursement = Unit
data Reimbursement
data Advance
data CorporateCard
data ClientCost
data Staff
----

Unit data appear frequently as sum cases (such as `+Staff+` in `+data Rank = Manager | Staff+`).

A unit written where a module *introduces* a type needs no declaration of its own: the name alone declares it. Three positions introduce a type — a sum's case list (<<sum-data>>), a behavior's output (<<behavior-io>>), and the output declared for a `+>->+` composition, written either on the composition or in `+exposing+` (<<declared-composition-output>>).

[,souther]
----
data CostBearer = OwnCost | ClientCost
data OwnCost = Reimbursement | Advance | CorporateCard
// Reimbursement, Advance, CorporateCard and ClientCost are units; nothing else declares them

behavior quote : (c: Cart) -> PricedCart | EmptyCart // EmptyCart is a unit
----

Everywhere else a type is *referred to*, and a name nothing declares is unknown there and reported: a parameter type, a field's type, `+constructs+`, `+depends on+`, a `+match+` arm, a codec variant, a `+let+`'s declared return type.

Whether a name is declared is answered within the module — its own declarations and the names its `+import+` lines list. A qualified name (`+inventory.InsufficientStock+`) names another module's declaration, so it is a reference wherever it is written. A sum's cases are declared with the sum, so they are module-local in any event (<<e1606>>).

The explicit declaration stays available, and is how a case that carries a comment of its own is written.

[[a-unit-introduced-by-a-case-list-is-not-declared-again]]Where a position introduces the type, writing the declaration as well SHOULD be avoided: `+data Rank = Manager | Staff+` beside `+data Staff+` says one thing twice, and the second spelling is the one that goes stale — a case renamed in the list leaves the bare declaration behind as a type nothing refers to. The vocabulary a model reads by is the case list, so let it be the declaration. Write the separate `+data+` when the case has something of its own to carry: a comment, or an `+exposing+` entry the sum does not give it. This is a modelling convention rather than a consequence of <<constructs-excludes-unit-data>>; what the two amount to together is that for most units the case list is the only place the name is written at all.

A unit data MUST NOT carry an `+invariant+`: it has no field for one to observe and no value it could reject (<<invariant-declaration>>). Writing one is a compile error rather than a clause with no effect.

A data body MUST NOT be empty: `+data T = { }+` names a type with one value, which is what a unit data is, so it is a second spelling of one. The two are not interchangeable where it counts — an empty product is built as `+T {}+` and a unit by its name alone — so the declaration form silently decides how the value is written. Writing it is a compile error naming the unit form. A body of spreads alone (`+data T = { ...CommonItems }+`) is not empty: what it includes gives it its fields (<<field-spread>>).

A unit data value is constructed by *writing the name directly* (as a zero-argument constructor becomes a value in functional languages). An empty record such as `+T {}+` is not used.

[,text]
----
// construct a unit in an implementation (let). Requires the behavior's constructs
[ClientBorne | bearer == ClientCost]     // ClientBorne is a unit, constructed by name alone
----

This is a guard comprehension (<<stdlib-list>>): it yields one `+ClientBorne+` if the condition holds, an empty list otherwise.

A bare identifier in an expression resolves as: a variable if a bound local exists; otherwise a built-in value (such as `+None+`; <<algebraic-types>>); otherwise the construction of a unit data of the same name; otherwise undefined. Souther does not distinguish identifiers syntactically by case (so that Japanese business vocabulary can be used as identifiers), so this resolution is done in the symbol table.

Because a local shadowing a built-in value is always an error, using a *built-in value name* (`+None+`; <<algebraic-types>>) as a binding name — `+let+`, a `+match+` binding, or a lambda argument — is a compile error. This prevents a shadowed built-in from silently becoming unreachable. A unit data name is domain vocabulary and may be used as a binding, in which case the local takes precedence and constructing a unit data of that name is not writable in the same scope. The rounding-mode cases (<<stdlib-decimal>>) are ordinary unit data and follow this rule; a shadowed case has no qualified escape syntax.

[#field-visibility]
=== Field visibility

A field is read as `+e.field+`, where `+e+` is a name, a call, or a construction, and `+.a.b+` chains from the left. So `+amountOf(i).value+` opens a newtype on the call that builds it, with no `+let+` written to hold the result. This never competes with the bare `+.field+` getter of <<blocks>>: a getter starts an expression, a read follows one.

*Reading* a field is possible wherever that data's type is visible. Within the same module, any data of that module may be read. A data of another module may have its fields read if it is exposed with `+exposing+` — being importable means it is exposed. An exposed data has all fields readable (there is no per-field exposure). A behavior may read the fields of its input data even when they come from another module.

A value of a type its module keeps to itself never arrives in another module, because the field that would have carried it there cannot exist: what a module reaches out with may not rest on what it keeps (<<exposed-surface>>). So "the data's type is visible" is the whole rule, and there is no second one about what a reader may do with a value it has no name for.

What is constrained is *not reading but construction*. Reading a field and constructing a new value of that data are separate authorities, so allowing cross-boundary reads leaves the guarantee that no unvalidated value can be built (<<asymmetric-interop>>) intact. There is no need to define a getter-only behavior just to pass a value out (<<behavior-composition-unit>>).

On the JVM, a public read accessor is generated for every field of a data, and a data is a record over its fields (<<jvm-product>>). Because module = package, the accessor is what reads across the boundary; on a data the module does not expose, the class is package-private, so the accessors are out of reach from outside all the same. Java can read an exposed one, but that is the same exposure as the encoder already emitting all fields as JSON; what is guarded is construction. The constructor stays non-public, and to build a value Java must go through the invariant-checking decoder (<<jvm-construction-privacy>>).

[#union-intersection]
=== Union types and intersection types

Souther has union types. They appear in three forms.

* named sum `+data X = A | B+` (sealed; subject to exhaustiveness checking)
* anonymous union in a behavior output `+-> Submitted | AwaitingPreApproval+` (<<unmarked-output>>)
* anonymous union with failure cases in a behavior output `+-> Out | E1 | E2+` (<<type-routing>>)

An anonymous union is read as a type wherever a whole type is read (<<a-type-is-written-in-a-type-position>>). Being read in a position is not being admitted by it, and one of those positions refuses the form: a behavior's parameter, which names what a caller has to write (<<a-parameter-names-one-type>>). A behavior's output is the position the form is *for* — it is where a behavior's answer is shaped, and the two other forms above are written nowhere else — which is a different sentence from the only one it may stand in. To carry a sum across a behavior's input, make a named data (<<unmarked-output>>); naming it makes it sealed, with exhaustiveness checking and a JVM type (<<jvm-sum>>). Where the spec DSL writes `+(A OR B)+` in an input, the sum is named as a `+data+` and then referenced. A JVM type is generated for an output anonymous union (<<jvm-anonymous-union>>).

[#union-member]
[[a-union-member-is-nameable-in-an-arm]]A member of an anonymous union MUST be nominal and MUST tell itself apart from the other members at run time, and it MUST have a form a `+match+` arm can write. The primitives (`+Int+` / `+Bool+` / `+Decimal+` / `+String+` / `+Date+` / `+Time+` / `+DateTime+` / `+Instant+`) and named data types meet both, a data whether this module declares it or imports it. A collection meets neither: its type argument is erased, so `+List<Order>+` and `+List<Item>+` are one type at run time and no arm could choose between them, and no arm form names one. `+Option+` and a function type are outside for the same reason. What a member may be does not depend on where the union came from — a union written as a behavior's output and one a `+>->+` composition arrived at (<<type-routing>>) are the same union.

A named sum member contributes its cases and not itself, as it does in an external representation (<<sum-discrimination>>). The members a union is checked and generated from are therefore its *effective* members: every member written, with each named sum expanded to its leaves.

Each effective member goes by a name of its own. A `+match+` arm names a member by the name written for it, and so does the `+"type"+` discriminator (<<sum-discrimination>>), so one name cannot stand for two members. `+-> up.Yen | other.Yen+` is refused, and so is a `+Yen+` this module declares beside an imported one — including where one of them arrives as the leaf of a named sum member.

Structural intersection types (`+A & B+`, a type operator for "a value that is both A and B") are *not* provided. The spec DSL's `+AND+` means "has all of A's fields, plus more", not "is both A and B", and is fully expressed by the `+...+` spread of <<field-spread>> (take fields in flatly, inherit invariants, not a subtype).

[#newtype]
=== newtype (`+data X = Y+`)

When the right-hand side is a single type rather than a `+{ ... }+` product or a `+|+` sum, `+X+` is a newtype wrapping that type `+Y+`.

[[newtype-wraps-a-value-that-is-always-there]]What a newtype wraps MUST be there. A newtype is one value under another name and takes that value's representation, so an optional leaves it nothing to be when the value is absent; the absence belongs to the field that holds the newtype (<<optional>>).

[[newtype-wraps-an-external-representation]]What a newtype wraps MUST have an external representation of its own, since the newtype takes it as its own (<<external-representation>>). What has none cannot be wrapped (<<what-has-no-external-representation>>); a named data can be.

 `+Y+` is written the way a type is written anywhere else: a primitive (`+String+` / `+Int+` / `+Date+`, etc.), a named data, a sum, or a collection (`+List<T>+` / `+Set<T>+` / `+Map<K, V>+`, <<collections>>), including a collection of any of these. `+X+` is a distinct type from `+Y+` (nominally distinguished) with the same (bare) external representation as `+Y+`. The spec DSL's `+data X = Y+` (right-hand side a single name) is written directly.

[,souther]
----
// spec DSL: data EmployeeId = String // not empty
data EmployeeId = String
    invariant String.length(value) > 0

// spec DSL: data InsufficientAuthority = Rank
data InsufficientAuthority = Rank

// spec DSL: data CostLine = CostType (CostType is a sum)
data CostLine = CostType

// a collection the domain has a name for, with an invariant on the collection itself
data TagSet = Set<Tag>
data Stock = Map<ProductId, Int>
----

`+Y+` MUST have an external representation, since `+X+` takes it as its own: a tuple (<<tuple>>) and a function type (<<fn>>) are refused as a newtype's base, where they are written. `+Y+` MUST NOT be optional — neither `+data X = Y?+` nor `+data X = Option<Y>+` — because whether a value is present is a property of the place it is used, written there as `+f: X?+`. A `+Map+` that a newtype wraps crosses the boundary as a JSON object, so its key follows the same rule a field's does (<<collections>>).

`+data X = Y+` is equivalent to having a single field `+value: Y+`. Construction applies the value to the type name, `+X(v)+` (`+Amount(500)+`, `+MemberId("m-01")+`), equal to `+X { value = v }+`. Reference is `+x.value+`; an invariant refers to the inside as `+value+`. The difference from `+data X = { value: Y }+` (with braces) is *the external representation only*: `+data X = Y+` reads and writes `+Y+`'s representation bare (`+EmployeeId+` is `+"e-01"+`), while `+data X = { value: Y }+` becomes an Object `+{"value": "e-01"}+`.

The constructor `+X(v)+` is written with parentheses like function application. Juxtaposition `+X v+` is not adopted, because Souther's application is uniformly `+f(x)+` and because, absent a capitalization rule on identifiers, `+Amount -5+` would be ambiguous between construction and subtraction. An invariant violation aborts like any other data construction (<<violation-destination>>). A construction whose argument is a compile-time constant statically verified to satisfy the invariant cannot abort, so it may appear anywhere in the type body; a construction where a constant breaks the invariant, such as `+Amount(-5)+`, is a compile error. A construction passing a runtime value may appear wherever an expression may — a behavior's result, a non-tail `+let+`, a `+match+` arm — and aborts on violation.

Whether it is a newtype is *decided by this syntax*, not inferred from the single-field shape. Adding a field to `+data Amount = { value: Int }+` does not silently change its representation (`+{...}+` is always an Object, even with a single field). For the bare representation, write `+data Amount = Int+`.

Because there is no `+|+`, there is no room to read `+data InsufficientAuthority = Rank+` as a one-case sum. A sum always has `+|+` (<<sum-data>>). When `+Y+` is a sum, `+X+`'s external representation is `+Y+`'s discriminated representation, and if it becomes a case of a nested sum it is folded to the leaf and dispatched (<<sum-discrimination>>).

[#invariant]
== invariant

[#invariant-declaration]
=== Declaration

A value constraint written as a comment in the spec DSL is made executable as an `+invariant+`.

[[invariant-expression-is-bool]]An invariant clause states a condition, so its expression MUST have type `+Bool+`.

[[invariant-needs-a-value-to-constrain]]A type an invariant is declared on MUST have a value for it to observe. A unit data has one value and no field, so there is nothing for a clause to read and nothing it could reject.

[[an-invariant-is-declared-where-a-construction-owes-it]]An invariant MUST be declared on a type something constructs. A sum names which cases a value can be and is never itself built — `+T(...)+` is refused wherever it is written, and a value of `+T+` is written as one of its cases (<<a-sum-case-is-a-declared-data>>) — so a clause declared on one would be owed by no construction and checked at no boundary. Write it on the case, or on a newtype over the sum, which is built and carries its own check (<<newtype>>). Writing one on a sum is a compile error rather than a clause with no effect, for the reason the same rule is stated for a unit data: a clause that is dropped takes whatever is written inside it along with it.

[[an-invariant-observes-and-does-not-build]]An invariant observes the value being built and MUST NOT build one. A construction inside it would run the built type's own check inside this one, and the answer to whether this value holds would depend on whether another one did.

[[an-invariant-answers-on-every-path]]An invariant MUST answer on every path through it. It is checked on every construction and at every boundary (<<invariant-mvp>>), so it may not reach an `+unreachable+` and may not reach a `+partial+` helper, which publishes no termination guarantee (<<fn-rules>>).

[,souther]
----
// spec DSL: data Amount = Integer // at least 0, in yen
data Amount = Int
    invariant value >= 0

data DateRange =
    { start: Date
    , end: Date
    }
    invariant start <= end
----

[#invariant-clause-name]
=== A clause MAY be named

A clause MAY be given a name, which is what tells it apart from the type's other clauses: `+invariant <name> = <expr>+`.

[[invariant-clause-names-are-distinct]]No two clauses of one type may carry the same name. The name is what an attempted construction and a boundary failure call one rule by, so two rules cannot answer to it.

[[invariant-clause-name-is-not-underscore]]A clause may not be named `+_+`. `+| _ -> …+` answers the clauses that carry no name (<<attempt-departures>>), so a clause named `+_+` could never be the one it answered. An attempted construction departs by the named clause that did not hold (<<attempted-construction>>), and a decode failure reports the name (<<decoder-error>>).

[,souther]
----
data QuoteLines = List<QuoteLine>
    invariant nonEmpty = List.length(value) >= 1
    invariant uniqueProducts = List.allDistinctBy(.product, value)
----

Two clauses that apply to one data MUST NOT share a name, a `+...+` spread's included clauses among them: a name that answered two rules would answer neither in particular. A clause MUST NOT be named `+_+`: that is what an attempt writes for the clauses carrying no name, so a clause named so could never be answered by name. A clause a spread takes in keeps the name its own declaration gave it.

A clause written without a name is enforced exactly as before one could be named: nothing tells it apart from the type's other unnamed clauses, so it aborts inside the domain and fails the decode outside it without saying which rule it was, and no arm can name it.

[#invariant-mvp]
=== Checking on construction

Invariants are checked at runtime. The compiler inserts an invariant check on every path that constructs a data. Invariants taken in by a `+...+` spread are checked on the same path.

The clauses are checked in the order they are declared — a `+...+` spread's first, then the declaration's own — and the first that does not hold is the one the failure names. Where several clauses fail, only the first is reported, so reordering the clauses of a published type changes what a caller is told and is a breaking change.

[#invariant-expressions]
=== Expressions allowed / forbidden in an invariant

Allowed: comparison, logical operations, arithmetic, field reference, built-in functions, blocks, and a `+let+` satisfying the three conditions below.

[,souther]
----
let isNonNegative (v: Int) = v >= 0

data Amount = Int
    invariant isNonNegative(value)
data Cart =
    { items: List<Line>
    }
    invariant List.all(x -> isNonNegative(x.quantity), items)
----

What may be called is a `+let+`, not a behavior (<<fn>>). An invariant is part of what a type is: it travels to an importing module as the source it was written as, together with the helper `+let+`s it names (<<published-modules>>). It names only what travels with it, so a behavior MUST NOT be called from an invariant whatever its requirement set — including one that depends on nothing and is callable from a `+let+` body (<<calling-a-behavior>>). To name a business rule, use a `+let+`.

A value or a helper another module publishes travels with it, so an invariant MAY name one, as a `+let+` body may (<<exposed-values>>). A limit or a format several modules' types are written against is therefore written once, in the module that owns it.

[,souther]
----
module limits exposing ( maxLength )

let maxLength = 20
----

[,souther]
----
module Member exposing ( MemberName )

import limits ( maxLength )

data MemberName = String
    invariant String.length(value) <= maxLength
----

The value is substituted and the helper expanded where they are named, closed by the module that declares them (<<exposed-values>>), so what the rule says does not change with where it is read. A project importing `+MemberName+` from a jar reads the invariant as it was written and resolves `+maxLength+` against `+limits+`, which is why that module is on its class path (<<published-modules>>).

A `+let+` may be called from an invariant when it satisfies all of the following, all statically decidable.

* the requirement set is empty (the corresponding behavior writes no `+depends on+`; <<depends-on>>)
* it constructs no data (the construction set of any helper `+let+` it calls is also empty; <<blocks>>)
* it reaches nothing `+partial+`

The first is decided by reading the declaration. `+let preApprove+` does not touch the outside world directly and so is pure in the sense of <<purity>>, but `+behavior preApprove+` writes `+depends on now+`, so its requirement set is non-empty and that `+let+` MUST NOT be called from an invariant.

A helper `+let+` always satisfies the first and second. To touch the outside world it can only receive a function as an argument (<<fn-declaration>>), and an invariant has no way to pass one. The third is about termination: an invariant is checked on every construction and must terminate. A `+partial+` helper carries no termination guarantee, so it cannot appear in an invariant — nor be reached from one through any chain of calls, which is how the condition is decided. Inlining leaves a recursive helper standing, so what a clause names is not all it runs, and the whole path is reported. A *total* helper is admissible even when it recurses — the compiler has proven it terminates (<<fn-declaration>>). This is what lets the list quantifiers `+List.all+` / `+List.any+` / `+List.contains+` and the uniqueness checks `+List.distinct+` / `+List.allDistinctBy+` appear in an invariant: each derives from the total stdlib fold `+List.foldFrom+`, which is bounded by the finite list.

Forbidden: Decoder, Encoder, data construction, `+partial+` recursion, `+unreachable+` (<<unreachable>>), non-deterministic processing. Data construction is barred throughout the invariant expression, including inside a quantifier's closure — an invariant observes the value being built, it does not build another. `+unreachable+` is barred for the neighbouring reason: an invariant answers whether the value holds, and it answers on every path through itself, so a path that aborts instead decides a construction by a premise the clause never stated. A behavior call is barred by the rule above, whether written in the invariant or reached through a `+let+` it calls; and a `+let+` that goes through a behavior touching the outside world has that much of a requirement set and is rejected by condition 1 as well. Only pure (empty-requirement-set) computation enters an invariant.

[#violation-destination]
=== A violation's destination differs inside and outside the boundary

The same invariant is handled differently depending on where it breaks.

* *Decoder (boundary)*: the violation enters a failure (`+Issue+`) of Raoh's `+Result+` (<<decoder-error>>). A value that came from outside was invalid — an *expected result*. Which field broke it can be stated via `+path+` and `+code+`.
* *behavior (inside the domain)*: it *aborts* (<<algebraic-types>>). A computation among validated values broke a rule — a *model bug*.

[,souther]
----
data Discounted =
    { amount: Int
    }
    invariant amount >= 0

behavior discount : (d: Quote) -> Discounted // no violation case needed
    constructs Discounted
----

*A behavior's output needs no slot for the violation.* Constructing an invariant-bearing data does not change the output type.

If the business decides "return a different result when the amount is negative", that is not an invariant violation but a business judgment and MUST be written explicitly with `+guard ... else+` (<<guard>>). These are different: an invariant declares "a value passing here is always so", while `+guard+` is a business branch of "if not so, return this". The former breaking is a bug; the latter not holding is an expected result.

[#invariant-discharge]
=== Discharging an invariant at compile time

An invariant is checked at run time on every construction (<<invariant-mvp>>), and inside the domain a violation aborts (<<violation-destination>>). The compiler *also* checks, per behavior, whether each construction's invariant is *established* where the value is built — so a construction that must abort is caught at compile time, and one a guard makes safe is confirmed.

[[a-construction-is-judged-by-what-it-builds]]A construction is judged by what it builds. What each field is given is what the check reads, whether the source wrote that field a value, took it from another value with a `+...+` spread (<<record-literal>>), or wrote no field at all — a newtype's constructor and the arithmetic that re-wraps one (<<newtype-arithmetic>>) build a value like any other and owe its invariant where they stand. Two ways of writing one construction are one construction: they owe the same clauses, over the same values, and come to the same verdict.

The check is intraprocedural. Within a behavior's `+let+` body it seeds what the input types guarantee — a newtype's own invariant, a product data's invariant over its fields — which is sound because an input of type `+T+` was built through `+T+`'s checked construction. It refines that along each `+guard+` / `+if+` guard (<<guard>>, <<if>>), and at every construction asks whether the guards prove its invariant.

* *Discharged* — the guards prove the invariant. No diagnostic; the construction cannot abort here (overflow aside).
* [[construction-satisfies-its-invariant]]*Definitely violated* — the guards prove the invariant false on a reachable path. A compile error — the path-sensitive generalization of the constant check `+Money(-5)+` (<<newtype>>).
* [[construction-invariant-is-discharged]]*Possibly violated* — expressible but not proven. A warning: a possible abort. Guard it with `+guard+`, reify the relation into a type invariant (below), or attempt the construction (<<attempted-construction>>), which cannot abort.
* *Not expressible* — a clause outside the checked fragment. No diagnostic for it; the run-time check stands. The other clauses of the same invariant are answered on their own, so writing one the check cannot read beside one it can does not cost the second its report (<<invariant-discharge-capability>>).

Guards that cannot all hold describe a path the behavior does not take, and a construction standing on one is neither of the middle two: nothing is built there, so nothing is reported. That the guards contradict is itself derived over the same fragment — `+a <= b+` with `+b <= 0+` bounds `+a+` above without a word about `+a+` — so a path is dead whenever the relations say so, whatever order they were written in.

[,souther]
----
data Money = Decimal
    invariant value >= 0m
data WithdrawRequest =
    { balance: Money
    , amount: Money
    }

behavior withdraw : (req: WithdrawRequest) -> Money | InsufficientBalance
let withdraw (req) = {
    guard req.amount <= req.balance else InsufficientBalance
    req.balance - req.amount // discharged: the guard proves balance - amount >= 0
}
----

The checked fragment is bounded: the decision procedure derives over intervals and differences (`+value >= c+`, `+a <= b+`, and the like) with no external solver. A linear relation of neither shape — a sum of two lengths against a bound — is kept as it was written: nothing is derived from it on its own, and two of them are never added together. It is a premise all the same, used to prove a clause where the difference between the two is one the intervals and differences derive. So a guard *restating* an invariant discharges it, and so does a guard over a computed value the fragment relates to what the clause names: `+guard x - y <= req.amount.value+` discharges a clause about `+req.balance.value+` where `+WithdrawRequest+` declares `+amount <= balance+`, whatever `+x - y+` was computed from. The set of *flagged* constructions is tied to that fragment, so every flagged construction has a guard the procedure can verify — an invariant it cannot express (a non-linear one, or one over a value it cannot name) is left to the run-time check rather than reported as a possible violation no guard could silence.

An *attempted* construction (<<attempted-construction>>) is never possibly violated: a failing invariant takes its `+else+` branch, so there is no abort to warn about. It is what silences the warning for an invariant outside the fragment, where no guard could. A violation the procedure *decides* is still an error there, because with the outcome settled at compile time there was no branch. In the success branch the built value seeds the check with its own invariant, as an input of that type does.

[#invariant-discharge-terms]
==== What the procedure can name

A term it compares is the numeric content of a location — a parameter, a field chain, a newtype's wrapped value — or the size of a container or a string: `+List.length+`, `+String.length+`, `+Set.size+`, `+Map.size+` applied to such a location. A value the body reaches nowhere else is a location too, and carries what its type guarantees exactly as a parameter does: what a `+match+` arm binds, and what a standard-library operation hands its closure — an element, and a fold's accumulator. A size is named by the location it is taken of, so a guard and an invariant clause mean the same term exactly when they take the size of the same one. The size of a container is never negative, and the procedure knows that much without being told.

[,souther]
----
data Lines = List<Line>
    invariant List.length(value) >= 1

behavior build : (rows: List<Line>) -> Lines | NoRows
let build (rows) = {
    guard List.length(rows) >= 1 else NoRows
    Lines(rows) // discharged: the guard sizes the same list
}
----

A size taken of a container built by an operation is read through that operation, by the rules in <<invariant-discharge-preservation>>. A size taken of anything else — of what a behavior answered — is not a term, and a clause needing it is left to the run-time check.

Beyond those, a value is named by what computes it wherever that computation is a function of named parts. `+List.sum(xs)+` is a term, and so is `+a * b+`, which is not a form the fragment carries and is bounded by what its factors are bounded to (<<invariant-discharge-arithmetic>>), and so is a value chosen by an `+if+` or opened by a `+match+`, and one built by a construction. Naming a value says that two writings of it are one value, and says nothing else: nothing follows from `+List.sum+` being a sum, and nothing has to. A `+guard List.sum(xs) >= bill+` states a relation about that value, `+List.sum(xs) - bill+` is arithmetic over it, and the two meet because both mentions of the call are the same value. A term exists before anything is said about it, so the first guard to mention one states what it states, exactly as a later one does.

What is named and what is known of it are two questions, and the second is the one that has rules. A named value the procedure has no rule about is still a value a guard can be written about, and a construction from one is reported as unproven rather than left unsaid — the author is told that the invariant was read here and not established, which is a different thing from its not having been read. Giving the procedure a rule about an operation moves such a construction from unproven to discharged without anything being named differently.

[,souther]
----
data NonNeg = Int
    invariant value >= 0
data Fine

behavior settle : (xs: List<Int>, bill: Int) -> NonNeg | Fine
    constructs NonNeg
let settle (xs, bill) = {
    guard List.sum(xs) >= bill else Fine
    NonNeg(List.sum(xs) - bill) // discharged: the guard is about this value
}
----

A value the procedure cannot name all the way down is not a term. What names a call is what the call was resolved to: a `+let+` of this module or of another is pure and total (<<fn-rules>>), and the language's own operations are what the language defines, so a call to either answers one value wherever it is written with the same arguments. What a `+behavior+` answered is named by nothing, and neither is what applying a function value answered — a function a body was handed may be a behavior injected from outside (<<injected-behavior>>), whose implementation is not the language's and may answer differently each time it is asked. So neither is a size taken of one, nor arithmetic over one.

That a helper's body was written into the body that called it, rather than left as the call, is not one of these questions. A `+let+` that recurses cannot be written in, and answers the same value it would have answered written in; a clause about a construction from either is read the same way.

A value the procedure does not carry as a number is a term all the same where what computes it is one — a string a `+let+` answered is the term that call is. What it has none of is anything said about it: the procedure carries no strings, and there is no rule about the operation, so a clause is read against such a value only where a guard on the path has stated something about that very term. Until one has, a construction from it is left to the run-time check, and the silence is that nothing could be asked rather than that the value has no name.

[,souther]
----
data Q = Int
    invariant value >= 0
data Held =
    { q: Q
    }
data M = A | B
data A =
    { q: Q
    }

behavior viaMatch : (x: Held, m: M) -> Q
let viaMatch (x, m) =
    match m with
        | A as a -> x.q + a.q // discharged: `a` carries A's invariant, as `x` does
        | B -> x.q

behavior total : (xs: List<Held>) -> Q
let total (xs) = List.fold((acc, i) -> acc + i.q, Q(0), xs) // discharged: both are Q
----

A binding given a location *is* that location, so `+let c = cart+` makes `+c.quantity+` the term `+cart.quantity+`. A helper is expanded by binding each argument and reading the parameter through that binding (<<invariant-discharge-representation>>), which is why a construction moved into a helper still reads the terms the caller's guards and input types settled. A binding given anything else is the term that expression is, so a name and the expression it was given are read alike: a guard about either states the same thing about the same value, and a construction from either owes the same clauses.

[,souther]
----
data Qty = Int
    invariant value >= 1
data Cart =
    { quantity: Int
    }
    invariant quantity >= 1

let one (c: Cart): Qty = Qty(c.quantity) // `c` is `cart`, so this is discharged
behavior toQty : (cart: Cart) -> Qty
    constructs Qty
let toQty (cart) = one(cart)
----

[#invariant-discharge-representation]
==== What the check reads

The check reads a behavior's body and the invariants around it at the level the rules above are written at: the standard library's operations are operations, and a `+let+` a body or an invariant names is read as its body where its body can be written in (<<blocks>>). The language defines what `+List.map+` does to a length, and every module has that definition, so a call to it says something on its own. A `+let+` carries no such definition, so what there is to know about it is what its body does — the body of one another module published as readily as of one written here.

Reading a `+let+` as its body is how more is known, and not how it is named. A `+let+` that recurses is read as the call it is, and the value it answers is named all the same (<<invariant-discharge-terms>>): what is lost is the arithmetic inside it, so a clause is discharged there by a guard about the call rather than by the body's own steps. Nothing about a construction's verdict turns on which of the two a reading did.

A case split standing where a value is handed is read as the arms it answers one of: the body is read once with each arm standing there, under what choosing that arm settles, and what those readings find about one construction is said once. An `+if+` and a `+match+` are read the same way, so what may be discharged does not turn on which of the two an author wrote, and what an arm binds carries what its type guarantees exactly as it does in tail position (<<invariant-discharge-terms>>). A helper whose body is a `+match+` is this rule and not another one: reading the `+let+` as its body puts the `+match+` where the call stood.

How far the splits down one path are opened is bounded by what re-reading a body costs, and a value written under more of them than that has the rest left to the run-time check. The bound is a number of readings and not of splits, because an `+if+` has two arms and a `+match+` has one per case. What is asked of a split is what opening it would bring the path to, so the first one on a path is opened however wide it is: a sum's cases are the model's, and a rule that read fewer of them the more cases the model named would be a rule about the declaration rather than about the expression.

[[provenance-does-not-decide-the-fragment]]Where a declaration was written does not decide what can be discharged against it. A type declared in this module, one imported from a module compiled in the same run, and one imported from a module compiled separately and reached through its published classes all state the same invariant, and a construction of any of them is read against the same clauses and comes to the same verdict (<<published-modules>>). How a published declaration travels is the compiler's business; that it says what its author wrote is the language's.

An operation the rules name is named as the call that survives to here, which is not every spelling an author can write: `+List.fold+` is the walk `+List.foldFrom+` from the head (<<stdlib-list>>) and is rewritten to it before any of this, so it is the second name the rules are written against. A rule under a name nothing here holds could not be reached, which is why the compiler's own tests hold each rule to a program that fires it.

[#invariant-discharge-preservation]
==== What an operation keeps of what it was built from

An invariant is stated of a value, and a value is often built from another by a standard-library operation. Whether the clause carries across that operation is a property of the operation, and is stated here rather than left to the checker: an author predicts which constructions are judged safe by reading this table.

Four shapes cover the operations.

[cols="1,3"]
|===
| shape | what the result is

| *permutes* | the same elements in another order — `+List.reverse+`, `+List.sort+`, `+List.sortBy+`
| *subset* | some of the same elements — `+List.filter+`, `+List.distinct+`, `+List.distinctBy+`, `+List.take+`, `+List.drop+`, `+Set.filter+`, `+Set.remove+`, `+Set.intersection+`, `+Set.difference+`, `+Map.filterEntries+`, `+Map.remove+`, `+Map.intersection+`, `+Map.difference+`
| *maps* | one new element for each — `+List.map+`, `+List.mapIndexed+`, `+Map.mapValues+`, `+Map.updateIfPresent+`
| *collapses* | at most one new element for each — `+List.filterMap+`, `+Set.map+`
|===

What each keeps:

[cols="1,1,1,1"]
|===
| shape | size | a property of every element | a particular element

| *permutes* | the same | kept | kept
| *subset* | no greater | kept | not kept
| *maps* | the same | not kept | not kept
| *collapses* | no greater | not kept | not kept
|===

So `+List.length(List.map(f, xs))+` *is* `+List.length(xs)+` — how the elements are made has no bearing on how many there are — and a guard on the length of `+xs+` discharges a construction from the mapped list. `+List.length(List.filter(p, xs))+` is its own term, no greater than `+List.length(xs)+`: an upper bound on the input bounds the result, and a lower bound on the input says nothing.

[,souther]
----
data Lines = List<Line>
    invariant List.length(value) >= 1

behavior build : (rows: List<Row>) -> Lines | NoRows
let build (rows) = {
    guard List.length(rows) >= 1 else NoRows
    Lines(List.map(toLine, rows)) // discharged: a mapping keeps the length
}
----

"A property of every element" is a clause like `+List.all(p, value)+` or `+List.allDistinctBy(f, value)+`: nothing new is in a sublist, so what held of every element still holds. "A particular element" is `+List.contains(x, value)+` or `+Set.contains(x, value)+`, which a selection may drop.

An operation not in the table keeps nothing that can be relied on, and a clause over what it built is left to the run-time check.

===== A construction whose result is no smaller than what it read

An operation given two containers, or given a container and something to put in it, establishes no element relation to a single source that any of the four shapes states. Appending and union read two sources, so neither of them alone is what the result was built from; an insert puts in an element or an entry the container it read did not hold. A property of every element does not survive either way. How many there are is bounded from below all the same, and that bound is a fact of its own.

[cols="2,3"]
|===
| what holds of the result | the operations

| no smaller than either container it was given | `+List.append+`, `+Set.union+`, `+Map.union+`
| no smaller than the container it was given | `+Set.insert+`, `+Map.insert+`
|===

`+List.append+` is written `+a ++ b+`, and a `+String+` is joined with the same operator and read the same way: `+String.length(a ++ b)+` is no less than `+String.length(a)+` and no less than `+String.length(b)+`.

So a lower bound on any source named above is a lower bound on the result, and a non-empty list appended to any list is non-empty.

[,souther]
----
data Lines = List<Line>
    invariant List.length(value) >= 1

behavior joined : (a: Lines, b: List<Line>) -> Lines
let joined (a, b) = Lines(a.value ++ b) // discharged: no shorter than `a`
----

Nothing is said the other way. A union answers one of what both sides hold, and an insert of something already there adds none, so the result is not the sum of what was read; an upper bound on the sources does not bound it.

[#invariant-discharge-projection]
===== A projection the closure copied

A mapping keeps no property of its elements in general, but a clause stated over a *projection* is a clause about one field, and a closure that copies that field across keeps it. Two mapped elements differ at a copied field exactly when the two they came from did, so uniqueness by that field carries.

[,text]
----
data QuoteLines = List<QuoteLine> invariant List.allDistinctBy(.product, value)

let toLine (r: LineRequest): QuoteLine =
    QuoteLine { product = r.product, quantity = Quantity(r.quantity), ... }

behavior buildQuote : (lines: List<LineRequest>, ...) -> Quote | DuplicateProduct | NoLines
let buildQuote (lines, ...) = {
    guard List.length(lines) >= 1
        else NoLines
    guard List.allDistinctBy(.product, lines)
        else DuplicateProduct
    Quote { lines = QuoteLines(List.map(toLine, lines)), ... }   // both clauses discharged
}
----

The field the closure copied need not be the one the clause names: when the closure writes `+code = r.sku+`, uniqueness of the result by `+.code+` is uniqueness of the input by `+.sku+`, and that is the guard to write.

This is bounded to a field *copied*. A field the closure computes from others — `+code = String.concat([r.sku, r.name])+` — is not this: two rows that differ can land on one value, and nothing about the result follows.

[#invariant-discharge-guarantees]
==== What an operation guarantees of what it answers

<<invariant-discharge-preservation>> says what carries across an operation from what it was built from. An operation also says something on its own: `+Int.clamp(0, 100, x)+` is between 0 and 100 whatever `+x+` is, and a construction standing over it owes nothing a guard would have to restate. What each operation guarantees is stated here, in the same way and for the same reason: an author predicts which constructions are judged safe by reading it.

A guarantee takes one of four forms.

*A bound on the result.* It holds wherever the call is written, and is read into the clause as it stands.

[cols="2,3"]
|===
| the operation | what holds of its result

| `+Int.abs+`, `+Decimal.abs+` | not negative
| `+Int.floorMod(x, k)+` | not negative, and below `+k+` — both only where `+k+` reads as a constant above zero
| `+Decimal.toInt(mode, d)+` | within one of `+d+`, whichever mode it is handed
|===

Neither end is known without the divisor: `+Int.floorMod+` takes the sign of its divisor, so a divisor that could be negative puts the result the other side of zero — `+Int.floorMod(1, 0 - 3)+` is `+-2+` — and one the check cannot read says nothing about either end. A name given a constant is that constant, so `+let k = 100+` and a written `+100+` are one divisor. A divisor that is above zero for some other reason — a parameter whose type says so — is not read here: what an operation guarantees holds wherever it is called, and that one holds where the caller happens to have such a value.

*One of the values it was given.* What such an operation answers depends on what it was given, so it is stated as the cases its definition is written in, and a clause holds of the result when it holds in every case.

[cols="2,3"]
|===
| the operation | the cases

| `+Int.min(a, b)+`, `+Decimal.min+` | `+a+` where `+a < b+`; `+b+` otherwise
| `+Int.max(a, b)+`, `+Decimal.max+` | `+a+` where `+a > b+`; `+b+` otherwise
| `+Int.clamp(lo, hi, n)+`, `+Decimal.clamp+` | `+lo+` where `+n < lo+`; `+hi+` where `+n >= lo+` and `+n > hi+`; `+n+` otherwise
|===

So a smaller of two non-negative numbers is non-negative, and a value clamped to `+0+` and `+100+` is a percentage. `+clamp+` does not ask that `+lo+` be below `+hi+`: written the other way round it answers `+lo+` for a low `+n+` and `+hi+` for a high one, and a clause that only the ordering would establish is not discharged.

[,souther]
----
data Pct = Int
    invariant value >= 0 && value <= 100

behavior score : (raw: Int) -> Pct
let score (raw) = Pct(Int.clamp(0, 100, raw)) // discharged: every case is inside both ends
----

A case is reached where its condition, the equality saying the call answered that argument, and what the path already establishes can hold at once. What the path knows can put a case out of reach: a value the guards put above four and below six is neither below zero nor above ten, so `+Int.clamp(0, 10, n)+` answers `+n+` and the clause is read there and nowhere else.

A clause reads one such operation. Where it names two — `+Int.min(a, b) + Int.min(c, d) >= 0+` — the cases are not multiplied out, and the clause is left to the run-time check however much is known of the four arguments.

Over the cases that are reached, the clause is established where every one of them establishes it and refused where every one of them refuses it — `+AboveTen(Int.min(0, x))+` is neither of two numbers above ten, and is reported as a violation the values decide. Where no case is reached at all the guards cannot all hold, and this says nothing: a value the program never builds establishes nothing, and what a guard said about the call itself is what answers there.

*A shift stated through a measure.* An operation that moves a value by an amount says how far through the operation that counts two such values apart, which is what an invariant over a pair of them is written in.

[cols="2,3"]
|===
| the operation | what holds

| `+Date.addDays(n, d)+` | `+Date.daysBetween(d, result)+` is `+n+`
| `+DateTime.addMinutes(n, dt)+` | `+DateTime.minutesBetween(dt, result)+` is `+n+`
| `+DateTime.addHours(n, dt)+` | `+DateTime.minutesBetween(dt, result)+` is `+60 * n+`
| `+DateTime.addDays(n, dt)+` | `+DateTime.minutesBetween(dt, result)+` is `+1440 * n+`
|===

`+Date.addMonths+` and `+Date.addYears+` state nothing: months and years hold different numbers of days, so neither moves a date by a count `+Date.daysBetween+` can name.

*A value it was already given.* `+Decimal.fromInt(n)+` answers the number `+n+` is, so a clause about the call and a guard about `+n+` are about one value rather than two.

An operation not named here guarantees nothing that can be relied on, and a clause over what it answered is left to the run-time check. What is known of the arguments at one call is a different question: it is what the path establishes, and it is read where the clause is.

[#invariant-discharge-arithmetic]
==== What arithmetic outside the fragment lies between

A product of two values and a truncating division are not linear, so neither is a form the procedure derives in and each names one value the guards may say nothing about (<<invariant-discharge-terms>>). What is known of such a value is read off what it was computed from, where the construction stands: a quantity times a unit price is not negative wherever both of them are, and there is no third `+guard+` about the product that an author could write instead.

This is not a rule about `+*+` or `+/+`, in the way <<invariant-discharge-guarantees>> states rules about operations. Nothing holds of a product wherever it is written. What holds is decided by what the path establishes about the two factors at the point the clause is read, and a construction under guards that say nothing about them is reported exactly as before.

*A product.* It lies between the least and the greatest of the four products of its factors' ends. A factor with an end nothing bounds leaves the product unbounded that way, except against a factor that is only ever zero, since every value times zero is zero. Both `+Int+` and `+Decimal+` are read: multiplication of either is exact, and an `+Int+` that overflows aborts rather than wrapping (<<primitives>>).

Where an end of the product is at zero, whether the product *reaches* it is a separate question from where the end is. A product is constant along the axis where a factor is zero, so zero is a value it takes wherever either factor holds zero at all — which a factor may do between its ends, or at an end the other factor's ends do not pair with. So `+a * b+` for an `+a+` in `+[0, 1]+` and a `+b+` above zero without reaching it is at or above zero *and reaches it*, at `+a = 0+`, whatever `+b+` is.

[,souther]
----
data NonNeg = Int
    invariant value >= 0

behavior total : (a: Int, b: Int) -> NonNeg | Bad
let total (a, b) = {
    guard a >= 0 else Bad
    guard b >= 0 else Bad
    NonNeg(a * b) // discharged: both factors are at or above zero, so the product is
}
----

*A quotient.* `+x / y+` over `+Int+` truncates toward zero (<<stdlib-int>>). Where what is established at the construction holds every divisor it admits away from zero, the quotient lies within the hull of the quotients of the two ranges' ends — the four of them, as a product's are. A divisor held off zero is of one sign throughout, and over such a range the divide moves one way along each operand with the other held, in a direction that may turn with that other's sign; so what the rule is about is the corners and not an order the ends are taken in. Truncation toward zero never sends a larger value to a smaller one, so the corners put through it are still where the answer stops. A divisor the reading reads as a constant is this with both ends at one number, and a name given a constant is that constant (<<invariant-discharge-terms>>).

What is bounded is the values the operation produced. `+/+` does not answer everywhere — a zero divisor aborts, and so does the one pair whose quotient no `+Int+` holds (<<stdlib-int>>) — and a pair it aborts on leaves no value to bound. So what is stated here says nothing about whether every pair the construction admits is one the operator answers. Whether a divide can abort at all is a separate question, and it is the divisor's own type or a `+require+` that settles it (<<primitive-arm>>).

Nothing follows from this rule unless what is established at the construction keeps zero out of every divisor it admits. That is the condition for stating anything and not a claim about the divides that do answer — over the whole numbers a divisor between zero and five divides by one at the nearest, and where those quotients lie is simply not what this rule says. Nothing follows either where the divisor is left no value an `+Int+` is — the arithmetic a form is read through composes numbers of any size, and what the operator divides by is an `+Int+`, so `+x / (9223372036854775807 + 1)+` divides by a value this operator never has. Neither is a finding about the path. The construction is owed its clause, as it is owed over any other value nothing is known of.

`+/+` over `+Decimal+` is *not* read. It rounds half away from zero at a precision the run time sets (<<stdlib-decimal>>) rather than truncating, and where an end of the quotient lands under that rounding is not something the procedure states. A clause over one is left to the run-time check.

A bound derived this way is not a fact about the value that is kept and read back afterwards. A construction is read twice — under what the guards established, and under what holds of the values whatever the guards did (<<invariant-discharge>>) — and each reading derives its own from what that reading holds. So a bound that rests on a guard is in the first and not the second, and one that follows from what the values' own types guarantee is in both: a product of two values a type puts at or above zero is at or above zero wherever it is built, and a construction the two refuse is refused wherever it is built rather than on a path an author is told to guard. And it refuses as readily as it discharges — a product the guards put on the other side of zero is a construction the value fails, which is an error (<<construction-satisfies-its-invariant>>) and not a warning.

What is derived is derived once, from what is known where the clause is read. Nothing derived here is read back to derive something further about the same value: a bound that tightened under its own answers would be a procedure that decides when to stop, which this is not. A value computed from another such value is read through — `+a * b / 100+` reads what the product was bounded to — because that is the expression's own structure and not a second round over the same one.

[#invariant-discharge-reduction]
==== What a walk from a seed answers

A `+List.fold+` walks a container and hands its step an accumulator and an element, over and over, and answers the accumulator it ends with. What it answers is a value the guards may say nothing about, in the way a product is (<<invariant-discharge-terms>>), and reading it needs neither the length of the container nor the walk written out.

A range holds the answer where two things hold: the seed is inside it, and the step, given an accumulator inside it and what the container guarantees of everything else it hands the step, answers inside it again. Each is asked once. Nothing here iterates towards an answer, nothing widens, and nothing depends on how many elements there are or on the order they arrive in — so the same rule reads a walk over a `+Set+` and over a `+Map+`.

What that establishes is conditional, and the condition is not this procedure's to discharge: *if the walk answers at all, its answer is in the range*. A step whose arithmetic aborts part way — an addition past what an `+Int+` holds (<<primitives>>) — answers nothing, and a range saying where an answer lies says nothing about a run that produces none. Whether the arithmetic can abort and whether the walk terminates are other questions, and neither is weakened by this and neither is assumed by it.

[,souther]
----
data Money = Int
    invariant value >= 0

data Line =
    { amount: NonNeg
    }

behavior total : (xs: List<Line>) -> Money
let total (xs) = Money(List.fold((sum, x) -> sum + x.amount.value, 0, xs))
// discharged: the seed is at zero, and an accumulator at or above zero
// plus an amount at or above zero is at or above zero again
----

The ranges tried are made from the seed and there are four of them: the seed's own range, the range open above it, the range open below it, and the seed's range together with what the step answers where nothing is assumed about the accumulator — which is the whole answer for a step that does not read the accumulator at all. Where more than one is established the answer is what they all say. Which ranges are tried is not a rule an author has to predict: one that cannot be established costs nothing and settles nothing, and a range established is established whichever others were tried beside it.

The seed and what the step answers must both be arithmetic the procedure derives in. A step that chooses between two answers is not read, and neither is one over a value that is what a behavior answered. A step whose answer is arithmetic the fragment cannot carry — a product of the accumulator and the element — is read, and it is read *under* the range being checked: the product is one value nothing is known of until its factors are, and one of its factors is the accumulator the range is about.

[,souther]
----
data Positive = Int
    invariant value >= 1

behavior product : (xs: List<Positive>) -> AtLeastOne
let product (xs) = AtLeastOne(List.fold((acc, x) -> acc * x.value, 1, xs))
// discharged: one is at or above one, and something at or above one times
// something at or above one is at or above one
----

[,souther]
----
behavior total : (xs: List<Line>) -> Money
let total (xs) =
    // not read: the step is a choice and not arithmetic, so the walk is left
    // to the run-time check exactly as before
    Money(List.fold((sum, x) -> if x.amount.value > 0 then sum else 0 - 1, 0, xs))
----

What the container guarantees of what it hands the step is what a value of that type guarantees, and what a container written out holds. It is the same reading that seeds a parameter (<<invariant-discharge>>), so a rule written on a record bounds the field a step reads exactly as a rule written on a newtype the field wears does, and it is read of every parameter the step is handed but the accumulator — a `+Map.fold+` is read over its key as well as its value. A `+[1, 2]+` written on the line holds those two numbers and nothing else, so every element of it is between them.

A relation a guard states about *some* element is not this: it says nothing about the element the walk is at, and a walk is not established by it. Nor is a relation between two places under one parameter — `+end >= start+` written on the element's type is not carried, and a step that needs it gets no range rather than a wrong one. Nor is a container written out with nothing in it: what that would establish is that the step never runs, which is a fact about how many elements there are and not about what any of them is.

[,souther]
----
behavior total : () -> Money
let total = Money(List.fold((sum, x) -> sum + x, 0, [1, 2]))
// discharged: every element written there is above zero

behavior other : () -> Money
let other = Money(List.fold((sum, x) -> sum + x, 0, [1, 0 - 2]))
// reported: an element written there is below zero, so the answer may be
----

The operations read this way are `+List.fold+` and the `+List.foldFrom+` it is sugar for, `+List.foldRight+`, `+Set.fold+` and `+Map.fold+`. That an operation walks a container from a seed is not something its declaration says — an operation of the same shape could apply its closure once — so it is stated rather than read off, and every operation the library declares with that shape either is stated to be one or is written down as not being one. Where the seed and the accumulator are is read off the declaration: the seed is the argument of the type the operation answers, and the accumulator is the closure parameter of that type, which is why `+List.foldRight+` — whose step takes the element first — needs nothing written about it.

A bound established this way belongs to the reading that established it, as a derived bound does (<<invariant-discharge-arithmetic>>): what the step is read against is what the reading holds, and nothing established about the accumulator while the step is read is true anywhere else.

[#invariant-discharge-quantified]
==== A relation over every element, read at the element

`+List.all(p, xs)+` states `+p+` of each element of `+xs+`. Where a standard-library combinator hands its closure an element of `+xs+`, the check reads the relation again there, with the quantifier's own parameter standing for the element the closure was handed.

[,souther]
----
data Qty = Int
    invariant value >= 1
data Line =
    { quantity: Int
    }
data Cart =
    { items: List<Line>
    }
    invariant List.all(i -> i.quantity >= 1, items)

behavior toQty : (cart: Cart) -> List<Qty>
    constructs Qty
let toQty (cart) = List.map(i -> Qty(i.quantity), cart.items) // discharged
----

Both sources of a relation read alike: an input type's invariant clause, and a `+guard+` or `+if+` on the path stating the relation of the same container.

The container the closure walks need not be the one the relation was stated of. Where every element of it is an element of the one that was — a *permutes* or a *subset* construction (<<invariant-discharge-preservation>>) — the relation is read there too, so `+List.map(f, List.filter(q, xs))+` carries what was stated of `+xs+`. A *maps* or *collapses* construction makes elements the relation says nothing about.

A relation the clause in turn states of a container is read at that container's elements as well, so a clause over a container of containers reaches the innermost element.

`+List.any+` is not this: one element having a property says nothing about the rest. Neither is a `+List.all+` the path has *denied*, which says some element fails the predicate without naming which.

[#invariant-discharge-facts]
==== A clause that states no relation is settled, not derived

Not every invariant is a comparison. `+List.allDistinctBy(.product, value)+`, `+List.contains(value, codes)+` and `+String.matches("[A-Z]{2}-[0-9]{4}", value)+` state something about a value that no arithmetic relates to anything else. Such a clause is *settled* by a guard rather than derived: a `+guard+` states its condition where it does not depart and denies it where it does, and a construction under a guard stating the clause is discharged, one under a guard denying it a violation.

[,souther]
----
data Lines = List<Line>
    invariant List.allDistinctBy(.product, value)

behavior build : (rows: List<Line>) -> Lines | DuplicateProduct
let build (rows) = {
    guard List.allDistinctBy(.product, rows) else DuplicateProduct
    Lines(rows) // discharged: the guard states the clause
}
----

Two clauses are one thing exactly when they are the same expression over the same terms. Nothing relates two different ones — uniqueness by `+.product+` says nothing about uniqueness by `+.sku+`, and one pattern says nothing about another. What a binding is *called* is not part of the expression, so `+r -> r.product+`, `+row -> row.product+` and `+.product+` state one thing; a name the closure reads from outside it is part of the expression, so bounding by one field is not bounding by another. Which of the six comparisons an author reached for is not part of it either: `+a /= b+` is `+a == b+` denied, `+a >= b+` is `+a < b+` denied, and which side of an equality a term is written on says nothing — so a `+guard+` on `+a == b+` discharges a clause reading `+b /= a+` where it departs.

This is what a clause outside the derived fragment falls back to, whatever its shape. A comparison over a value the procedure cannot name is settled the same way, which is why `+guard+`-ing it verbatim discharges the construction.

[#invariant-discharge-capability]
==== A clause says how it can be discharged

Some clauses are statically dischargeable and others are not, and the same `+invariant+` keyword is written for both. Left implicit, an author believes there is a static guarantee where there is none. So the compiler answers, per clause, how that clause can be discharged. It is a query rather than a diagnostic — a correct program would carry one on every declaration, which is noise — and the LSP surfaces it on the clause.

[cols="1,3"]
|===
| | what discharges it

| *derivable* | a relation the procedure reasons over. Any guard that *implies* it discharges the construction: `+guard amount >= 10+` discharges `+invariant value >= 0+`.
| *exact match* | a term the procedure can name but not reason about. Only a guard establishing *the same* property discharges it, and nothing weaker: `+invariant List.allDistinctBy(.product, value)+` takes `+guard List.allDistinctBy(.product, lines)+`.
| *always holds* | the clause settles on its own. `+invariant 1 >= 0+` holds of every value, so the obligation is met without a guard.
| *never holds* | it settles the other way, so no guard establishes it and no value of the type can be built. The declaration is refused where the whole compiler runs (<<a-data-can-be-constructed>>); this says what the clause is, so an editor reading half-written source is not told that a guard would discharge it.
| *runtime only* | not representable as a static obligation. No guard discharges it, and the run-time check on construction is the whole of its enforcement.
|===

The first two are the *statically dischargeable* ones, and they are separate because they admit different guards — which is what an author needs to know before writing one. The last carries a reason, because there is more than one way to be outside the fragment and which one it is decides what can be done about it.

A clause that settles on its own is not one outside the fragment. Both leave the construction with no guard to write, and only one of them is something the model says: `+invariant 1 >= 0+` was read to the end and asks nothing, while a clause outside the fragment is one the reading could not carry. They are told apart the way the report tells *not derivable* from *not read* (<<example-report-vocabulary>>), and for the same reason — one is about the model and the other about the compiler.

And a reading that did not finish is *not determined* rather than any of these. Whether a clause is inside the fragment is exactly what such a reading did not find out, so answering it as `+runtime only+` would tell an author that no guard discharges their clause because this compiler fell over on it. Being a fact about the compiler on that run, it names nothing about the clause.

The classification is the clause's own, read with the construction assumed to name what it is given. A construction that names nothing the procedure can name discharges nothing whatever its clauses say (<<invariant-discharge-terms>>); that is a fact about the construction and not about the clause.

The answer carries the clause's name where it has one (<<invariant-clause-name>>), so what the LSP shows on a clause says both how it can be discharged and what an attempt departs by. The unit is the conjunct, as it is for the rest of the check, so a named clause written as `+a && b+` is classified twice under that one name — what discharges each half is what an author acts on, and the name is what a caller reads.

One conjunct has one answer, and the answer has a shape. What is written as one thing need not be read as one thing — a conjunct naming a helper is one thing to its author and is whatever that helper states to the checker — so a reading may find the conjunct to have *several parts*, and *every* part of it has to be established for the conjunct to be. A part may in turn have *several routes*, and *any one* of them establishes that part: a relation the procedure reasons over is often a term it can name as well, and either guard will do.

The two multiplicities are not the same and are not written as one. Read as a set of answers, a conjunct needing a bound in one part and a term in another said that either guard discharges it — so an author writes the one they prefer and finds the construction still refused. A part the checker made nothing of is `+runtime only+` and is a part like the others, since a conjunct is discharged only where every part of it is.

All of them are given at the one place the conjunct is written; the parts have no places of their own, having been written as one.

[#invariant-reify]
==== Reifying a cross-behavior relation

The check does not infer relations across behaviors: an implicit relation would be an implicit cross-unit contract, which the language declines — `+depends on+` and an exposed composition's output are always declared, not inferred (<<depends-on>>). The consistent way to carry a relation that fits in one value is to *reify it as a type invariant*, which the seeding then assumes downstream for free. A relation between a behavior's parameters and its answer, which cannot fit in the answer without changing the model's shape, is declared with `+ensures+` (<<ensures>>).

[,souther]
----
data WithdrawRequest =
    { balance: Money
    , amount: Money
    }
    invariant amount <= balance // the relation lives in the type

behavior withdraw : (req: WithdrawRequest) -> Money
let withdraw (req) = req.balance - req.amount // discharged with no guard here
----

So a possible-violation warning over the fields of one input data is a prompt to declare their relation as that data's invariant. When a value's validity is guaranteed *outside* the language — a Java-side or injected contract stronger than any type — there is no type to reify onto, and that construction keeps its run-time check.

[#decoder]
== Decoder

[#decoder-role]
=== Role

A Decoder is the boundary that constructs internal data from external input. It implements the spec DSL's parse-don't-validate (validate raw data at the boundary and construct a domain type only on success). Souther derives a Raoh Decoder from a data's shape. The derived Decoder reads an external representation (<<external-representation>>) and, on success, constructs T; failure is represented by Raoh's `+Result+` (the failure side carrying `+Issues+`).

[,text]
----
MemberId.decoder() : Decoder<MemberId>     // external representation -> Result<MemberId> (failure = Raoh Issues)
----

A decode failure is not a Souther domain type. Malformed input is not a business result but a boundary concern (<<unmarked-sum>>), so it is not listed among domain sum cases but placed on the failure side of Raoh's `+Result+`. Decode is not a behavior but the edge of the boundary and is not a `+>->+` stage (<<sequential-composition>>). Composition in the domain world is done only with behaviors and `+>->+`.

A Decoder does not appear in Souther's syntax. A default Decoder is derived from a data's field names and types. A data definition writes only `+data+` / `+invariant+` / `+behavior+`. This keeps the mapping to external representation (key names, normalization, discriminators) out of domain definitions. When the default derivation is insufficient, a custom Decoder is written on the boundary side (Java / Raoh) (<<custom-codec>>).

[#decoder-derivation]
=== Derivation rules

The default Decoder is derived from a data's shape by the following conventions.

* A JSON key matches the field name. Each field of an Object is read from the key of the same name.
* A newtype (`+data X = Y+`; <<newtype>>) is read directly from `+Y+`'s representation, not an Object (a bare scalar if `+Y+` is primitive, the derived Decoder of `+Y+` if a named data or sum). `+{ ... }+` is read as an Object even with a single field.
* A nested named data is read recursively with that data's derived Decoder.
* `+List<T>+` is read element by element with `+T+`'s derived Decoder.
* `+T?+` (Option; <<optional>>) at a field is `+None+` if the key is absent or the value is `+null+`, and `+Some+`-wrapped after reading `+T+` if a value is present. Where an optional stands with no key of its own — a `+List+`'s or a `+Set+`'s element, a `+Map+`'s value — `+null+` is the whole of what `+None+` is read from (<<absence-is-written-as-null>>).
* Invariants are checked on construction; a violation becomes a failure (`+Issue+`) of Raoh's `+Result+`. Because decode always succeeds or fails, the destination of a violation always exists (<<violation-destination>>).
* Errors of independent fields are accumulated (Applicative; <<case-propagation>>). Accumulation is handled by Raoh combinators.

[,souther]
----
// a newtype reads from a bare integer (the data X = Y form)
data Amount = Int
    invariant value >= 0
// derived Decoder: Int(n) -> Amount { value: n } (with invariant check)

// a multi-field data reads from an Object, field name = key
data Employee =
    { id: EmployeeId
    , rank: Rank
    , managerId: EmployeeId
    }
// derived Decoder: reads { "id": ..., "rank": ..., "managerId": ... } with each field's derived Decoder
----

The invariant check on construction and spread (`+...+`) follow the same semantics as a record literal (<<field-spread>>, <<record-literal>>).

[#sum-discrimination]
=== Discrimination of sum data

The derived Decoder of a sum data reads the discriminator field `+"type"+` and dispatches to the case's derived Decoder. The tag is the case name.

[,souther]
----
data TravelRequest = Drafting | Submitted | AwaitingPreApproval | PreApproved
// derived Decoder: reads "type" and dispatches "Drafting" -> Drafting, "Submitted" -> Submitted, ...
----

A sum every one of whose cases is a unit data (folded to the leaves) is an *enumeration*: it carries nothing but which case it is, so it travels as that case's name — a bare string — and not as an object holding a discriminator alone. This is the form a fieldless enumeration has everywhere else, and it is what lets such a sum key a `+Map+` at the boundary (<<collections>>): a JSON object's key cannot be an object. A name no case answers to fails the decode at that value's path. One field-bearing case is enough to make the sum an object with `+"type"+` again.

[,souther]
----
data Stage = Prospecting | Won | Lost
// derived Decoder: reads "Prospecting" / "Won" / "Lost"
// derived Encoder: Won -> "Won", so a field is { "stage": "Won" } and a key is { "Won": 300 }
----

A nested sum is folded to the leaf case and dispatched.

[,souther]
----
data OwnCost = Reimbursement | Advance | CorporateCard
data CostBearer = OwnCost | ClientCost
// derived Decoder: the name is one of "Reimbursement" / "Advance" / "CorporateCard" / "ClientCost"
// (every case is a unit data, so this one is an enumeration).
// The tag "OwnCost" does not appear in the external representation.
----

Folding is done because only the leaf identifies the value. Tagging a direct case would break the round trip (<<round-trip>>). Once `+CostBearer+`'s encode writes `+"OwnCost"+`, which leaf it was is lost, and the subsequent decode of `+OwnCost+` would reject that name as not one of its own. Folding at the leaf lets the same external representation be read by any layer's decoder. This is the same as case→sum assignment folding to the leaf (<<sum-data>>). Exhaustiveness in `+match+` is checked per layer (<<match>>), which is a separate matter from external representation.

A case's contents are decided by the case's shape. A product case lays fields flatly in the same Object as `+"type"+` (`+{"type":"Submitted", "applicant":…, …}+`). A unit case writes only `+"type"+` (`+{"type":"Staff"}+`). A newtype case (`+data X = Y+`; <<newtype>>) cannot place `+"type"+` on a bare scalar, so it puts the contents under the `+"value"+` key.

[#discriminator-field]
[[a-case-does-not-declare-the-discriminator-field]]Because a product case lays its fields there, a case MUST NOT declare a field named `+"type"+` — the field and the tag would want one key, and whichever was written second would be all that was left. This is a compile error where the case is declared, not a value quietly written over where it is encoded. It holds of a member of a behavior's output union for the same reason (<<union-encoder>>), and of a field a case takes in by spread as much as one it writes. A data that is neither a case nor a union member may have the field: nothing writes a discriminator beside it.

[,souther]
----
data Rank = Manager | Staff
data Manager = Level // newtype
data Staff // unit
// derived: Manager -> {"type":"Manager","value":3}, Staff -> {"type":"Staff"}
----

This maps directly onto Raoh's `+discriminate("type", …)+`. The discriminator selects `+"Manager"+` and hands that case's decoder what is under `+"value"+` — the bare `+3+` — while a product case is handed the Object itself and reads its fields there. Each case's decoder reads what the case is on its own, and the sum's decoder takes the contents out of where membership put them. A standalone newtype (not a sum case) stays bare (`+3+`); the `+"value"+` wrapping is what membership in a sum adds, so a newtype's own representation does not change when some sum names it.

Folding to the leaf concerns nested *sums*; a newtype case stops there. A newtype is not a sum, so making `+data InsufficientAuthority = Rank+` (Rank is a sum) a case of `+PreApprovalReason+` yields `+{"type":"InsufficientAuthority","value":{"type":"Manager","value":3}}+`, where the inner Rank is discriminated independently under `+"value"+`.

When the discriminator key or tag is business-specific (reading `+"status"+` and dispatching to `+"draft"+` / `+"submitted"+`, or reading a persistence `+representation_version+` for versioning), that is a matter of the external representation and cannot be derived from a data's shape. It is done with a custom Decoder on the boundary side (<<custom-codec>>).

[#custom-codec]
=== Custom boundary codec

[[a-custom-codec-agrees-with-its-type]]A codec written by hand MUST be a codec for the type it is declared on: a decoder for `+T+` constructs a `+T+`, an encoder for a sum writes every case, an element encoder matches the element type, and a source-shaped encoder is given that shape. What is derived agrees with the type because it was read off it; what is written has to be checked against it.

[[a-codec-reached-for-exists]]`+T.decoder+` and `+T.encode+` MAY be written only where `+T+` has one. A type whose representation was never derived and never given has neither to reach for.

When the default derivation is insufficient (a different key name, normalization, default filling, a business-specific discriminator, persistence version branching), write a Decoder on the Java side that assembles the external representation and calls the data's (invariant-checking) construction. The `+JdbcFindMember+` calling `+Member.decoder()+` in <<java-example>> is this form. External-representation conversion stays confined to the boundary and is not brought into domain definitions.

A business-specific sum tag does not mean rebuilding the case decoders. Souther generates a public per-case decoder (`+Submitted.decoder()+`) and a sum decoder that is only a `+discriminate("type", …)+` over them (<<sum-discrimination>>). The boundary keeps the per-case decoders and rewrites just that one layer with its own key and tags — reading `+"status"+` and dispatching `+"draft"+` / `+"submitted"+` is `+discriminate("status", variant("draft", Drafting.decoder()), variant("submitted", Submitted.decoder()))+`. Field assembly and invariant checks are reused; only the tag table is written. A newtype case is reached the same way through the key its contents sit under — `+variant("manager", field("value", Manager.decoder()).asDecoder())+` — since its decoder reads what the case is on its own. The encoder side is the mirror of it: a per-case encoder writes what the case is on its own, so a custom sum encoder writes the tag beside it, in the case's own Object where it has one and beside the contents where it does not.

[#decoder-error]
=== Decoder errors

A decode failure is carried by the failure side of Raoh's `+Result+`. An error is a Raoh `+Issue+` (with `+path+` / `+code+` / `+message+`), and failures of independent fields are accumulated (<<case-propagation>>). Souther does not own the *boundary's* failure vocabulary. No domain type such as `+DecodeFailed+` or `+DecodeError+` is introduced; the representation of boundary failure is delegated to the peripheral library Raoh.

This differs from a violation inside the domain (<<algebraic-types>>). A break at the boundary means a value from outside was invalid — an expected result, and which field broke it should be statable (`+path+` and `+code+`), which Raoh holds. A break inside the domain means a computation among validated values broke a rule — a model bug — so it aborts (<<violation-destination>>). The boundary's vocabulary is not brought into the domain, and the domain's vocabulary is not taken out to the boundary.

At the boundary (HTTP, etc.), the `+Result+` failure is received and mapped to an appropriate response (e.g. 400). The `+MemberController+` branching on the decode `+Result+` in <<java-example>> is this form.

==== Codes an invariant produces

A newtype's invariant reaches the boundary as the Raoh constraint that states the same rule, so the failure carries that constraint's `+code+`, its `+messageKey+` and its metadata: `+String.length(value) >= 1+` fails as `+too_short+` with `+min+`, `+String.matches(p, value)+` as `+invalid_format+`, a numeric bound as `+out_of_range+` with `+min+` / `+max+`, `+List.length(value) >= 1+` as `+too_small+` with `+min+` / `+actual+`, and `+List.allDistinctBy(x -> x, value)+` as `+duplicate_element+` with the duplicates it found. The vocabulary is Raoh's, so a `+MessageResolver+` resolves and localises these without Souther supplying message text. It is keyed on the issue rather than on the code alone: one code covers several constraints — `+out_of_range+` states a lower bound, an upper bound, or both, and `+value > 0+` and `+value >= 0+` carry the same bound — so which constraint rejected the value is what the `+messageKey+` says and the `+code+` cannot. Which type rejected it is told by `+path+`, as for any other decode failure.

Only a rule with an exact Raoh equivalent is mapped this way. Anything else — a quantifier over a list, a uniqueness under a projection, a check across two fields, an object's invariant — runs as the rule it is and fails with the code `+invariant_violation+`, carrying the rejecting type in the metadata — its declaring module as `+module+` and its name as `+type+` — and, where the clause was named (<<invariant-clause-name>>), that name as `+clause+`. The module is its own entry because a type is its module and its name (<<modules>>): two modules may each declare an `+Id+`, and a resolver keyed on the name alone would answer for either. So a boundary failure says which rule was broken and not only that the value was rejected. A clause's name never becomes the `+code+`: the code stays the shared one, so a resolver keyed on Raoh's vocabulary keeps working and the metadata is what it switches on.

The clauses are checked in the order they are declared, which is the order construction decides in (<<invariant-mvp>>) — so the boundary and an attempted construction name the same clause for the same value. A mapped clause declared after an unmapped one gives up its Raoh code to keep that order: Raoh answers a plain `+Decoder+` once a rule of its own has been chained on, and a typed constraint cannot follow. Construction re-checks every clause regardless (<<violation-destination>>): the constraints are how a failure is *reported*, not what makes a value valid.

[#codec-generation]
=== Generation method and Raoh

The derived Decoder, without going through an independent Decoder IR, is generated *directly as bytecode* by the ClassFile backend as a Raoh Decoder (`+net.unit8.raoh.decode.Decoder+` and combinators). The generated decoder reads from the neutral tree of the external representation (<<external-representation>>; Raoh's `+Object+` / `+Map+` input) and returns `+Result<T>+`.

[,text]
----
Source -> AST -> (fill decoder/encoder into the AST by derivation) -> type check -> ClassFile backend -> .class (emits Raoh Decoders)
----

Souther's runtime (souther-runtime) does not depend on Raoh. Only the generated bytecode and the apps using it depend on Raoh. Primitive parsing, error accumulation (Applicative), and discriminator dispatch (<<sum-discrimination>>) are handled by Raoh combinators; Souther only maps a data's shape (keys, types, discriminator, case tags) onto Raoh decoder construction.

Decoders are generated per input source. Bare values / Map use raoh-core combinators, JSON `+JsonNode+` uses raoh-json, and jOOQ `+Record+` uses raoh-jooq. The skeleton (keys, types, discriminator, case tags) is common across sources; only terminal field access is swapped per source. For each exposed data, decoders are generated for the sources its shape supports. Bare values / Map and JSON are always generated. JSON temporal fields are read as ISO 8601 strings (`+string().date()+` / `+string().time()+` / `+string().dateTime()+` / `+string().iso8601()+`), and a `+Time+` and a `+DateTime+` are then held to the second: text carrying a fraction of one is a decode failure at that field's path, not a value rounded on the way in (<<a-local-temporal-is-held-to-the-second>>). jOOQ `+Record+` decoders are generated only for a flat shape with no nesting, List, or Map (scalar and newtype columns only) — a row is flat, so a nested object does not fit in one column. Unit data ignore input, so they support all sources.

[#encoder]
== Encoder

[#encoder-role]
=== Role

An Encoder is a total function converting internal data to an external representation (<<external-representation>>). Souther derives a Raoh Encoder from a data's shape.

[,text]
----
TravelRequest.encoder() : Encoder<TravelRequest>     // TravelRequest -> external representation (Raoh's neutral tree)
----

Like a Decoder, an Encoder does not appear in Souther's syntax. A default Encoder is derived from a data's field names and types (<<decoder-role>>). Encode is also an edge of the boundary and is not a `+>->+` stage.

[#encoder-derivation]
=== Derivation rules

The default Encoder is derived by rules symmetric to the Decoder.

* A newtype (`+data X = Y+`; <<newtype>>) writes directly to `+Y+`'s representation (a bare scalar if `+Y+` is primitive). `+{ ... }+` writes to an Object even with a single field.
* A multi-field data writes to an Object. The key is the field name; the value is converted by each field's derived Encoder. Fields taken in by a `+...+` spread are laid flatly too.
* A nested named data is converted by that data's derived Encoder.
* `+List<T>+` is converted element by element with `+T+`'s derived Encoder.
* `+T?+` (Option; <<optional>>) at a field: if `+Some(v)+`, write `+v+` at the key; if `+None+`, omit the key, which is read back as `+None+`. Where an optional stands with no key of its own — a `+List+`'s or a `+Set+`'s element, a `+Map+`'s value — `+Some(v)+` writes `+v+` and `+None+` writes `+null+`, which is read back as `+None+` (<<absence-is-written-as-null>>). The round-trip law (<<round-trip>>) holds either way.
* A sum data tags the discriminator field `+"type"+` with the case name and converts with the case's derived Encoder. A product case lays fields flatly, a unit case writes only the tag, a newtype case puts contents in `+"value"+` (<<sum-discrimination>>). A nested sum writes the leaf case's tag.

[,souther]
----
data Amount = Int
    invariant value >= 0
// derived Encoder: Amount -> Int(self.value) (bare integer)

data Submitted =
    { ...TravelRequestCommon
    , submittedAt: DateTime
    }
// derived Encoder: Submitted -> { "applicant": ..., "estimatedCost": ..., ..., "submittedAt": ... }
----

To match key names or representation to an external spec, write a custom Encoder on the boundary side (<<custom-codec>>).

[#round-trip]
=== Round-trip law

The following is recommended (not required).

[,text]
----
decode(encode(value)) == value
----

`+encode(decode(raw)) == raw+` is not required, because a Decoder may normalize, fill defaults, or drop surplus items.

[#encode-law]
=== What equal values write

At a boundary position — a field, a behavior's input or output — the type is declared, and that declaration fixes one derived Encoder. Under that Encoder, a value's *value* is all that is written about it:

[,text]
----
a == b  ⟹  E.encode(a) == E.encode(b)      // E the Encoder the position's declared type derives
----

Two values the language calls equal (<<equality>>) are written the same JSON, byte for byte. This is what lets a response body be compared, diffed, cached under its own bytes, or written into a golden test. It holds because everything a value carries that its identity does not is settled on the way out: a `+Decimal+` is written as its amount and not with the scale it was read at (<<primitives>>), and a `+Set+` and a `+Map+` are written in the order their members' own representations give and not in the order they were built (<<collections>>).

The position is part of the law and not a technicality about it. A case value *is* a value of its sum (case→sum is transparent, <<sum-data>>) and equals itself at either type, but the two positions do not write it alike: a case's own Encoder writes no discriminator and its sum's writes one (<<sum-discrimination>>). So `+Approved { id = 1 }+` is written `+{"id":1}+` where `+Approved+` is what is declared, and `+{"id":1,"type":"Approved"}+` where `+Approved | Rejected+` is. What is written is decided by the type the position declares, so the law is read one position at a time — it says that a position answers the same bytes for equal values, not that a value has one representation wherever it goes.

A *custom* Encoder (<<custom-codec>>) is written by hand and is bound by nothing here. The law is about the Encoders derived from a data's shape (<<encoder-derivation>>).

[#behavior]
== behavior

A behavior defines the *specification* of a behavior over internal data. It is a top-level input/output relation with no owner.

A declaration takes the form `+behavior name = right-hand side+`. There are two right-hand sides, and what is written and what is left undetermined are inverted between them.

If *an argument list and output* (<<behavior-io>>) are written, what is written is the type. The implementation is undetermined: writing a `+let+` makes it the implementation (<<fn>>); writing none injects it from Java (<<injected-behavior>>). A behavior of this form has no body. What the spec DSL notes as `+// depends:+` comes here.

If a `+>->+` composition (<<composition>>) is written, what is written is the assembly step of chaining stages in this order, and that is itself the implementation. So no `+let+` is needed and no injection occurs. The type is undetermined and computed by the rules of <<type-routing>>.

Both are the same form as the spec DSL's `+behavior+`. The DSL writes both with `+=+`.

[,text]
----
// spec DSL
behavior preApprove = AwaitingPreApproval AND ApproverId -> PreApproved OR NotAuthorized   // right side is a type
behavior placeOrder = validateOrder >-> priceOrder >-> allocateStock                      // right side is a composition
----

That a type or a term may come to the right of `+=+` is because the DSL writes it that way; it is not a distinction Souther introduced. The same two lines map to Souther as:

[,souther]
----
behavior preApprove : (
    request: AwaitingPreApproval,
    approverId: EmployeeId
) -> PreApproved | NotAuthorized
    constructs PreApproved // a type. Implementation is a let or Java injection

behavior placeOrder = validateOrder >-> priceOrder >-> allocateStock
// a composition. It is itself the implementation

// spec DSL: // depends: clock
behavior now : () -> DateTime // write the type, no let → injected from Java
----

Specification and implementation are separated not because `+data+` already does so, but *so that one line of the spec DSL remains as is* (<<non-functional>>). Just as `+invariant+` rides on `+data+`, `+constructs+` rides on `+behavior+`. Both are the part Souther makes executable of what the spec DSL left in comments (<<mapping-principle>>).

[#behavior-io]
=== Input and output

The spec DSL's input `+AND+` maps to an argument list. A parameter is always written `+name: type+`.

[,souther]
----
// spec DSL: behavior decidePreApproval = EstimatedCost AND Applicant AND CostBearer -> PreApprovalReasons
behavior decidePreApproval : (
    estimatedCost: Amount,
    applicant: Employee,
    costBearer: CostBearer
) -> List<PreApprovalReason>
----

A parameter name is for the reader and does not affect type identity. The implementation (`+let+`) gives its own names and corresponds by position. A type MUST NOT be written without a name: if `+(String)+` were writable, whether it meant "the type String" or "the name String" would be unreadable.

Zero arguments is written `+()+` (as in `+now+`). "No argument" and "takes one `+Unit+`" are different; `+Unit+` is not exposed at the surface (<<algebraic-types>>).

[#unmarked-output]
=== Output is a bare sum

The spec DSL's output `+OR+` maps directly to a bare sum. No success/failure distinction and no marker such as `+error+` is attached. A behavior is a function from input to one of the possible business results.

[,souther]
----
// spec DSL: behavior submitTravelRequest = Drafting -> Submitted OR AwaitingPreApproval
behavior submitTravelRequest : (request: Drafting) -> Submitted | AwaitingPreApproval
    constructs Submitted, AwaitingPreApproval

// spec DSL: behavior preApprove = AwaitingPreApproval AND ApproverId -> PreApproved OR NotAuthorized
// NotAuthorized is just data. Not an error, not a Result.
behavior preApprove : (
    request: AwaitingPreApproval,
    approverId: EmployeeId
) -> PreApproved | NotAuthorized
    constructs PreApproved
----

The commonest shape a model asks for is "the amount, or the reason there is none", and the value it answers with is usually a primitive or a type another module owns. Both are members (<<union-member>>), so the behavior answers with the value itself and no type is declared to hold it.

[,souther]
----
// spec DSL: behavior computeTaxableAmount = Remuneration AND UpliftPay AND SocialInsurance -> Yen OR DeductionsExceedGross
behavior computeTaxableAmount : (
    remuneration: Remuneration,
    upliftPay: Yen,
    socialInsurance: Yen
) -> Yen | DeductionsExceedGross // Yen is imported; it is the answer, not a wrapper around it
    constructs Yen, DeductionsExceedGross

behavior half : (n: Int) -> Int | NoAnswer // a primitive is a member the same way
    constructs NoAnswer
----

The value is read back with the arm that names it — `+| Yen as y+`, `+| Int as n+` (<<primitive-arm>>) — and binds bare.

Which case "departs as a failure" is not decided by this behavior alone. When composed with `+>->+`, the case the immediately following stage does not consume departs the mainline (<<composition>>). If a downstream stage takes `+NotAuthorized+` as input, that case is processed; if not, it departs and is carried to the output. The same value departs in one composition and stays on the mainline in another.

An input may be a sum (spec DSL `+(Submitted OR PreApproved) AND ...+`). A behavior's parameter takes a declared name rather than an anonymous one (<<a-parameter-names-one-type>>), so declare the sum as a named data and write that name. The implementation opens the cases with `+match+`.

[,souther]
----
// name (Submitted OR PreApproved)
data CompletableRequest = Submitted | PreApproved

// spec DSL: behavior completeTravel : (Submitted OR PreApproved) AND ActualCost AND TravelReport -> TravelCompleted OR InvalidActualCost OR NoTravelReport
behavior completeTravel : (
    request: CompletableRequest,
    actualCost: Amount,
    travelReport: String
) -> TravelCompleted | InvalidActualCost | NoTravelReport
    constructs TravelCompleted, InvalidActualCost, NoTravelReport
----

A behavior's *input* type is always a single named type, including a named sum (<<a-parameter-names-one-type>>); of the two positions a signature has, the *output* is the one an anonymous union is written in (<<union-intersection>>). Naming the input lets the reader fix the candidates by looking once at `+data CompletableRequest = Submitted | PreApproved+`, and on the JVM the input has a name so a Java implementation receives it as a single type (an anonymous union has no common supertype; <<asymmetric-interop>>, <<jvm-anonymous-union>>).

[#constructs]
=== Data construction authority (constructs)

A behavior that constructs data MAY write the constructed type in `+constructs+`. This is an internal construction authority; unlike a dependency (`+depends on+`), it is invisible to callers. So for a behavior with a `+let+` implementation it is *optional* — omit it and it is inferred from the body. If written, it MUST *exactly match* the body. An injected behavior has no body to infer from, so what it writes is what its implementation may build, and it MUST write every such data (<<java-base-class>>). A behavior whose construction set is empty writes no clause, injected or not: the unit cases of an injected behavior's output get their factories from the output type rather than from this clause (<<constructs-excludes-unit-data>>).

Exact agreement is two obligations, and a declaration can fail either one on its own:

* [[constructs-authority-complete]]every data in the behavior's construction set MUST appear in the clause (<<e1002>>);
* [[constructs-no-overdeclaration]]every data the clause names MUST be in that set (<<e1006>>).

What the set is made of is <<constructs-excludes-unit-data>>: a unit data is constructed but holds no construction authority, so it is not in the set and MUST NOT be named.

[,souther]
----
behavior applyChange : (input: ChangeEmailRequest) -> Member
    constructs Member
----

To construct multiple data, list them: `+constructs Member, AuditRecord+`.

An entry names a type the way a type is named anywhere else, so a type another module declares may be written through it (`+constructs up.Amount+`) as well as under the name an `+import+` brings in. Which spelling the clause uses and which the body uses are independent — both name one type, and that is what E1002 and E1006 compare. Naming one type twice, however spelled, is a duplicate entry.

The following count as data construction: a record literal (`+TypeName { ... }+`), a sum data constructor, a unit data construction, and constructing a collection containing new data as elements. Returning an existing value unchanged does not count as construction.

Construction and *membership of a behavior's construction set* are not the same question, and a unit data is where they part. [[constructs-excludes-unit-data]]A unit-data construction does not contribute to a behavior's construction set and requires no construction authority, and a unit data MUST NOT be named in `+constructs+` (<<e1026>>). Writing `+Domestic+` still constructs the `+Domestic+` value (<<unit-data>>); what it does not do is put `+Domestic+` in the set the clause declares.

A unit data has no fields, cannot declare an invariant (<<unit-data>>), and has exactly one value. So there is nothing for <<invariant-on-construct>> to guard, and no observable difference between minting another `+Domestic+` and passing an existing one through — which is the very distinction the clause exists to record. Authority is not what a unit needs either: a case is reachable through the decoder or the `+protected+` factory rather than by being named, which is what <<closed-construction>> already rests on for cases (<<jvm-anonymous-union>>).

This keeps the clause exact rather than optional. Were a unit merely allowed to be omitted, `+constructs Member+` and `+constructs Member, Missing+` would both be correct for one body and the exact match above would say nothing.

[,souther]
----
data Kind = Domestic | Overseas
data Missing

// `Domestic` is compared against, not declared: the clause names `Decision` alone
behavior classify : (r: Request) -> Decision
    constructs Decision
let classify (r) =
    if r.kind == Domestic then Decision { auto = true } else Decision { auto = false }

// and `Missing` is a unit, so it is not named either
behavior find : (id: MemberId) -> Member | Missing
    constructs Member
----

Every one of these constructs the unit value and none of them puts it in the set: `+Domestic+` written bare, `+f(Domestic)+`, `+[Domestic]+`, `+r.kind == Domestic+`, and `+guard allowed else Domestic+`. A helper `+let+` that mentions or returns a unit likewise carries none to its caller.

The built-in `+Some+` / `+None+` (<<algebraic-types>>) do not count as data construction. Option is not domain data but an auxiliary type and has no invariant, so <<invariant-on-construct>> has nothing to guard. So `+constructs+` is not required to put a value in a `+?+` field (<<optional>>) — and since Option cannot be written as a type, there is no way to declare `+constructs Some+` anyway (<<algebraic-types>>).

*If you write `+constructs+`, you may not omit anything in the behavior's construction set.* The departure value of `+guard ... else+` (<<guard>>) is no exception: `+else TooManyPending { limit = n }+` builds a record on the failure path and belongs in the clause, and what <<closed-construction>> closes is construction regardless of success or failure. A departure written as a bare unit case — `+else NotAuthorized+` — is the usual shape and is a unit-data construction, so it is not in the set and is not named (<<constructs-excludes-unit-data>>).

==== What constructs is for

`+constructs+` is not for guarding invariants — that is handled by `+__construct+` and the Decoder, both of which always check on construction (<<invariant-mvp>>). What `+constructs+` handles is *reading the model*: from the declaration, whether a behavior *creates* a new value or merely *passes through* an existing one. This is not visible from the output type.

[,souther]
----
behavior preApprove : (
    request: AwaitingPreApproval,
    approverId: EmployeeId
) -> PreApproved | NotAuthorized
    constructs PreApproved // creates new states

behavior checkCeiling : (a: Amount) -> Amount | OverCeiling
    constructs OverCeiling // Amount returns the input as is; not created
----

Both have `+Amount+` / `+PreApproved+` in the output, but one creates and the other passes through. This distinction does not appear in the output type. Writing it leaves it in the declaration and makes a check against reading and mis-taking. Omit it and it is inferred — write it (opt-in) if you want the reading and the check.

Creating a type that does not appear in the output also shows here. The `+constructs HighValueTravel, InsufficientAuthority+` of `+decidePreApproval = (...) -> List<PreApprovalReason>+` is information nowhere in the output type.

A helper `+let+` (with no corresponding behavior) may also construct data, but a helper `+let+` is not a specification statement and does not declare its own `+constructs+`. Its *construction set is inferred transitively* (folding in any helper `+let+` it calls). If the calling behavior writes `+constructs+`, it MUST include that set (E1002 if it does not); if omitted, that set is folded into the inference. A block is the same, constructing under the enclosing behavior's authority (<<blocks>>).

A *value* is the other case. It too is substituted where it is named (<<fn-value-semantics>>), but what it builds is built by its definition: a behavior stating a rule against a named limit originates none of it, and the limit does not belong in that behavior's `+constructs+`. This is what lets a threshold be named at all — the figure gets one place to live and one comment saying where it comes from, and the rules reading it go on saying what they do. Writing the entry anyway is accepted, the same answer a construction another module's published body carried in gets (<<exposed-values>>).

A dependency (`+depends on+`) is different and MUST *always* be written (<<depends-on>>). `+depends on+` is an outward contract to the caller and injector; leaving it to inference would let the contract change silently. `+constructs+` is an internal authority invisible to callers, so they diverge here. Thus `+depends on+` is a declaration and `+constructs+` (for a behavior with a `+let+` implementation) is optional. A `+>->+` composition infers both output and requirements (<<composition-with-requirements>>).

[#record-literal]
=== Record literals and spread

Construction is done with a record literal that writes the constructed type name.

[[a-construction-names-a-data]]What is constructed MUST be a data. [[a-construction-names-fields-of-its-type]]A field written in the literal MUST be one that data declares, and [[a-construction-is-written-where-one-may-be]]a construction MUST be written where one is allowed — not, for instance, inside an `+invariant+`, which states a condition over a value rather than building one (<<invariant-expressions>>).

[[a-data-can-be-constructed]]A data MUST be constructible at all, and it comes to have no value in more than one way. Rules that cannot all hold leave nothing satisfying them. A `+Set+` asked to hold more values that differ than its element has cannot be filled. A self-referential data reaches itself with nothing to bottom out on, so no first value of it can be built; the self-reference belongs at an occurrence that has a base case — an optional, or a collection the rules let be empty (<<self-referential-data>>). All of them are one count coming to nothing, and the refusal says which of them was shown.

[[rules-leaving-a-position-no-value-are-refused]]*Rules that leave a position no value its order holds are refused, on every ordered type.* A position bounded above a value it is bounded below of has none, and so does one bounded past where its order stops — above the last case an enumeration declares, below the empty string, outside the day a `+Time+` runs through — and so does one two equalities name two values of. This is one rule and it does not depend on what carries the value: `+value > 5 && value < 3+` over an `+Int+`, `+value > Date("2020-01-01") && value < Date("2010-01-01")+` over a `+Date+`, and `+c > Blue+` over an enumeration are one thing shown and are refused alike, in one sentence naming the position. A rule stated as one of several alternatives is read as one: a choice among alternatives none of which admits anything admits nothing.

This is the same list as the values a line is drawn on (<<a-line-is-drawn-where-the-values-can-carry-one>>). Deciding that a rule admits nothing needs the order and the values a literal names, and drawing a line needs a value to be written back at the position as well; every ordered type this language has can be written back at, so the two lists coincide.

[,text]
----
PreApproved {
    applicant     = request.applicant,
    estimatedCost = request.estimatedCost,
    submittedAt   = request.submittedAt,
    preApprovedAt = now(),
    preApprover   = approverId
    // ... list the rest of the common items too
}
----

To fill same-named fields together, use a spread `+...src+`. It copies `+src+`'s same-named fields and states the rest explicitly. Surplus fields of `+src+` are ignored; a field missing from `+TypeName+` is a compile error.

[,text]
----
// copy the common items and submittedAt of request (AwaitingPreApproval), and add two
PreApproved { ...request, preApprovedAt = now(), preApprover = approverId }
----

To replace only some fields while keeping the same type, also write the type name and use a spread.

[[a-spread-copies-a-product]]What is spread MUST be a product data, or a sum whose cases all spread one. There has to be a set of fields to copy, and a sum whose cases share nothing has none.

[[a-field-is-supplied-once]]A field is supplied once. A field the literal writes takes the value written, whatever any spread carries; where several spreads carry one field, the first of them supplies it and the rest carry it no more than they carry any other field the literal does not want. So what every field of the construction is given is settled by the literal alone, and it is the same answer wherever it is asked — what runs, what the field's type is checked against, and what the invariant check reads.

[[a-spread-supplies-what-the-fields-need]]What a spread provides MUST have the type the field it lands on needs. Copying by name is not copying by shape: two fields of one name and two types are two fields. If `+x+` has type `+Member+`, write `+Member { ...x, address = newAddress }+`. No dedicated update syntax is provided.

When a field value is a binding of the same name as the field, the value may be omitted. `+Member { address }+` is shorthand for `+Member { address = address }+`, filling from the same-named binding in scope.

A record literal and a spread both require construction authority (`+constructs+`). Because the type name appears at the construction point, the authority check and field check are done on the spot.

[[construction-supplies-every-field]]Every field the constructed type declares MUST be given a value — written out, filled from a same-named binding, or supplied by a spread. A spread supplies what the type it copies from declares and nothing else.

[#blocks]
=== Blocks

To apply processing to each element of a list, pass a block as an argument. A block is written `+<args> -> <expr>+`.

[,souther]
----
// spec DSL: data UnvalidatedOrder  = ... AND List<UnvalidatedOrderLine>
//           data ValidatedOrder = ... AND List<ValidatedOrderLine>
// validate and repack each detail. A block is needed here.
behavior validateLines : (xs: List<UnvalidatedOrderLine>) -> List<ValidatedOrderLine>

let validateLines (xs) = List.map(x -> validateLine(x), xs)
----

A block is an anonymous `+let+`. It is the same as a `+let+` (<<fn>>), only unnamed. A named one may be passed by name.

[,text]
----
let isValid (r: PreApprovalReason) = ...

List.all(r -> isValid(r), reasons)   // wrap in an anonymous block
List.all(isValid, reasons)           // pass by name directly. Same thing
----

A name that names a function *is* that function where a value goes, whoever declared it: a `+let+` of this module, a `+let+` another module published, and a standard-library function alike. `+String.trim+` written where a value goes is a `+(String) -> String+`, and `+String.trim(s)+` is that function applied to `+s+`. So the two lines above are one rule rather than two, and a function reaches every position a block reaches — an argument, a binding, a branch of an `+if+`, an element of a list a `+fold+` walks.

[,souther]
----
let step: (String) -> String = if flag then String.trim else String.uppercase
let steps: List<(String) -> String> = [String.trim, String.uppercase]
----

Application is written after what is applied, and what is applied is any expression: a name (`+f(x)+`), a parenthesised expression (`+(if flag then String.trim else String.uppercase)(s)+`), or the result of an application (`+adder(1)(x)+`). What is applied is evaluated once, and before the arguments. An argument list MUST begin on the line its callee ends on — a `+(+` that opens a line is a parenthesised expression, so a block whose result is a tuple written under a call reads as that block's result and not as an application of the line above.

A behavior's name handed over is the *behavior*, not the `+let+` that implements it: a Java implementation replaces the behavior, so passing one to `+List.map+` and calling it by name reach the same place (<<calling-a-behavior>>). A behavior with a requirement arrives bound through `+depends on+` and is a binding by the time it can be named, so nothing carries a requirement past that statement.

A block and a `+let+` may be passed as arguments, and a `+let+` may take one as an argument (<<requirement-propagation>>). *What is not allowed is a boundary that requires an external representation*: a data field, a newtype's base, and a behavior's input and output (<<fn-rules>>). A function has none, so placing one would create an exception to decoder / encoder derivation (<<decoder>>, <<encoder>>), and it is refused wherever it sits — directly, or inside a `+List+` or a `+Map+` written there. This is a representation constraint unrelated to side effects. The spec DSL itself holds a rule it wants to swap not as a function but as data (`+data fieldDefinition = ... AND validationRule?+`).

A block may also be bound to a name by a block's `+let+` and applied (`+let f = (x) -> ...+`, then `+f(a)+`); written at the top level the same line is a definition, and its parameters move to the left of `+=+` (<<fn-declaration>>). Like a named `+let+` (<<fn-declaration>>), as long as it does not escape — applied only, never leaving as a value — a block is expanded into its call/apply site and forms no closure. When it escapes — *returning* a function, or choosing at runtime (`+let f = if ... then (x) -> ... else ...+`) — a closure capturing free variables is generated. It is a synthesized class implementing the runtime `+Fn+`, holding the captured variables in fields. Where expandable it is expanded; only where necessary is it a closure. The *cannot place in a data field* constraint above is unchanged by closures: it is a representation constraint, separate from whether a closure exists.

A bare field access `+.field+` is a getter block: it desugars to `+(x) -> x.field+`, so a field is projected point-free — `+List.map(.value, xs)+` instead of `+List.map(x -> x.value, xs)+`, and with the pipe `+xs |> List.map(.value)+`. It is single-layer (`+.value+`, not `+.a.b+`; reach through a newtype over a newtype with the constructor pattern of <<binding-patterns>> or <<match>>) and works for any field, so a newtype's `+.value+` and a record's `+.a+` read the same way. It is a block like any other: it may sit in an argument, be bound with `+let+`, and — since it names its own parameter's type nowhere — be given one by the binding it is written on (`+let g: (Item) -> Int = .n+`). It is a lambda shorthand, not a first-class value, and never collides with the in-scope `+value+` an invariant refers to (<<invariant>>), which is a field name, not a getter.

A block and a helper `+let+` are the same in constructing data. A block constructs under the enclosing behavior's `+constructs+`, and a hoisted helper `+let+` constructs under the `+constructs+` of the behavior that calls it (the construction set is inferred transitively and reconciled; <<constructs>>). Either way the construction authority is on the behavior and does not disappear with naming. So hoisting a constructing `+[HighValueTravel { threshold = threshold } | …]+` (<<stdlib-list>>) into `+let makeHighValueTravel (threshold) = HighValueTravel { threshold = threshold }+` passes if the calling behavior declares `+constructs HighValueTravel+`.

[#binding-patterns]
=== Binding patterns

Where a name is bound — a block's `+let+`, a behavior implementation's parameter, a helper's parameter, a lambda's parameter — a *pattern* may be written instead, opening the value where it is named. The patterns admitted here are the ones every value of the type satisfies: a name, a tuple, a newtype opened by its constructor, a record's fields.

[,text]
----
let TagSet(ts) = set                    // open a newtype
let { name, quantity } = line           // read fields under their own names
let { quantity = n } = line             // ... or under another
let (k, v) = pair                       // open a tuple (<<tuple>>)
let (k, _) = pair                       // ... reading one of its members

let count (TagSet(ts)) = Set.size(ts)   // a helper's parameter

behavior size : (set: TagSet) -> Int
let size (TagSet(ts)) = Set.size(ts)    // a behavior implementation's parameter

List.map((TagSet(ts)) -> Set.size(ts), sets)   // a lambda's parameter
----

[[a-discard-stands-inside-a-pattern-for-what-is-not-read]]Inside a pattern, `+_+` stands for a part of the value the pattern does not read: a member of a tuple (`+let (_, out) = pair+`), the value a newtype's constructor opens, and a lambda's parameter. `+_+` is not a name (<<identifier>>), and nothing reads what it received — an expression writing `+_+` is refused — so a discarded part has no name anywhere.

A whole binding is not written `+_+`. `+let _ = e+`, a helper's or behavior's parameter, and the name a record pattern renames a field to (`+{ f = _ }+`) each take a name: a binding that reads nothing of what it binds is a line to delete rather than a form to write.

A pattern that some values of the type do not satisfy — a sum's case, `+Some+` — MUST NOT be written here; it belongs to `+match+` (<<match>>), which has the arm that catches what fails. A binding has one arm and nothing to fall to. Whether a name is refutable is decided by what it declares, so `+let X(v) = e+` is refused when `+X+` is not a newtype, and the report names `+match+`.

Outside `+match+` the name a constructor pattern writes is also checked against the value's own type: `+let TagSet(ts) = LabelSet+` is refused. In a `+match+` the arm's name is one of the scrutinee's cases and exhaustiveness has already settled it; a binding names a type on its own authority.

A helper's parameter written as a constructor pattern needs no type beside it — `+let count (TagSet(ts))+` already says the parameter is a `+TagSet+`. A tuple or record pattern names no type, so a helper whose parameter type its body does not determine still annotates it (`+let twice ({ quantity }: Line)+`).

On a behavior's implementation the pattern is not a type either way: the parameter types come from the behavior (<<fn-declaration>>), and a pattern only opens the input the behavior has already typed. The name the pattern writes is checked against that input, so `+behavior size : (set: TagSet) -> Int+` implemented as `+let size (LabelSet(ts))+` is refused. A `+depends on+` parameter names an injected behavior, so it takes a name, not a pattern.

A lambda's `+(a, b) -> ...+` is its parameter list, not a tuple pattern; a single tuple parameter is `+((a, b)) -> ...+`.

[#depends-on]
=== Dependencies (depends on)

A behavior that touches the outside world writes what it depends on in `+depends on+`.

[,souther]
----
behavior preApprove : (
    request: AwaitingPreApproval,
    approverId: EmployeeId
) -> PreApproved | NotAuthorized
    depends on now
    constructs PreApproved
----

What is written is the name of a behavior whose *requirement set is not empty* (<<requirement-propagation>>) — one with no implementation (<<injected-behavior>>), or one whose own `+let+` declares `+depends on+` — declared in this module or imported from another (<<modules>>). Write it if the `+let+` uses that name. Even when used indirectly by passing it to a helper `+let+`, the one passing is you, so you write it.

A behavior with an empty requirement set is not written here. It depends on nothing, so there is nothing to inject, and it is called directly (<<calling-a-behavior>>). Writing one in `+depends on+` is rejected where the clause is written (<<e1607>>), as is a `+>->+` composition — whose requirements are inferred from its stages and are not written, so requiring one would make a caller declare a set that changes when an upstream stage changes — and as is a name that is no behavior in scope.

Only a simple behavior whose implementation is Souther's may have `+depends on+`. An injection target does not write it: it is the leaf of the dependency, and its name being listed is itself the statement of dependency. Writing the clause is what says the implementation is Souther's, so a behavior that writes it and has no `+let+` yet is one nobody has written rather than an injection target (<<unwritten-behavior>>). A `+>->+` composition does not write it either: it is decided by the union over stages, readable from the stages (<<composition-with-requirements>>).

==== Correspondence to the spec DSL

The DSL attaches `+// depends:+` / `+// side effect:+` to a *leaf*.

[,text]
----
// responsibility 2: save the business-trip application to the database
// depends: travelRequestDatabase
// side effect: save to the database
behavior saveTravelRequest = UnvalidatedTravelRequest AND PreApprovalNeeded -> SavedTravelRequest
----

In Souther this becomes a behavior with no implementation, so no `+depends on+` is needed. The same as the DSL attaching no note to a composition: it is readable from the stages.

The DSL holds only these two because it places side effects at the ends of a workflow. The form "use the outside world once in the middle of a pure computation" does not appear in a specification model. Souther allows it, so a position with no source in the DSL — a behavior with a `+let+` — arises. `+depends on+` lets the DSL's note be written on a non-leaf behavior too. The positions increase, but the content is the same "what it depends on" as the DSL; no lines are added.

==== depends on becomes a let argument

What is written in `+depends on+` appears as a `+let+` argument, after the business inputs, in declaration order.

[,souther]
----
let preApprove (request, approverId, now) = {
    guard approverId == request.applicant.managerId else NotAuthorized

    PreApproved { ...request, preApprovedAt = now(), preApprover = approverId }
}
----

The behavior has 2 arguments and the `+let+` has 3 because *the behavior's type is the type after partial application*.

[,text]
----
let:      (request, approverId, now) -> PreApproved | NotAuthorized
bind:     fix now                             f_a(x) = f(x, a)
behavior: (request, approverId)      -> PreApproved | NotAuthorized
----

`+PreApprove.bind(new JdbcClock())+` (<<jvm-behavior>>) is this partial application. What remains after binding is the behavior's declared type, and Java calls `+apply(request, approverId)+` with 2 arguments. `+depends on+` does not appear in the caller's arguments.

This merely surfaces the "requirements are constructor arguments" of the end of <<blocks>>. In the `+let+`, `+now+` is just an argument, not an effect or a special type. So the `+let+` side needs no place to write `+depends on+`.

A helper `+let+` (with no corresponding behavior) fits the same form. To touch the outside world, it receives a function as an argument — a helper writes its own type (<<fn-declaration>>), so it writes it there.

[,text]
----
let record (x: Int, now: () -> DateTime) = ...       // helper. Receives it as an argument
----

The `+now+` appearing inside a `+let+` is an argument in either case. There is no path to call a behavior with no implementation directly as a top-level name. So a helper `+let+` having no place to write `+depends on+` is not a restriction. The passer writes it in its own `+depends on+` and passes it as an argument.

==== Checking

The set of behaviors with a non-empty requirement set that a `+let+` uses and the set written in `+depends on+` MUST match. As with `+constructs+`, matching is two obligations and either can fail alone. An over-declaration erring on the safe side is not admitted.

* [[depends-on-names-every-requirement]]every behavior the implementation reaches that requires something MUST appear in the clause (<<e1602>>);
* [[depends-on-names-no-more]]every behavior the clause names MUST actually be reached by the implementation (<<e1603>>).

[[depends-on-names-a-dependency]]An entry MUST name something there is to inject: a behavior in scope whose own requirement set is not empty (<<e1607>>). A behavior that requires nothing is reached by name, and a `+>->+` composition infers its requirements from its stages rather than being handed them.

[#ensures]
=== `ensures`

An `+ensures+` clause declares the relation between a behavior's named parameters and the value it answers. It is a clause of the behavior signature, beside `+constructs+` and `+depends on+`, and travels with the published declaration.

[,souther]
----
behavior findBuildings : (cond: SearchCriteria) -> List<Building>
    ensures inTheAreaAsked = List.all(b -> b.area == cond.area, value)

behavior findTodo : (id: TodoId) -> Todo | NotFound
    ensures Todo | NotFound -> value.id == id
----

The answer is named `+value+`.

A rule states a relation, so it refers to both sides of one. [[an-ensures-clause-names-the-answer]]A rule MUST refer to the answer, which it does through the arm it is written under or through `+value+`; where it names no arm there is nothing else to refer to the answer by, so its expression MUST name `+value+`. [[an-ensures-clause-names-a-parameter]]And it MUST name at least one parameter. A property of the answer alone belongs on the answer type's invariant; a property of the inputs alone is a precondition and is modelled as ADR-0003 decides. [[a-behavior-with-a-clause-has-no-parameter-named-value]]A behavior carrying a clause MUST NOT have a parameter named `+value+`.

An arm is a reference to the answer, so `+ensures Missing -> id == tag+` states a relation: where the answer is `+Missing+`, the inputs stood so. Asking for the word `+value+` in every expression would read the text for the relation rather than the rule, and would refuse a rule that states one.

What a rule refers to is not what it is about, and only the first is decided here. `+value == value && id > 0+` refers to the answer and says nothing about it, and an arm of an answer with one case holds of everything it answers. Telling those apart is a proof; what this rule holds is that a relation is written with both of its sides in it.

An answer that can be more than one thing has cases, and they are the ones a `+match+` over that answer opens: the members of an anonymous sum, or the cases declared with a named one. A case of a case is not one of them — a caller opens the outer case and only then what is inside it — so a rule naming one would be a rule the caller has no arm to assume it at. What `+value+` is read as in an arm is what that case holds, which for a case named by a primitive (<<unmarked-output>>) is that primitive.

Where the answer has cases the form is `+ensures [name =] Case { | Case } -> expression { | Case { | Case } -> expression }+`. [[an-ensures-clause-over-a-sum-names-arms]]Each expression MUST name one or more arms, and [[an-ensures-arm-is-an-output-case]]every named arm MUST be a case of the answer. Several cases MAY share an expression, and a case named twice under one arrow states one rule: what a clause states is a conjunction, so naming a case twice says what naming it once said. Where the answer has none the form is `+ensures [name =] expression+`, and [[an-ensures-clause-over-a-single-type-names-no-arm]]it MUST name no arm. A case no rule names carries no stated postcondition, and there is no wildcard arm.

Two arms of one clause MAY name the same case, and both of their rules then hold of it. That is the same reading as two clauses naming it, which is what makes an arm a statement rather than a choice.

A behavior MAY carry more than one `+ensures+`, and every rule of every one of them holds. Where the answer is a case, the rules whose arms name that case are the rules it is held to — all of them, and not the first — so writing two clauses and writing their rules under one are the same statement about the model. What differs is where a violation is reported: a clause is what a failure is reported by, and its name is what a reader is given. Where more than one does not hold, the first that does not is the one reported, as a data's clauses are (<<invariant-declaration>>).

Cases may overlap. A data may be a case of two sums declared in its module, so one answer can be named by an arm of each, and both of their rules hold of it. This is what makes a rule's arms a statement rather than a dispatch: `+match+` takes the first arm that answers, which is a control-flow reading, and a declaration has no order to read.

A clause MAY be named. [[ensures-clause-names-are-distinct]]Named clauses are distinct within the behavior, and [[an-ensures-clause-name-is-not-underscore]]`+_+` is not a clause name. A name here says which clause is meant and nothing more: unlike a data's clause name, it names nothing a departure arm can be written for (<<attempt-departures>>), an `+ensures+` having no such form. The expression fragment is the invariant fragment unchanged: it is total, constructs nothing and contains no `+unreachable+` (<<invariant-expressions>>).

[[a-composition-carries-no-ensures]]A `+>->+` composition MUST NOT carry `+ensures+`: it writes no parameter list whose names a clause could relate to its answer, and a stage's relation is not inferred as the composition's.

A clause travels with the published declaration, as a data's does and for the same reason: a reader that cannot read it cannot assume it.

Where a value the model writes down states both sides of the relation — an `+example+` row, a row of a `+fake+` table — that value is held to the clause while the module is compiled (<<example-ensures>>).

==== What a caller may assume

Where a caller has reached the case a rule is about, that rule holds of the answer, and the checker takes it as holding — the same seeding an input's type gives (<<invariant-discharge>>). This is what declaring a relation buys: the answer does not carry the input, so before it a caller could only re-establish the relation with a `+guard+` it had no way to derive.

It is reached by matching that case and by nothing weaker. An arm answering for several cases says the answer is one of them, which is not that it is any one of them, so a rule about one of those cases is a rule about a value that arm may not have. A rule under no case applies to every answer and is reached wherever the answer is.

What is taken in is what the checker can read of the rule, conjunct by conjunct, as everywhere else a clause is read (<<ensures-discharge-capability>>). A rule one part of which it cannot read still says the part it can.

The answer has to be a value the checker can name, which a matched case's binding is. An answer no arm opens — a behavior whose output has no cases — is named by nothing the checker reasons about, so what is stated about such an answer is held when the behavior answers and reaches no caller.

[#ensures-discharge-capability]
==== A rule says how much of it the check reads

A rule is classified as a data's clause is (<<invariant-discharge-capability>>), by the same answers and for the same reason: what is written decides how much of it the checker can carry, and nothing in the source tells the two apart. The unit is the *rule* and, under a rule, the conjunct. A rule is already about one case — `+ensures Found | Missing -> value.id == id+` is a rule for `+Found+` and a rule for `+Missing+` — and what `+value+` is differs between them, so the same words can be read to different depths. One answer for the clause would be one of those readings shown as if it were the other's.

A rule has one answer, shaped as a data's conjunct's is (<<invariant-discharge-capability>>) — every part of it to be established, any route of a part to establish that part — and it is given at the one place the rule is written.

The classification says nothing about whether the rule is enforced. Every rule holds when the behavior answers, whatever this says of it; what it says is how much of the relation is written in a form the checker reads — `+derivable+` a relation the numeric domain reasons over, `+exact match+` a term it can name and compare and nothing weaker, `+always holds+` and `+never holds+` a rule that settles without the answer, `+runtime only+` nothing it can represent.

The rule is read as it is written. An operation whose meaning the language defines stands as that operation, as a data's clause is read (<<invariant-discharge-representation>>), so a rule naming one is classified by what the language says of it and not by the algorithm it is emitted as.

It is a query rather than a diagnostic, again as a data's is, and the LSP surfaces it on the clause. So is which of the answer's cases no rule names: nothing being stated about a case is what the declaration says (<<ensures>>), and a reader who wants to know asks rather than being told.

[#calling-a-behavior]
=== Calling a behavior

A behavior's `+let+` may call a behavior. Which ones it may call is decided by one question — the requirement set — and the answer does not depend on where the behavior was declared. A behavior this module declares and one it imported are called the same way.

[[behavior-does-not-reach-itself]]A behavior MUST NOT reach itself, whether through calls, `+depends on+` or `+>->+` stages, or a mixture of them. A `+depends on+` cycle leaves nothing to build first and a call cycle does not terminate; recursion belongs to a helper `+let+`, where it is proven total (<<fn-rules>>).

[[a-helper-does-not-call-a-behavior]]A helper `+let+` may not. A helper is expanded into what calls it, and an invariant that names one carries it to an importing module as source (<<published-modules>>); it names only what travels with it, which is what keeps an invariant's rule a single question about the requirement set rather than a second one about where a name came from. A behavior a helper wanted is called from the behavior's own `+let+` and its result passed in.

* An *empty* requirement set: call it by name. Nothing is injected and nothing is written in `+depends on+`.
* A *non-empty* requirement set: write it in `+depends on+` and call it through that name (<<depends-on>>). It arrives already bound, as any requirement does.

[,souther]
----
behavior preApprove : (request: AwaitingPreApproval) -> PreApproved | NotAuthorized
    depends on now

behavior rejectAndNotify : (request: AwaitingPreApproval) -> Notified | NotAuthorized
    depends on preApprove
let rejectAndNotify (request, preApprove) =
    match preApprove(request) with
        | PreApproved as p -> Notified { to = p.applicant }
        | NotAuthorized as r -> r
----

A call yields the behavior's declared output. Where that output is a sum, the caller opens it with `+match+`, and a case it does not handle is its own to declare in its output. Nothing departs on its own: departure is what `+>->+` does (<<type-routing>>), and a call is not a stage.

A behavior's `+constructs+` is its own and adds nothing to its caller's. A helper `+let+` is different — it is not a specification statement and declares no authority, so its construction set folds into the behavior that calls it (<<constructs>>).

Behaviors do not recurse. A behavior MUST NOT reach itself, through calls, through `+depends on+`, or through a mixture of the two (<<e1608>>). Recursion is a property of a named module helper (<<fn-rules>>).

[#tuple]
== Tuples

`+(a, b, ...)+` is a tuple bundling two or more values into one. It is an expression-level value, used only in the middle of a computation — a helper `+let+` returning multiple values and destructuring them — and MUST NOT be written in a data field or a behavior input/output. Like a function (<<blocks>>), it does not cross the boundary, so no codec is derived. Domain vocabulary stays as named data.

A tuple *type* `+(A, B, ...)+` is written in a type position like any other type (<<a-type-is-written-in-a-type-position>>), and what refuses it is the positions requiring an external representation — a data field, a newtype's base, and a behavior's input and output — where a codec would have to be derived (<<what-crosses-the-boundary-has-an-external-representation>>). A signature is not a codec boundary, so a helper's or the standard library's takes one, `+List<(String, 'a)>+` included. A one-element `+(T)+` is not a tuple type.

A tuple is compared by its elements (<<equality>>), so two built separately from the same values are one value. It is therefore a `+Set+`'s element and a `+Map+`'s key like any other, which is how a rule over a composite key is written: `+allDistinctBy(t -> (t.from, t.event), transitions)+` says one row per `+(from, event)+`. A tuple has no *ordering*, so it is not a `+sort+`'s element or a `+sortBy+`'s key (<<stdlib-list>>) — order by one key and then the other instead.

[,text]
----
let stats (xs: List<Int>) = (List.sum(xs), List.length(xs))   // return multiple values

let average (xs) = {
    let (total, count) = stats(xs)      // positional destructuring bind
    ...
}
----

`+let (x, y) = t+` destructures a tuple positionally and binds. Names correspond to elements left to right. A one-element `+(e)+` is just a parenthesized expression and is not a tuple. A tuple may be held as is in a `+let+`, passed to a helper, or be an element of `+List<(a, b)>+` (a first-class value). Extraction is by destructuring only; there are no index functions such as `+fst+` / `+snd+`.

A lambda argument `+(a, b) -> ...+` is multiple arguments (<<blocks>>), distinct from a tuple. A single tuple argument is written with its own parentheses, `+((a, b)) -> ...+` (<<binding-patterns>>).

[#fn]
== let — implementation


What holds wherever something is applied:

* [[what-is-applied-is-a-function]]what is applied MUST be something that can be — a helper, a behavior, a newtype, a block, a function-typed parameter. A name the language gives is not one;
* [[an-application-gives-the-arguments-it-takes]]an application MUST give as many arguments as the thing takes. A newtype wraps one value, a block takes what it was written with, a helper takes its parameters;
* [[an-argument-has-the-type-its-parameter-takes]]an argument MUST have the type its parameter takes.

A block is written where a function is taken, and that position is part of what the standard library states:

* [[a-function-goes-where-a-function-is-taken]]a block or a function name goes in the argument that takes a function, and only there. Which argument that is belongs to the function being called (<<stdlib>>);
* [[a-block-answers-the-type-its-position-takes]]a block MUST answer with what its position takes — the accumulator of a `+fold+`, `+Bool+` for a predicate, the element type of what is being grown.

[[a-block-is-not-a-value]]A block is not a value. It may be passed as an argument, or bound with `+let+` and applied, and it MUST NOT be returned or stored in a field: a function has no external representation (<<external-representation>>), so nothing carries it across a boundary.

[[a-function-binding-has-one-type]]A binding holding a function has one type. Applied at two argument types, or given by two branches that produce different function types, it has no single one to be.

[[a-function-bindings-type-is-known]]That type MUST be readable where the binding is written — from its annotation, or from an application in the same scope. Nothing outside the scope is consulted for it.

[[an-annotation-on-a-function-binding-is-a-function-type]]An annotation on a binding that holds a function MUST be a function type. An ordinary type does not describe a function, so it settles nothing and disagrees with the value in the same breath. Leaving the annotation off is the other way to write the binding, and then the applications settle it.

A `+let+` is the implementation of a behavior. A behavior is the specification (<<behavior>>), and a `+let+` is what satisfies it.

[#fn-declaration]
=== Declaration and binding

A `+let+` is written `+let <name> (<args>) = <body>+`. If a behavior of the same name exists, it implements it.

Parameters go on the left of `+=+`, and a `+let+` written without them defines a *value*. The parameter list, not the shape of the body, is what tells a function from a value. An empty `+()+` is refused, since it would be a second spelling of the value form.

[,souther]
----
let ceiling = Amount(100000) // a value
let isOver (amount: Amount) = amount > ceiling // a function
----

A value is named where the value it stands for would be written, and it may be spread (`+Drafting { ...standardRequest, estimatedCost = Amount(50000) }+`). A behavior taking nothing is implemented by a value, since the parameter counts agree.

[,souther]
----
behavior preApprove : (
    request: AwaitingPreApproval,
    approverId: EmployeeId
) -> PreApproved | NotAuthorized
    depends on now
    constructs PreApproved

let preApprove (request, approverId, now) = {
    guard approverId == request.applicant.managerId else NotAuthorized

    PreApproved { ...request, preApprovedAt = now(), preApprover = approverId }
}
----

The type comes from the behavior. The `+let+` side only names its arguments, corresponding by position. The behavior-side names are for the reader; what binds is the `+let+`-side names. `+constructs+` is on the behavior, so it is not written on the `+let+` (<<constructs>>).

Arguments are the behavior's argument list followed by `+depends on+` in declaration order. A `+let+` is the pre-injection function and a behavior's type is the post-injection type, so a `+let+` has as many more arguments as there are `+depends on+` (<<depends-on>>). If `+depends on+` is empty, both have the same arguments.

A `+let+` with no corresponding behavior is a *helper*. Not being a domain behavior, it does not pollute the behavior list, cannot be placed in a `+>->+` stage, and does not appear in `+exposing+`. It states its own type, though a parameter whose type its body determines may leave it unwritten (below).

[,souther]
----
let isNonNegative (v: Int) = v >= 0
let applyAll (xs: List<Int>, p: (Int) -> Bool) = List.all(p, xs)
----

The return type is inferred. However, a *recursive helper* MUST declare its return type — `+let depth (s: OrgMember): Int = ...+` — because the result type cannot be inferred through the cycle, and only with a declaration can a self-call be type-checked. The declaration is required only for a recursive helper. A non-recursive helper is inferred as before, and a `+let+` with a same-named behavior cannot write a return type (the type comes from the behavior).

A written return type is a declaration into the body, and it stays one where the helper is expanded: it is pushed into the body at each call site, so a value only its context can type — an empty `+[]+` / `+Map.empty+` / `+Set.empty+`, or a `+fold+` over one — takes the declared element type even where the call site itself expects nothing concrete (a generic parameter such as `+Map.toList+`'s). This is the same direction a binding's `+let x: T = v+` has (<<block-expression>>).

The body is an expression. `+{ ... }+` is a block expression (<<block-expression>>), written to place a `+let+` or `+guard+`.

`+=+` introduces a named definition. The bodies of an anonymous block (<<blocks>>) and a `+match+` case are introduced by `+->+` — the same symbol as the type arrow. A lambda written on the right of `+=+` is the parameter-list form written the other way round: `+let f = (x) -> e+` and `+let f (x) = e+` define one thing, and which of the two a source writes changes nothing about what is defined. Only a lambda the source wrote is read this way. A `+.field+` getter is a block too (<<blocks>>), but its parameter is synthesized, so it stays a block — and a block is not a value, which is what a definition writing no parameters would need it to be. A definition that writes a function type is a *value* of that type, and the lambda on the right is that value rather than the definition's parameters. In `+let bump: (Int) -> Int = (x) -> next(x)+` the written type says what the definition is, and reading `+x+` as a parameter of `+bump+` would leave that type describing a definition answering a function rather than being one. A lambda's own parameters take no annotation, so this is where a function value states its type, as a binding does (<<block-expression>>).

[#fn-value-semantics]
=== A value is substituted where it is named

A value is not module state. Its expression is elaborated in the module that declares it and substituted at each reference, so nothing is held between references and the module's values come into being in no order.

* Its names are resolved once, where it is written (<<qualified-reference>>). What is substituted is the resolved definition, not text to be read again at the reference.
* It is evaluated at each reference. A value's body obeys a helper's rules — it cannot call an injected behavior (<<depends-on>>) — so it is pure and total, and re-evaluation is not observable.
* Two references may yield distinct values. Nothing observes that: there is no reference identity in the language, and comparison is structural (<<newtype-comparison>>).
* An invariant a value breaks is reported at the value, whether or not anything names it.
* What a value constructs is the value's own. A behavior that names one reads something already made, so the construction stays with the definition and is not required in that behavior's `+constructs+` (<<constructs>>). A spread of a value reads one the same way. This is where a value parts from a helper, whose construction set does fold into its caller.
* A spread names a value like any other position: `+Drafting { ...standardRequest, estimatedCost = Amount(50000) }+` reads the same in a behavior's body and in an `+example+` row. A binding in force wins there too, so a parameter or a `+let+` sharing a value's name is what the spread copies.
* [[a-value-does-not-reach-itself]]A value MUST NOT reach itself, through naming another value, spreading one, or calling a helper that does. A helper on a call cycle is lowered to a method and recurses (<<fn-rules>>); a value has no such form, so a cycle through one is a compile error naming the path it goes round. This is reported apart from the recursion check, which would ask for a return type the author never wrote.

[#fn-rules]
==== Rules


* [[a-helper-parameter-states-its-type]]a helper's parameter MUST have a type that is stated or determined. A parameter only read through a field, or used as a function, names no type by being used, so it is annotated;
* [[a-helper-answers-what-it-declares]]a helper that declares a return type MUST answer with it;
* [[a-recursive-helper-declares-its-return-type]]a recursive helper MUST declare its return type, its result not being readable from a body that names itself;
* [[a-recursive-helper-does-not-reach-an-injected-behavior]]a recursive helper's own body MUST NOT call an injected behavior. It is emitted as a pure static method, and the effect belongs to the behavior that calls it. Applying a function argument that reaches one is unaffected — the effect is then the caller's, declared in the caller's `+depends on+` (<<fn-rules>>).

* A `+let+` with a same-named behavior does not write argument types; the type comes from the behavior. A count mismatch is a compile error. A behavior whose type takes nothing (`+() -> T+`) is implemented by a value, which writes no parameter list.
* A `+let+` written with a parameter list defines a function; one written without defines a value. An empty `+()+` is a compile error — a definition taking nothing is the value form. A helper therefore has at least one parameter.
* A module's `+let+` MUST NOT be named like a data the module declares. A data is written where a value goes — a unit data is one, a newtype is applied to what it wraps, a record is constructed by its name — so the two would be one spelling with two answers. An identifier may be written in any script and case carries no meaning (<<unit-data>>), so the collision is refused where it is declared. A behavior and a same-named `+let+` are not a collision: they are one thing's declaration and implementation.
* The same holds for a name that reaches the value namespace by *import*: `+import lib ( Amount )+` in a module that declares `+data Amount+` is refused at the import line. Where a name comes from does not change how many answers one spelling may have. A binding is not a collision — it is in force over a region and everything outside it is unaffected (<<blocks>>) — and neither is an import a module's own `+let+` of that name shadows, which is that `+let+`.
* A helper (a `+let+` with no same-named behavior) states its own argument types, except where its *body* determines them. A parameter takes the type the body asks of it, at any position it stands in: an operand of an operator against a value of known type (`+let twice (n) = n * 2+`), an argument of a call, the value of a field in a construction, an `+if+` condition, an arm of an `+if+` or a `+match+` beside an arm that answers a value, an element of a collection literal beside another element, the answer of a body whose return type is declared (`+let same (v): Int = v+`), the value of a binding — its written type where it has one, and otherwise whatever the binding's own body determines, so `+let y = x in y * 2+` types `+x+` through `+y+`. A *signature's type variable* is solved at the call it is written in, from the other arguments (`+Set.contains(v, s)+` types `+v+` from `+s+`), from what the call as a whole is asked for (`+List.sum(xs) + 1+` types `+xs+` as `+List<Int>+`), and from the closure it is handed (`+List.map((v) -> v * 2, xs)+` types `+xs+` the same way) — and a combinator's closure is read in a scope its own parameters are in force over, so `+List.map((x) -> x + k, xs)+` over a `+List<Int>+` types `+k+`. The whole body is read, so a position further down it — or a helper declared further down the file — determines the parameter just as well; written order decides only which of several determining positions wins. Call sites are not read — what a helper means is settled by reading the helper, not by reading its callers.
* A *non-recursive* helper MAY carry the type variables a signature left unsettled, where the body decided what the value *is* and left open only what it *holds* — the outermost layer of the type names a type constructor. `+let count (xs) = List.length(xs)+` names the container and not the element, so `+xs+` is a list of something, and the helper is monomorphized at each expansion exactly as a core helper carrying `+'a+` is (<<type-variables>>): `+count([1, 2])+` and `+count(["a"])+` are both written in one module. A type stated outright always wins over one that leaves something open, wherever either is found, so `+let total (xs) = List.sum(xs) + 1+` is `+List<Int>+`. Two readings of one parameter are taken together, and two that disagree about what the value is settle nothing. What the body gets wrong is reported where the disagreement is, as it is for a type written beside the parameter, rather than as a parameter nothing determined. The variables belong to the helper: a library signature is read once and shared by every call site, so `+let sizes (xs, ys) = List.length(xs) + List.length(ys)+` holds two different things although both calls spell the variable `+'a+`, while `+let sameHead (xs, ys) = List.get(0, xs) == List.get(0, ys)+` holds one, because `+==+` reads the two together.
* A parameter the body leaves open MUST be annotated: `+let id (v) = v+` says nothing about `+v+` at all, and `+let qtys (ls) = List.map((l) -> l.qty, ls)+` reads a field off a value nothing names, which is a structural question a nominal model does not ask (<<union-intersection>>). A *function-typed* parameter MUST be written: neither applying it nor handing it to a combinator that applies it determines its type. A *recursive* helper is lowered to a method rather than inlined (below) and is typed on its declaration, so it writes all of its argument types.
* A helper *may recurse* (self- and mutual recursion). A non-recursive helper is inline-expanded at its call site; a recursive helper is not inlined but lowered to a method — self- and mutual calls become plain method calls. Traversal of self-referential data that `+fold+` cannot write (climbing the manager chain to the root, counting tree depth) is written this way.
* A recursive helper MUST *declare its return type* (above). Without a declaration it is a compile error.
* A recursive helper MUST be *pure*: its own body MUST NOT call an injected behavior (`+depends on+`). It MAY apply a function argument that does — the effect is then the caller's, declared in the caller's `+depends on+` and carried in by the closure (<<blocks>>). An effect touching the outside world is placed on the behavior that calls the helper, or on the function it is handed.
* [[helper-carries-its-termination-guarantee]]Recursion is *total by default*. A recursive helper — or a group of mutually-recursive helpers — MUST terminate, proven by *size-change termination*: each recursive call relates caller parameters to callee arguments, an argument that is a strictly smaller part of a parameter is a strict decrease and one passed through unchanged is non-increasing, and every recursive cycle MUST strictly decrease some parameter. A strictly smaller part comes from a `+match+` on a field or a case (`+match s.manager with | Some b -> depth(b)+` passes the unwrapped `+OrgMember+` — an org chart, or a two-`+Option+`-field binary tree), or from a value handed to a standard-library container combinator's closure over a container that is part of a parameter — a list element (`+List.fold((acc, c) -> acc + depth(c), 0, t.children)+` — an element of `+t.children+` is smaller than `+t+`, so an n-ary `+List<T>+`-children tree is total), or an `+Option+`'s unwrapped `+Some+` payload (`+s.manager |> Option.map(b -> depth(b) + 1)+` — the payload of `+s.manager+` is a smaller `+OrgMember+` than `+s+`, so `+Option.map+` recursion over a manager chain is total, exactly as the equivalent `+match+` is). Either way it bottoms out on the finite self-referential data. This one check covers self-recursion, mutual recursion, and structural lexicographic recursion (different calls decreasing different positions). A helper or group that cannot be shown to terminate is a compile error (E2001). Write `+partial let f ...+` to opt out. Numeric (`+n - 1+`) and index (`+List.get(i, xs)+` with `+i + 1+`) recursion is not structural — Souther has no inductive `+Nat+` — so it MUST be `+partial+`; counting is otherwise written with `+fold+`. There is no way to *claim* totality on non-structural recursion; `+partial+` only disclaims it. The standard library's `+List.foldFrom+` — private, reached through `+List.fold+` — is trusted total.
* `+partial+` *declares that the helper does not carry Souther's termination guarantee*. It does not assert that the helper diverges: a recursion that does terminate and that the analysis cannot prove is written this way too, and so is one the analysis gave up on as too large. What it disclaims is the termination guarantee and nothing else — the helper is type-checked, its `+match+` is checked for exhaustiveness, and its invariants hold as any other's do. A helper written without the word does carry the guarantee.
* *A helper's totality covers everything it reaches.* An unmarked helper MUST NOT reach a `+partial+` one, directly or through any chain of calls; doing so is a compile error (E2001) naming the path. Its caller has to say `+partial+` as well. Every member of a mutually-recursive group holding a `+partial+` member therefore needs the word — each reaches the others. Reading a `+let+` written with no parameter list is reaching it, because the value is substituted where it is named. A behavior's implementing `+let+` is not a helper and publishes no termination guarantee, so this rule is not asked of it: it MAY call a `+partial+` helper. That is not a boundary the walk stops at — it is a declaration the walk is never asked about, and a helper between the behavior and the `+partial+` one still needs the word.
* A `+partial+` helper MUST NOT be written where a value goes. It may be applied; it may not be handed over. A function type (<<fn-value-semantics>>) says nothing about termination, so a `+partial+` helper stored in one leaves the call graph and arrives somewhere the rule above cannot see it. Writing the name in an argument, or anywhere else a function value is expected, is a compile error (E2001). A `+let+` with no parameter list is a value rather than a function and is not covered by this: reading it is reaching it, which the rule above already answers.
* Across a module boundary the answer is the word on the declaration being called. A published helper's body travels as source (<<exposed-values>>), but a reader does not walk it: an imported helper written without `+partial+` promises that nothing it reaches is `+partial+`, because the module that published it was held to the same rule. A jar built under a different boundary version is refused rather than believed.
* An `+example+` is evaluated within a time budget, so a recursion that does not terminate is reported rather than hanging the compiler.
* A local lambda (`+let f = (x) -> ...+`) cannot recurse. Recursion is a property of a named module helper.
* Two `+let+`s MUST NOT be written for one behavior.
* [[an-implementing-let-takes-its-shape-from-the-behavior]]A `+let+` implementing a behavior takes its shape from that behavior: how many parameters it has, what they are, the order the injected ones arrive in, and what it answers with. None of them is written again on the `+let+`, and none of them may disagree.

[[a-composition-has-no-let]]A `+>->+` composition is its own implementation, so no `+let+` may implement one.

A `+let+` may implement a behavior only when both are in the *same module*. A behavior of another module may be imported and called, but its implementation MUST NOT be given by a `+let+` in another module. This is the line that keeps the name-binding rule from conflicting with separate compilation.
* A helper `+let+` may construct data, but its construction set is inferred transitively and MUST be included in the calling behavior's `+constructs+` (<<constructs>>, <<blocks>>) — except for what another module's published body carried in, which that module already stated (<<exposed-values>>). A helper cannot be placed in a `+>->+` stage, because it does not appear in the spec DSL. It may be written in `+exposing+`: what a module offers is not only its list of behaviors.
* A function type (`+(A) -> B+`) is an ordinary type and is written in a type position like any other (<<a-type-is-written-in-a-type-position>>). What refuses one is the position's requirement, not the grammar: a data field, a newtype's base and a behavior's input and output require an external representation, and a function has none — including a function carried inside a `+List+` or a `+Map+` there (<<what-crosses-the-boundary-has-an-external-representation>>). A behavior output is a sum of business results, and a function is not a business result.
* A `+let+` may return a function (`+let adder (n: Int) = (x) -> x + n+`). An escaping function — a return value or a runtime choice — becomes a closure capturing free variables (<<blocks>>). If only passed or applied on the spot, it is expandable and no closure is needed. A helper's name written where a value goes is the function it names. A binding MAY state its type (`+let p: (Int) -> Bool = ...+`), and that is what settles the parameters of the lambda it binds. Where no type is written they are inferred from how it is applied (a local rule, distinct from global type inference, which the declarative design does not do — a behavior takes its type from the specification and a helper states its own, <<fn-declaration>>).

[#injected-behavior]
=== A behavior with no implementation — outside-world dependency

*If a behavior with only a type has no implementation and declares nothing to depend on*, its implementation is injected from Java.

Two things are written the same way, and the clause tells them apart. A behavior with no `+let+` that declares `+depends on+` takes those dependencies as arguments of a `+let+` (<<depends-on>>), so it is Souther's to implement; with no `+let+` it is one nobody has written (<<unwritten-behavior>>). An injection target is the one that declares nothing to depend on: it is what gets injected, and what a dependency is supplied to is the behavior that calls or composes it. It corresponds to the spec DSL's `+// depends:+` and `+// side effect:+`.

An implementation is either a same-named `+let+` (<<fn-declaration>>) or a right-hand `+>->+` composition (<<composition>>). A `+>->+` composition behavior has no `+let+` but does have an implementation, so it is not subject to injection. What is injected is only one that wrote a type and no `+let+`.

[,souther]
----
// spec DSL: // depends: clock
behavior now : () -> DateTime

// spec DSL: // depends: memberRepository
behavior findMember : (id: MemberId) -> Member | NoSuchMember | CorruptStoredData
    constructs NoSuchMember, CorruptStoredData
----

Output cases are domain outcomes only. `+NoSuchMember+` (not found) and `+CorruptStoredData+` (a stored value breaks a domain invariant) are business-meaningful results, so they are cases. A *platform failure* such as DB down is not placed here — the Java Binding throws an exception and Souther passes it through (<<java-impl-rules>>).

No keyword is needed. Having no implementation is what "provided from outside" means. The spec DSL also has no word for `+required+`; whether something is a dependency is only noted in a comment. This form keeps the DSL line as is.

`+constructs+` is required on a behavior with no implementation too (<<constructs>>). The declaration reads the same as when implemented in Souther. `+findMember+` creates two failures and does not create `+Member+`; `+Member+` is obtained by passing a value read from outside through the decoder. The factory of <<java-base-class>> distributes from here.

Typical uses: DB query, HTTP call, file I/O, clock, ID generation, message sending. The concrete APIs are not brought into the language core. The distinction between dependency (only reads) and side effect (changes) is documentation of intent and does not affect value composition.

Forgetting to write an implementation is not an error; that behavior simply becomes subject to injection. But a caller MUST write that name in `+depends on+` (<<depends-on>>), and doing so surfaces it in a `+bind+` argument (<<jvm-behavior>>). If there is no caller, it is not caught within the `+.sou+`.

Which behaviors get a `+let+` and which are injected is not decided mechanically from the DSL. The spec DSL's `+// depends:+` is a note, not an obligation, so its absence does not mean an internal implementation. The 1:1 of <<non-functional>> is a declaration correspondence: one DSL behavior line maps to one Souther behavior declaration. It does not decide the implementation form (`+let+` / injection / `+>->+` composition). The model author chooses the form using the DSL's `+// depends:+` and `+// side effect:+` as cues: a note suggests injection, an internal computation a `+let+`, a chain of state transitions a `+>->+`.

[#unwritten-behavior]
=== A behavior Souther is to implement and nobody has

A behavior that declares `+depends on+` and writes no `+let+` is one whose implementation is Souther's and has not been written. It is not an injection target: what it depends on is passed to it by its own `+let+` (<<depends-on>>), so there is nothing for Java to be handed and no base is generated for it (<<java-base-class>>).

[,souther]
----
behavior quote : (cart: Cart) -> Priced
    depends on price
----

This is the state a model written example-first passes through. The rows go down before the body, and until the `+let+` arrives they are recorded rather than evaluated (<<example-pending>>) — the same as the rows of a behavior with no `+let+` and no clause. Waiting is not an error and is not warned about; how many rows are waiting is reported by `+souther examples+`.

Its name exists. Another behavior MAY declare `+depends on+` on it, a module that imports it reads its declaration, and where each behavior's body comes from travels with the module rather than being worked out again from it — a reader with two states to sort three declarations into would take this one for an injection target and tell an importer it may hand an implementation in.

[[nothing-built-rests-on-an-unwritten-behavior]]What is refused is resting on it from something that is built here: a behavior with a `+let+` of its own that names it in `+depends on+`, or a `+>->+` composition that names it as a stage (<<e1627>>). Such a behavior holds it as an injected field and nothing can supply that field. Resting on it from another behavior that is itself unwritten is not refused: that graph is the specification, and the bodies arrive one at a time from the leaves.

[#java-base-class]
=== Java base-class generation

A behavior subject to injection (wrote a type, no `+let+`; <<injected-behavior>>) is generated as an *abstract base class*, and has a `+protected+` factory for each unit data among its output's cases and for each data its `+constructs+` names. The two come from different places on purpose: a unit is told from a decoded pass-through output by its shape alone, while a field-bearing one is not, so only the latter needs the clause to say it (<<constructs-excludes-unit-data>>). Whether it takes one input or several is orthogonal to injection, exactly as for a `+let+`-implemented behavior (<<jvm-behavior>>): a *single* input extends the unary `+Behavior<In, Out>+` (so it still composes with `+>->+`) and inherits its `+apply+`; *any other count* makes it a standalone abstract class with a typed `+apply(A, B, …)+` and no `+Behavior+` supertype. Two or more inputs are more than `+>->+` passes along, and a behavior taking none has nothing to receive, so neither can follow an arrow; both can still be a pipeline's first stage, which receives the pipeline's own arguments (<<sequential-composition>>). A zero-input behavior therefore declares `+apply()+` and is handed nothing — it produces rather than transforms. A Java implementation overrides that typed `+apply+` directly, so several inputs are passed as arguments rather than bundled into a record. A collection parameter keeps its `+java.util.List+` / `+Map+` / `+Set+` (or the runtime `+Option+`) type with its element type, not `+Object+`.

[,souther]
----
behavior findMember : (id: MemberId) -> Member | NoSuchMember | CorruptStoredData
    constructs NoSuchMember, CorruptStoredData
----

generates, conceptually:

[,java]
----
public abstract class FindMember implements Behavior<MemberId, FindMemberResult> {
    protected FindMember() {}
    protected final NoSuchMember      NoSuchMember()      { ... }   // only declared unit cases
    protected final CorruptStoredData CorruptStoredData() { ... }
    // apply(input) is inherited from Behavior (unimplemented). An implementation overrides it.
    // A platform failure such as DB down is not a case but an exception. The implementation throws; Souther passes it through.
}
----

Java `+extends+` this and implements `+apply+` (<<java-example>>). Success values (such as `+Member+`) are assembled by the decoder; failure cases are created by the inherited `+protected+` factory. Because a data's constructor is non-public, the implementation may be placed outside the generated package (in any package). An output case that is not a unit or that carries an invariant gets no factory and is obtained via the decoder.

This abstract base class is *always public*, regardless of whether it is in `+exposing+`, so that a Java implementation can `+extends+` it from any module; a behavior subject to injection is by definition implemented outside the module. `+exposing+` controls the exposure of domain types, but the injection base class is scaffolding for implementation and is exposed independently. Without it the injection example of <<example>> would not hold.

Because the base is public and the implementation overrides `+apply+` from outside the module, the implementation *writes* the behavior's input type and its output type. A type the module keeps to itself cannot be written there, and no raw-typed override stands in for it — the erased signature clashes with the one being overridden. So an injected behavior's input and output MUST be exposed by the module that declares them, whether or not the behavior itself is in `+exposing+` (<<exposed-surface>>).

[#injected-constructs-are-creatable]
When a data listed in `+constructs+` is not exposed with `+exposing+`, it is a compile error (E1305). A non-exposed data has no public decoder, so a Java implementation would have no way to create that value; if exposed, it can be created via the decoder (<<jvm-codec>>). Every entry is a non-unit data (<<constructs-excludes-unit-data>>), so this reaches all of them. A unit case of the output is creatable without being exposed, through the `+protected+` factory the base class carries for it.

What is confined is <<invariant-on-construct>>. An invariant-bearing data is always checked whether it goes through `+__construct+` or the decoder. So even if it can be created from outside, *no unvalidated value can be created*. A unit data has nothing to validate, so nothing is lost by allowing it to be created from outside.

The base is an ordinary JVM abstract class, so any JVM language implements it. Kotlin and Scala `+extends+`/`+:+` it and override `+apply+` as Java does; a collection parameter arrives as `+java.util.List+`/`+Map+`/`+Set+`, which a Kotlin or Scala collection or a Clojure persistent collection satisfies directly. Clojure extends the abstract base with `+proxy+` or `+gen-class+` (not `+reify+`, which is interface-only) and dispatches `+apply+` by arity; because the `+constructs+` factories are `+protected+`, a Clojure implementation reaches them through `+gen-class+`'s `+:exposes-methods+` (a `+proxy+` sees only public methods). The factories stay `+protected+` — that is the construction confinement, not an interop choice.

[#java-impl-rules]
=== Rules for Java implementations

A Java implementation returns a *domain outcome* — a success value (such as `+Member+`) and a declared failure case (such as `+NoSuchMember+` / `+CorruptStoredData+`).

A *platform failure* (DB down, network drop, HTTP failure — anything that is not a domain outcome) is not folded into a case but *MAY be thrown as an exception*. Souther does not make the exception a case but passes it through to the boundary (a generated `+>->+` pipeline or `+apply+` does not swallow the exception). The language (`+.sou+`) has no exceptions (<<out-of-scope>>), but a Java Binding that touches the outside world may throw. The boundary (the Java / framework side) receives and handles the exception — HTTP 503, a retry, a transaction rollback. A VM failure or an implementation bug also escapes as an exception (out of scope for guarantees).

Guidance for the distinction. A *domain outcome* (not found, insufficient stock, a stored value breaks a domain invariant — a business-meaningful result) is a case, appears in a domain type, and is subject to exhaustiveness checking. A *platform failure* (infrastructure unavailable or not responding) is an exception, does not pollute domain types, and is handled at the boundary as a cross-cutting concern. Folding DB-down into a case would make every DB-touching behavior's domain type carry an infrastructure case, and when the DB actually goes down there is no honest case value to return (one would have to fake "insufficient stock" or fabricate an infrastructure case). So the former is a case, the latter an exception.

The exception *type* is agreed between the boundary and the implementation. With jOOQ + Spring, the boundary catches `+org.springframework.dao.DataAccessException+` and maps it to 503. jOOQ's own `+org.jooq.exception.DataAccessException+` is not a subclass of Spring's type, so this assumes Spring's exception translation for jOOQ is in effect (Spring Boot's jOOQ auto-config adds it by default). An implementation that throws a checked exception (such as raw JDBC's `+SQLException+`) wraps it in a non-checked exception of the same `+DataAccessException+` family (e.g. `+DataAccessResourceFailureException+`) — not `+IllegalStateException+` or a bare `+RuntimeException+` (which would pass through a `+DataAccessException+`-typed handler and make an infrastructure failure look like a program bug). The handler catches this failure type narrowly, and an implementation bug (an exception) falls through to a general 500.

`+CorruptStoredData+` (a stored value breaks a domain invariant on decode) stays a domain case. Infrastructure worked and returned data, and that data breaking a domain rule is a statement about the *domain*. Whether it deserves a name is the model author's judgment; this rule forces neither.

[#set-invariant]
=== Set invariants are passed as input data

An invariant that a single object cannot decide (schedule overlap, uniqueness) MUST be stated by passing the set needed for the decision as input data, not as a hidden dependency. The side effect of reading existing applications is placed at the workflow boundary (a behavior with no implementation).

[,souther]
----
// spec DSL: behavior checkScheduleOverlap = SameApplicantRequests AND Drafting -> Drafting OR ScheduleOverlap
data ScheduleOverlap

behavior checkScheduleOverlap : (
    sameApplicantRequests: List<TravelRequest>,
    request: Drafting
) -> Drafting | ScheduleOverlap
// neither is created: `Drafting` is the input returned as is, and `ScheduleOverlap` is a unit

// a behavior with no implementation (reads the outside)
behavior readRequestsOfApplicant : (applicant: EmployeeId) -> List<TravelRequest>
----

`+checkScheduleOverlap+` stays a pure judgment; the side effect is on the outer behavior with no implementation. This corresponds to the conclusion of SMDD's DDD trilemma (purity is not layer separation but making explicit which functions have side effects).

[#requirement-propagation]
=== Requirement propagation

Anything referencing a behavior with no implementation inherits its requirement. Requirements accumulate outward, toward the side that wrote the name. If a `+let+` calls a behavior with no implementation, it becomes the requirement of the behavior holding that `+let+`. It accumulates further onto a composition placing that behavior in a stage.

At the destination, whether to write or infer diverges (<<depends-on>>).

* a simple behavior with a `+let+`: *write* in `+depends on+`; receive it as a `+let+` argument
* a `+>->+` composition: *infer*; decided by the union of the stages' requirement sets

[,souther]
----
behavior loadAndConvert = findMember >-> toView // depends on not written
----

This requires the implementation of `+findMember+`. Composing several gives a union. A composition is not made to write it, because it is readable from the stage declarations; the spec DSL also attaches no `+// depends:+` to a composition.

A stage and a call differ in who owns the behavior. A `+>->+` composition *builds* its stages, so a stage's requirements become the composition's and accumulate outward. A `+let+` that calls a behavior *receives* it, so the behavior itself is the caller's requirement and its own requirements stay with it (<<calling-a-behavior>>). One behavior may be reached both ways; which a module writes says which relationship it means.

The call graph is static at every stage, so the requirement set is decided as a concrete set. That no variable enters is what makes this design hold: inference and declaration can be reconciled, and the composition union can be computed, because the set is concrete (<<core-semantics>>). A behavior reaching itself would leave nothing to build first, so it is a compile error (<<e1608>>).

This is unchanged by making a `+let+` higher-order.

[,souther]
----
let applyAll (xs: List<MemberId>, p: (MemberId) -> MemberLabel) = List.map(p, xs)
----

The requirement set of `+applyAll+` is empty. Whatever `+p+` requires is irrelevant, because `+p+` arrives already closed. Requirements are injected as constructor arguments (<<jvm-behavior>>) and enter at the moment `+PreApprove.bind(now)+` builds the implementation. The receiver need not know what it required. `+bind+` is the operation that partially applies that argument.

[#composition]
== behavior composition

[#sequential-composition]
=== Sequential composition `+>->+`

[[a-stage-after-the-first-takes-one-input]]Every stage after the first takes one input: what reaches it is the value the stage before answered with. A behavior taking more is called inside a stage rather than made one.

[[a-pipeline-composes-behaviors]]`+>->+` composes behaviors and nothing else. A decoder and an encoder are the edges the pipeline runs between (<<decoder>>, <<encoder>>), not stages within it.

`+>->+` connects behaviors. Of the previous stage's output cases, those the next stage's input type can receive flow to the next stage; the cases it does not receive are carried to the output (departing the mainline).

[,souther]
----
behavior handle = findMember >-> toResponse
----

The stage following `+>->+` is single-input (a 1-argument behavior). `+>->+` passes one value to the next stage, so the receiver cannot take two or more. To place a multi-argument behavior downstream, call it inline in the implementation or receive it with `+match+` instead of making it a stage.

*The first stage is not restricted.* The first receives the pipeline's own arguments, so any number is fine; the pipeline receives them as is. A behavior subject to injection is a first stage on the same terms as an implemented one (<<java-base-class>>): the pipeline inherits it as a requirement and calls it with those arguments.

[,souther]
----
// spec DSL: behavior reject = AwaitingPreApproval AND RejecterId -> PreApprovalRejected OR NotAuthorized
//           behavior returnToDraft = PreApprovalRejected -> Drafting
//           behavior rejectAndReturn = reject >-> returnToDraft
behavior rejectAndReturn = reject >-> returnToDraft
// rejectAndReturn : (AwaitingPreApproval, EmployeeId) -> Drafting | NotAuthorized
----

The rule of <<type-routing>> requires single input only for the right side; however many the left side takes is irrelevant to routing.

A multi-input pipeline is itself a multi-input behavior, so it cannot be placed downstream of another pipeline. It can be the first stage.

A stage is a behavior, not a `+let+`. `+>->+` is composition on the specification side, and the spec DSL writes it so. A helper `+let+` does not appear in the spec DSL and cannot be a stage.

Decode / encode are not behaviors but edges of the boundary and are not `+>->+` stages. At the boundary, decode first, pass only the successful domain value to the behavior pipeline, and emit the output with encode (<<decoder>>, <<encoder>>). `+>->+` composes only the domain world (behaviors and their sum outputs).

[#type-routing]
=== Type-routing composition rule

[#composition-stages-type-route]
Every stage of a `+>->+` composition MUST be able to receive what the stage before it answers with: of the previous output, the cases the next input accepts flow on and the rest depart, and a stage that accepts none of them routes nothing.

A pipeline carries the *mainline*. Only the mainline is passed to a stage; a case a stage did not receive departs the mainline and is *never passed to the remaining stages* of that pipeline. A departed case is piled onto the output.

Let the previous stage's output sum be `+O_f+` and the next stage's input type `+I_g+`. Of `+O_f+`, cases `+I_g+` can receive flow to the next stage; the remaining cases `+R+` depart the mainline.

[,text]
----
f : A -> B | R           g : B -> C           f >-> g : A -> C | R
f : A -> B | R           g : B -> C | S       f >-> g : A -> C | S | R
f : A -> B1 | B2         g : B1 | B2 -> C     f >-> g : A -> C
----

The same holds for three or more stages: *only the mainline passes to the next stage*.

[,text]
----
f : A -> B | C     g : B -> D     h : C | D -> E
f >-> g >-> h : A -> E | C
----

`+h+` can receive `+C+`, but `+C+` already departed the mainline at `+g+`, so it does not reach `+h+`. What reaches `+h+` is only the `+D+` that `+g+` produced. This is the Railway. "Failure" is not special-cased; an unconsumed case simply departs the mainline, and which case departs is decided by the composition partner (<<unmarked-sum>>).

The output sum is treated as an open error row: `+(C | R) | S+` is flattened and same-named cases folded. If no case of `+O_f+` matches `+I_g+`, composition is impossible (<<e1701>>).

That a case departed the mainline is remembered by the composition, and not forgotten by naming a midpoint.

[,souther]
----
behavior fg = f >-> g // mainline D, departed C
behavior x = fg >-> h // only mainline D passes to h; C stays departed. x : A -> E | C
----

`+fg+` remembers mainline `+D+` and departed `+C+`. In `+fg >-> h+`, only mainline `+D+` passes to `+h+`, and `+C+` stays departed and is carried to the output. So a named-midpoint `+fg >-> h+` is the same `+A -> E | C+` as the one-line `+f >-> g >-> h+`.

`+>->+` is associative. Hoisting a sub-pipeline by name or regrouping does not change the result.

The mainline/departed split is composition plumbing, not a marker on values (<<unmarked-sum>>). The plumbing unwinds at both ends of the pipeline. Viewing `+fg+` alone, its output is the folded sum `+D | C+`, and the external representation is as before. Even if the same type of case appears in both mainline and departed, the plumbing remembers which rail, so they are not mistaken.

When a downstream stage receives only some cases, or you want to branch per case, open branches with `+match+` instead of `+>->+`.

[,text]
----
match submitTravelRequest(request) with
    | Submitted as s   -> ...
    | AwaitingPreApproval as p -> ...
----

[#composition-with-requirements]
=== Composition with outside-world dependencies

A behavior's internal representation is:

[,text]
----
Behavior<Requirements, Input, Output>
----

`+Output+` is the sum of possible business results, with no fourth slot separating failure. Composition rule:

[,text]
----
f : Behavior<R1, A, O_f>
g : Behavior<R2, B, O_g>        // I_g = B receives cases of O_f
f >-> g : Behavior<R1 ∪ R2, A, (O_g) | (sum of O_f minus the consumed part)>
----

`+R1+` and `+R2+` are what the stages declared (<<depends-on>>). The composition's `+R1 ∪ R2+` is inferred by the composition; a composition does not write `+depends on+`. Whether an implementation is internal or Java-side does not affect value composition. `+Requirements+` (dependencies needed) and `+Output+` (the sum of results produced) are composed independently: the former by set union, the latter by consumption / propagation.

`+Output+` consumption / propagation splits into the *mainline* passing downstream and the cases that departed unconsumed. The composition carries this split, so, like the `+R1 ∪ R2+` set union, `+>->+` is associative (<<type-routing>>). The mainline / departed split stays in the composition plumbing and folds back into a sum when it exits as `+Output+`. The internal representation needs no fourth slot for failure.

[#single-case-composition]
=== Composition of single-case output

A behavior with a single-case output (`+-> A+`) can be seen as having no passed-through case (an empty failure row, `+Never+`). If a downstream stage receives `+A+`, it flows through. The user need not wrap it in anything. If `+guard ... else+` returned another case early, that case propagates as `+R+` of <<type-routing>>.

[#declared-composition-output]
=== Declaring and reconciling a composition's output

A `+>->+` composition's output is inferred from its stages (<<composition-with-requirements>>). A spec DSL composition line writes no output (`+behavior placeOrder = validateOrder >-> ...+`), so the default is this inference.

An inferred output *changes silently on a distant change* when a stage is a behavior of another module. If an upstream stage adds a case, the composition output grows silently; if a downstream stage narrows its input, a previously processed case moves to departed (<<type-routing>>) and likewise increases the output. The composition's definition site has nothing to signal this.

[#composition-output-agrees-with-inference]
So a composition *MAY optionally declare its output*. If written, it MUST *exactly match* the inferred output or it is a compile error (E1604). Neither too few (a case appears that is not declared) nor too many (a declared case does not appear) is admitted.

[,souther]
----
behavior placeOrder = validateOrder >-> priceOrder >-> allocateStock -> PlacedOrder | OutOfStock
----

`+-> PlacedOrder | OutOfStock+` is the composition's declaration, not the output of the last stage `+allocateStock+`. Because a stage is a behavior reference (name only), the trailing `+->+` reads unambiguously as the composition's. Writing this line makes an error appear here when an upstream stage adds a case, so it does not grow silently downstream.

Input is not declared. A pipeline is point-free, and the input is decided by the first stage (<<sequential-composition>>). Only the output may be declared. A dependency (`+depends on+`) has the same distant-change risk, but a composition leaves it inferred (<<depends-on>>).

[#exposed-composition-declares-its-output]
For an internal composition this declaration is optional. But for a composition exposed with `+exposing+` it is *required*, written not on the definition line but in the exposing list as `+name : cases+` (<<modules>>). Leaving a public boundary to inference means that when an upstream stage adds a case, it reaches another compilation unit (another module, or a non-exhaustive Java switch or JSON serialization) silently, and the definition site has nothing to signal it. Placing a signature in `+exposing+` makes the composition's definition error (E1604) the moment a case is added, and the author chooses to handle the case or update the signature. If no signature exists, it is E1605. A non-composition behavior writes its type at the definition, so it MUST NOT write a signature in `+exposing+` (doing so is E1605). This division corresponds to.

[#case-propagation]
== behavior case propagation and Decoder error accumulation

A behavior's output sum is treated as an open error row; a case the downstream does not consume in a `+>->+` propagates through to the tail (<<type-routing>>). This is the composition style of the domain world.

Decode-side error accumulation is handled by the peripheral library Raoh. A derived Object Decoder reads each field from the key of the same name and collects all failures of independent fields (Applicative).

[,text]
----
{ "name": ..., "email": ..., "age": ... }
// read name / email / age with each field's derived Decoder and accumulate all independent failures (Raoh)
----

A derived Decoder's field accesses are mutually independent and collect all failures. Accumulated failures aggregate into Raoh's `+Result+` failure (`+Issues+`). In a custom boundary codec (<<custom-codec>>), one access may depend on a preceding result; from that point it becomes sequential (monadic), and a subsequent access is not run if the preceding one fails.

Accumulation (applicative) is inside the decode boundary; case propagation is behavior composition. They belong to different layers. Because decode is not a `+>->+` stage, the two never mix on the same sum (<<sequential-composition>>). Accumulation holds because a Decoder's field accesses are mutually independent (do not depend on a prior value).

[#examples]
== Examples

An `+example+` attaches compile-time-checked test rows to a behavior. It is evaluated as part of compilation: a row that does not hold is a compile error (<<e1905>>), so a stale example cannot survive a build. An `+example+` is not exposed and produces no run-time class.

[,souther]
----
example submitTravelRequest
    | "within the ceiling it submits"
        : (Drafting { applicant = EmployeeId("e-001"), estimatedCost = Amount(50000) })
        -> Submitted
    | "over the ceiling waits for pre-approval"
        : (Drafting { applicant = EmployeeId("e-001"), estimatedCost = Amount(100001) })
        -> AwaitingPreApproval
----

What a row is held to, and what holding a build to it requires:

* [[example-target-is-a-behavior]]the target MUST be a behavior of the module the example is attached to;
* [[example-holds]]an evaluated row's result MUST match what it expects;
* [[example-does-not-reach-unreachable]]a row's evaluation MUST NOT reach an `+unreachable+` (<<unreachable>>): either the row states a combination the model rules out, or the reason written there does not hold;
* [[example-evaluation-stays-within-budget]]a row's evaluation MUST come to a value within the counted budget it is held to — the points it may pass and the depth a recursion may go to;
* [[example-evaluation-stays-within-stack]]and it MUST reach that counted depth limit before the JVM stack runs out, the two being set so the counted one is met first;
* [[example-evaluation-answers]]the work around the counted evaluation — loading a class, decoding a value, comparing two of them — MUST answer within the wait the policy allows. Unlike the two above, this says nothing about the model.

An `+example+` names a target behavior, then lists one or more `+|+`-led rows. A row is an optional name (a string, followed by `+:+`), an input argument tuple, `+->+`, and an expected result. `+example+`, `+examples+`, and `+for+` are contextual keywords, not reserved words, so they do not collide with an identifier or the `+example.*+` namespace.

[#example-row-name]
=== A row's name

The string a row is written with is its name, and a name is what says which row is meant from somewhere the row is not written: a report line two runs are compared on, a test that prepares what one row needs.

[[a-row-name-is-unique-within-its-behavior]]A name written on a row MUST be carried by no other row of that behavior, over the module's own source and every `+examples for+` file attached to it (<<example-placement>>). Every row carrying a name more than one row carries is reported, each in the file it is written in (<<e1925>>). Which of them is the one to write differently is not decided here: two rows of one behavior are what they are whichever order a compile was handed its files in.

A string with nothing in it is not a name, and a row written with one is refused where it is written (<<e2304>>). A row written without a name is held to none of this. It can be shown — as which of its behavior's rows in that file it is — and it cannot be named from outside, which is the whole of what writing no name means.

Editing a name renames the row. What was written elsewhere against the old name no longer says anything about this row, the same way a renamed behavior no longer answers to its old name.

[#example-expected]
=== The expected result

The expected side asserts at one of two granularities.

[[example-expected-arm-is-an-output-case]]Where a row expects an arm, that arm MUST be one of the target behavior's output cases.

* A bare type name (`+-> Submitted+`) asserts only the *arm* — which case of the output sum the result is — and ignores its fields.
* A construction (`+-> Rejected { reason = "high_cost" }+`), a newtype application (`+-> Amount(0)+`), or a literal asserts the *whole value* by structural equality.

The cases a row may name are the output's cases whether the output is written as a case list (`+-> Submitted | Rejected+`) or under the sum's own name (`+-> Verdict+`, <<sum-data>>) — a case stands where a value of its sum is expected here as everywhere else. A sum among those cases contributes its own cases, so a row names a leaf. The sum's name is not itself an expected arm: it says which cases are possible, not which one this input yields (<<e1904>>).

Types make totality and the case set exhaustive (<<match>>, <<unmarked-sum>>); an `+example+` fixes the value rule for a representative input — which arm, or which value, a given input yields.

[#example-evaluable]
=== What can carry an example

[#example-target-is-evaluable]
The target MUST be something this module can run: a behavior with a `+let+` implementation, or a `+>->+` composition of them. A helper `+let+` is not one.

[[example-fixtures-are-buildable]]Every fixture a row writes MUST build at the position it supplies. An input builds against its parameter type: a row's operand is compiled as one of this module's own definitions and run in the program the behavior is applied in, so what holds it to that type is the language's own typing, and its invariant is read where its construction runs. The expected result builds as the value the row wrote, which may be a case the behavior does not answer with — that is a disagreement the row reports (<<e1904>>) rather than a fixture that cannot be built. A `+with+` value or a `+fake+` output MUST be a value of the type the dependency answers with (<<example-fakes>>): it *is* that dependency's answer while the row runs, so there is nothing about it for the row to disagree with. The expectation is built before the behavior is applied.

[[fixture-is-written-not-carried]]A fixture MUST be written the way Souther writes the value, not the way a boundary carries it. A derived Decoder reads the representation a value arrives in from outside — a date-time as ISO text over JSON, a temporal out of a JDBC row — and a fixture goes through no Decoder at all: it is written in the source and compiled as source, so what a boundary additionally reads is not a second way to write one. So `+"2026-07-25T09:00"+` is a `+String+` wherever it is written, and a `+DateTime+` is `+DateTime("2026-07-25T09:00")+` (<<temporal-literal>>); a newtype over one takes the value it wraps, `+貸出日(Date("2026-07-25"))+`, and a `+?+` field holds the value itself, so it holds one there too. This is a rule about the form and not about the text, so it holds of a value a helper answered with (<<example-evaluable>>) as much as of one written out: a helper returning a `+String+` where a temporal stands has not written one. A fixture whose position's type does not admit it is refused where the same value written in a body is (<<e1812>>, <<e1317>>), wherever it stands. An expected value is not one the model is given: a row writing `+"2026-07-25T09:00"+` where a `+DateTime+` comes out has written a `+String+`, and that the two are of different types is the disagreement the row reports (<<e1905>>).

[[fixture-names-what-it-supplies]]A fixture the model is given MUST be a value of the position it is written at, and MUST say so itself. This is the language's own rule about types and not one examples have of their own: at a `+data AmountN = Int+`, `+1+` is an `+Int+` and `+AmountN(1)+` is an `+AmountN+` wherever either is written, and a row is one of the places either may be written.

A nominal position takes one of the names it holds. A newtype takes its own name applied to what it wraps, and one wrapped in another takes both — `+DecisionNN(DecisionN(Approved { … }))+`; a record takes its own construction, so a record of the same fields under another name is not one; a position typed by a sum takes any of its cases (<<sum-data>>). A position that is a primitive or a collection takes a value under no name at all, which is the same rule read the other way: an `+AmountN+` is one wherever it is written, so `+AmountN(1)+` at an `+Int+` is refused as `+1+` at an `+AmountN+` is. An optional takes what it holds, or `+None+` (<<optional>>), and `+None+` stands where an optional makes room for it and nowhere else: a position that holds a value takes one, whatever its Decoder would make of a missing one — a unit case's reads nothing and would answer with the case the row never wrote.

Where a helper is applied, what it answers with is what says what it supplied — so a helper answering `+Int+` does not supply an `+AmountN+`, and one answering `+List<Int>+` does not supply a `+List<AmountN>+`. Whether that type was declared or settled from the body is not a difference a reader of the call can see, and no rule here turns on it. The rule reaches inside what it answered with, because a position's element type is part of what the position is.

Where a field is taken, the declaration of the field taken is what says what it supplied: its declared type, and not the value found there. A field declaring a `+List<AmountN>+` supplies one wherever it is read, and an empty one supplies it too — which is the case the value could not have answered, an empty collection having no element to name. Where the value a field is taken off is one a helper answered with, what a helper was declared to answer with is not read, so what says what it supplied is the value it answered with, as it is wherever a helper stands. Which field may be taken is still the declaration's, read from what that value is.

This holds at every position a value stands at — a parameter, a record field, a collection element, a map key, an argument of an applied helper, the value under a newtype — and not only the outermost: `+Order { amount = 1 }+` states no `+AmountN+` at the field that takes one. A `+...+` spread is the one form that states no value of the type being written: it supplies the fields of a value of another type (<<field-spread>>), which is how one record is written from another, and the positions inside it are written the way any other is.

An *expected* value is not one the model is given and is not held to this rule. A row may state what the behavior does not answer with, and reporting that disagreement is what the row is for (<<e1905>>), so an expected value is compared rather than held to its position. A value the model is given that states no value of its position is refused as it is anywhere else the language reads a value of that type.

[[expected-is-built-as-written]]An expected value MUST be built as the value the row wrote, at every position and not only the outermost. The position it stands at says what the text does not — whether `+[ ]+` is a list, a set or a map — and says nothing about what type a value there is: `+Receipt { total = 1 }+` states an `+Int+` at a field declaring an `+AmountN+`, and a behavior answering `+Receipt { total = AmountN(1) }+` did not answer with it. What a construction's parts are is that construction's to say — a newtype's argument is the base it wraps, a field is what its record declares — so a value states its own type wherever it stands. Where the two sides differ by their type rather than by their contents, that is what the row is told, at the position they part (<<e1905>>). A value that states nothing of its own type is refused before anything is compared: `+AmountN("x")+` is <<e1317>> because an `+AmountN+` wraps an `+Int+`, which is what the same construction written in a body is. A newtype's invariant is read where the construction runs, whether what it wraps was written out or answered by a helper, and a row whose expectation breaks one states nothing to compare (<<e1903>>).

What a row wrote is more than the names in it. The form states which collection it is where it says so — `+Set.fromList([ 1 ])+`, `+Set.empty+`, and a helper answering with a `+Set+` state a set, while `+[ 1 ]+` and `+[ ]+` state neither and take the answer's — that a map's keys are distinct, and that a record has a value for every field it has. A row writing a set where a list came out has written a value of another type (<<e1905>>); a construction with no value for a field that is not optional is refused as it is in a body (<<e1005>>), and two entries under one key are `+Map.fromList+`'s to resolve there as anywhere, so the map the row states is the one it resolves to. Leaving a field out states `+None+` and states only that (<<optional>>).

A behavior with a `+let+` implementation is evaluated (<<fn>>), and so is a `+>->+` composition (<<composition>>). A behavior with no `+let+` has nothing to run, so its rows are *recorded* rather than evaluated (<<example-pending>>) — whether Java is to supply the body (<<injected-behavior>>) or Souther is and nobody has (<<unwritten-behavior>>). A target that is not a behavior at all — a helper `+let+` of that name — is <<e1902>>. A behavior's `+depends on+` dependencies are supplied by fakes at the example (<<example-fakes>>).

A composition's row is written like any other. Its arguments are the first stage's (<<sequential-composition>>), and its expected result is any case of the composition's output — including one that departed the main line at an earlier stage (<<type-routing>>), which is the part no stage's own example can state, because the routing is not any stage's rule.

[,souther]
----
behavior winOrder = settleQuote >-> makeOrder -> Order | BelowFloor

example winOrder
    | "meeting the floor wins the order"
        : (Negotiating { floor = Amount(100) }, Amount(200))
        -> Order
    | "below the floor it stops here"
        : (Negotiating { floor = Amount(100) }, Amount(50))
        -> BelowFloor { floor = Amount(100), offered = Amount(50) }
----

An input and an expected value are fixtures: literals, newtype applications, record constructions, and helpers applied to those. A `+?+` field takes a value directly (wrapped in `+Some+`) or `+None+` for empty, the same way a `+let+` body writes an optional (<<optional>>); omitting the field reads as `+None+` too. A row's argument is compiled as this module's code and run, so a fixture that breaks an invariant is rejected (<<e1903>>) — by the construction that carries it, as one in a body is.

A row may also *name a value* (<<fn-declaration>>) rather than write the whole input again, and may spread one to vary a field. A spread copies the fields of the value it names — one another module published as readily as one of this module's own, since a published value crosses closed — and the case is the one the construction writing it names; a name on its own stands for the case the value it denotes constructs. So a fixture in a position typed by a sum says which case it is however it is written.

[,text]
----
let acmeLead = QualifiedLead { companyName = CompanyName("Acme"), budgetFixed = true, ... }

example convert
    | "a fixed budget converts" : (acmeLead) -> Converted
    | "an unfixed budget does not convert" : (QualifiedLead { ...acmeLead, budgetFixed = false }) -> NotConverted
----

What a row may name is a property of the value graph, not of one definition's text: a value is *fixture-evaluable* when its body is a literal, a construction, a spread, `+Set.fromList+` / `+Map.fromList+` over one, `+Map.empty+` / `+Set.empty+`, a helper applied to those, or a reference to another fixture-evaluable value. A field taken off a fixture-evaluable value is one: where `+e+` is fixture-evaluable and its type declares a field `+f+`, `+e.f+` is fixture-evaluable, and a newtype declares `+value+`, so what a newtype wraps is read the same way. The rule is recursive on its own conclusion, so a field taken off a field is one. So a chain of values holds even though each link names the next. A value that reaches itself has no fixture to be, and is reported as the cycle it is.

A row may also *apply a helper* — the rule the row is about — wherever a fixture value is written: an input, an expected result, a `+with+` value, a `+fake+` table's inputs and outputs, a record field, a list element, a map key or value, and an argument of another helper application. The helper is *run*, as the module that compiled it runs it. The value it returns is the value the module's own code produced, standing where the application was written: nothing is read back into another form, because what encloses the application is compiled around it. In an expected value it is the value it answered with wherever it stands, since a helper produced a value of its own type and an expected value is built as what the row wrote (<<expected-is-built-as-written>>). Which case that value is comes from the value rather than from the text. Its arguments are fixtures like any others, so a fixture that breaks the argument type's invariant is <<e1903>>.

[,souther]
----
let taxIncluded (amount: Amount) = Amount(amount.value * 110 / 100)

example bill
    | "billAtTaxInclusivePrice" : (taxIncluded(Amount(100))) -> Bill { total = Amount(110) }
----

Without this a row restates what the rule computes — `+Amount(110)+` where it means "the taxed price of 100" — and a change to the rule leaves a row that is wrong and still passes.

A fixture applies the one instance the call it wrote determines. Where a declaration carries type variables, the arguments the row wrote settle them — `+List.length([1, 2])+` settles `+'a+` as `+Int+` — and the method the row runs is emitted at that instance, as an expansion is monomorphized at a call site (<<type-variables>>). A variable no argument settles is settled as it is in a body, an empty collection carrying no element to name: `+List.isEmpty([ ])+` answers as it answers anywhere it is written.

A function with a parameter that declares no type cannot be applied in a fixture, because the type it takes comes from the call sites the helper is expanded into, and a fixture is not one (<<e1903>>). One whose body produces a function answers no value, and is refused where a block that escapes is refused anywhere (<<e1809>>). How a function is implemented does not enter: a standard-library kernel is applied like any other function a row names, and the method it is applied through is emitted for the row that applies it. `+Map.empty+` and `+Set.empty+` are values and not functions (<<fn-declaration>>), so nothing is applied and nothing has to run: the name is the empty collection.

Building the fixtures, running the helpers they apply, applying the behavior and comparing the result are one evaluation under one time budget, so a row cannot buy more time by applying more helpers.

`+Set.fromList+` and `+Map.fromList+` are admitted because a value is ordinary code, where a list literal is a `+List+` whatever the position declares, while a row writes a set as its elements and a map as its entry pairs. They are the forms that notation stands for, so one record serves as both a value and a row. `+Map.empty+` and `+Set.empty+` are admitted for the same reason: they are what a body writes for the empty collection a row writes `+[ ]+`, and a row that could not name them would refuse the spelling the body it is exampling uses.

A collection is written as a list, and its declared type says what the list means: a `+List+` keeps its order and repeats, a `+Set+` drops duplicates on decode (`+labels = [ Label("bug"), Label("bug") ]+` is one label), and a `+Map+` takes its entries as `+(key, value)+` pairs — `+counts = [ ("bug", 2), ("ui", 1) ]+` — the form `+Map.fromList+` takes (<<stdlib-map>>). A temporal takes a written one (`+Date("2026-07-25")+`, `+Time("09:00")+`, `+DateTime("2026-07-25T09:00")+`, `+Instant("2026-07-25T00:00:00Z")+`; <<temporal-literal>>). This is what lets a rule over dates, sets, or maps be fixed by its examples rather than only by a hand-written test at the boundary.

This holds wherever a value is written, not only inside a record: a behavior taking `+(lines: List<Line>)+` is exampled with `+([ Line { quantity = 1 } ])+`, one returning a `+Map+` is expected as its pairs, and a fake's value is written the same way. A collection does not need a data wrapped around it to reach an example. A map keyed by a newtype (<<collections>>) writes its keys as they are constructed (`+[ (ProductId("A-1"), 3) ]+`), and each key goes through that type's invariant like any other fixture.

When a row does not hold, the diagnostic (<<e1905>>) shows what the behavior produced against what the row expects, both encoded through the data's derived Encoder (<<encoder>>) so the two read against each other.

[#example-fakes]
=== Faking a dependency

[[example-dependency-has-a-usable-fake]]Every dependency required to evaluate a row MUST have a usable fake: one supplied, by a `+with+` on the row or a `+fake+` table, and one whose fixture builds as a value of what that dependency answers with.

[[fake-answers-the-inputs-it-is-asked]]A `+fake+` table MUST answer every input it is asked for, by listing it or by carrying a `+_+` default row.

[[fake-table-builds]]Building a `+fake+` table MUST come to a value within the budget and the wait it is held to; where it does not, the table states nothing that was checked.

[[every-row-of-a-fake-table-can-answer]]Every row of a `+fake+` table MUST be one the table's dispatch can answer with. A row stating what an earlier row states is never the first match, and a `+_+` row followed by another `+_+` is never the one a table falls through to; either is a statement the author is making and nothing reads, and is reported at the row that answers nothing, naming the row that answers instead (<<e1926>>). Which of a pair is the dead one is the dispatch's to say and is not the order they are written in: of two rows stating one input the later answers nothing, and of two `+_+` rows the earlier does.

To evaluate a behavior that `+depends on+` an injected dependency (<<depends-on>>), the example supplies a fake for it. Best is to need none: an outside-world value written as an *input* rather than an injected behavior keeps the behavior example-able with no fake (<<purity>>). When a dependency stays injected, a fake stands in, scoped to the example and never shipped (no run-time class is produced).

A *value dependency* — one whose faked result is a constant — is given on the row with `+with+`. Its value is a fixture and is written like one: as a value of the type the dependency answers with (<<example-fixtures-are-buildable>>), so a `+now+` answering `+DateTime+` is stood in for by `+DateTime("…")+` and not by the string a boundary would carry one as (<<fixture-is-written-not-carried>>).

[,text]
----
example preApprove
    | (AwaitingPreApproval { ... }, EmployeeId("boss-1"))
        with now = DateTime("2026-07-20T09:00") -> PreApproved
----

A *function dependency* — one whose result varies with its input — is given by a `+fake+` declaration: an input→output table matched by value equality, defaulting to a `+_+` row or, absent one, a miss error (<<e1909>>). A multi-input injected dependency (<<java-base-class>>) is faked the same way, with the row's inputs matched as a tuple.

[,souther]
----
fake findManager
    | (EmployeeId("e-1")) -> EmployeeId("boss-1")
    | _ -> EmployeeId("unknown")
----

Each dependency of the target must have a fake — a `+with+` on the row or a `+fake+` table — or it is a compile error (<<e1908>>). A fake, like an example, is written inline or in an attached `+examples for+` file, and is local to its module. `+fake+` is a contextual keyword.

A `+fake+` table is built because it is written. Each table that answers for a dependency — the first one written for it (<<example-pending>>) — is built once, where it is written, whether or not any row reads it: a module may write a table nothing depends on and no row records an answer for, and what is wrong with such a table is wrong wherever it is read from. A table with a row that will not build is an error at the fake (<<e1908>>), said once however many rows would have read it; a table that does not finish being built is a warning there (<<e1921>>). A row that would have stood in with a table that will not build does not run, and says nothing of its own about the table.

A composition's dependencies are its stages' (<<composition-with-requirements>>), so a row exampling one supplies a fake for each of those. That holds whichever path the row takes: an input that departs the main line at the first stage never reaches the second, and the second stage's dependency is faked all the same. What a row has to supply is a property of the composition, not of the input this row happens to use. One dependency two stages share is one fake — the composition holds one of it (<<composition-with-requirements>>) — and the diagnostic names the stage that wants it (<<e1908>>).

[#example-pending]
=== A row waits for the implementation it is about

[[stand-in-and-recorded-row-agree]]A stand-in and a recorded row MUST NOT state different answers for one input. Which of the two the model is to be held to is not readable from the text, so neither is named as the right one.

[[stand-in-comparison-completes]]Reading a `+fake+` against the rows recorded for that behavior MUST come to a value, so that what the two state was compared. Being told nothing was found and being told nothing was compared are different answers.

[[an-answer-is-of-the-module-being-evaluated]]What answers a behavior MUST be built against the module the rows are written for, and where that cannot be established the rows MUST NOT be run against it (<<e1927>>). An implementation supplied from outside a compile reads a row's values by the declarations it was built against, and a row handed to one built against another revision is decided by the two builds disagreeing rather than by the model: an invariant narrowed since refuses a value the model admits, a case added to a union arrives where there is no such case, a field renamed is not found under its name. Such a row is undecided rather than failing — everything it can be held to without being run has been, and what it would have decided was not found out.

What is compared is what a value crossing between the two depends on: which cases a union has, what a data holds, what an invariant admits, what a newtype wraps, what a behavior's signature is, which behaviors are left to be injected, and the `+let+`​s a declaration cannot be read without — whichever module published one, since a rule written where a type is declared and called by a reader's invariant is as much a part of what the reader's values are as one written beside it. Both builds' declarations are read the way this compiler reads any published module, so what is compared is what they mean rather than the words they were written with. A name that reaches a declaration is the declaration it reaches — a bare name an `+import+` brought in, a name written under an alias and one written out in full are one name (<<modules>>) — a name a declaration binds is the binding it is, so a binding renamed with its uses admits what it admitted, and a number is the number it is, so `+1.0m+` and `+1.00m+` are one bound. None of that is decided by the comparison: it is what the language already says, and asking it twice is how the two answers come to differ. What a field is called is compared as the word it is, because a decoder reads a value under that word.

Everything else may differ. A `+let+` body, an example, a `+fake+`, a published `+let+` no declaration is read through, an `+import+` line nothing compared is written with, the order two declarations are written in, and how any of it is spaced are not things a value can be read differently by, and a build that differs only in those is not another build for this purpose.

The modules held to are the ones those declarations reach into — read off the names they are written with, not off the import lines, since a type is reachable qualified whether or not it was imported and a module may import what only an unread `+let+` wanted. They are read from what this compile reads declarations of, which is what it generated and what its path carries.

Whether the two agree and whether it can be told are different answers. Classes carrying no declarations, and declarations this compiler cannot read, establish nothing about either build — the answer may well be of exactly this module — so they are reported as what they are rather than as a build that has moved, and the rows are not run either way. Which side could not be read is said with it: what the answer brought is that build's to fix, and what this compile reads is its own path's. This is not the boundary revision (<<a-published-module-agrees-with-this-compiler>>): two builds of two different models carry the same one, and it says which compiler wrote a module rather than which model it was.

A run whose answers are the ones the compile generated has nothing to establish. There is one build, so what applies a behavior is of the module being evaluated by being of this compile of it.

An injected behavior (<<injected-behavior>>) has no `+let+` to apply, so a row naming one is *recorded* rather than evaluated. It is not refused. A model that a system is being migrated onto is injected everywhere at the start, and the rows harvested from the system it replaces are the record of what each behavior will owe; refusing them would mean keeping that record somewhere the compiler cannot read, which is where it stops being checked.

What a row can be held to without a body, it is held to. Its arity, each input built against its parameter type — compiled as this module's code and run, so an input that breaks an invariant is <<e1903>> the day it is written — the expected arm against the output's cases (<<e1904>>), and the expected value built (<<e1903>>). What is left needs something to run.

Evaluation begins by itself. The row is unchanged when the `+let+` arrives; from that compile on it is applied and compared like any other, and a row that stated the wrong answer is <<e1905>> then rather than never.

An implementation supplied to a run is the other way it begins. Where something outside a compile answers the behavior, the row is applied and compared the same way — the same fixtures, the same arm resolution, the same comparison — and a row that stated the wrong answer is <<e1905>> there too. What the implementation answered is held to what the behavior declares of what it answers (<<an-implementation-keeps-what-the-behavior-states>>), which for an injected behavior is where that clause is checked at all. Whether such a run happens is not the model's to say and nothing about the row changes for it: what is recorded is what applied it, and the rows are the same rows either way.

A `+fake+` is not what a pending row wants. A fake stands in for a dependency while some *other* behavior's row runs (<<example-fakes>>); an `+example+` of an injected behavior says what that behavior itself will have to answer. Neither is run from the other, so both belong. An injected behavior declares no `+depends on+` of its own (<<depends-on>>), so a pending row needs no fake at all.

They are read against each other all the same. Where a stand-in and a recorded row answer one input differently, that is <<e1919>>.

Each side is read the way it is read where it is used. What a fake answers for an input is what the fake itself would answer — the first row stating that input, and otherwise the `+_+` row (<<example-fakes>>) — so a table's default is what a recorded row is held against wherever no explicit row states its input; a table with a row that will not build answers nothing at all, being one no fake can stand in with; and a second table written for one dependency states nothing, since the first is the one that answers. A recorded row is read as the evaluator reads it: its arity, its inputs built against their parameter types, and its expectation held against the output's cases (<<e1904>>) and then built. A row that fails any of those states nothing here, because it states nothing there.

A stand-in and an assertion are not the same kind of thing. A recorded row may name a case and say nothing under it (<<example-expected>>); a `+fake+` row and a `+with+` may not, because what they install is a value, and a case with fields is not one. So a `+with lookup = Missing+` where `+Missing+` has fields stands in for nothing (<<e1908>>) and there is nothing for a row to disagree with.

A `+with dep = value+` is read this way only where `+dep+` takes no input. A fake row states its own inputs, so a recorded input picks the row that answers it and nothing is evaluated in between. A `+with+` states none: it is bound to the run of the row it is written on, and what reaches the dependency there is whatever the parent behavior computes and passes. Held against every recorded row it would drop the parent row's own input, and two rows faking two answers for two different inputs — each right about its own — would read as contradicting each other. A dependency taking nothing has one input, `+()+`, so there the `+with+` and the row recorded for it are about the same call. A `+with+` takes precedence over a `+fake+` table while its own row runs, and that settles nothing here: the table remains a statement about the same behavior, written for every other row and every other run.

Neither side is ranked. A model being migrated onto may run against a stand-in while the real answer is still being harvested, which is written the way a mistake is; what is known is that the two disagree, and where each is written. So it is a warning, and it is said at both statements rather than at one of them.

[,souther]
----
behavior findMember : (id: MemberId) -> Found | Missing

example findMember
    | "a member that is registered" : (MemberId("m-1")) -> Found { id = MemberId("m-1") }
    | "a member that is not" : (MemberId("m-9")) -> Missing { reason = "no such member" }
----

How many rows are waiting is what `+souther examples+` reports; it is not a compile warning, because waiting is the normal state of a model being written and a warning on every one of them says nothing.

[#example-ensures]
=== A written value is held to what the behavior states

A row and a `+fake+` row each write an input and the answer that goes with it, which is what an `+ensures+` relates (<<ensures>>). Where a clause is declared, the values written are held to it.

[[a-row-keeps-what-the-behavior-states]]The values an `+example+` row states MUST keep what the behavior declares of what it answers (<<e1928>>), and [[a-fake-row-keeps-what-the-dependency-states]]the values a `+fake+` row stands in with MUST keep what the faked behavior declares of what it answers (<<e1929>>). A declaration is what says what a behavior answers, and a row stating otherwise records something the model rules out.

[[an-implementation-keeps-what-the-behavior-states]]Where an implementation supplied to a run answers an `+example+` row of an injected behavior, what it answered MUST keep what that behavior declares of what it answers (<<e1930>>). A behavior with a body checks its own answer where it answers; an injected one has none, so what it answers is checked where it enters the domain, and applying it for a row is such an entry.

This is not the policy ADR-0093 declined. Two written statements disagreeing is a disagreement with no right side (<<stand-in-and-recorded-row-agree>>), which is why it is a warning naming both; a written value against a declaration is not two descriptions but a description held to what the model states, and the declaration decides.

It is the declaration's check that decides it, run over the values the row wrote. A row's operands are compiled and applied as the module's own code, so what a row states exists as a value while the module is compiled, and the same check that runs where the behavior answers is applied to it. Nothing reads the clause a second time and decides what it means. This is *compile-time fixture validation* and not a proof: what is established is that these values keep the clause, not that the behavior does — which is <<invariant-discharge>>'s distinction, on the other side of it.

A row waiting for an implementation is held all the same. The values are written whether or not anything applies the behavior, and a record of an answer the model rules out is a wrong record however long it waits (<<example-pending>>).

What is held is decided by what the row wrote, and an arm is part of what it wrote. A bare case name naming a unit case states the whole answer — a unit case has one value, so naming it is writing it — and such a row is held like any other. This is the same reading of an arm as <<ensures>>'s: an arm is a reference to the answer, so `+ensures NotFound -> id > 0+` is decided by a row stating an input and that arm, with no construction written.

Where only one side is written there is no relation to hold. A `+_+` row of a `+fake+` states no input, and a `+with+` supplies an answer for whatever it is asked; what either states is held where the behavior answers.

A table with a refused row answers nothing wherever else it is read. Nothing stands in with it (<<example-fakes>>), and it is not compared with the rows recorded for the behavior (<<stand-in-and-recorded-row-agree>>): that comparison names neither side as the right one, and this has already named one. The two cannot both be said about one pair.

Where a bare case name does not determine a value on its own, what the row states is the case and nothing under it, and the declaration is asked what that alone decides. A rule reading only the inputs is decided: its arm names the answer, and the row has said which case that is. A rule reading `+value+` is not, and stays undecided rather than being decided against a value nobody wrote — it is held where the behavior answers, as everything nothing wrote a value for is. Which rules a case decides is worked out where the declaration's check is emitted, so an arm naming a sum decides for each of the cases it has, and nothing here reads a clause to find out.

[#example-placement]
=== Inline or an attached file

[[examples-file-holds-only-examples]]An `+examples for+` file MUST hold only examples, the fakes they run against and the values their rows name, and MUST NOT declare a name the target module already declares.

[[examples-file-names-a-compiled-module]]An `+examples for <module>+` file MUST name a module among those being compiled.

[[an-attached-files-values-are-for-its-rows]]A value an attached file declares MAY be named by a row, by a `+fake+` and by another value of an attached file, and MUST NOT be named by the model: not by a `+data+`'s `+invariant+`, a `+behavior+`'s `+ensures+`, a `+let+`'s body, or the module's `+exposing+` list.

The base form is written inline, in the same source as the behavior. To keep fixtures out of the model source, examples may instead be written in an attached file that begins with `+examples for <module>+`. It holds the rows, the `+fake+` tables they run against, and the values they name; a data, a behavior, an `+import+` and a `+let+` with parameters belong in the module itself, and writing one there is <<e1906>>. The compiler merges an attached file into its target module; it is not a module of its own and needs no `+import+` (it sees the target module's names directly). Its values join the module the rows join, so a name the module already declares is <<e1906>> too — the rows would read that declaration and the one written here would say nothing. An attached file that names a module not being compiled is <<e1907>>.

The merge makes those values reachable under the same names as the module's own, and the direction they may be reached in is one way. A row is what the file is for, and a row written inline in the model source is a row too, so it names them as freely as one written beside them. The model is the other way: a module that named one would not compile without a file of fixtures, and a clause that named one would travel to an importer under <<a-published-module-carries-what-it-says-it-declares>> spelling a name the jar has no source for — the module's own source is what is published, and an attached file adds nothing to it. Naming one from the model is <<e1626>>. A value the model needs is declared in the model.

[#example-adequacy]
=== What the rows establish

An `+example+` fixes the value rule for one input. Whether a model's rows together say enough about it is a different question, and `+souther examples+` answers it from what evaluating them observed. Nothing here changes what compiles; it reports. And what compiles does not decide whether it reports: a compilation that raised a diagnostic observed whatever it observed, and the rows covered whatever they covered (<<example-report-emission>>).

These are the questions the dial asks of a model's rows, each answered where the measurement was complete:

* [[every-output-case-is-expected-by-a-row]]where a behavior's output is a sum, some row SHOULD expect each of its cases;
* [[every-input-case-is-used-by-a-row]]where an input position is a sum, some row SHOULD apply the behavior to each of its cases;
* [[every-border-has-a-row-against-its-line]]some row SHOULD be at each of the two points against the line of every border a rule draws — an `+invariant+`'s bound, a comparison in an `+ensures+` (<<a-clause-draws-a-line-on-what-it-compares-an-input-against>>), or a line a fork of a body draws. Those two are the border's `+ON+` and `+OFF+` points (<<a-border-owes-four-points>>), and together they are what fixes where the border falls: without the nearer one inside it, nothing shows the border is not a step further in, and without the nearer one outside it, nothing shows it is not a step further out. The two points away from the line are reported and asked for by nothing (<<e1916>>). Answering this takes running the rows again, so a build that did not ask for it is not told.

*Evidence is graded, not counted.* How far a row got decides what it proves, and three claims are kept apart rather than collapsed into one:

* *specified* — a row expects this case. Somebody wrote down that the model owes it.
* *observed* — the behavior was seen to answer with this case. A row that expected something else still saw what it saw: a row expecting `+Approved+` that got `+Rejected+` is no evidence for `+Approved+`, and is evidence that `+Rejected+` can happen.
* *verified* — a row expected this case and the behavior produced it.

A pending row (<<example-pending>>) reaches *specified* and no further, which is the honest account of a behavior that has been described and not yet written. A row that failed reaches *observed* — a measure that dropped it would report the case it produced as one nothing produces.

An input position carries the same three, with *executed* in place of *observed*: the behavior was applied to a value of that case, which claims nothing about what came back. Covering every case an input can be is the mechanised half of asking whether the behavior is total (<<match>>).

*What counts as a case to cover.* A position that is a sum — a union, or a named `+data S = A | B+` — has cases to cover, folded to their leaves. A position typed as one data has one, and covering it is not a question any row can fail, so it is not counted. This is narrower than what a row's expected arm is held against (<<e1904>>), which uses the single name to catch a row that wrote the wrong one.

[[a-case-the-rules-refuse-is-not-covered-or-uncovered]]*A case the model's own rules refuse is neither covered nor uncovered.* It is *excluded*: a third answer beside the two, reported and not counted against the model. Nothing can be constructed at such a case (<<e1903>>), so asking for a row at one is asking for work the compiler refuses, and a model whose rules narrow an input would report a coverage it can never reach. The case stays *declared* — the type has it, and that is part of what the model says — and leaves what the rows are held to, so what a report counts is `+declared - excluded+`. Which cases those are is what the declarations reaching the position leave (<<what-a-position-admits>>), read once and read the same way by every measure; a case nothing refuses is counted whatever any body says about it.

[[a-case-a-claim-is-made-about-is-counted-until-the-rules-refuse-it]]*An arm declaring that a case cannot arrive is a claim, and it is held against those rules rather than read into them.* It has three answers and no fourth. Where the rules refuse the case, the claim says what they say: the case is already out of every denominator, and what the claim adds is the author's own words, which the report prints beside it. Where the rules leave the case standing at a position nothing stands between and the caller, the claim is refused where it is written (<<e1326>>). Where nothing settles it — a case behind another fork, or one at a position whose rules could not be read — the case keeps whatever it was owed and the report says the claim was not proven. What removes an obligation is a proof; the absence of one is not, and an exclusion that is both unproven and silent is what leaves an author unable to tell a gap they can fill from one they cannot.

*A measurement says how much of itself it could make.* A row whose case cannot be read from its text — a helper's answer, which names no case (<<example-expected>>) — is counted as unclassified rather than dropped. While there is one, a case with no rows is *undecided* rather than missing, because one of those rows may be covering it. A behavior with no rows at all is not measured: an absence of evidence is not a set of gaps.

*An editor answers from the workspace or not at all.* Analysing one document cannot say what a behavior's rows cover, because they are written across its module's own source and any number of attached files (<<example-placement>>); reading one file would report what another covers as uncovered. So an editor computes this from the workspace compile, and where it has only a single document the measures are unavailable and nothing is reported.

*Not every measure has a code.* Which of these a build is told about is a separate decision from measuring them, and it is made per measure: a case of the signature nothing claims (<<e1913>>, <<e1915>>), a row missing against a border's line (<<e1916>>) and an arm nothing goes through (<<e1918>>) are warnings a build can ask for; the rest — how many rows are waiting for a `+let+`, which positions have no finite partition to derive, and a row missing away from a border's line — are reported by `+souther examples+` and never warned about. A position the model draws no line through is a fact about the model rather than a mistake in it, and a row waiting for a `+let+` is the normal state of a model being written. The warnings are off unless a build asks for them, and asking for the arms costs a second run of every row.

*Whether the measurement could be made and whether the rows cover it are two answers.* A report says both. The first is `+complete+` or `+partial+`; the second is `+satisfied+` where every measure that was asked for came to an answer and none of them found a gap, `+not_satisfied+` where one did, and `+undetermined+` where a measure that could have found one was not asked for or could not be made. One word for both would call a model adequate on the strength of a measurement that never ran.

Which measures could have found a gap is what was asked for and what the model has, and not what the evidence looks like. A behavior whose arms were never asked about, one that has no arms to reach — a `+>->+` composition, whose stages have them and which has none of its own — and one whose arms could not be measured are all three without arm evidence, and only the last of them leaves the answer undetermined. The same holds of a position dropped for being past the axis limit: dropping one a rule had drawn a line on loses boundaries nothing can ask about now, and dropping one that was only classifying loses a measure no build refuses over.

*What refuses a build.* A gap a measure came to an answer about and the criterion the build asked for asks for. Both ways of asking read the same answer: `+souther compile --adequacy all --warnings error+` and `+souther examples --strict+` refuse the same findings, both being held to simplified domain coverage. `+souther examples+` names no criterion of its own, so a build wanting the other one asks `+souther compile --adequacy reliable-domain --warnings error+`. How many rows are waiting for a `+let+` refuses nothing. A build that failed on one would fail on the record of what an injected behavior owes, which is what <<example-pending>> says a model being migrated onto is made of.

*What is measured and what a build is held to are two things it asks for.* `+--adequacy+` names both at once, and the words are presets rather than a single ladder. `+off+` measures nothing. `+witness+` reads what the rows already established. `+all+` adds what the arms and the boundaries take a second run to find out, and holds the build to simplified domain coverage. `+reliable-domain+` measures the same as `+all+` and holds the build to reliable domain coverage, which adds the two points away from each line (<<a-border-owes-four-points>>, <<e1917>>). The last two differ in the bar and not in the work: the points away from a line come off the same assessment as the points against it, so a fourth level would have said nothing about how much to measure.

*A selector names a subject or the command is refused.* `+souther examples --module+` and `+--behavior+` narrow what is reported, and a name that resolves to no subject is a usage error rather than a report of nothing. A report of nothing says the measurement was made and found no gap, which is what a build reading it is entitled to accept. The two names are resolved together, so names that each resolve and hold nothing between them select nothing; and they are resolved against what the sources declare rather than against what reached the report, so a subject that resolves and then cannot be measured is reported as `+partial+` and is not a selector that named nothing.

*The question is asked of a module, not of a source.* A behavior's rows are written in the module's own source and in any number of attached files (<<example-placement>>), so a source-by-source reading would report what another file covers as uncovered.

[#example-partition]
=== The classes a model already states

The equivalence classes an input is measured at are read off what the model already declares. Nothing is invented: a position the model draws no line through has no classes, and is reported as *not derivable* rather than filled in with values nobody asked for. An `+Int+` with no invariant is not split at zero, because a partition nobody wrote is a rule nobody wrote, and reporting coverage of it reports a gap for failing to test something the model does not say.

A type says the classes. `+Bool+` is two; an optional is `+None+` and `+Some+`; a sum is its cases, folded to their leaves (<<sum-data>>). A record is not a class of its own — it is taken apart field by field, two levels deep, which reaches a field of a record a parameter holds and stops where a report would stop being about anything the author recognises as one input.

A threshold says where one class ends and the next begins. A `+guard+` comparing an input against a constant (<<guard>>) divides the position, because both sides of that comparison hold values a row can write, and so does a comparison in an `+ensures+` (<<a-clause-draws-a-line-on-what-it-compares-an-input-against>>). A `+guard+` comparing one input against another divides neither of them and still draws a line (<<a-comparison-of-two-positions-draws-a-line-between-them>>). Thresholds at one position merge into one partition, intersected with what the position admits: an invariant of `+x >= 0+` and a guard of `+x < 10+` divide the position into `+0 <= x < 10+` and `+10 <= x+`, and nothing below zero, because nothing below zero can be constructed. Two rules drawing a line at one value merge into one partition and stay two obligations, since reaching one says nothing about the other.

[[boundary-coordinates]]*What a line is drawn on is a boundary coordinate, which is not always a position — and being ordered is what such a value must be rather than what makes it one (<<a-line-is-drawn-where-the-values-can-carry-one>>).* A coordinate is the content of a location — a parameter, a field chain, a newtype's wrapped value — or the size of a container or a string taken of one: `+List.length+`, `+String.length+`, `+Set.size+`, `+Map.size+`. Which values divide an input space is its own question, and this states the answer rather than borrowing one: a value some other reading of the model can point at is not thereby an axis rows are asked to cover. So `+data Name = String invariant String.length(value) > 0+` draws a line at a length of 1, and a `+guard String.length(t.name.value) > 3+` draws one at 3 with a 4 beside it, exactly as a bound and a guard on a number do. A line is named by the coordinate it is drawn on — `+String.length(t.name) = 1+` — and an invariant and a guard mean the same line when they take the same measure of the same location. A size is a whole number and is never negative, and neither has to be written down: no row is asked for at a length below zero however the rules about it are phrased.

A position whose values no rule measures still has no classes. A `+String+` nothing says anything about is *not derivable* and is named by the position, not by a length nobody wrote a rule about.

A position has one coordinate, and which one is settled by its own type's rules where it has any. A rule reaching the position from the value it sits in states an end on a coordinate and does not say which coordinate the position is measured at: `+data Name = String invariant value >= "m"+` held in a record bounding the length of it is measured on `+Name+`'s order still, and the length bound states an end on a coordinate this position is not measured at. Where the position's own type says nothing, one such rule settles it; where they say where both of its coordinates stop, neither does, and the position is one the model draws no line through.

[[a-line-is-drawn-where-the-values-can-carry-one]]*Which ordered values a line is drawn on is stated, and where none is drawn the position says so.* A line is drawn on an `+Int+`, a `+Decimal+`, a `+Date+`, a `+DateTime+`, a `+Time+`, an `+Instant+`, a `+String+`, an enumeration — ordered by the order its cases are declared in — and a single-value newtype over any of them, which is ordered by the value it wraps (<<primitives>>). A line on an enumeration is named by its cases and never by the place a case takes in the declaration, and one on a `+String+` by the string. No line is drawn on a position that is one case of an enumeration, or a union of some of them: it is comparable on its sum's order and ranges over less than it, so a line drawn from the sum's places would divide values the position does not hold. Such a position is named as one a rule was written about that this could not draw, which is a fact about the compiler and not about the model. That is told apart from the other answer because they ask for different work: *not derivable* says the model draws no line at this position, and *not read* says it draws one this could not read (<<example-report-vocabulary>>).

The value beside a line is a separate question from the line. It is asked for only where the values have a smallest step. A whole number, a date, a date-time, a time of day, a moment and an enumeration have a neighbour — a `+DateTime+` and a `+Time+` are held to the second (<<a-local-temporal-is-held-to-the-second>>) and an `+Instant+` to the nanosecond (<<an-instant-carries-what-a-timestamp-said>>), which is the step each moves in — and a `+Decimal+` and a `+String+` have none: no smallest step is named for a decimal, and naming the string just above another means choosing a character this language does not name either. Inventing one would test a rule the model never stated. A line whose neighbour does not exist is still a line and is still owed a row at the value itself.

A rule is read the same way wherever it is written. An invariant's bound, a `+guard+`'s comparison and a comparison in an `+ensures+` draw lines on the same values, so a position bounded at a date and a position compared against one are measured alike; a rule any of them writes that this could not read is named as unread, whichever wrote it.

[[a-clause-draws-a-line-on-what-it-compares-an-input-against]]*A comparison in an `+ensures+` draws a line, and one a row can be written at.* `+ensures asked = NotFound -> id.value > 0+` says the behavior may not answer `+NotFound+` at or below zero and may above it, which is a value the stated relation changes at. Both sides hold values a row can write, so the value and its neighbour are owed a row, exactly as a `+guard+`'s line is. What differs is what meeting it takes, and the two lines are not measuring the same thing. A `+guard+`'s is about control flow: the comparison is a place in a body, and meeting the line takes getting it to answer. A clause states a relation the behavior is held to, and its line is covered by the input the relation changes at — so a row writing the value has met it, which is what an invariant's bound is met by as well, and it is why a clause draws its lines whether or not anything implements the behavior. Whether some evaluation of the clause reached that comparison is a separate question and is not what a boundary measures.

Only what a rule requires, and only about an input. A conjunct is part of what the rule asks of an answer, so the relation changes where it does; a disjunct is not, so `+id.value > 0 || urgent+` draws nothing at zero — the rule is satisfied wherever the other side is, whatever the comparison comes to. A comparison reading `+value+` states where the relation changes on the answer, and no row can be written there: what a row chooses is what the behavior is applied to.

A rule comparing one input against another draws a line between the two, the way a `+guard+` doing so does (<<a-comparison-of-two-positions-draws-a-line-between-them>>), and that line is met the same way this one's others are: by a row putting one count in both positions. It owes all four of its points, which are the four of a border on the difference the two fall apart by (<<a-relational-border-is-a-border-on-the-difference>>).

A comparison is read wherever in a condition it is written. `+kind == Domestic && cost <= 100000+` draws a line at 100000 as much as the comparison written on its own does: which position the model divides is not a question about the shape of the condition around it.

What the condition does decide is which arm stands as evidence that the comparison was evaluated. `+&&+` and `+||+` evaluate left to right and stop as soon as the answer is settled, so under `+A && B+` reaching either arm shows `+A+` ran and only the arm where the whole condition held shows `+B+` did; under `+A || B+` it is the arm where it did not hold. A condition mixing the two leaves neither arm separating the rows that reached a comparison from the rows that did not. A line no arm can witness on the side a row would have to be written is still a line the rows are owed, and is reported as one nothing measured rather than as one nothing drew.

[[what-a-position-admits]]*What a position admits is not what its type admits.* A field's type says which values exist of that type; the record holding it may state a rule relating that field to another, and what the position admits is that rule read at this field. `+data WorkInterval = { startsAt: MinuteOfDay, endsAt: MinuteOfDay } invariant startsAt < endsAt+` over minutes of a day running to 1440 leaves `+startsAt+` stopping at 1439 and `+endsAt+` starting at 1: a `+startsAt+` of 1440 would want an `+endsAt+` the day has no room for, and no row can be written at it.

[[a-clause-places-an-edge-where-it-reaches-one-coordinate]]*Which coordinates a clause reaches decides what it may do; the declaration it is written on does not.* A clause bounding one boundary coordinate against a constant, without relying on another coordinate, places an edge there wherever it is written: `+data Tally = { n: Int } invariant n >= 1+` places one at 1 exactly as `+data Count = Int invariant value >= 1+` does, and `+data Bag = { xs: List<Int> } invariant List.length(xs) >= 1+` places one at a length of 1. A clause relating coordinates takes an existing edge in and places none — `+startsAt < endsAt+`, and `+List.length(xs) >= minLines+` as much — because which values of one are on which side depends on the other, and a rule between two fields is not a partition of one of them (<<what-a-position-admits>>).

Placing an edge and taking one in are separate, and a position with no edge placed on it has no line however far its range was taken in. `+data R = { a: Int, b: Int } invariant a < b invariant b <= 10+` leaves `+a+` running to 9 and draws no line through it, because that 9 is where `+b+` stops and not something any clause said about `+a+`; `+b+` has a line at 10.

A clause reaches a position from wherever it governs it: the coordinate's own type and the names wrapped round it, the record the position is a field of, the declarations under that record, and the names wrapped round any of them. `+data NonEmptyBag = Bag invariant List.length(value.xs) >= 1+` places the edge `+Bag+` would have placed by stating the same rule, and names `+NonEmptyBag+`. A name wrapped round a value is not a step of the coordinate's path, so the coordinate is the `+xs+` a reader of a `+NonEmptyBag+` sees, and it is the same coordinate whether the name is worn at a parameter, at a field of a record, or several deep. A line is named by the declaration the clause is written on.

A name wrapped round a record is a governing declaration like any other, so what its clauses do is what any clause does: place an edge where one reaches a single coordinate, and take an existing edge in where one relates coordinates. `+data Wrapped = Base invariant value.a < value.b+` places no edge and still leaves `+a+` stopping where the room `+b+` needs begins. Where a record's own clause is the tighter of two, the line is the record's: `+data Moved = { n: Count } invariant n.value >= 5+` over a `+Count+` of at least 1 draws its line at 5 and names `+Moved+`. Where two clauses place an end at one value, both name it. Only a clause relating coordinates is named beside a line rather than as one, since it moved an end another clause placed — and it is named by the declaration whose clause is holding that end, which need not be the value the position sits in: a `+Held+` holding a `+Wrapped+` whose clause relates two of its fields reports the edge as taken in by `+Wrapped+`. Each end is named on its own, and a relation that reaches nothing the coordinate had not already passed names neither.

[[a-comparison-of-two-positions-draws-a-line-between-them]]*An ordering comparison of two terms draws a line, and it is not a partition of either position.* A `+guard+` comparing one position against another — `+charge.value > ceiling.value+` — puts its line where the two hold the same value, and so does a comparison in an `+ensures+`: `+ensures ordered = Ok -> from.value < to.value+` puts one at `+from = to+`. That is a line and not a class: which values of one position are on which side depends on the other, so neither position is divided, and the two answers are separate. The position is reported as one nothing divides, and the line is one the rows are owed (<<every-border-has-a-row-against-its-line>>). An equality is not one of these: `+a == b+` puts a whole arm on the line, and that arm is a row the arms are already asked for (<<every-arm-has-a-row>>).

Which ordered values such a line can be drawn on is the same list as anywhere else (<<a-line-is-drawn-where-the-values-can-carry-one>>), and it is what the two sides must share rather than a type. Two operands compare only when they are of one type, and the values a line is drawn on are wider than that in one direction and narrower in another: `+Charge+` against `+Ceiling+` is two types whose values are ordered alike and has a line, while two positions each declared as one case of an enumeration compare on their sum's order and range over less than it, so they have none. Both sides must be terms a row can be written at; where one is something taken of them (`+charge > ceiling + 1000+`) the line is at no value either position has, and it is reported as a comparison nothing was read from.

Whether a row can be written on such a line is whether the two positions can hold one value *at once*, and the ranges their own rules leave them do not say. A value in both ranges is one each of them admits on its own, and what refuses the pair need not be in either range: `+data Pair = { a: Int, b: Int } invariant a < b+` leaves `+a+` and `+b+` running over each other everywhere while the line `+a = b+` holds nothing, and a rule the ranges could not take in — a disequality leaving a hole, a pattern leaving one string — refuses values the range says are there. So such a line is settled by a witness: a row already on it, or a value that went through the decoder, which is what reads every rule of a value at once. Where nothing settles it the line is reported and not counted, the same account any other edge nothing promises gets. A line at a value of one position keeps its own account, where the rules that bound the position are the rules that drew the line.

Failing to compose a row settles nothing either way. A line the rules prove stays counted whatever a search made of it, and a tool with no way to write the value says so about itself rather than about the model.

[[a-relational-border-is-a-border-on-the-difference]]*A line between two positions owes the same four points as any other border, and they are the four of a border on the difference the two terms fall apart by.* A point of it is a pair standing exactly so many steps apart and a side of it is a pair standing further apart than that, so `+guard a < b+` over whole numbers is at its `+OFF+` point where the two are equal, at its `+ON+` point where `+a+` is `+b+` less one, and has its `+IN+` point under that and its `+OUT+` point over the line:

....
guard a <  b    ON  a = b - 1   OFF a = b       IN  a < b - 1   OUT a > b
guard a <= b    ON  a = b       OFF a = b + 1   IN  a < b       OUT a > b + 1
....

Read as the line against itself with no step, the pair one step inside the border fell into the side beside it — a row at the `+ON+` point counted as the point away from the border, and the point against it reported as one nothing owed. Where the carrier names no value one step from the line the two points against it are not named, which is the same answer a border at a place gives for the same reason.

What is read of such a rule is what a comparison of numbers states — an ordering between two fields, or between a field and a constant. A rule of another shape leaves the position where its type left it, and that edge is then not one anything promises: the rule that could not be read refuses a value at the edge as readily as one past it. Such an edge is reported and not counted, and no build is refused over it (<<example-adequacy>>) — the same account a combination nothing has settled gets. A concrete value accepted at the edge settles it, being a value that went through the decoder: an `+example+` row written at it does, and so does a candidate the compiler composed and the decoder took. Failing to compose one settles nothing — it leaves the writability unknown, the same as never having tried — so an edge becomes counted when a witness is found and is never dropped for want of one.

An invariant's bound is a boundary and not a partition. Everything outside it is refused at construction (<<e1903>>), so there is no class on the far side to cover — what remains is an edge worth a row, and the two points out there are excluded rather than left unsaid (<<a-border-says-why-a-point-is-not-owed>>). Both sides of a `+guard+`'s line do have values, so it wants the line and its neighbour, and the neighbour only where the type has one: `+cost <= 100000+` asks for 100001 on an `+Int+`, and asks for nothing beside it on a `+Decimal+`, which has no next value.

A rule that drew a line is kept per line. The same value can be a boundary of an invariant and of two separate `+guard+`s, and reaching it through one says nothing about the others, so one value can be several obligations. A rule that took a line in is not a second line: the bound and the record that narrowed it are one boundary, named by both, because neither of them wants a row of its own.

[[what-is-read-off-a-body]]A class the rules refuse is *excluded*, as a case of the signature is (<<a-case-the-rules-refuse-is-not-covered-or-uncovered>>), and it is one reading that says so — for this position, for the combinations this position takes part in, and for the rows a tool offers to fill what nothing covers. One universe, so that the three cannot come to disagree about what there was to cover.

What a body declares cannot arrive is read beside that reading and never into it (<<a-case-a-claim-is-made-about-is-counted-until-the-rules-refuse-it>>). Only what the body states without anything having to be decided is read:

* the body is followed to the first fork through what a `+let+` binds;
* a function written where a call takes one is not entered. Evaluating that position makes the function, and what it matches on when the call applies it is about its own parameters rather than about this behavior's inputs;
* where that fork is a `+match+` on an input position, each case whose arm answers nothing (<<an-arm-that-answers-nothing-is-not-an-arm>>) is a case that arm declares cannot arrive at that position;
* nothing below a fork is read. An arm of one `+match+` holding a `+match+` on a second input says what that input cannot be *given the first one*, which is not a fact about the second input and is not something a class of it can carry;
* nothing is read off a condition. Which values satisfy `+somePredicate(f)+` is a question with a solver behind it, and an answer taken on faith here removes a case the author should have been asked for.

So claims that could be read are missed, and that is the direction to miss in: a claim nothing read leaves the class in, and a class left in is one the report asks for a row at, which tells the author something true about their model. It is safe here because a claim takes nothing away on its own — read as a reason to drop a class, the same miss would be a class taken out with nothing having asked whether it can arrive, and that tells the author nothing at all.

[#example-branch-coverage]
=== Which arms of the body the rows go through

Where a behavior has a body, `+souther examples+` also reports which of its arms the rows run through. An arm is one branch of an `+if+` or a `+guard+` (<<if>>), one arm of a `+match+` (<<match>>), or the success branch or one departure of an attempted construction (<<attempted-construction>>). Cases written together on one arm are one arm: they are one run of code, and a row takes it or does not.

One more the dial asks, needing the arms measured:

* [[every-arm-has-a-row]]some row SHOULD go through each arm of an `+if+`, a `+guard+` or an attempted construction.

The line such a fork draws is asked for as well, and it is asked for wherever a rule draws a border rather than here (<<every-border-has-a-row-against-its-line>>): a `+guard+` comparing one position against another draws its line where the two are equal (<<a-comparison-of-two-positions-draws-a-line-between-them>>), exactly as an `+invariant+` and an `+ensures+` draw theirs. For a guard the arms do not stand in for it: both arms of `+charge > ceiling+` are reached by rows nowhere near the line, and a row on it takes the same arm as one well below it.

This is *branch-arm* coverage and nothing wider. Going through both arms of two nested conditions is four arms and says nothing about whether their combinations were tried, so covering every arm is a lower bound on covering what a body can do rather than the same thing.

An `+unreachable+` (<<unreachable>>) is not an arm. Reaching one is already <<e1911>>, so counting it would leave every correct model permanently one arm short. An invariant clause is not one either: it is a property of a type rather than a fork in this body, and whether the rows reach its edges is <<example-adequacy>>'s question, asked another way.

[[an-arm-that-answers-nothing-is-not-an-arm]]More precisely, an arm is not an arm where evaluating it cannot answer a value. That is the whole of what an arm is made of and not the shape it is written in: an arm whose every path ends in an `+unreachable+` answers as little as one written as a bare `+unreachable+`, and so does a value one of whose parts is an `+unreachable+`. An arm that has one path answering a value is an arm, whatever else is beside that path and whether or not a row has been written for it — what a body has to cover is a property of the body, and rows do not move it.

[[an-arm-for-a-case-the-rules-refuse-is-not-an-arm]]Nor is an arm an arm where no value can reach it. A `+match+` arm is written for one or more cases of what it matches on, and where the rules reaching that position refuse every one of them (<<what-a-position-admits>>) there is no value to take the arm: `+data Active = Flag invariant value /= Off+` leaves no `+Off+` to arrive, so the arm written for one is owed no row. Read wherever the `+match+` is, and not only at the first fork — a refusal is about the position and holds under however many arms the fork sits inside, which is the opposite of what a case being *admitted* says, since that says nothing about whether the fork is reached at all.

[[a-fork-nothing-reaches-has-no-arms]]Nor is an arm an arm where nothing reaches the fork it belongs to. A row cannot get past something that aborts, so a fork written after one — a `+match+` under a binding whose value is an `+unreachable+`, anything at all inside an arm that answers nothing — is a fork no row takes: the row that would take it reaches the `+unreachable+` first and is <<e1911>>, which states nothing. Such arms are as much not arms as the one that aborts, and for the same reason. This reaches the whole body, so a body that cannot answer at all has no arms and draws no line for a row to be at.

What a call answers is not read. A non-recursive helper is already inlined into the body that uses it, so its arms are here; a recursive one is a shared method, and an arm whose only content is a call to a helper that never answers is counted as an arm. That is a gap in the measure and is stated rather than worked around, because reading through a call means deciding what a call does.

A function written where a call takes one is made there and run elsewhere. Evaluating that position produces the function; its body runs when the call applies it, on arguments the position does not have — a step that answers `+unreachable+` for an element it may never be handed does not stop the expression around it from answering. The arms inside such a function are arms, and the rows that run it go through them.

*The measurement is opted into by writing a row.* A behavior that no row names is not reported as having arms nothing reaches, whether or not another behavior's rows run through it. A row about `+A+` that calls `+B+` did go through `+B+`'s arm and that arm counts as reached; what does not follow is that `+B+`'s author has been asked anything.

*A row that did not finish contributes nothing.* What such a row went through before its budget ran out (<<e1910>>) is some of what it would have gone through, and it is recorded as undecided rather than counted.

*A boundary a guard drew is met by reaching the comparison, not by writing the value.* A threshold read off a `+guard+` is a boundary to reach (<<example-adequacy>>), and a row can carry exactly that value and never get to the guard because an earlier branch went the other way. Such a boundary counts as met only where a row both wrote the value and went through one of the two arms of the `+if+` whose condition compares it. That is a fact about the rule that drew the border and holds of all four of its points (<<a-border-owes-four-points>>): a row well inside a guard's line that never reached the guard says as little about it as a row on the line that did not. A boundary an invariant drew is met by writing the value: nothing outside the bound can be constructed, so the edge is the whole of what there is to reach.

[#example-report-vocabulary]
=== The words the report uses

`+souther examples+` writes its own words, and a reader who wants to know what one of them means has nowhere to look it up unless it is written down. A diagnostic carries a code and a code names a rule; a line of this report carries neither, so its vocabulary is fixed here.

Most of it is borrowed rather than coined. Deriving partitions from a model's declarations, drawing a line where a rule places one and asking for a row at the line and at the value beside it is domain testing, which has a syllabus and a body of practice behind it (ISTQB CTAL-TA v4.0 §3.1.1, generalising equivalence partitioning and boundary value analysis). Where this report means what that vocabulary already means, it says it in that vocabulary — a reader who practises the technique should not have to translate, and a word this compiler invented for something with a published name is a word nothing can be looked up in.

[[the-report-claims-no-coverage-criterion]]What the report does not do is name a criterion as satisfied. The syllabus defines two — simplified domain coverage and reliable domain coverage — and either one, printed as a verdict, would be a single word standing for a whole report, which ADR-0089 refuses. So the report says which coverage items a border owes and which of them a row is at, and a reader working to one of those criteria decides for themselves whether it adds up. That is the honest reading while some coverage items cannot be produced at all. A point beside a border is derived only where the carrier names the neighbouring value, and a `+Decimal+` names none: the syllabus asks for the closest value "according to the given precision" and this language carries no precision for a decimal. Such a point is not counted as one a row is owed, because no row could be written at it — an item that cannot exist is not a gap.

Which is a separate decision from what a build is told about. A row against the line is what simplified domain coverage asks for and is a gap a build can be made to refuse over (<<e1916>>); a row away from it is one of the two items reliable domain coverage adds, and is reported and refuses nothing. Both come off one measurement of one border, so what a build is held to is a reading of it rather than a second measure made to other rules.

*What the rows establish about a case.* `+specified+` — a row expects this case; `+observed+` — the behavior was seen to answer with it; `+verified+` — a row expected it and the behavior produced it. An input position carries `+executed+` where an output carries `+observed+` (<<example-adequacy>>).

*What the rows reach of the model's own distinctions.* A class of a position is an *equivalence partition* of it, and a line some rule drew between two of them is a *border*. `+axes+` is how many positions the model divides at all; `+equivalence partitions+` is how many of those positions' partitions some row is in. `+not derivable+` is a position the model draws no line through, which is a fact about the model and not a gap in the rows (<<example-partition>>).

Named for what it counts, which is not what the syllabus calls an `+IN+` point. An `+IN+` point is a value inside a partition and away from *one border of it*, so it is keyed on a border and this count is keyed on a class. A row on a line is in one of the two partitions it parts and counts here; whether that partition also has a row away from the line is what the border's `+IN+` point says, and neither number answers for the other. The two are different measures of different things and both are reported: this one is equivalence partitioning's item, one row per class, and the four keyed on a border are domain testing's.

*What a border owes.* `+borders+` is how many lines some rule drew, each named by the coordinate it was drawn on (<<boundary-coordinates>>), and `+coverage items+` is how many of the points those borders owe a row is at. A border is *closed* where the values on the line satisfy the rule that drew it and *open* where they do not, which is what tells a rule written `+<=+` from one written `+<+`.

[[a-border-owes-four-points]]*A border owes a row at four points, and every one of them is keyed on the border.* They are the four the syllabus names: an `+ON+` point is inside the partition the border bounds and closest to it, an `+OFF+` point is outside it and closest to it, an `+IN+` point is inside it and away from it, and an `+OUT+` point is outside it and away from it. The same value can be a different point of the border beside it, which is why none of them is a property of the value. A closed border is at its own `+ON+` point with an `+OFF+` point one step over, and an open border is at its own `+OFF+` point with the `+ON+` point one step back; the two sides run from those points outwards:

....
guard n <= 100    ON  n = 100    OFF n = 101    IN  n < 100    OUT n > 101
guard n <  100    ON  n =  99    OFF n = 100    IN  n <  99    OUT n > 100
....

[[a-point-away-from-a-line-lies-in-the-partition-that-line-bounds]]`+ON+` and `+OFF+` ask for a place. `+IN+` and `+OUT+` ask for a *partition* — the one the border bounds and the one it keeps out — less the value against the line, so a row anywhere in that partition other than at the line is at the point. Which is not the same as everywhere past the line, and the difference only shows where a position has more than one line through it:

....
guard n > 10                      IN  11 < n              OUT n < 10
guard n > 10 ; guard n > 20       IN  11 < n <= 20        OUT n < 10
....

With the second line written, a row at thirty is in a third partition and answers for neither point of the border at ten. Read as everything past the line it answered for the `+IN+` point, and a model whose rows reach the middle partition only at its edge had nothing said about it: the count of equivalence partitions reports the partition as reached, which it is, and the point that tells a row inside from a row on the line was already discharged by a row that is in neither.

What the report writes for a point is the relation and what it is against, whole — a value alone would name a witness of a partition as though it were the partition.

* [[a-border-says-which-point-it-owes]]a point the report names MUST say which of the four it is. A border can owe a row at all of them, two of the values differ by one step, and a report naming neither role leaves an author to work the closed-border rule out for themselves — which is the reading this vocabulary exists to hand over rather than ask for.

[[a-border-says-why-a-point-is-not-owed]]Which of the four a border owes a row in is the rule's to say, and it is not always all four — and a point it owes none in MUST say which of three things settled that. Left out, they read as the report being short of items the technique asks for, and two of the three are not shortfalls at all:

* the rules leave no value there. An invariant leaves nothing outside its bound, so its `+OFF+` and `+OUT+` points are *excluded* — the same reading a case its rules refuse gets (<<a-case-the-rules-refuse-is-not-covered-or-uncovered>>), one step further in — and a side the rules leave one value wide has no `+IN+` point for the same reason;
* the carrier names no neighbouring value, so the point one step away is not named at all. A closed border there has only its `+ON+` point and an open one only its `+OFF+` point — the missing one is decided by which side the cut falls on and not by a rule that decimals lose their `+OFF+` points. This is the item that cannot exist rather than one nobody has got to (<<the-report-claims-no-coverage-criterion>>);
* the rule names a value rather than ordering the values around it. An equality says which value is singled out and nothing about which side of it anything is on, so neither neighbour is nearer to being outside than the other and there is no `+OFF+` point to ask for. What it does divide is the value from every other value, which is what its `+OUT+` point covers.

A strict bound on a `+Decimal+` is further along again: its cut is a value the type refuses, and this draws no border there at all rather than drawing one and owing a row at it.

*What combinations the rows reach.* `+combination+` is a line of its own, and `+pairs+` under it counts combinations across two positions, as counts rather than a ratio (<<example-partition>>). This is combinatorial testing (ISTQB CTAL-TA v4.0 §3.1.2) and a different technique from the one the lines above are about — printed at the end of the partition line it sat beside the border counts, where a reader could add the two into a total neither is part of.

*What a case behind an `+unreachable+` is told.* `+excluded+` is a case the model's own rules refuse, counted out of what the rows are held to and printed with whatever the body wrote about it (<<a-case-the-rules-refuse-is-not-covered-or-uncovered>>). `+declared unreachable ... and nothing here proves it+` is the other answer: a case a body says cannot arrive that nothing settled, which is still counted and still owed a row. The gap it names may be one no row can fill — a row reaching the point is <<e1911>> — and saying so is what the words are for, since an exclusion that is both unproven and silent leaves an author unable to tell that gap from one they can close.

*Whether a measure has a number.* Three states, and the report's own word for each. `+complete+` — the measure was made and everything it reads was readable, so a gap is a gap. `+partial+` — it was made and something it reads was not, so it shows what it saw and what it could not decide; a line under a partial measure reads `+undecided+`, which is that measure's account of one thing rather than a state of its own. `+unavailable+` — there is no number.

*Why a measure has none.* `+unavailable+` alone does not say, and the two states it covers ask opposite things of a reader:

* [[a-measure-says-why-it-has-no-number]]a measure that has no number MUST say why, and one that has a number MUST NOT.

`+not applicable+` — there is nothing here for the measure to be about, and no row could change that: a `+>->+` composition and a behavior with no `+let+` have no arms, and a position that is not a sum has no cases to cover. `+not measured+` — it could have been made and was not, and which of the reasons is given: no row names this behavior, the build did not ask for the arms, or the run's instrumentation could not be tied back to the body.

That difference is the one worth reading. `+not applicable+` asks nothing of an author. `+not measured+` says what to do: write a row, ask for the arms, or find out why the run could not read them. Rows that nothing could read are neither — a source that would not evaluate may hold the row being asked for, so what it was carrying is undecided rather than absent.

*A measure's reason is the measure's own.* It is not worked out from the numbers beside it, from how many rows a behavior has, or from whether the behavior is injected. Those correlate with the reason and are not it.

*The JSON form carries the same two.* Every measure emits `+status+`, and emits `+reason+` exactly where `+status+` is `+unavailable+`. `+schemaVersion+` is raised when a field is removed or renamed, and not when one is added: a document written before a field existed is still a document of this version. A word added to an enumerated field is read the same way, for the same reason — no document written before it carried it. That holds one way only. Every object in the schema is closed, so an older copy of it refuses a document carrying a field added since; a reader that has to accept newer documents wants the newer schema rather than a higher number.

*What the rows could not be observed doing.* Beside the measures, the report carries the reasons the rows were not observed — what happened, and what it happened to: a behavior, a source, the module, or a position in a value. A row that did not come back, a source none of whose rows were evaluated, a run whose instrumentation could not be tied back to the body. The JSON writes them as `+incompleteness+`.

A reason about a source is written in the two forms apart, and this is the one place in the report where they differ:

* [[a-reason-about-a-source-names-the-file]]in the form a person reads, a reason about a source MUST name that source the way the same run's diagnostics name it. A run over several files is where such a reason is worth reading, and one that cannot say which of them it could not read says nothing an author can act on;
* [[the-json-carries-what-identifies-a-source]]in the JSON, a reason about a source MUST carry what identifies that source to whoever handed it over — the position in the list a build passed, the document URI an editor is keyed on — unchanged.

The two are apart because the second is not a name. What a file is called is the shortest thing that tells this run's files apart, so it is a fact about the set in front of a reader rather than about any one of them, and it is neither stable across runs nor a key. A document that carried it in place of the identity would say what a person should be shown and stop saying which source it is about.

Which leaves a document that cannot be read on its own, since nothing in it says what a position in a list was:

* [[the-json-explains-the-identities-it-carries]]every source identity the JSON writes anywhere MUST have an entry in `+sources+` saying what that source is called, under the name the same run's diagnostics give it. This is about the identities the document carries and not about the sources the compile was handed: a source the report says nothing about needs no entry, and a document listing every input would be saying a second thing.

A reason's subject is one place an identity is written and never the only one — a position points into a source too. The rule is written over what the document carries rather than over the fields it is carried in, because a field that comes to carry one is the case a list of fields does not cover.

This is not the whole account of what an assessment went without, and an empty list does not say that everything was read. What stopped a measure from reading what was observed is said by that measure instead: a position dropped for being past the axis limit, a combination count that stopped at its own limit, a value too large to be read back. Those carry no entry here, and a report may say `+partial+` with this list empty. The division is which half of the work did not happen — the rows not observed, or the observations not read — and `+status+` is what a reader asks about completeness either way.

These are not the compilation's diagnostics restated. A diagnostic says what is wrong with the source and where; one of these says which evidence the assessment went without, which is a fact about the measurement. Most of them will sit beside a diagnostic that accounts for it — a source that would not evaluate is a source something was wrong with — and one of them never does: the runtime not being reachable is a fact about the host that no source can be corrected for. Neither substitutes for the other: a reader wanting to know what to fix reads the diagnostics, and one wanting to know how far the report's own numbers can be trusted reads these.

[#example-report-emission]
=== When the report is written

What the command answers with, what it says went wrong, and whether it succeeded are three results, decided apart:

* [[a-diagnostic-does-not-withhold-the-report]]a diagnostic MUST NOT on its own withhold the report. The rows covered what they covered, and that is the question this command was asked;
* [[no-subject-leaves-no-report]]where nothing the compilation produced can be measured — no module formed at all — there is no subject for a measure to be about, and no report is written;
* [[an-error-still-refuses-the-command]]an error MUST still be reported, and MUST still leave a non-zero exit status. `+--strict+` decides the exit status of a compilation that raised none (<<example-adequacy>>).

This is what makes the reasons above reportable. All but one of them arrive with a diagnostic beside them, so a report written only for a compilation that raised none could carry almost nothing that field is for — the exception being the runtime not being reachable, which no source is at fault for.

A reader that took an empty stdout for a refused command reads the exit status instead: a failing invocation may write a report and a non-zero status together.

[#control-flow]
== Control structures

[#let]
=== let

[,souther]
----
let normalized = String.trim(input)
let counted: Map<String, Int> =
    List.fold((acc, k) -> Map.updateOrInsert(k, 1, n -> n + 1, acc), Map.empty, keys)
----

Reassignment is forbidden. A `+let+` is placed inside a block expression (<<block-expression>>). What follows `+let x = v+` is the scope of that binding, nested at desugaring time.

A `+let+` at the top level is a definition (<<fn-declaration>>); a `+let+` inside a block is a binding. Which one a `+let+` is follows from where it is written, not from how it is spelled, and the two are typed from different places. A definition takes its parameter types from its own body and its call sites are not read (<<fn-rules>>). A binding has no declaration to read, so a lambda bound here takes its parameter types from the applications in the body that binds it. A block holds no parameter list — `+let f (x) = …+` is written at the top level — so the two forms never meet in one place.

A binding may carry its type, `+let x: T = v+`. The type is optional — a binding otherwise takes the type of its value — and writing it does two things: the declared type is pushed into the value, so a value only its context can type (an empty `+[]+`, `+Map.empty+`, `+Set.empty+`, including one produced by a `+fold+` over such a seed) takes the declared element type instead of staying undetermined (<<algebraic-types>>); and the value is checked against it, so a declared type the value does not have is a compile error rather than an ignored comment. This is the same declaration-into-body direction a helper's declared return type has (<<fn-declaration>>) — it is a declaration, not an extension of inference. As with a helper's return, `+T+` is an ordinary type, a function type included: `+let p: (Int) -> Bool = (x) -> ...+` settles the parameters of the lambda it binds, and is how a lambda whose body names no type for them is given one (<<fn-rules>>). A tuple destructure (`+let (a, b) = t+`) takes no annotation.

[#if]
=== if

[,text]
----
if condition then expression1 else expression2
----

Both branches are written. An `+if+` MUST have its `+else+`, so no `+if+` is left one-armed and an `+else+` closes the nearest `+then+` that is still open: `+if a then if b then c else d else e+` gives `+d+` to the inner `+if+` and `+e+` to the outer one. A branch taken only when the condition fails is written `+guard ... else+`, whose other arm is the rest of the body (<<guard>>).

`+if+` is an expression. If both branch types agree, its type is that type; if they are different data, it is a sum of both (`+if c then Submitted{...} else AwaitingPreApproval{...}+` is `+Submitted | AwaitingPreApproval+`). A mismatch between primitives is a compile error.

The condition is a `+Bool+`, with one exception: a construction followed by `+as <name>+`, whose invariant decides the branch and whose value the name carries into the success branch (<<attempted-construction>>).

The widening reaches inside a collection as well, since a collection is element-covariant (<<collections>>): branches giving lists of different cases give a list of both, the same direction `{plus}{plus}` takes on its elements (<<stdlib-list>>). It descends `+List+`, `+Set+`, `+Map+`, `+Option+` and tuples, so an empty collection whose element type nothing has fixed yet takes it from the other branch at any depth, not only at the top. Where the context did fix it — a declared return or field type reaches both branches (<<collections>>) — the two branches merge by the widening like any other pair. `+match+` arms merge by this rule too.

[,text]
----
if total >= 100000 then [HighValueTravel { threshold = threshold }] else [ClientCost]   // List<HighValueTravel | ClientCost>
----

[#match]
=== match

[,text]
----
match request with
    | Drafting as d             -> ...
    | Submitted as s            -> ...
    | AwaitingPreApproval as p  -> ...
    | PreApproved as a          -> ...
----

Place `+with+` after the scrutinee and list cases one per `+|+`. The leading `+|+` of the first case is optional. Braces are not used. Souther is layout-independent, so only `+|+`-separated cases hold without layout.

The one place layout decides anything is which match an arm belongs to. A `+match+` written in an arm's body is ended by a `+|+` that reaches back to the enclosing match's arm column; a `+|+` indented past it is the inner match's own arm.

[,text]
----
match settlement with
    | FixedSettlement { budgetRange } ->
        match budgetRange with
            | RangeBudget { ceiling } -> ...      // an arm of the inner match
            | UndecidedBudget          -> ...
    | HourlySettlement { unitPrice } -> ...              // back to the outer match
----

Nothing else reads indentation, and a misplaced arm cannot silently change meaning: a case is a named data (<<sum-data>>), and a name belongs to one data, so an arm that lands on the wrong match is a compile error naming the case and the sum. Where the two matches are on one line, the columns cannot separate them — write the inner one in a block (`+-> { match … }+`), which ends it explicitly.

A case value is bound with `+as+` (the `+s+` in `+| Submitted as s+` is Submitted, read as `+s.submittedAt+`). `+as+` binds the whole matched value. A bare identifier beside the case name is not a second spelling of it: `+| Submitted s+` is a compile error naming the `+as+` form to write instead. A case is a reference to a named data (<<sum-data>>), so it carries no payload to bind positionally; the one case that does is Option's `+Some+`, and the positional form is written only there (below).

Fields may be bound directly with a destructuring pattern, the inverse of construction. It uses the same `+{ }+`, `+,+`, `+=+` as construction `+Member { id = x }+`, and `+{ id }+` is shorthand for binding to a same-named variable (paired with <<record-literal>>). Write only the fields used. Destructuring is possible only for a single named case; an or-pattern (below) binds to a sum type and has no fields. It may be combined with `+as+`: `+| Member { id } as whole+` binds both fields and the whole.

[,text]
----
match c with
    | EmailContact { email = e } -> ...   // e is the email field
    | Member { id, mail }        -> ...   // { f } binds f to a same-named variable
----

A newtype case is opened with the constructor form `+X(v)+`, the inverse of construction `+X(v)+` (<<newtype>>). `+v+` binds the wrapped value. Nesting it reaches through a newtype over a newtype in one pattern — `+| Activated(EmailAddress(s)) -> s+` binds `+s+` to the base `+String+`, so `+.value.value+` is not written; the shorter `+| Activated(a) -> ...+` binds `+a+` to the one wrapped layer (`+EmailAddress+`). Each written name MUST be the newtype opened at its layer: `+Activated(Source(s))+` is a compile error when `+Activated+` wraps `+EmailAddress+`, not `+Source+`. The single-layer accessor `+.value+` (<<newtype>>) stays for reading one layer inline; the pattern is for reaching through several. The same form opens a newtype where a name is bound (<<binding-patterns>>); what `+match+` adds is the arm for a value that may be one of several cases.

[,text]
----
match m with
    | Activated(EmailAddress(s)) -> s          // s is the base String, two layers opened
    | Unactivated(a)             -> a.value    // a is EmailAddress, one layer opened
----

[#primitive-arm]
An arm may name a primitive type. A behavior or a standard-library function that answers a value or a reason there is none returns a union with a primitive as a member (<<union-member>>) — a model's own `+-> Int | NoAnswer+`, and the core's `+Int.divide+` / `+Int.truncatingRemainder+` returning `+Int | DivisionByZero+` (<<stdlib-int>>), `+Decimal.divide+` returning `+Decimal | DivisionByZero+` (<<stdlib-decimal>>), `+String.toInt+` / `+String.toDecimal+` returning `+Int | NotANumber+` / `+Decimal | NotANumber+` (<<stdlib-string>>) — and the arm that takes the value names that primitive. Nothing wraps it: the matched value is the number itself, so `+as+` binds it as it binds any other case.

[,text]
----
match Int.divide(i.numerator, i.denominator) with
    | DivisionByZero -> 0
    | Int as n       -> n
----

`+| Int(n)+` is rejected: the parens open a newtype (above) and a primitive is not one, so the message names the `+as+` form instead. `+| n -> n+` is not a fallthrough either — Souther has no `+_+` wildcard, every arm names a case, and `+n+` is looked up as a type name and not found.

Where the model has already ruled the failure out — a `+require+` (<<attempted-construction>>) or the divisor type's own invariant keeps the divisor away from zero — the union need not arise: the operator `+/+` divides, and its abort on zero (<<stdlib-int>>) cannot be reached. The arm is for the divisor the model admits as an input.

To route multiple cases to the same processing, list `+|+` within a case, `+| A | B -> ...+` (an or-pattern). It writes "these states are treated the same" for a state-machine sum in one line. The case-separator `+|+` and the or-pattern `+|+` are the same symbol, but `+->+` ends a case's pattern part, so they do not conflict.

[,text]
----
match request with
    | Submitted | AwaitingPreApproval as x -> ...   // x is TravelRequest (the sum type)
    | Drafting as d    -> ...
    | PreApproved as a -> ...
----

An or-pattern's binding differs from a single case. A single case's `+as+` binds to that case's type (the `+s+` in `+| Submitted as s+` is `+Submitted+`), but an or-pattern's `+as+` binds to *the scrutinee's sum type*, because no single case type fits all alternatives. So an or-pattern binding cannot read case-specific fields; carry it as the sum or open it again with match. Exhaustiveness counts cases one by one; a case received by two cases is a duplicate error.

The `+Some+` of `+Option+` (<<algebraic-types>>) extracts the wrapped value by *positional binding*. The `+v+` in `+| Some v+` is bound to the contents, not to `+Some+`. `+Some+` is the only built-in case with a payload, so it reaches the contents by positional binding rather than `+as+` (which binds the whole value elsewhere). `+| Some as v+` cannot be written. A `+?+` field and the return of `+List.get+` / `+Map.get+` are extracted this way.

When the contents are a newtype, the constructor-destructuring form nests inside the positional binding: `+| Some(EmployeeId(v)) -> v+` opens the wrapped `+EmployeeId+` and binds `+v+` to its base value, so the `+.value+` on the positional binding is not written. The first written name MUST be the element type — `+Some+` wraps `+EmployeeId+`, so `+Some(EmailAddress(v))+` is a compile error — and each further name opens one more layer, the same rule as `+X(Y(s))+` on a user case. This nesting is allowed only in a pattern; `+Some(...)+` is still not a construction form (<<algebraic-types>>). The short `+| Some v+` (bind the whole contents) stays.

[,souther]
----
data TravelCompleted =
    { preApprover: EmployeeId?
    }

let approverLabel (c: TravelCompleted) =
    match c.preApprover with
        | Some(EmployeeId(v)) -> v // v is the base value, no .value
        | None -> "None"
----

[#match-covers-every-case]
A match on sum data MUST be exhaustive.

Four more hold of the arms themselves:

* [[a-match-subject-is-a-sum]]what a `+match+` is written on MUST be a sum. There is nothing for arms to tell apart otherwise;
* [[a-match-has-arms]]a `+match+` MUST have at least one arm;
* [[a-match-arm-names-a-case-of-the-subject]]an arm MUST name a case of that sum;
* [[no-two-arms-match-one-case]]no two arms may match one case, a second one for it being unreachable.

[[branches-agree-on-a-type]]The arms MUST agree on a type: a `+match+` answers with one value, so what its arms produce is one thing. The same holds of an `+if+`, whose two branches are arms of the same kind.

[[a-pattern-opens-what-the-value-is]]A pattern that opens a value with `+(...)+` MUST be opening something that has one to open, and the type it names MUST be what is inside — a newtype's wrapped value, an optional's payload. This holds wherever the pattern is written, in a `+match+` arm or in a binding (<<binding-patterns>>).

[[an-or-pattern-binds-the-sum-and-opens-nothing]]An or-pattern binds the sum and opens nothing. It says these cases are treated the same (<<match>>), and what they carry is not the same, so there is nothing shared for it to name. Exhaustiveness is decided over the direct cases the sum declared. A nested sum (`+OwnCost+` of `+data CostBearer = OwnCost | ClientCost+` being further `+Reimbursement | Advance | CorporateCard+`) does not flatten leaf cases; receive `+OwnCost+` and split with a nested `+match+`. Exhaustiveness is checked independently per layer, so cases never overlap. Case→sum assignment (<<sum-data>>) folds to the leaf; match is the reverse, descending the layers.

[#unreachable]
==== An arm may say its combination cannot arise

A table indexed by two sums has a cell for every pair, and some of those pairs the model does not admit. `+unreachable "reason"+` is what such an arm answers: it produces no value, and reaching it aborts.

[[unreachable-states-its-reason]]`+unreachable+` MUST state why the point cannot be reached, written as a string. The reason is what a reader is shown when a row gets there (<<e1911>>), and an `+unreachable+` with nothing to say leaves them the position alone.

[,text]
----
match age with
    | UnderThirty ->
        match period with
            | UnderOneYear      -> BenefitDays(90)
            | TenToTwentyYears  -> BenefitDays(180)
            | TwentyYearsOrMore ->
                unreachable "twenty insured years cannot precede the age of thirty"
    ...
----

This is not a proof. The compiler does not decide that the point cannot be reached — it could not, since what rules the pair out is a fact about the world or a check some caller already made, and neither is written in the types this rule sees. What the form does is state that premise where it is relied on, and make its failure an abort rather than a number a report goes on to print.

The rules it obeys:

* The reason is a string literal and MUST be written. It is read without running the model — by a reader, by the diagnostic an `+example+` gets (<<e1911>>), and by the abort's message — so it is not an expression that builds one.
* Its type is `+Never+` (<<algebraic-types>>), which fits whatever the position expects. An arm answering it contributes no case to what the `+match+` answers: the type is the one the other arms agree on.
* [[unreachable-stands-where-a-type-is-stated]]The position it stands in MUST state what it holds, or a branch beside it MUST — a field, a declared return, an annotated binding, an arm beside one that answers a value. Written where nothing does (`+let n = unreachable "..."+`, `+1 + unreachable "..."+`), it is <<e1307>>: the value it stands for is never made, but the code around it is still compiled, and nothing there says what shape that code reads.
* Exhaustiveness counts the arm like any other. Nothing here relaxes <<e1201>>, and there is still no `+_+`.
* Reaching it *aborts*, as an invariant violation does (<<invariant-abort>>) and for the same reason: what failed is the model, not the business. It is not catchable, and a boundary maps it to a 500, apart from a business failure's 400.
* It is not written in an `+invariant+` (<<invariant-expressions>>): an invariant answers whether the value being built holds, on every path through it.

[[unreachable-is-for-what-the-model-rules-out]]What it is not for is a case the model can produce. `+| InsufficientFunds -> unreachable "the caller checked the balance"+` states an assumption no type carries, and if the caller ever does not check, the rule aborts where it should have answered. Where the combination is one the model admits as input, the arm answers a value or the input types change; `+unreachable+` is for what the model rules out.

*Where that can be read, it is checked, and a case the model admits is <<e1326>>.* An arm of the first `+match+` a behavior's body reaches, on one of its own input positions, is the end where it can: nothing stands between the fork and the caller, so what may arrive there is what the declarations reaching that position leave (<<what-a-position-admits>>). A case they leave is a case a caller can supply, and an arm declaring it cannot arrive is a premise the model's own signature denies.

Nothing further is proven and nothing further is refused. A case behind a second `+match+`, behind a `+guard+`, or at a position whose rules this compiler could not read is one nothing here settles — a table indexed by two sums says what it says about *pairs*, and neither axis states anything about the other — so the arm stands and what is not settled is said rather than decided (<<a-case-a-claim-is-made-about-is-counted-until-the-rules-refuse-it>>). Every such arm is read, wherever in the body it is written; what is not read is an arm of a `+match+` on something that is not an input position at all, which is a claim about a value the model's rules say nothing about.

[[a-branch-the-rules-make-dead-is-said-so]]The other direction is a branch the model's own rules make dead, and it is *said*. Where the conditions on the way to a branch cannot all hold, no value the model admits arrives at it: no row can be written through it, and the arm asks for work nobody can do.

[,text]
----
guard a.value < 5000 else Free
guard a.value < 6000 else Free      // nothing under 5000 is 6000 or more
----

Reported as a warning (<<e1327>>) and not a refusal. Nothing about such a program is ill-formed — it runs, and the branch simply never runs — and there is no way to write "I know" at a `+guard+`'s departure the way `+unreachable+` says it at a `+match+` arm. Making it a refusal would leave an author no way out.

The rules it obeys:

* Only a proof reports. What is read of a path is an over-approximation of the values that can arrive, so a branch disjoint from it is disjoint from the values themselves; a branch the reading cannot settle keeps its rows and is said nothing about. The absence of a proof is not a proof.
* Nothing here proves that a branch *is* reached. A reading that found no contradiction found none — which is not the same as a value existing — so what would be needed to refute an author's claim is a value, a run that went through, or a rule that states it settles the question, and none of those is this.
* What takes an obligation away and what reports a defect read one answer and act on the one arm of it. A branch left unsettled keeps its rows and is silent; the two never disagree about which branches those are.
* It is reported to the module whose source wrote the fork. A call splices another module's branches into this body, and an argument handed to them can leave one of their arms unreachable *here* while it is alive wherever else that module is used — which is a fact about the call, and a branch the author of this module cannot take out.

[#guard]
=== guard

[,text]
----
guard amount <= balance
    else InsufficientBalance
----

`+guard+` is used inside the implementation (`+let+`) of a behavior whose output has a departure case. If the condition is false, it returns the `+else+` value and ends on the spot. The `+else+` value is one of the output sum's cases (just data). The implementation only places the value of the case it wants to return as the last expression. No special wrapping.

`+guard+` is sugar for `+if+`. The following two mean the same.

[,text]
----
guard <condition> else <value>
<rest of the body>
----

[,text]
----
if <condition> then <rest of the body> else <value>
----

Desugaring is done at parse time; later stages (type check, construction-authority check, backend) see only `+if+` or the attempted form below. That both branches may have different types (<<if>>) is what makes this desugaring hold.

[#attempted-construction]
=== An attempted construction

[,text]
----
guard DomainName(d) as name else BadDomain
if DomainName(d) as name then [ name ] else []
----

[#attempt-subject-is-a-construction]
Where the condition is a construction followed by `+as <name>+`, that construction's invariant decides
the branch. `+as+` names the value a construction builds, so what it is written on MUST be one; a
condition that is anything else states no invariant for the branch to be decided by.

The invariant is run — the type's own and every invariant a `+...+` spread brought in
(<<invariant-mvp>>, <<field-spread>>). Holding, the value is built and `+<name>+` names it in the
success branch; failing, the `+else+` value is taken and no value is built. The invariant runs once.

A construction is either spelling — a newtype's `+T(v)+` or a record literal `+T { ... }+`
(<<record-literal>>). A behavior that reports every business failure rather than the first one it meets
builds the reasons as a list and attempts the case that carries them, so "at least one reason" is that
case's own invariant rather than a guard written beside it.

[,text]
----
data NotConverted = { reason: List<BlockedReason> }
    invariant List.length(reason) >= 1

if NotConverted { reason = blockers(lead, enquiry) } as blocked
    then blocked
    else Converted { ... }
----

The name is in scope in the success branch only. *Nothing carries the failure.* There is no value for
it — no `+Option+`, no `+Result+`, no reason — and the name a business failure gets is the one written
in `+else+`, which MAY be a value naming nothing (`+else []+`).

`+guard+` here is the same sugar for `+if+` (<<guard>>), so the `+if+` form is the base one and a
helper `+let+`, which has no departure case to name, reaches it too.

[#attempt-departures]
==== Departing per clause

The `+else+` MAY instead answer each invariant clause the type names (<<invariant-clause-name>>), so a
behavior that departs differently per rule states the rules once rather than restating them as guards.

[,text]
----
guard QuoteLines(List.map(r -> toLine(r), lines)) as ls else
    | nonEmpty       -> NoLines
    | uniqueProducts -> DuplicateProduct
----

What an arm answers is which rule failed. The name a business failure gets is still the one the arm
writes, and no value crosses from the failure into the arm.

Which arm is taken is decided by the clause that failed, so the order the arms are written in has no
effect; which clause failed is decided by the order the clauses are declared in (<<invariant-mvp>>).

The arms MUST cover what can fail, so that no failure reaches a branch that is not there:

* [[attempt-answers-every-failing-clause]]every named clause of the type MUST have an arm;
* [[attempt-arm-names-a-declared-clause]]an arm MUST name a clause the type has;
* [[attempt-answers-each-clause-once]]no clause may be answered twice — the arm is chosen by the clause
  that failed, so a second arm for it could never be reached;
* [[attempt-answers-unnamed-clauses]]`+| _ -> …+` MUST be written when the type has a clause carrying no
  name — it answers the clauses that cannot be named;
* [[attempt-catch-all-has-clauses-to-answer]]and MUST NOT be written when the type has none, there being
  nothing left for it to answer.

[[attempt-arms-answer-an-attempt]]Arms of this shape answer for an attempted construction, so there MUST
be one for them to answer for. The `+else+` of a condition that is not an attempt takes one value.

Adding an unnamed clause to a type whose clauses are all named is therefore a compile error at every
mapping site, which is what adding a rule to a published type should be: today it shows up only as more
aborts and more decode failures.

The `+else <value>+` form keeps its meaning — one value for any failure — so naming a clause does not
disturb a site that does not care which rule broke.

A construction inside an argument is an ordinary construction and aborts on violation
(<<violation-destination>>); what is attempted is the outer construction alone.

An attempt builds a value on its success branch, so the enclosing behavior's `+constructs+` MUST name
the type (<<constructs>>).

[#attempt-subject-declares-an-invariant]
The attempted type MUST carry an invariant, its own or one a spread brought in: without one the
`+else+` value could never be reached, so writing it is a compile error rather than a branch that is
not one.

Writing `+T(v)+` on its own still aborts (<<violation-destination>>). This is the form a writer
chooses at the site, and it is what lets a rule be stated once — the rule lives in the type's
`+invariant+`, and a guard restating it can drift from it. Across a module boundary the restatement
cannot be written at all: a helper `+let+` an invariant names is not exposed (<<exposed-surface>>), and a
behavior cannot be called from an invariant (<<invariant-expressions>>). The attempt names the type,
which the declaring module does expose.

Newtype arithmetic (<<newtype-arithmetic>>) re-wraps rather than constructs, so it is not what `+as+`
takes; write the construction out (`+T(a.value - b.value)+`).

[#block-expression]
=== Block expressions

`+{ ... }+` is an expression. Place `+let+`s and `+guard+`s, then put the result expression last.

[,souther]
----
let preApprove (request, approverId) = {
    guard approverId == request.applicant.managerId else NotAuthorized

    PreApproved { ...request, preApprovedAt = now(), preApprover = approverId }
}
----

If only the result expression is placed, no block is needed. `+let isNonNegative (v: Int) = v >= 0+` and `+let isNonNegative (v: Int) = { v >= 0 }+` are the same.

A block is an expression, so it may be written anywhere an expression may (`+if c then { let x = f(a) g(x) } else h+`). Its contents desugar per the rules of <<let>> and <<guard>> and appear in the AST as bindings and `+if+`. A bare `+{+` resolves to a block expression; a record literal is preceded by a type name (<<record-literal>>), so there is no confusion.

[#purity]
== Purity

Souther code *does not touch the outside world directly*. The following are forbidden.

Mutable variables, field assignment, static mutable fields, random, current time, environment variables, files, network, threads, exceptions, reflection.

An outside-world dependency is declared as a behavior with no implementation, and its implementation is injected from the Java side (<<injected-behavior>>). A `+let+` may receive one as an argument and call it, so a `+let+`'s result may depend on the outside world. But the way it touches must go through a call to the injected thing, and that fact is written in `+depends on+` (<<depends-on>>). This corresponds to the conclusion of SMDD's DDD trilemma — purity is not layer separation but making explicit which functions have side effects (<<set-invariant>>). *Souther made this "explicit" literal.* Left to inference, which functions have side effects cannot be read from the declarations.

*"The requirement set is empty" is stronger than this sense of purity.* This chapter's sense is "does not touch directly", which every `+let+` satisfies. An empty requirement set means receiving nothing injected, which only some `+let+`s satisfy. What may be called from an invariant is the latter (<<invariant-expressions>>). To avoid conflating the two, the latter is henceforth called "empty requirement set" and not "pure".

[#stdlib]
== Standard library


[[an-empty-collections-element-type-is-determined]]An operation seeded with an empty collection MUST have its element or accumulator type determined where it is written — by the binding it feeds, by the field or output position it lands in, or by a non-empty seed. What an empty collection holds does not follow from it.

[#reserved-namespace]
=== Reserved namespace

[[core-privileges-stay-in-the-core]]Some forms are the core's and not a model's: `+intrinsic+`, `+private+`, a type variable, and an optional written anywhere but on a data field. A module outside the reserved `+souther+` namespace MUST NOT write them — the core is what stands between the JVM and a model, and a model that reaches for its forms is writing the layer below the one it is in.

The standard library is bundled with the compiler and referenced by short qualifiers — `+List+` (list), `+Map+` (map), `+Set+` (set), `+String+` (string), `+Bool+` (boolean), `+Int+` / `+Decimal+` (numbers), `+Date+` / `+DateTime+` (temporal), `+Option+` (optional). Every function is called through this qualifier: `+List.map(f, xs)+`, `+String.trim(s)+`, `+List.length(xs)+`. There is no automatic import — names are not delivered implicitly. What may be written bare is operators (`+==+` `+<+` `+{plus}{plus}+`, etc.), `+if+` / `+match+`, literals, and the current module's helpers. Argument order follows the convention of <<pipe>> (primary subject last).

A standard-library function is therefore never a bare name and never crosses what a module writes bare. What a module writes bare in a value position is one of: a binding in force, one of its own `+let+`s, a data of its own written as a value, a behavior it can reach, a name an import let it write without its qualifier, a name the language gives (`+None+`), or a case of a data core declares (`+HALF_UP+`; <<stdlib-decimal>>). One spelling means one of these — a `+let+` may not be named like a data (<<fn-rules>>), a name the language gives may not be taken, and a binding in force wins over a declaration.

A qualifier is the only spelling that reaches its library, so a *type* MUST NOT be named like one: `+data List = String+` is a compile error, as `+module souther.foo+` is. In expression position the qualifier still yields to a binding — a parameter or a `+let+` named `+Map+` makes `+Map.empty+` that binding's `+empty+` field — because a binding in force wins over everything.

To call a library name bare, import it explicitly: writing `+import List (map, filter )+` lets you write `+map(f, xs)+` unqualified thereafter. Qualified access always works, and `+import+` is only sugar to elide the qualifier. Importing the same bare name from two modules is a compile error, so qualify one of them (e.g. `+String.length+` and `+List.length+` used together in one module; <<imports>>).

Qualifiers such as `+List+` denote a module only in expression position, distinct from the type-position `+List<T>+` (angle brackets), which does not conflict. `+length+` / `+get+` span `+String+` and `+List+`, and `+List+` and `+Map+`, but they become distinct names `+String.length+` / `+List.length+`, `+List.get+` / `+Map.get+`, so they are not overloads but each module's own function.

The entity the short qualifier denotes is placed in the reserved namespaces `+souther.string+` / `+souther.list+` / `+souther.map+` / `+souther.set+` / `+souther.bool+` / `+souther.int+` / `+souther.decimal+` / `+souther.date+` / `+souther.datetime+` / `+souther.option+`. The `+souther+` namespace is reserved, and a user module MUST NOT name itself under it — `+module souther.foo+` is a compile error. Only the reserved namespace holds core's privileges (generics, recursion, intrinsics), and this is the line preventing a user from granting them to itself (the boundedness of <<algebraic-types>>). A name that merely starts with the string `+souther+`, such as `+southernmost+`, is an ordinary user name, not under this namespace.

[#type-variables]
==== Type variables

A core function may write a type variable `+'a+` in a type position — a parameter, a return value, `+List<'a>+`, `+Map<String, 'a>+`, or a function type `+('a) -> 'b+`. No declaration list (`+<A>+`) is written; a `+'a+` appearing in a signature is implicitly universally quantified. Because identifiers do not distinguish case (<<unit-data>>), a type variable is marked by a leading apostrophe rather than by a lowercase name.

Only core in the reserved namespace may write a type variable; a user module writing `+'a+` is a compile error. The user model stays bounded (<<algebraic-types>>). When a non-recursive core helper has `+'a+`, it is monomorphized by inline expansion at the call site — `+'a+` is resolved to each call's concrete type, and no polymorphic method is generated. An `+example+` row that applies one is not an exception: the row settles the variables from what it wrote and the method emitted for it is at that instance (<<example-evaluable>>), so two instances are two methods and two rows settling one instance share the one.

Writing one and carrying one are different things. A user module's non-recursive helper may *carry* a variable its body left unsettled and is monomorphized the same way (<<fn-rules>>); what it may not do is write one. The variable is inferred, never spelled, and appears in no declaration a reader of the module sees.

A single application of a function instantiates each variable its declared signature wrote once, and every occurrence of that variable — in its parameter types and in its result — denotes that one instantiated type. Two applications of one function instantiate separately. This holds however the application is compiled: a call to a `+core+` primitive stands and a call to a Souther-bodied function is expanded, and neither decides anything the other does not. So `+let has (xs, y) = List.contains(y, xs)+` types `+y+` as what `+xs+` holds, because `+List.contains+` wrote one variable for its element and its list.

[#intrinsics]
==== intrinsics

A primitive is declared as a core function with a body of `+= intrinsic "key"+`. `+key+` names the JVM operation the backend emits and is *prefixed per type* to make it unique (`+"string.trim"+`, separating `+list.length+` and `+string.length+`).

[,souther]
----
let trim (s: String): String = intrinsic "string.trim"
----

An intrinsic MUST declare its return type (it cannot be inferred from `+intrinsic+`). A call is type-checked against the declared signature — that is, `+trim+` is "a built-in function whose signature is placed in core", and this declaration, not a hardcoded compiler table, decides its arguments and return value — and the backend emits the bytecode for `+key+`. It is neither inlined nor lowered. Only the reserved namespace may write it. A user cannot call the JVM directly and only calls blessed functions (<<asymmetric-interop>>).

A core module may also write `+private let+`. A `+private+` declaration is part of the library's implementation and not of its surface: it is reachable from the library's own modules and from nowhere else, so a module outside the reserved namespace that writes its name is told the library has no such member — the same answer a misspelling gets, because from where the caller stands they are the same thing. `+private+` is a core privilege like `+intrinsic+`; writing it in a user module is a syntax error, and there would be nothing for it to do there, since everything a user module declares is published (<<modules>>). `+List.foldFrom+`, which `+List.fold+` supplies the starting index for, is one.

*Every* standard-library function is declared in a core module this way, whether its body is Souther (self-hosted, like `+List.map+` over `+foldFrom+`) or `+intrinsic "key"+` (a JVM kernel). The declaration is the single source of truth for the signature — there is no compiler table of signatures, and a function the compiler can call but a declaration does not describe does not exist. What stays in the compiler is what the *type language cannot say*, as a side condition on a declared signature rather than in place of one:

* the operators (`{plus}` `+-+` `+*+` `+/+` `+==+` `+<+` `+<=+` `+>+` `+>=+`) are syntax, and the arithmetic ones carry the closed-newtype rules (<<newtype>>) a plain function does not;
* [[what-is-ordered-by-is-an-ordered-value]]"the element is an ordered value" — Souther has no `+comparable+` type class, so `+List.sort+` / `+max+` / `+min+` declare `+List<'a>+` and the constraint on `+'a+` is checked in the compiler. `+sortBy+` states it of what its key function answers with, which is the value being ordered by;
* [[what-is-summed-is-a-number]]"the element is a number" — `+List.sum+` / `+product+` declare `+List<'a>+` for the same reason, and the constraint that `+'a+` is `+Int+` or `+Decimal+`, the two types arithmetic is defined for, is checked in the compiler.

Neither is a type mismatch, and a diagnostic about one does not read as one: the argument has the type its parameter takes, and what it does not have is a capability the signature had no way to ask for.

Because a signature is a declaration and nothing else, a standard-library function is a function value like any other: `+List.map(String.trim, xs)+` and `+let f = String.length+` are ordinary (<<blocks>>). A value the library offers is declared the same way, with no parameter list — `+let empty : Map<'k, 'a> = intrinsic "map.empty"+` — and is written `+Map.empty+`.

[#pipe]
=== The value pipe and argument order

To thread a value through functions, write `+|>+` left to right. `+e |> f(a)+` is `+f(a, e)+` — the left side enters the *last argument* of the right-hand call. A parenthesis-less function name `+e |> f+` is `+f(e)+`. `+|>+` binds most loosely and left-associatively, so `+a |> f |> g+` reads as `+g(f(a))+`.

[,text]
----
input.raw
    |> split(",")                          // split(",", input.raw)
    |> map(piece -> piece |> trim |> lowercase)
    |> filter(piece -> String.length(piece) >= 1)
    |> sort                                // sort(...)
----

Standard library arguments put *the primary subject (collection, string, Map) last, functions, separators, and keys first*: `+List.map(f, xs)+`, `+List.filter(p, xs)+`, `+List.fold(step, seed, xs)+`, `+String.split(sep, s)+`, `+Map.get(key, m)+`. So the left value drops into the last argument and the pipe flows top to bottom. A `+fold+`'s folding block stays in `+(acc, x)+` order.

The right side admits only a function call or a function name; `+e |> 3+` is a compile error. A bare field getter is not a pipe target either — write `+e.field+` for a single value, or use the getter inside a call (`+xs |> List.map(.field)+`), not `+e |> .field+` (<<blocks>>). `+|>+` is expanded to a call at parse time, so no later stage sees `+|>+` — the same pure syntactic sugar as `+guard+` expanding to `+if+`. It is distinct from the `+>->+` that composes behavior stages (<<composition>>); that connects behaviors, this threads expression values.

[#stdlib-string]
=== String

[,text]
----
length   isEmpty  trim     lowercase  uppercase  contains  startsWith  endsWith
slice    append  reverse  repeat     padLeft    padRight  lines
split    join  replace    words      fromInt   concat       matches
characters  codePoints  toInt  fromDecimal  toDecimal
----

`+append(a, b)+` joins two strings in their written order; the `{plus}{plus}` operator is the common form, `+append(a, b)+` equals `+a {plus}{plus} b+` (<<stdlib-list>>).

[#string-code-points]
Every length, index and range in this module counts *Unicode code points*. It is not the UTF-16 code unit a JVM `+String+` is stored in — that is the backend's representation and says nothing a domain wants — and it is not the grapheme cluster a reader sees as one character. `+髙+` and `+𠮷+` are one code point each; a base letter followed by a combining accent is two, and an emoji joined with zero-width joiners is several. So `+String.length("𠮷")+` is `+1+`, and an invariant written `+String.length(value) <= 20+` admits twenty code points — not twenty columns on a printed form, and not twenty UTF-16 units. A derived decoder carries the same bound to the boundary in the same unit, so a value the model admits is not rejected on the way in (<<decoder-error>>).

What the choice buys is that the operations agree with one another and that none of them can answer with half a surrogate pair:

[,text]
----
String.length(s) == List.length(String.characters(s))
String.length(s) == List.length(String.codePoints(s))
String.slice(0, String.length(s), s) == s
----

Grapheme-cluster counts and display widths depend on Unicode segmentation and on where the text is shown; they are a different question from how long a stored value is, and would be a differently named function.

[#string-canonical]
**Text that arrives from outside is canonicalized to NFC.** Unicode calls `+が+` written as one code point and as か followed by a combining mark *canonically equivalent* — the same text — and a comparison by code units calls them different. Left alone they would be two values: two `+Map+` keys, two `+Set+` members, `+==+` false, and a length that depends on which keyboard produced them. There are two places text arrives, and both canonicalize: a derived decoder, at its string leaf and therefore before every constraint chained after it (<<decoder-error>>), and a string literal in a source file, when the compiler reads it. So a pattern written in one form matches a value that arrived in the other, whichever form each was typed in.

What is canonical is the *value*; nothing about comparison changes. NFC and not NFKC — compatibility folding turns `+①+` into `+1+` and a half-width kana into a full-width one, which is a claim about the text that a stored name does not want. A variation sequence (`+葛+` followed by U+E0101) is stable under every normalization form and stays two code points, so this is not a way to make a name count as its characters look; that is the grapheme-cluster question above. A decoder therefore transforms as well as validates, and what it answers is not always byte-for-byte what arrived — anything needing the bytes as sent keeps them outside the decoder.

`+matches(pattern, s)+` is a whole-string regular-expression match (Java `+Pattern+` flavour, anchored: the *entire* string must match), returning a plain `+Bool+`. It is what states a format-constrained value as an invariant — `+data PostalCode = String invariant String.matches("[0-9]{3}-[0-9]{4}", value)+`. The `+pattern+` MUST evaluate to a string at compile time: it is validated there (compiled once to confirm it is well-formed), so a malformed regex is a compile error rather than a runtime exception. A string literal evaluates to one, and so does a `{plus}{plus}` of literals and of values — a `+let+` with no parameter list (<<fn-declaration>>) — which is how types whose formats share a part hold that part once instead of spelling it out in each of them:

[,souther]
----
let sitePrefix = "001"

data SiteId = String
    invariant String.matches(sitePrefix ++ "[0-9A-Za-z]{12}", value)
----

What is validated is the string the whole expression evaluates to, not the pieces it is written in. At run time the pattern is compiled on first use and cached, not recompiled on each call. A regex backslash class is written with a doubled backslash (`+"\\d{3}"+`), since `+\d+` is not an escape a string literal reads and a lone backslash before `+d+` is refused (<<string-literal>>). A pattern a run-time value takes part in is therefore not accepted (a future `+Regex+`/`+fromString+` would carry that, returning a case for an invalid pattern); a pattern the compiler can read is what the invariant needs. Regex lives in core rather than a package, since Souther has no package layer. A pathological pattern can backtrack; keep invariant patterns simple.

The middle row is the rest of what Souther's types allow, which have no `+Char+`, `+Maybe+` or `+Float+`. `+split(sep, s)+` splits by a separator into `+List<String>+`, keeping empty pieces (`+split(",", "a,,b")+` is `+["a", "", "b"]+`). `+words(s)+` splits by runs of whitespace, dropping empty pieces. `+join(sep, xs)+` joins with a separator. `+replace(target, replacement, s)+` replaces every occurrence of `+target+`. `+fromInt(n)+` makes an `+Int+` a decimal string. `+concat(xs)+` joins a `+List<String>+` with no separator (equal to `+join("", xs)+`). The primary subject string is last (<<pipe>>). There is no `+Char+` type and no character-mapping operation.

`+isEmpty(s)+` is `+length(s) == 0+`, the sibling of `+List.isEmpty+` / `+Map.isEmpty+` / `+Set.isEmpty+`. It belongs in a predicate — `+filter(piece -> not(String.isEmpty(piece)))+` — rather than in a newtype invariant: a derived decoder recognises `+String.length(value) >= 1+` and reports a violation as `+too_short+` with its bound, while the helper's inlined form leaves it the generic `+invariant_violation+` (<<decoder-error>>).

`+reverse+` / `+repeat+` / `+lines+` / `+padLeft+` / `+padRight+` are the rest of what Souther's types allow. `+reverse+` turns the code points around, so a character outside the basic plane survives it. `+repeat(n, s)+` joins `+n+` copies (nothing for `+n+` of 0 or less; a count no string could hold *aborts* rather than quietly giving fewer copies than asked for, as an `+Int+` overflow does — <<stdlib-int>>). `+lines(s)+` breaks on newlines, accepting both `+\n+` and `+\r\n+`, and keeps empty pieces — a trailing newline leaves an empty last line. `+padLeft(width, pad, s)+` / `+padRight+` widen `+s+` to `+width+` code points with copies of `+pad+` and leave a string already that wide alone. `+pad+` is repeated and cut to the code points still needed, so it need not be one character and the width is exact: `+String.length(padLeft(width, pad, s))+` is `+Int.max(width, String.length(s))+` for any non-empty `+pad+`. An empty `+pad+` fills nothing and gives `+s+` back, the one input that equation does not hold for. Padding is what *builds* a fixed-width identifier — a bay number becomes `+"03"+` with `+padLeft(2, "0", fromInt(bay))+` — so a code whose shape a newtype invariant states can be produced in the domain and not only checked there.

`+fromDecimal(d)+` renders a `+Decimal+` in plain notation, never in exponent form, keeping the scale the value carries: `+1000.00m+` is `+"1000.00"+`. Scale is incidental to the number (<<primitives>>) but not to how it is written, so a caller wanting a fixed number of places sets it first with `+Decimal.round(scale, mode, d)+` and names the rounding there. Grouping separators and a currency symbol are presentation and stay outside the domain. `+toDecimal(s)+` parses back, returning `+Decimal | NotANumber+` — the sibling of `+toInt+`, and a compiler built-in for the same reason.

`+slice(fromInclusive, toExclusive, s)+` takes the code points from `+fromInclusive+` up to but not including `+toExclusive+`. Half-open is the rule for *extracting* a range, as `+Inclusive+` in a name is the rule for *generating* one (`+List.rangeInclusive+`, <<stdlib-list>>): two slices that meet at an index join back into the whole, and `+slice(0, String.length(s), s)+` is the string. An index the string has not got, or a `+toExclusive+` before `+fromInclusive+`, *aborts*.

The bottom row takes a string apart into the units it is made of, without a `+Char+` type — a character is a one-code-point `+String+`. `+characters(s)+` gives one code point per element (`+characters("a12")+` is `+["a", "1", "2"]+`), so `+List.fold+` can walk them; `+codePoints(s)+` gives the same split as the integers. Both are total, so the empty string gives the empty list. There is no "first code point" function: a caller who wants one writes `+List.get(0, String.codePoints(s))+` and takes the absence from `+List.get+`, where every other absence in the library comes from. `+toInt(s)+` returns `+Int | NotANumber+`: a named case, since `+Maybe+` has no surface form — the same case-return the `+/+` operator and `+Int.divide+` draw between abort and case. `+toInt("007")+` is `+7+` (leading zeros kept, unlike a numeric parse-and-reformat); `+toInt("12x")+` is `+NotANumber+`, and the parsed value is taken out with `+| Int as n+` (<<primitive-arm>>). `+toInt+` is a compiler built-in rather than a core declaration (<<intrinsics>>); `+characters+` / `+codePoints+` are ordinary intrinsics.

Together these make a checksum (Luhn, EAN, a mod-10 check digit) a plain `+let+` over `+List.fold+` in a behavior: validate the digits with `+matches("[0-9]+", value)+` in the type's invariant, read each digit as its code point less that of `+"0"+` over `+String.codePoints+` (total, because the invariant proved every character is a digit), and take the remainder with `+Int.floorMod+`. A checksum is *not* an invariant — it folds, and an invariant may not recurse (<<invariant-expressions>>) — so it lives in a behavior, but a short one.

[#stdlib-int]
=== Int

[,text]
----
add  subtract  multiply  divide  truncatingRemainder  compare  floorMod
abs  min  max  clamp
----

`+Int+` is signed 64-bit (<<primitives>>). If an operation exceeds that range it *aborts* — it does not wrap. Overflow is a model bug, not a business result, so like an invariant violation it throws `+ConstraintViolation+` (<<algebraic-types>>, <<violation-destination>>, <<jvm-abort>>).

Division by zero returns `+Int | DivisionByZero+`. This is a possible input on the business flow (a divisor of 0), so it is returned as a case rather than aborting. The arm that takes the quotient names the primitive and binds it with `+as+` — `+| Int as n+` (<<primitive-arm>>).

The four operations may also be written with the operators `{plus}` `+-+` `+*+` `+/+` (the symbol table of <<primitives>>). `+/+` is truncating division and *aborts* on division by zero (like overflow). To receive `+Int | DivisionByZero+` as a case, use the functions `+divide+` / `+truncatingRemainder+` — operator = casual and aborting, function = safe with a case. All three take the dividend first and the divisor second: a binary arithmetic operation keeps its operands in the order the mathematics writes them, and the pipe's subject-last rule is for the unary ones (<<pipe>>).

`+abs+` / `+min+` / `+max+` / `+clamp+` are ordinary core `+let+`s over `+<+` and `+-+`. `+abs(n)+` negates with the `+-+` operator, so the smallest `+Int+` — the one value with no positive counterpart — aborts on overflow like any other overflow here; it is what turns a signed difference into a distance (`+Int.abs(Date.daysBetween(a, b))+` says "how far apart" without the caller choosing an argument order to dodge the sign). `+min+` / `+max+` are the two-value form the comparison operators imply, distinct from `+List.min+` / `+List.max+`, which take a list and answer an optional because a list may be empty. `+clamp(lo, hi, n)+` holds a value inside a range, the subject last as the pipe convention wants.

`+floorMod+` and `+truncatingRemainder+` are the two remainders, and the names say which is which because languages disagree on what a bare `+mod+` or `+remainder+` means for a negative operand. `+floorMod(dividend, divisor)+` is the remainder of a *floored* division, so the result takes the sign of the divisor: `+Int.floorMod(-7, 3)+` is `+2+`. `+truncatingRemainder(dividend, divisor)+` is the remainder of a division whose quotient is truncated toward zero, so the result takes the sign of the dividend: `+Int.truncatingRemainder(-7, 3)+` is `+-1+`.

They also differ in what a zero divisor does, and that difference is *not* in the names. `+truncatingRemainder+` returns `+Int | DivisionByZero+` like `+divide+`. `+floorMod+` *aborts* like `+/+`, so its result is a plain `+Int+` that reads in an invariant — `+data Box = Int invariant Int.floorMod(value, 12) == 0+`. That makes `+floorMod+` the one standard-library function whose failure is not in its type; it is a stated exception, not a pattern to copy (ADR-0095). Use `+truncatingRemainder+` where a zero divisor must be a business case.

[#stdlib-decimal]
=== Decimal

[,text]
----
add  subtract  multiply  divide  compare  fromInt  toInt  round
abs  min  max  clamp
----

Division makes the rounding scale and mode explicit as arguments. `+Decimal.divide(dividend, divisor, 2, HALF_UP)+` rounds HALF_UP at 2 decimal places. The mode is an ordinary value of the data core declares:

[,souther]
----
data RoundingMode = HALF_UP | HALF_EVEN | HALF_DOWN | UP | DOWN | CEILING | FLOOR
----

A mode argument is evaluated like any other, so a policy can be stated once and passed by name, chosen by an `+if+` or a `+match+`, or computed by a helper, and the three operations taking one are ordinary functions that can be handed over by name (<<blocks>>). The cases are unit data and follow the unit-data rules (<<unit-data>>): written bare, shadowable by a binding. The type provides no codec, so it cannot appear where one is required — a field of it, an example fixture over it — and this is intentional: a rounding policy is a computation's input, not a value that crosses a serialization boundary.

The cases correspond to `+java.math.RoundingMode+`. `+Decimal.divide(dividend, divisor)+` without scale and mode cannot be written — to surface rounding as a domain decision, the same position as "scale is incidental" in <<primitives>>. Division by zero returns `+Decimal | DivisionByZero+`, opened by `+| Decimal as d+` (<<primitive-arm>>).

`+Int+` and `+Decimal+` meet only where the conversion is written. `+Decimal.fromInt(n)+` widens an `+Int+`; it is exact, so nothing has to be stated. `+Decimal.toInt(mode, d)+` narrows back, and because that drops a fraction it names the rounding at the call, as `+divide+` does. There is no implicit widening: an arithmetic operator takes two operands of one type (<<primitives>>), so a rate applied to an amount in yen reads `+Decimal.fromInt(netAmount) * taxRate+` — the conversion sits where the value stops being an amount. A `+Decimal+` too large for an `+Int+` aborts, as an `+Int+` overflow does (<<primitives>>).

`+Decimal.round(scale, mode, d)+` sets the number of decimal places, naming the rounding at the call as `+divide+` and `+toInt+` do. Unlike `+toInt+` the result stays a `+Decimal+`, so it is how a value is prepared for `+String.fromDecimal+` (<<stdlib-string>>), which keeps whatever scale the value carries: a rate of `+0.10m+` times `+100.0m+` is `+10.000m+`, and `+round(0, HALF_UP, d)+` is what makes it read as `+"10"+`.

`+round+` and `+toInt+` each work on one `+Decimal+`, so the subject comes last and the rounding policy leads — `+d |> Decimal.round(2, HALF_UP)+`. `+divide+` is a binary operation, so its two operands come first and the policy follows them. The `+(scale, mode)+` pair therefore sits in front of the subject in one and behind the operands in the other; that is the two rules meeting, not an inconsistency (<<pipe>>).

`+abs+` / `+min+` / `+max+` / `+clamp+` are the same `+Basics+` arithmetic `+Int+` carries (<<stdlib-int>>), on the same reasoning: `+<+` and `+-+` are defined for `+Decimal+`, so these are ordinary core `+let+`s. `+abs+` compares against `+0.0m+`, so a value whose scale differs still answers by numeric value, as `+compare+` does. `+clamp+` answers one of the three values written, never a re-scaled copy.

The four operations may also be written with the operators `{plus}` `+-+` `+*+` `+/+`. The operator `+/+` divides with a default rounding — significant digits (about 29) and HALF_UP — so `+10m / 3m+` is `+3.3333…+` and does not abort. Division by zero *aborts*. To make rounding an explicit domain decision, or to receive division by zero as a case, use the function `+Decimal.divide(dividend, divisor, scale, mode)+`.

[#stdlib-list]
=== List

==== Construction

A list is made by a literal or a guard comprehension.

[,text]
----
[e1, e2, ...]         // literal. One or more elements
[<element> | <condition>, ...]     // guard comprehension. One element if all conditions hold, empty otherwise
----

An empty literal `+[]+` may also be written. It has no element type of its own; its type is decided by context — the counterpart of `{plus}{plus}`, the other branch of an `+if+` / `+match+`, the seed a `+fold+` grows, or a `+List<T>+` a field or return value requires. An empty list does not depend on elements at runtime, so this is always sound. To transcribe a DSL bullet list, a guard comprehension (empty if the condition is false) is natural.

`{plus}{plus}` is concatenation. Both sides MUST be two lists or two strings: two lists give a list, two strings give a string (`+"a" {plus}{plus} "b"+` is `+"ab"+`, the operator form of `+String.append+`; <<stdlib-string>>). A mixed pair (a list and a string, or either with a non-list non-string) is a type error.

The element type is folded if the same, and widened to a sum of cases if different data (the same direction as <<sum-data>>).

[,text]
----
[HighValueTravel { threshold = threshold }] ++ [ClientCost]      // List<HighValueTravel | ClientCost>
----

Guard comprehension and `{plus}{plus}` exist to transcribe conditions the spec DSL listed as bullets.

[,text]
----
// the spec DSL (chapter 10) lists reasons as bullets:
//   reason 1: estimatedCost is at least 100,000 yen / reason 2: applicant is Staff / reason 3: cost borne by the other party
   [HighValueTravel { threshold = threshold } | estimatedCost.value >= threshold.value]
++ [InsufficientAuthority { value = applicant.rank } | isStaff(applicant)]
++ [ClientCost                 | isClientBorne(costBearer)]
----

Each reason makes a 0- or 1-element list and concatenates. *One DSL bullet line becomes one Souther line.* The result is `+List<HighValueTravel | InsufficientAuthority | ClientCost>+`, matching the three cases of `+PreApprovalReason+`. A unit data is constructed by name alone (<<unit-data>>), so `+ClientCost+` is written directly.

Note that `+applicant.rank == Staff+` cannot be written as a condition. `+==+` works only between the same type (<<primitives>>), and `+Rank+` and its case `+Staff+` are different types. Case judgment is done with `+match+`, and to make it a predicate wrap it in a helper `+let+` (the `+isStaff+` above). Ordering (`+>=+`) exists only for `+Int+` / `+String+` / `+Decimal+` and the temporals, so compare `+.value+`, not `+Amount+` (a newtype) directly.

==== Operations

[,text]
----
map  filter  fold  foldRight  all  any  length  get  mapIndexed
reverse  contains  sum  product  append  concat  flatMap  isEmpty  sort  rangeInclusive
distinct  distinctBy  allDistinctBy  partition  groupBy  indexBy  filterMap  max  min  find  sortBy
take  drop  zipShortest  unzip
----

All are called through the qualifier `+List+` (<<stdlib>>). `+List.get+` returns `+Option<T>+`. `+List.map+` / `+List.filter+` / `+List.fold+` / `+List.all+` / `+List.any+` / `+List.mapIndexed+` take a block (<<blocks>>). Import them as `+import List (map, all )+` to write unqualified thereafter.

`+rangeInclusive(from, to)+` is the consecutive integers from `+from+` to `+to+`, both ends included; `+from+` above `+to+` gives the empty list, and a span longer than a list can hold *aborts* before the walk starts rather than filling memory until it dies. Whether a bare `+range+` closes at `+to+` or stops before it is a thing languages disagree on, so the name carries the answer — the same reason `+zipShortest+` carries its own, and the counterpart of the half-open rule for *extracting* a range (`+String.slice+`, <<stdlib-string>>). It is the one list that is neither written out nor read from outside, so a walk over positions — the levels of a bay, the digits of a code — has something to walk. Like `+reverse+` and `+sort+` it is a primitive: building it from `+fold+` would need a list to fold over in the first place.

`+flatMap(f, xs)+` maps each element to a list and joins the results in one pass; writing it as `+map+` then `+concat+` builds an intermediate `+List<List<'b>>+`, which is why the shape is otherwise hand-rolled as a fold with `{plus}{plus}` in the step.

`+foldRight(step, seed, xs)+` walks from the end. Its step takes the element first and the accumulator second — `+('a, 'acc) -> 'acc+` — the shape a right fold has and the opposite of `+fold+`'s. It is implemented as the left fold over the reversed list with the step's arguments swapped, which *is* the right fold rather than an approximation of one: Souther is strict and a list is finite, so the two orders of combination give the same value. Time is O(n), and it holds one reversed copy, so extra space is O(n) too. `+fold+` is the left one and carries no `+Left+` in its name: `+Map+` and `+Set+` publish no first and no last element, so `+fold+` is the name they can share, and only `+List+` has a direction for a second name to be about (<<stdlib-map>>, <<stdlib-set>>).

`+zipShortest(xs, ys)+` pairs two lists element by element and `+unzip(ps)+` takes the pairs apart again. A length mismatch truncates to the shorter list rather than aborting, and the name says so because languages disagree — Elm's `+map2+` truncates, F#'s `+List.zip+` raises. Souther has tuples, so a two-list map is written `+zipShortest(xs, ys) |> map(f)+`. The tuples stay inside the behavior — a tuple has no external form (<<tuple>>).

[,text]
----
import List ( map, all )

map(x -> fixLine(x), xs)
all(r -> isMinor(r), reasons)
----

If an injected thing is called from within a block, its requirement appears in the enclosing behavior's `+depends on+` (<<depends-on>>).

None of these is a compiler primitive. `+List.fold+` is a recursive `+let+` in `+souther.list+`: it walks the list with `+List.get+`, threads an accumulator, and takes its step as a function value (<<blocks>>). Its tail-recursive self-call is compiled to a loop, so it reads as a loop would (<<stdlib>>). `+map+` / `+filter+` / `+all+` / `+any+` are `+let+`s derived from it, expanded at the call site: `+map+` / `+filter+` seed with an empty list `+[]+` and grow it, `+all+` / `+any+` fold a `+Bool+`. `+mapIndexed(f, xs)+` is `+map+` that also hands `+f+` the element's 0-based index: `+fold+` carries no index, so it threads one in a pair accumulator `+(i, ys)+` and drops the counter at the end — the caller writes a weighted sum or positional transform (`+mapIndexed((i, c) -> w(i) * d(c)) |> sum+`) without hand-rolling that `+(i, acc)+` fold. `+List.length+` / `+String.length+` and `+List.get+` / `+Map.get+` are separate module functions, reached by their qualifier.

In the lower row, `+append(a, b)+` joins two lists and `+concat(xss)+` flattens a list of them — the same split `+String.append+` and `+String.concat+` make (<<stdlib-string>>), with `{plus}{plus}` as the common form of the first. `+contains+` folds with `+==+` so it works for comparable elements, and `+isEmpty+` looks at length. `+contains+` is the word `+Set.contains+` and `+String.contains+` use, because it is the same question; `+Map+` asks it as `+containsKey+`, which says which half of an entry is being looked for. `+sort+`, `+reverse+`, `+sum+` and `+product+` are primitives. Self-language sorting needs, besides comparison, a place to hold intermediate results, which `+fold+` does not give; `+sort+` sorts by the element's natural order.

`+sum(xs)+` / `+product(xs)+` answer the total and the running product of a list of numbers. The element MUST be `+Int+` or `+Decimal+` — the two types arithmetic is defined for (<<stdlib-int>>, <<stdlib-decimal>>) — and the answer has the element's type. A newtype over one of them is not a numeric element: it declares no addition and no zero of its own, so a list of them is summed by mapping to the wrapped value first (`+sum(map(h -> h.value, hours))+`). They are primitives rather than folds because a fold written in the language would have to write its seed, and a written seed is `+0+` or `+0.0m+` — one of the two types, never both. Over the empty list the answer is that seed (`+sum([]) == 0+`, `+product([]) == 1+`, as in Elm), and which of the two comes from the position the call is written in — the field it fills, the annotated binding it feeds, the declared output it returns. A `+?+` field asks for the value it wraps rather than for an optional (<<optional>>), so it says which as a plain field does. Written where nothing states a type, it is a compile error rather than a default: choosing `+Int+` there would make a numeric-default rule out of one library function, and the annotation says what the domain meant. Written where the position states some other type, it is a type mismatch — nothing about the element is unknown, and what is wrong is that a sum does not go there. Elements MUST be an ordered value — one of the ordered types (`+Int+` / `+String+` / `+Decimal+` / `+Date+` / `+Time+` / `+DateTime+` / `+Instant+`; <<primitives>>), an enumeration, ordered by its declaration (<<primitives>>), or a single-value newtype over any of those, which is ordered by the value it wraps (<<primitives>>) and carries that ordering on the JVM (<<jvm-product>>). Passing a list of product data to `+sort+` is a compile error (a product has no ordering, so it is rejected up front rather than at runtime; to sort them, map to an ordered field first with `+List.map+`, or order by that field with `+sortBy+`).

The third row adds more list operations. `+distinct+` keeps the first occurrence of each element and drops later duplicates, folding a `+Set+` of what it has seen; `+partition+` splits into `+(kept, rest)+` by a predicate; `+groupBy+` buckets elements into a `+Map<'k, List<'a>>+` by a key; `+indexBy(key, xs)+` keys them one entry each into a `+Map<'k, 'a>+`, which is how a list of rows read from outside becomes something a caller looks up by key instead of scanning per lookup — two elements with the same key collapse and the later wins, as `+Map.fromList+` does, so state the key's uniqueness with `+allDistinctBy+` when it matters. `+filterMap(f, xs)+` maps and drops in one pass: `+f+` answers with an optional and only what is there joins the result, which is how an optional field becomes a plain list (`+filterMap(i -> i.assignee, issues)+`) without a stand-in value to filter on afterwards. Its step is the one stdlib signature that writes an optional as a type, `+('a) -> 'b?+` (<<algebraic-types>>). These five are self-language `+let+`s that grow their result from within `+List.fold+`. `+max+` / `+min+` return the largest / smallest as an `+Option+` — `+None+` on an empty list — and compare by natural order, so like `+sort+` the element MUST be ordered; `+find+` returns the first element satisfying a predicate as an `+Option+`. `+sortBy(key, xs)+` orders by a projected ordered key rather than the element itself: unlike `+sort+`, whose comparison is built in, its key is a runtime function value, so the sort carries an actual `+Fn+`. It is stable, so ordering by two keys is a sort by the secondary key followed by one by the primary; a key MUST be a single ordered value, not a tuple. `+max+` / `+min+` / `+find+` / `+sortBy+` are primitives (they step outside `+fold+`); there is no `+head+` — a domain reads the first element with `+List.get(0, xs)+`. `+distinct+`, `+distinctBy(key, xs)+` and `+allDistinctBy(key, xs)+` are one family under one word. `+distinctBy+` keeps the first element of each key rather than of each element; `+allDistinctBy+` holds when nothing would be dropped — the "this projection is a unique id" invariant, written directly instead of comparing a `+distinct+` of the projected keys against the length by hand. Deriving from `+fold+`, they may stand in an invariant, as `+distinct+` itself may.

`+take(n, xs)+` keeps the first `+n+` elements and `+drop(n, xs)+` skips them — the top-N of a ranking and the rest of it. Both clamp instead of failing: a count of 0 or less takes nothing / drops nothing, and a count past the end takes the whole list / leaves it empty. Like `+mapIndexed+` they thread a pair accumulator `+(i, ys)+` through `+List.fold+` and keep the elements whose index falls on the wanted side of `+n+`.

[#stdlib-map]
=== Map

A `+Map+` is immutable and string-keyed (<<collections>>). The accessors read; the building operations return a fresh map.

[,text]
----
get  containsKey  keys  values                   // read
empty  singleton  insert  remove  isEmpty  size  // build
toList  fromList                                 // convert to / from entry tuples
fold  mapValues  filterEntries                   // higher-order
updateIfPresent  updateOrInsert                  // higher-order, by key
union  intersection  difference                  // key algebra
----

`+get+` returns `+Option<V>+`. `+empty+` is the empty map: like the empty list `+[]+`, its key and value types are fixed by context (a field or return position, a written binding type, the other branch of an `+if+` / `+match+`, or the first `+insert+`), so it is a *value* and not a function — written `+Map.empty+`, never `+Map.empty()+` (<<fn-declaration>>). `+singleton(k, v)+` is a one-entry map; `+insert(k, v, m)+` adds an entry or overwrites the existing one; `+remove(k, m)+` drops one, leaving `+m+` unchanged when the key is absent. Because a `+Map+` is immutable, `+insert+` / `+remove+` return a new map rather than mutating. `+size+` is the entry count and `+isEmpty+` whether there are none. The map operated on is last (<<pipe>>), so `+m |> Map.insert(k, v)+` reads left to right.

`+toList(m)+` is the entries as a `+List<(String, V)>+` — a list of key/value tuples in the map's iteration order — and `+fromList(entries)+` builds a map back (a later entry wins on a duplicate key). Iteration order is not specified by the language. It is not the order the entries were inserted in and is not to be read as one: no particular order is part of a `+Map+`'s semantics, and two maps that are equal are not guaranteed to iterate in the same order, so a program must not depend on the order it gets. A boundary map's members *are* specified: they are written in ascending order of their rendered keys (<<collections>>), which is not `+toList+`'s order — what is written out there is keyed by the rendered string and not by `+K+`, and a map that has been through an `+insert+` and one that has only been decoded are not walked alike. The tuple type in these signatures is allowed because a signature is not a codec boundary (<<tuple>>).

These are the operations that take a step function. `+Map.fold(step, seed, m)+` is layered on `+List.fold+` over the entries (`+toList+`) rather than walking the map itself, and the entries combine in the map's own iteration order, which is the unspecified one above. Its step is `+(acc, key, value)+`. It is `+fold+` and not `+foldLeft+` because a map publishes no first and no last entry: there is no left here for a right to be the counterpart of, and `+List+` is where that distinction exists (<<stdlib-list>>).

`+mapValues(f, m)+` keeps the keys and rewrites the values, growing a fresh map through `+fold+`; `+filterEntries(keep, m)+` keeps the entries its predicate holds for, the same way. Both steps see the key and the value — `+(key, value)+` — and the names say which half of an entry each operation is about, since a bare `+map+` or `+filter+` on a map leaves a reader to guess whether the keys, the values or the entries are what the step answers for. Without `+filterEntries+` a caller goes out through `+toList+`, filters as a list, and comes back through `+fromList+`, leaving the map type behind for two steps and re-hashing every survivor.

`+updateIfPresent(key, f, m)+` rewrites the value under one key with `+f+` and does nothing when the key is absent — which the name says and a bare `+update+` would not. Its step is `+('a) -> 'a+`: `+Option+` is not surface-writable, so a step cannot return one — insert a new key with `+insert+` and drop one with `+remove+`. `+updateOrInsert(key, default, f, m)+` collapses insert-if-absent / modify-if-present into one call, named as the two branches in the order they are written: it applies `+f+` to a present key's value and inserts `+default+` for an absent one. Like `+updateIfPresent+` it keeps `+Option+` internal — the absent branch takes a plain value. Typical use is accumulation, e.g. counting `+updateOrInsert(k, 1, n -> n {plus} 1, m)+`.

`+union+` / `+intersection+` / `+difference+` are the key algebra, named as `+Set+`'s are (<<stdlib-set>>) and as the set operations they are. All three answer with entries taken from `+a+`: `+union(a, b)+` adds `+b+`'s entries under keys `+a+` does not have, so `+a+` wins a collision; `+intersection(a, b)+` keeps `+a+`'s entries whose key is also in `+b+`; `+difference(a, b)+` keeps `+a+`'s entries whose key is not. Being binary operations they keep their operands in the written order and take no subject last, so `+a |> Map.difference(b)+` is not the way to write `+a+` minus `+b+` (<<pipe>>). Values from `+b+` are never merged into `+a+`'s — combining two values under one key is a domain decision, so it is written with `+fold+` and `+updateOrInsert+` where that rule lives.

[#stdlib-option]
=== Option

`+List.get+` / `+Map.get+` / `+List.find+` and a `+T?+` field all yield an `+Option+` (<<algebraic-types>>). Before this module the only way to read one was a two-arm `+match+`, which ends the value pipe (<<pipe>>); these functions let the optional flow through `+|>+` instead.

[,text]
----
map  withDefault
----

`+withDefault(default, opt)+` reads the option: the wrapped value when present, `+default+` when absent. `+map(f, opt)+` transforms a present value with `+f+` and leaves an absent one absent, so its result is again an `+Option+`; `+f+` is a plain `+('a) -> 'b+` and never sees the absent case. Both take the option last (<<pipe>>), so it threads through the pipe: `+Map.get(k, m) |> Option.map(v -> v.value) |> Option.withDefault("None")+`.

`+Option+` stays non-surface: the caller never writes `+Option<T>+` nor builds a `+Some+` — the optional is inferred and unnamed as it flows here, and `+map+`'s result stays inferred as it reaches `+withDefault+`. There is no `+andThen+`: its step would have to return an `+Option+`, which no user function can build under that rule (insert-if-present style folding stays with `+Map.updateOrInsert+` / `+update+`, which keep `+Option+` internal). For the same reason `+map+` cannot be self-hosted — a body cannot construct a `+Some+` — so it is a compiler built-in, while `+withDefault+`, which only reads, is an ordinary helper.

[#stdlib-set]
=== Set

A `+Set<'a>+` is unordered with no duplicates, compared by value equality (<<collections>>). It is immutable, so the building operations return a fresh set.

[,text]
----
empty  singleton  insert  remove  contains       // build / query
union  intersection  difference                   // algebra
isEmpty  size  toList  fromList                   // convert
fold  map  filter  partition                      // higher-order
----

`+empty+` is the empty set — like `+[]+` and `+Map.empty+`, its element type is fixed by context (a field or return position, a written binding type, the other branch of an `+if+` / `+match+`, or the first `+insert+`), so it is a *value* and not a function — written `+Set.empty+`, never `+Set.empty()+`. `+singleton(v)+` is a one-element set; `+insert(v, s)+` adds `+v+`; `+remove(v, s)+` drops it (leaving `+s+` unchanged when absent); `+contains(v, s)+` tests membership. `+union+` / `+intersection+` / `+difference(a, b)+` are the set algebra (`+difference+` is `+a+` minus `+b+`). Being binary operations they keep their operands in the written order and take no subject last (<<pipe>>). `+toList(s)+` lists the members in an order the language does not specify and `+fromList(xs)+` builds a set, dropping duplicates. That order is not the order the members were inserted in and is not to be read as one: no particular order is part of a `+Set+`'s semantics, and two sets that are equal are not guaranteed to list in the same order, so a program must not depend on the order it gets. The set operated on is last (<<pipe>>), so `+s |> Set.insert(x)+` reads left to right.

`+fold+` / `+map+` / `+filter+` / `+partition+` are the operations that take a step function. As with `+Map+`, a `+Set+` adds no loop of its own: each one goes out through `+toList+`, uses the `+List+` combinator, and comes back through `+fromList+`, so `+List.fold+` stays the single primitive. `+map+` may shrink the set — two elements that land on the same image collapse, since a set holds no duplicates — and `+partition(keep, s)+` answers the pair `+(kept, rest)+`, the shape `+List.partition+` gives. `+fold+` runs over `+toList+`'s order, which is unspecified, so a step whose result depends on that order is a mistake the compiler cannot catch: combine with something associative and commutative.

A `+Set+` field decodes from a JSON array and deduplicates it, and encodes back to an array whose members are in ascending order of their own external representation (<<collections>>). That is *not* `+toList+`'s order: the boundary's is specified and a `+Set+`'s own is not, and only what is written has a representation to be ordered by — a `+Set+` inside a behavior body may hold a tuple, which has none (<<tuple>>). The input array's order has no semantic significance and is not guaranteed to be preserved.

Which member a `+Set+` holds, where two equal elements are *written* differently, is not specified: equality decides that they are one member (<<equality>>) and the set keeps one of them, and nothing fixes which. It does not reach the boundary — a `+Decimal+` is written as its amount, so `+1.0+` and `+1.00+` are one member and one number written (<<primitives>>) — but `+toList+` hands back the one the set kept, and a program that reads a scale off it is reading something the language does not decide.

[#stdlib-bool]
=== Bool

[,text]
----
not
----

`+not+` is the *first self-host function* placed in `+souther.bool+`. It is not a primitive but written in Souther as `+let not (b: Bool) = if b then false else true+`, called qualified as `+Bool.not+` (bare via `+import Bool (not )+`). Being non-recursive, it is inline-expanded at the call site and no dedicated class is generated. Negation is written with this `+not+` — there is no prefix `+!+` operator (redundant, since `+not+` exists).

[#stdlib-date]
=== Date

[,text]
----
addDays  addMonths  addYears  daysBetween  year  month  day  fromParts
----

[#temporal-literal]
A temporal is written as its type applied to the text: `+Date("2026-07-25")+`, `+Time("09:00")+`, `+DateTime("2026-07-25T09:00")+`, `+Instant("2026-07-25T00:00:00Z")+`. The argument MUST be a written string: the compiler parses it where it stands, so malformed text fails the build rather than a run, and an `+example+` fixture — which holds only written values (<<example-evaluable>>) — can carry a temporal at all.

`+Time+` and `+DateTime+` are written to the second, and text carrying a fraction of one is <<e1322>> — they hold none, so admitting the text would drop part of it (<<a-local-temporal-is-held-to-the-second>>). `+Instant+` is written in UTC, the `+Z+` form. Text carrying a numeric offset is <<e1322>> — not because the offset is wrong, since `+09:30:00+09:00+` and `+00:30:00Z+` are one moment and either determines it, but because a written value is written the way the value is written back (<<fixture-is-written-not-carried>>) and an `+Instant+` is written in UTC. An offset is a *spelling*, not a zone: a zone is a place with rules about when its offset changes, and neither is a thing this language names. A boundary reads the offset form and answers the same moment (<<an-instant-carries-what-a-timestamp-said>>).

[[a-leap-second-is-no-moment]]Text naming a leap second — a `+:60+` second — MUST be refused, where it is written and where it arrives. The language has no moment for one, and the reading that would otherwise be taken is the second before it, which is a *different* moment than the text says. This is the one place a written temporal is refused for what it means rather than for its form, and it is refused for the reason everything else in this section is: a value nothing downstream can tell from the value that was sent is worse than no value.

A newtype over a temporal takes the temporal it wraps: `+data 貸出日 = Date+` is built `+貸出日(Date("2026-07-25"))+`, in a fixture as in a model body. The string alone is a `+String+` there as anywhere (<<fixture-is-written-not-carried>>).

This is the only form for *writing* a temporal out. It is not the only way to *reach* one: `+fromParts+` below builds a `+Date+` from three Ints a model already holds, and answers a case where they name no day.

`+Date+` is a local calendar date (<<primitives>>). Calendar arithmetic is pure and belongs in the domain, not behind an injected behavior (<<injected-behavior>>) — only reading the *current* date needs the outside world. `+addDays(n, d)+` / `+addMonths(n, d)+` / `+addYears(n, d)+` shift `+d+` by `+n+` units (`+n+` may be negative), following `+java.time.LocalDate+`, which clamps the end of a month (`+addMonths(1, 2026-01-31)+` is `+2026-02-28+`). `+daysBetween(from, to)+` is the whole days from `+from+` to `+to+`, negative when `+to+` precedes `+from+`. The date operated on is last (<<pipe>>), so `+d |> Date.addDays(3)+` reads left to right.

[[a-shift-off-the-end-of-a-temporal-aborts]]A shift that leaves the range a temporal can hold MUST *abort*, as an `+Int+` overflow does (<<stdlib-int>>): it is a model bug and not a business result, so it throws `+ConstraintViolation+` and does not wrap, clamp, or answer a case (<<jvm-abort>>). A year past what a date holds and a count too large to add are one abort and not two, because what a caller can do about either is the same. This is the arithmetic and not <<a-temporal-built-from-parts-is-refused-where-the-parts-name-none>>: parts are a question about whether three numbers name a day, and a shift is a computation that ran out of room.

`+year(d)+` / `+month(d)+` / `+day(d)+` read a date's parts as `+Int+`s — the month 1 to 12, the day 1 to 31. `+year+` / `+month+` / `+day+` all answer `+Int+`: `+addMonths+` counts in months, and a named month would have to be converted back before adding one. There is no `+dayOfWeek+`: neither language answers that with a bare number, so it needs a named seven-case type.

A month boundary follows from `+day+` and belongs in the domain:

[,souther]
----
let monthStart (d: Date): Date = Date.addDays(1 - Date.day(d), d)
let endOfMonth (d: Date): Date = Date.addDays(0 - 1, Date.addMonths(1, monthStart(d)))
----

`+fromParts(year, month, day)+` reads three Ints as a day, answering `+Date | NotADate+`. It is the inverse of `+year+` / `+month+` / `+day+` and normalises nothing (<<a-temporal-built-from-parts-is-refused-where-the-parts-name-none>>), so a model holding a year and a month it took in as `+Int+` can say the date they name, and is told when they name none:

[,souther]
----
data 診療年月 =
    { 年: Int
    , 月: Int
    }
    invariant 月 >= 1 && 月 <= 12

let 時効内か (m: 診療年月, 申請日: Date): Bool =
    match Date.fromParts(m.年, m.月, 1) with
        | Date as 診療月の初日 -> 申請日 < Date.addYears(2, Date.addMonths(1, 診療月の初日))
        | NotADate -> false
----

One case covers both a day the calendar does not have and a year a date cannot hold, as `+NotANumber+` covers both text that is no number and a number too large to hold (<<stdlib-string>>): what a caller does with either is the same.

`+NotADate+` and `+NotATime+` are written in a `+match+` arm and nowhere else, as `+DivisionByZero+` and `+NotANumber+` are (<<algebraic-types>>). They are the library's names for the library's own refusals, so a `+let+` that wants to pass one of them on to its caller names a case of its own and answers that.

[#stdlib-time]
=== Time

[,text]
----
fromParts  hour  minute  second
----

`+Time+` is a time of day to the second, with no date and no zone (<<primitives>>) — what a model holds when it names an opening hour, where writing a `+DateTime+` would invent a day the domain does not have. `+hour+` / `+minute+` / `+second+` read the parts as Ints, and `+fromParts(hour, minute, second)+` reads three back into a `+Time+`, answering `+NotATime+` for hour 24, minute 60 and the leap second.

There is no time arithmetic. Adding to a time of day has to say what happens at midnight — whether 23:30 plus an hour is 00:30 or is nothing — and that is a decision about a domain rather than about a clock. A model that holds the day as well adds on the `+DateTime+`, where the day is there to carry the answer. A model that holds only a time of day says what crossing midnight means as a rule of its own — telling it to hold a `+DateTime+` instead would have it invent a date, which is the thing a `+Time+` exists to avoid.

[#stdlib-datetime]
=== DateTime

[,text]
----
addMinutes  addHours  addDays  minutesBetween  toDate  toTime  fromDateAndTime
----

`+DateTime+` is a local date-time with no zone (<<primitives>>); the zone stays a boundary concern. `+addMinutes+` / `+addHours+` / `+addDays+` shift by their unit; `+minutesBetween(from, to)+` is the whole minutes between (negative when `+to+` precedes `+from+`). As with `+Date+`, the date-time operated on is last (<<pipe>>).

`+toDate+` / `+toTime+` / `+fromDateAndTime+` are the three that say a `+DateTime+` is a `+Date+` and a `+Time+` (<<a-date-time-is-a-date-and-a-time>>). `+fromDateAndTime(d, t)+` cannot fail and has no case to write an arm for: whatever could have been wrong about the parts was refused where the `+Date+` and the `+Time+` were built. Its name is not `+fromParts+`, which the other two temporals use for the Ints they read — a `+DateTime+` comes apart two ways, into a date and a time or into six numbers, so `+fromParts+` would leave a reader to work out which it takes.

[#stdlib-instant]
=== Instant

[,text]
----
----

`+Instant+` has no operations, and the empty list above is not a section waiting to be filled: it has no parts to read without a zone, so it has none to be built from either, and the two directions are absent together (<<an-instant-carries-what-a-timestamp-said>>). What the missing zone rules out is the *calendar*. An operation that stays on the timeline — the elapsed time between two, a shift along it — needs no zone, and none is here because nothing has asked for one.

A model still holds one, compares two (<<primitives>>), keys a `+Map+` by one (<<collections>>) and writes one as a literal (<<temporal-literal>>). To read one as a local date-time it declares a behavior with no implementation and the Java side supplies the zone, the way the current time is supplied (<<injected-behavior>>):

[,souther]
----
behavior 地方日時 : (at: Instant) -> DateTime
----

Decoders and Encoders do not appear in Souther's syntax. Derived from a data's shape, they are generated by the ClassFile backend as Raoh Decoders / Encoders (<<codec-generation>>, <<encoder>>). Primitive parsing, error accumulation, discriminator dispatch, and encoding are handled by Raoh combinators; there is no facade on the souther-runtime side. A custom boundary codec (<<custom-codec>>) uses Raoh directly from the Java side.

[#jvm-output]
== JVM output

A declaration is emitted as classes and methods, and a class file has structural limits a declaration can be written past. What the compiler emits MUST fit inside them, since past any of the three the class cannot be loaded at all:

* [[generated-methods-fit-jvm-parameter-slots]]a generated method MUST fit the JVM argument-slot limit, which is what bounds how many fields a `+data+` may declare and how many parameters and dependencies a `+behavior+` may take;
* [[generated-methods-fit-jvm-code-size]]a generated method's code MUST fit the JVM code-size limit;
* [[generated-classes-fit-jvm-constant-pool]]a generated class's constant pool MUST fit what a class file can address and write out.

Two more come from names rather than sizes:

* [[no-two-declarations-become-one-class]]no two declarations of a module may emit one class. A behavior capitalizes into a class name (<<jvm-behavior>>), a union output becomes a sealed interface (<<jvm-sum>>), and a member joining a union from outside becomes a bridge case (<<bridge-case>>) — three ways to arrive at a name, and a model may already have declared it;
* [[a-field-does-not-collide-with-an-object-method]]a field MUST NOT be named after a no-argument method of `+Object+`, whose accessor the generated class already has (<<jvm-product>>).

The first is decided at the declaration, before anything is emitted. The other two are not known until the code is written, and are reported as the definition being emitted when the limit was met.

[#target-jdk]
=== Target JDK

The artifacts target Java 25 or later, which is also what the compiler is built and run on.

A name reaches the class file as it was written. What a class file may hold is an unqualified name, which admits a Unicode character generally and excludes `+.+`, `+;+`, `+[+` and `+/+` (and `+<+` and `+>+` in a method name) — none of which spells a Souther name (<<identifier>>). So a model written in Japanese is emitted in Japanese, and nothing is escaped or romanized.

What a class file holds and what Java source can write are two different limits, and neither contains the other. Souther's alphabet and Java's identifier rules do not coincide: each admits characters the other does not, and each is read against its own Unicode version. A name outside what Java writes is still emitted and still loads; what it costs is that Java code cannot name it — and Java names a generated type where it implements an injected behavior or calls a decoder (<<java-example>>). A model whose types are reached from Java is spelled in characters Java can write, which is the author's decision at the boundary and not a rule the compiler enforces on every name in the language.

The class-file version of the generated `+.class+` is fixed at Java 25 (major 69). It MUST NOT be left to the default of the JDK running the backend: pinning is what makes the floor a decision rather than a property of whichever JDK ran the build. souther-runtime is on the artifact side and is compiled to the same target.

[#jvm-nullness]
=== Nullness

No generated value is ever null. Absence is `+Option+` (<<optional>>), a failure is a case of the output union (<<unmarked-sum>>), and a collection holds neither a null key nor a null value — so no accessor, factory, codec or `+apply+` hands one back.

Every generated class carries JSpecify's `+@NullMarked+`, and souther-runtime carries it on its own packages, so a consumer's compiler knows that too. It matters most where a compiler acts on it: Kotlin reads the annotation off the class file and types what it names as non-null, where without it the type is a platform type and a null check is neither required nor possible. The annotation is read by name, so the JSpecify jar need not be on the consumer's classpath.

A record's component accessor is the exception. Kotlin types it from the record component and reaches it without applying the class's marking, so a component read would be a platform type again while every other member of the same class is not. Each accessor therefore says `+@NonNull+` on its return type itself, at every position of it — the `+List+` and the element it holds — and the `+Record+` component repeats it, for a reader that goes through reflection rather than the accessor. A primitive return (an `+Int+` accessor returns `+long+`) states nothing, having no nullness to state.

It rides on each class rather than on a `+package-info+` of the module's package. That package is where a consumer's own `+package-info.java+` sits, and a generated class of the same name would lose to that declaration — the marking would then reach only the modules that own no Java of their own.

A generated method whose descriptor names a generic type carries the `+Signature+` for it, so the type argument survives to a reader. `+__construct+` returns `+Result<T, String>+`; a raw `+Result+` would be a platform type again on the Kotlin side, where the class carrying it says everything else is not.

The few places that do admit a null are annotated one by one — a `+java.util.Map+` implementation's `+get+`, which returns null for an absent key because that interface says so.

[#jvm-product]
=== Product data

Generated as a record: a final class extending `+java.lang.Record+` whose `+Record+` attribute names its fields as components, in declaration order, with a non-public canonical constructor. Fields taken in by a `+...+` spread are laid flatly in the generated class.

[,text]
----
public final class Submitted extends java.lang.Record {
    private final Employee applicant;         // from ...TravelRequestCommon
    private final Amount estimatedCost;       // from ...TravelRequestCommon
    // ...
    private final LocalDateTime submittedAt;

    Submitted(...) { ... }                    // canonical, package-private

    public Employee applicant() { ... }       // one accessor per component
    public String toString() { ... }          // Submitted[applicant=..., ...]
}
----

Being a record is what a reader acts on without being told anything about Souther. javac deconstructs the value in a record pattern, and does so for a case of a sum in an exhaustive `+switch+` — the same way it reads the runtime's `+Ok+` / `+Err+`, which are records too. Kotlin reads each component as a property, so `+issue.id.value+` is written instead of `+issue.id().value()+`; without the `+Record+` attribute Kotlin resolves the backing field instead and reports that it cannot be accessed, naming a field where the accessor was meant.

Every data gets a public read accessor per field (<<field-visibility>>), so an exposed one is readable from another module and from Java; because module = package, package-private cannot read across the boundary. A data the module keeps to itself has accessors too — a component is read through its accessor, and the generated code of the module reads a field the same way — but its class is package-private, so nothing outside can call them. The constructor stays non-public either way, so a value can be read but not constructed.

`+toString+` prints the record form, `+Submitted[applicant=..., estimatedCost=...]+`. It is generated rather than inherited: `+java.lang.Record+` declares it abstract, so a class without one would throw `+AbstractMethodError+` when anything printed the value. `+equals+` / `+hashCode+` are generated over the components as well, and compare a `+Decimal+` by value rather than by scale (<<primitives>>) — a record's shape does not dictate what equality means.

A field MUST NOT be named after a no-argument method of `+Object+` (`+toString+`, `+hashCode+`, `+getClass+`, `+clone+`, `+finalize+`, `+notify+`, `+notifyAll+`, `+wait+`): the accessor would collide with a method the class already has. It is a compile error where the field is written, as a record component of that name is in Java. `+equals+` is not among them — `+equals()+` and `+equals(Object)+` are different methods — and is allowed, as Java allows it as a component name.

A framework that constructs records reflectively — Jackson, given a generated type as a request body — can reach the canonical constructor and build a value without passing the invariant `+__construct+` runs. Construction authority is governed by declaration rather than by visibility, and the same value was already reachable through `+setAccessible+`; what is guaranteed is what a decoder admits (<<decoder>>), not what reflection cannot forge.

A single-value newtype over an ordered type implements `+Comparable+` against itself, comparing the value it wraps. That is what `+sort+` / `+max+` / `+min+` / `+sortBy+` compare by, and it makes the type usable as a `+TreeMap+` key or a `+Collections.sort+` element from Java. Where it wraps an enumeration the comparison reads the sum's declaration order, the case values having no `+Comparable+` of their own (<<primitives>>) — so the wrapper is where a Java reader gets one. A newtype over an unordered type (`+Bool+`) and a product data get no `+Comparable+`.

[#jvm-collection-equality]
=== A collection's equality, seen from Java

A generated data, a newtype and a tuple compare with their own kind and nothing else, so their `+equals+` is the language's value equality (<<equality>>) and a Java reader sees exactly what Souther means.

A `+List+`, a `+Set+` and a `+Map+` implement the JDK interfaces of those names, and a foreign implementation may be on the other side of an `+equals+`. A `+List+` therefore keeps the `+List+` contract — elements compared by their own `+equals+` — because a list that compared them by value equality would be equal to a JDK list that was not equal back. The language's question is answered by a separate pair of methods the runtime calls.

Reading one is not comparing two. `+Map.get+`, `+Map.containsKey+` and `+Set.member+` read the collection they are given by the index it carries: every collection Souther builds is indexed by value equality, and so is the `+Set+` a field decodes into. A `+Map+` a field decodes into is a JDK map, and a boundary map's key is a `+String+`, a temporal, an enumeration or a newtype over one of those (<<collections>>) — for each of those the two agree. A `+java.util.Map+` handed in from Java with some other key is answered by its own index. Comparing two collections normalizes both first, since Java's equality is finer and a foreign collection may hold two elements the language calls one.

A `+Set+` and a `+Map+` cannot do both. Which element a set holds and which key a map finds is decided by the hash the trie is indexed by, and there is one index, not two: `+contains+` and `+get+` answer the language's question, and `+equals+` and `+hashCode+` follow them rather than the elements' own. Against a foreign `+java.util.Set+` holding the same `+Decimal+` at another scale, `+equals+` is therefore not symmetric. Only Java interop can build that pair — a set Souther built is compared with a set Souther built — and the alternative would be a set whose `+contains+` found an element and whose hash said it was not there.

[#jvm-sum]
=== Sum data

Generated as a sealed interface and final implementation classes. If a case is a named data, its final record class implements the sealed interface. A unit data is generated as a record with no components, holding its one value in a shared instance — so a Java `+switch+` deconstructs it as `+case Approved()+` next to the product cases (<<jvm-product>>).

A field every case spreads (<<sum-data>>) is declared on the sealed interface as an abstract accessor, which each case record's accessor of the same name implements. Java reads it off the sum the same way Souther does, without a `+switch+`.

A case may itself be a sum (<<sum-data>>), and then its interface extends the outer one. Only that one step is emitted: interface inheritance carries the rest, so a leaf is a value of every sum above it without being named in any of them. The relation has to exist because the rest of the language already reads it that way — the checker folds a nested sum to its leaves to judge a case, and the derived codec dispatches over those same leaves, so a decoder hands back a leaf where the outer sum is what was asked for.

[#jvm-codec]
=== Decoder / Encoder

An exposed data gets a per-source acquisition API. What is returned is a Raoh Decoder / Encoder (`+Decoder<I, T>+` is `+I -> Result<T>+`).

[,text]
----
public static Decoder<Map<String,Object>, TravelRequest> decoder()      // Map (raoh-core; a newtype is Decoder<Object,T>)
public static Decoder<JsonNode, TravelRequest> jsonDecoder()            // JSON (raoh-json)
public static Decoder<Record, TravelRequest> recordDecoder()            // DB row (raoh-jooq)
public static Encoder<TravelRequest, Map<String,Object>> encoder()      // object/sum -> Map (JSON via valueToTree at the boundary)
// newtype encodes to a bare scalar. e.g. MemberId.encoder() : Encoder<MemberId, String>
----

The concrete implementation is generated directly as bytecode by the ClassFile backend from Raoh combinators (<<codec-generation>>). Nothing in souther-runtime calls Raoh — the dependency is the generated code's — and souther-runtime declares it, because that is where a project and everything downstream of it resolve it from without being asked to name a version. `+jsonDecoder()+` / `+recordDecoder()+` are generated only when the data's shape supports it (<<codec-generation>>; JSON for a shape without temporal, `+Record+` for a flat shape). `+decoder()+` (bare values / Map) is always generated.

[#jvm-behavior]
=== behavior

A fn- or `+>->+`-implemented behavior is generated as a public interface named after the behavior (first letter capitalized), with the implementation in a `+<BehaviorName>$Impl+` class. Java declares the behavior by the interface name and never names the implementation. A single-input behavior's interface extends `+Behavior<In, Out>+` (so it composes with `+>->+` and its `+apply+` is typed by inheritance); an interface for any other input count is standalone and declares the typed `+apply+` directly — `+apply(A, B, …)+` for several inputs, `+apply()+` for none. The interface carries a static factory: `+of()+` for a behavior with an empty requirement set, or `+bind(...)+` taking one implementation per requirement.

[,text]
----
var handle = Handle.bind(new JdbcFindMember(dataSource));   // Handle is the interface; bind builds Handle$Impl
----

An injected behavior (a `+behavior+` with no `+let+`) is instead an abstract class the Java implementation extends (<<java-base-class>>), not an interface.

The generated names are derived from the behavior name (first letter capitalized). Because a data name already starts with a capital, a behavior `+quote+` becomes a type `+Quote+`, colliding with a `+data Quote+` in the same module. This is a compile error; rename one to distinguish them.

[#jvm-construction-privacy]
=== Preventing exposure of construction authority

A data's constructor is non-public (package-private): nothing reaches the fields without the invariant. The checked entry `+__construct+` is public for an exposed data and non-public otherwise, so a construction declared in another module (<<closed-construction>>) has the path its declaration names, and a data a module keeps to itself has none. The Java implementation of a behavior with no implementation extends the generated abstract base class (<<java-base-class>>) and constructs failure cases via the base's `+protected+` factories; it may be placed outside the generated package.

What is guaranteed to Java is that no unvalidated value can be created — not that no value can be created. `+decoder()+` is public API (<<jvm-codec>>) and `+__construct+` is public for an exposed data, so Java may build one at any time; the invariant is checked on both paths (<<invariant-on-construct>>), a decode violation becoming a `+Result+` failure. A unit data ignores input and decodes (<<codec-generation>>), so its `+decoder()+` lets anyone construct it — but a unit data has nothing to validate.

Reflection, Unsafe, and bytecode manipulation are out of scope for guarantees.

[#jvm-abort]
=== Abort on invariant violation

An invariant violation inside the domain (<<algebraic-types>>, <<violation-destination>>) appears in generated code as throwing `+souther.runtime.ConstraintViolation+`. It carries the broken data name and the invariant's location.

Souther code cannot catch this. There is no catching syntax, so it cannot control business flow (<<out-of-scope>>). The boundary Java can catch it, but handles it as distinct from a business failure. A business failure arrives as an output case (<<unmarked-sum>>) and maps to 400; an abort is a model bug and maps to 500.

A decode violation is not this; it returns as a Raoh `+Result+` failure (<<decoder-error>>).

[#jvm-anonymous-union]
=== A behavior output's anonymous union

When a behavior's output is an anonymous union (`+-> Member | NoSuchMember+`), a sealed interface representing that union is generated. Its name is the behavior's class name (first letter capitalized; <<jvm-behavior>>) plus `+Result+`, i.e. `+<BehaviorName>Result+`; `+permits+` lists the union's leaf cases, and each case data implements it. An anonymous union has no common supertype (<<asymmetric-interop>>), so this lets Java receive it as a single return type and exhaustively check with `+switch+`.

[,java]
----
// when findMember is -> Member | NoSuchMember
sealed interface FindMemberResult permits Member, NoSuchMember {}
// Member and NoSuchMember implement it; the injected behavior's abstract base FindMember implements Behavior<MemberId, FindMemberResult>
----

There are two cases where it is not generated. If the output is a named sum data, that is itself a sealed interface (<<jvm-sum>>) and is the return type. If the output is a single case, that case type is the return type.

One data type may appear in the output of multiple behaviors, so it may implement several `+<BehaviorName>Result+` at once. The output of a `+>->+` composition behavior (the folded sum of mainline and departed; <<type-routing>>) is also an anonymous union, generated by the same rule from the composition name.

`+<BehaviorName>Result+` takes a class name in this module, so it is subject to the rule every other name this module emits is subject to (<<bridge-case>>): a `+data BillResult+` beside a `+behavior bill+` whose output is a union is a compile error, and so is a `+behavior billResult+` beside it. Two result unions cannot collide with each other, their behaviors having already been refused for capitalizing into one class (<<jvm-behavior>>).

[#union-encoder]
The generated type carries an `+encoder()+` (<<jvm-codec>>) that dispatches its members by the discriminator `+"type"+`, like a sum over the same leaves (<<sum-discrimination>>). It has to: a member's own encoder writes no discriminator — only the union it sits in knows which member it is — so a behavior answering `+-> Member | NoSuchMember+` would otherwise put out a form nothing could tell apart. What each member writes is what <<sum-discrimination>> says a case of that shape writes. A member that is a product lays its fields flatly beside `+"type"+`; a unit writes `+"type"+` alone; a member whose own representation is not an object — a primitive, a newtype — goes under `+"value"+` beside it. A union every one of whose members is a unit data is an enumeration and travels as the member's name, a bare string, exactly as the named sum over those leaves does. A member reaching the union through a bridge case is read out of it first, so the bridge case appears in no external representation and still carries no codec of its own. A member declaring a field named `+"type"+` is refused, as a sum's case is (<<discriminator-field>>).

[,text]
----
// when bill is -> Issued | UnknownSku, and lengthOf is -> Int | NotFound
Issued { amount = 200 }  ->  {"amount":200,"type":"Issued"}
UnknownSku               ->  {"type":"UnknownSku"}
4                        ->  {"type":"Int","value":4}
----

There is no matching decode. Nothing that crosses a behavior's input is an anonymous union (<<a-parameter-names-one-type>>), so nothing reads one back. A boundary that wants a case back reads it with that member's own decoder — from the object itself where the member laid its fields flatly, and from what is under `+"value"+` where it did not, since a newtype and a primitive read their representation bare (<<newtype>>).

[#bridge-case]
A case class receives the unions it belongs to when *its own* module is generated. A member this module declared therefore carries the interface itself. A primitive cannot — `+Int+` is `+java.lang.Long+`, the JDK's class — and neither can an imported type, whose class the module that declared it has already emitted; giving it an interface from here would make a module's bytecode a function of who imports it, the mirror of what ADR-0024 refuses. Such a member joins the union through a *bridge case*: a record of one component, `+value+`, that this module emits and that implements the union in the member's stead.

[,java]
----
// when owed is -> Yen | NothingOwed, with Yen imported from `up`
sealed interface OwedResult permits NothingOwed, YenCase {}
public record YenCase(up.Yen value) implements OwedResult {}
----

The bridge case is named `+<MemberName>Case+`, and there is one per member per module: it implements every `+<BehaviorName>Result+` of this module the member belongs to. That is the rule a local case class already follows — what differs between the two is not what membership means but which module owns a class it can be put on.

|===
| Member | Where its result-union membership sits

| a data this module declares | the case class itself
| a primitive, or an imported data | a bridge case this module emits
|===

A bridge case takes a class name in this module, so it is subject to the rule every other name this module emits is subject to: no two of them may be one class. `+YenCase+` is a name a model may well have declared, and so are `+IntCase+` and `+DateCase+`, so the collision is reported where it happens rather than the names reserved — a name nothing collides with stays available. Three ways to collide: with a data this module declares, with the class a behavior capitalizes into (<<jvm-behavior>>), and with another bridge case, which is two types of one spelling from two modules, members of two different unions of this module. Within one union the member-name rule (<<union-member>>) has already refused them.

A bridge case has no codec. Belonging to a union does not change a member's own external representation, so a consumer that has switched to a bridge case takes the value out and uses the codec the member itself carries (<<jvm-codec>>). A consumer that wants the answer as the union writes it asks the union instead (<<union-encoder>>), which is where the discriminator no member writes on itself comes from.

The bridge case is a JVM form. It is not a Souther type, it is not written in a `+match+` arm or an `+example+` row, and it does not appear in an external representation. What each side sees:

|===
| Where | What a value of the union is

| Souther source, a `+match+` binding, an `+example+` row | the member's own value
| the external representation | the member's own contents, decided by its Souther type, under the union's discriminator (<<union-encoder>>)
| a Java `+switch+` and the `+apply+` that returned it | the case class, or the bridge case for a member this module could not put the interface on
|===

The two forms meet at exactly two places. A behavior's return puts the Souther value it answers with into its declared union's JVM form; a call takes it back out before the result is used. Both walk the union's members, which are known where they are emitted, so neither asks a value whether it happens to be wrapped — and because everything between the two is a Souther value, nothing is ever put into a bridge case twice. Reading a value out and putting it back gives the value that was there.

The model author does not write this type name in Souther source; they only write `+-> Member | NoSuchMember+`. A Java consumer declares the behavior by its interface name (<<jvm-behavior>>) and calls `+apply+`, whose return type is `+<BehaviorName>Result+` by inheritance, then matches the cases with `+switch+` — so the consumer never writes the `+Result+` name either. It surfaces explicitly only in an injected behavior's Java implementation, which writes it as the `+apply+` return type (<<java-example>>).

[#compiler-pipeline]
== Compiler structure

[[source-nesting-is-bounded]]A source MUST nest no deeper than the compiler reads. The limit is stated as the compiler's rather than as any one pass's, and it is reached while reading, so it is reported at the token that reached it — including by the formatter, which reads sources it cannot compile. Breaking the nesting into named parts is how a text past it is written instead.

[[source-structural-complexity-is-bounded]]A definition MUST have a structural complexity of 320 or less. Its complexity is counted over what the source wrote:

* a name or a literal is one;
* a construct is one more than the deepest thing it holds;
* an application of a helper that is substituted at the call (<<fn-rules>>) is its argument count more than the deepest of what it holds, since each argument is bound where the body is put; an application that is not substituted — a behavior, a recursive helper, a function a binding holds — is a construct like any other;
* a name that reaches a value counts that value's complexity where the value is substituted (<<fn-value-semantics>>).

A block is counted in the steps its statements take. Everything a statement is followed by is written inside what that statement introduced, so a block of statements and an expression written that deep are the same structure said two ways, and what a statement holds is counted from where that statement stands rather than from the block's end.

A step is not always a binding. A `+guard+` introduces no name and is one step, since what follows it is written inside the case it settles; an ordinary `+let+` is one; a `+let+` written with a pattern is one per binding that pattern introduces.

A pattern that is a name costs nothing beyond the name it binds. Any other pattern takes a value apart, and binds the value and each part taken out of it: a tuple binds the value and each of its elements, a record the value and one per field it names, a case the value and whatever its inner pattern binds, each of those counted the same way through a pattern written inside one.

Two things follow from that, and they are different numbers. What comes after the pattern is written inside one level per binding, since each part is named before the next is: that is what a `+let+` written with a pattern takes as steps. Taking a part out of the value is one level more, on the part rather than on what comes after — so a pattern on its own is one deeper than it binds, and a statement written with one encloses what follows it in the bindings alone. A definition's parameters, and a lambda's, are patterns with the body written after them, and cost the bindings and that one more.

This is a separate limit from the one above, and neither implies the other. Nesting is bounded as the source is read; complexity is bounded over what the source says, which the reading does not settle: a module writes its declarations flat and substitution composes them, so a source that nests barely at all can be arbitrarily complex. It is reported at the construct that exceeded it — the block, the definition, or the value whose substitution composed it — and never as a nesting to flatten, since naming parts is what composes complexity rather than what reduces it.

The limit is a property of the language, not of a compiler's internal representation. What a compiler builds from a definition, and how deep that is, is its own choice; what a definition may say is this.

A compiler states the runtime it holds this bound on. Every phase descends what it builds by recursion, so what a definition may say and what it takes to walk one are the same question asked from two sides, and a stack left to whatever a platform defaults to would make a source compile in one place and not another. This compiler is supported on a 4 MB thread stack, against a source at the bound needing about a megabyte. The `+souther+` binary sets it for itself and this compiler's tests run with it; a host that launches the language server, or the compiler as a library, is the one that gives it that stack, and this is what it has to give. The number is this implementation's and moves with it; the bound above does not.

[[running-out-of-room-is-reported]]A compiler that runs out of room MUST report it rather than fail. Every phase descends what it builds by recursion, and running out of stack is not a diagnostic, so left alone it reaches the author as a trace and takes an editor's session with it. No position is claimed: where the stack ended is not a fact about the source.

This is not either of the limits above said again. Both of those are reached at a place in the source and reported there; this is reached wherever a walk happened to end, and on the stated runtime a source that passed both should not reach it at all. Reaching it is therefore a report about the compiler or about the stack it was given, and it says so rather than asking the author to change anything.

[,text]
----
Source -> Lexer -> Parser -> AST
  -> Derive (fill decoder/encoder into the AST from a data's shape)
  -> Lower (behavior / let bodies: helper inlining, desugaring)
  -> Type Check -> Core IR (typed: the checker produces it as it types a body)
       name resolution / construction-authority check / requirement-set check and composition inference / exhaustiveness check / invariant type check
  -> ClassFile Backend -> .class (direct bytecode generation)
----

Behavior and `+let+` bodies are lowered — helper inlining and surface desugaring happen once here — and then typed. The type checker produces the Core IR as it types a body: every Core node carries the type decided for it, and the backend emits from that rather than inferring types a second time. Data definitions and derived decoders / encoders stay at the AST level, and their expressions are typed the same way where the backend emits them; there is no separate Domain IR / Decoder IR / Encoder IR. Type checking verifies construction authority, requirement sets, exhaustiveness, and invariants together: the surface checks read the AST, and the body check reads the lowered form. The backend generates `+.class+` with the Class-File API (`+java.lang.classfile+`) and does not go through javac.

[#ast-tracking]
== Information tracked by the AST and backend

Apart from the typed Core IR the type checker produces for behavior and `+let+` bodies (<<compiler-pipeline>>), the compiler keeps no separate intermediate representation; the following is held as the AST and the type-check results.

[#ast-nodes]
=== AST definition nodes

[,text]
----
Module  Data(ProductData)  SumData  UnitData  Include  Field  Invariant
DecoderDef  EncoderDef  SpecBehavior  PipeBehavior  FnDef  Import
Construct  FieldInit  Expression  Case  RawExpr  DecRef  Block
----

Behavior definition nodes are two, corresponding to the two right-hand sides of <<behavior>>. `+SpecBehavior+` holds input type, output type, `+constructs+`, `+depends on+`, and its `+ensures+` clauses, with no body. If a same-named `+FnDef+` exists it is the implementation; otherwise it is subject to Java injection (<<injected-behavior>>). `+PipeBehavior+` holds the sequence of stage names and no `+ensures+` slot. It is itself the implementation, so it takes no `+FnDef+` and is not injected; its type is computed from the stages (<<type-routing>>). Composition vs. simple is distinguished by these two nodes, so later stages need not re-read the right-hand side to tell them apart.

`+FnDef+` holds argument names and a body. If a same-named `+SpecBehavior+` exists the type comes from it; otherwise it is a helper `+let+` and holds its own argument types, except for a parameter its body determines (<<fn-declaration>>).

A `+SumData+` case holds only a reference to a named data (<<sum-data>>). A `+...+` spread inherits taken-in fields and invariants flatly. `+DecoderDef+` / `+EncoderDef+` are filled by derivation from a data's shape (<<decoder>>, <<encoder>>). `+guard+` and `+let+` are desugared, so they do not appear in the AST; they appear as an `+if+` and as block bindings (<<guard>>, <<block-expression>>).

[#ast-behavior-info]
=== Information tracked about a behavior

[,text]
----
input type  output type (sum of possible business results)  requirement set  construction authority (constructs)
----

The output type is a single type or a sum. `+SpecBehavior+` takes these four from the declaration — the requirement set is also taken from `+depends on+` and reconciled with the set inferred from the `+let+` (<<depends-on>>). `+PipeBehavior+` computes input and output types from the stage sequence (<<type-routing>>) and infers the requirement set as the union over stages (<<composition-with-requirements>>).

The body is not here. A behavior has no body; the `+FnDef+` holds it (<<behavior>>, <<ast-nodes>>). The backend combines this behavior-side information with the `+FnDef+`'s body, lowered to Core (<<compiler-pipeline>>), to generate bytecode.

[#compile-errors]
== Compile errors

A code (e.g. `+E1301+`) is the stable, locale-independent identity of an error; the title, message, and hints are prose shown in the selected locale. The catalogue below lists each code with the message and hint it states; the header, the quoted line and the caret described next are left out of the quotations.

An error is rendered from structured data, not a frozen string. The default (human) form is a titled report: a header naming the error and the source location, the offending line quoted with a caret under the token, found-vs-expected type blocks for a type mismatch, and any hints. An error that concerns more than one place — the two branches of a disagreeing `+if+`, the two sides of a failed composition — points at each with its own caret and label. A compile stops at the first error, except where a pass finds several independent ones at once: every failing `+example+` row is reported with its own position and reason, one report after another. `+--format json+` emits each diagnostic as a JSON object (code, source region, message, hints) for tools and agents, one per line; `+--lang+` selects the language the messages are written in — English unless `+--lang+` or `+SOUTHER_LANG+` names another, with Japanese available; the machine's locale never selects it, and the code is the same string in every language. The quoted line and caret are shown for a single-file compile.

[#e1001-removed]
=== (retired)

E1001 is retired. Construction is guarded by the declaration and not by visibility: what a behavior may build is what `+constructs+` names, so building outside that authority is E1002 and naming more than is built is E1006. On the JVM the factory a data generates is `+protected+`, so there is nothing for a direct construction to call (<<jvm-construction-privacy>>).

[#e1002]
=== Insufficient construction authority

[,text]
----
Behavior `changeEmail` constructs `Member` but does not declare `constructs Member`.
Hint: Add `constructs Member` to `changeEmail`, or stop constructing Member here.
----

This check works only when `+constructs+` is *written* (<<constructs>>). If written, the declaration MUST exactly match the construction set, and constructing something omitted from the declaration is E1002. If omitted, it is inferred and this check does not run. The subjects are both what the behavior constructs directly and what a called helper `+let+` constructs transitively (<<blocks>>).

[#e1006]
=== Over-declaration of construction authority

[,text]
----
Behavior `checkCeiling` declares `constructs Amount` but never builds Amount; it passes an existing value through.
Hint: Remove `Amount` from the `constructs` clause.
----

If `+constructs+` is written, it MUST NOT list something not constructed. Paired with E1002 (too few), it keeps the declaration exactly matching the construction set, so the declaration correctly reads "creates new / passes through" (<<constructs>>). Same position as the `+depends on+` over-declaration E1603.

[#e1026]
=== A unit data named in constructs

[,text]
----
Behavior `find` names `Missing` in `constructs`, but a unit data is not part of a construction set.
Hint: Remove `Missing` from the `constructs` clause.
----

A unit data is constructed and carries no construction authority, so it is in no behavior's construction set and is named in no clause (<<constructs-excludes-unit-data>>). Distinct from E1006, which says the body never built the data: here the body may well build it, and the entry is still wrong. Keeping the two apart is what lets E1006 go on meaning "declared but never built".

[#e1003-removed]
=== (retired)

E1003 is retired. It required a violation case on a behavior constructing an invariant-bearing data, but a violation inside the domain aborts, so no slot is needed in the output (<<algebraic-types>>, <<violation-destination>>).

[#e1004]
=== Spread field-name collision

[,text]
----
Field `submittedAt` from `...TravelRequestCommon` conflicts with a field of `Submitted`.
----

[#e1005]
=== Missing field in a construction

Reported where a construction finishes with a field of the constructed type left without a value (<<construction-supplies-every-field>>). A field may be written out, filled from a binding of its name, or supplied by a spread. The hint follows how the construction was written: nothing was spread, or something was and what it copies from does not declare the field, or a sum was and the field is outside the part its cases share.

Nothing was spread, so the field had to be written and was not:

[,text]
----
Construction of `PreApproved` is missing field `preApprover`.
Hint: Give `preApprover` a value: write `preApprover = …`, or write `preApprover` alone to fill
it from a binding of that name.
----

Something was spread, and what it copies from does not declare the field:

[,text]
----
Construction of `PreApproved` is missing field `preApprover`.
Hint: Supply `preApprover` explicitly; the spread does not provide it.
----

A spread of a sum copies the part every case of it spreads (<<sum-data>>), so a field outside that part is missing however plainly the cases declare it:

[,text]
----
Construction of `PreApproved` is missing field `preApprover`.
Hint: `preApprover` is not in the part every case of `Request` spreads, so the spread does not
provide it. Supply it explicitly, or open `Request` with `match` and read it from the case that
has it.
----

[#e1007]
=== A newtype wraps something with no external representation

[,text]
----
A newtype takes the external representation of what it wraps, and a tuple has none. Wrap a named
data instead.
----

Reported where a `+data X = Y+` names a `+Y+` carrying no external representation of its own (<<newtype-wraps-an-external-representation>>) — a tuple or a function type. A newtype's decoder and encoder are derived from the wrapped type's (<<newtype>>), so there would be nothing to derive them from.

[#e1008]
=== A data is written with an empty body

[,text]
----
data `Rank` has an empty body, which names a type with one value — that is a unit data, and it is
written without a body.
----

Reported at a `+data+` whose body is written and holds nothing (<<unit-data-is-written-without-a-body>>). The declaration means what a unit data means, and a unit data has one spelling.

[#e1009]
=== A newtype wraps an optional

[,text]
----
`Approver` wraps `EmployeeId?`, but a newtype wraps one value and takes its representation, so there
is nothing for it to be when the value is absent.
----

Reported at a `+data X = Y?+` (<<newtype-wraps-a-value-that-is-always-there>>). The absence belongs to the field that holds the newtype — `+approver: Approver?+` — where a reader can see it; inside the newtype it would be a value that is sometimes not one.

[#e1010]
=== A case declares the discriminator as a field

[,text]
----
`Manager` is a case of `Rank` and declares a field `type`, which is the discriminator its sum writes
beside the fields.
----

Reported at the field (<<a-case-does-not-declare-the-discriminator-field>>). A product case lays its fields where the tag goes, so the two would want one key and whichever was written second would be what a reader got back.

[#e1011]
=== A name is declared twice

[,text]
----
data `Amount` is already defined.
----

Reported at the second declaration (<<a-declaration-is-made-once>>): a `+let+`, a data, a field of one data, an entry written twice in one clause list.

[#e1012]
=== A `let` and a data share a spelling

[,text]
----
`let Amount` and data `Amount` are one name where a value is written.
----

Reported where a `+let+` and a data of one module carry one name (<<a-spelling-answers-one-thing-where-a-value-is-written>>). A data is written where a value goes, so the two would be one spelling with two answers. A behavior and its implementing `+let+` are not this: they are one thing's declaration and its body.

[#e1013]
=== A data cannot be constructed at all

[,text]
----
data `OrgMember` cannot be constructed: it needs a value of itself, with nothing to bottom out.

data `Discount` cannot be constructed: its rules cannot all hold, so no value satisfies them.

data `Pair` cannot be constructed: `value` must hold more values that differ than there are of what
it holds, which is at most 1.

data `Window` cannot be constructed: the rules leave `value` nothing to hold.
----

Reported at the declaration (<<a-data-can-be-constructed>>). One code for every way a data comes to have no value, and the sentence says which of them was shown. Self-reference is what an org chart or a tree is made of, and it bottoms out at an optional, or at a collection the rules on it let be empty (<<self-referential-data>>); with neither on the cycle there is no first value.

[#e1014]
=== A construction writes a field the type does not have

[,text]
----
`preApprover` is not a field of `Submitted`.
----

Reported at the field (<<a-construction-names-fields-of-its-type>>).

[#e1015]
=== A spread has no product to copy

[,text]
----
A spread `...request` copies the fields the cases of `TravelRequest` share, and they share none: no
data is spread by every case.
----

Reported at the spread (<<a-spread-copies-a-product>>). A spread is nominal (<<field-spread>>): what it copies is the data every case spread, and a sum whose cases share no such data has nothing for it to name.

[#e1016]
=== A spread provides a field with the wrong type

[,text]
----
The spread provides `estimatedCost` as Int, but `PreApproved` needs Amount.
----

Reported at the spread (<<a-spread-supplies-what-the-fields-need>>). Fields are copied by name, and a name is not a type: two fields spelled alike and typed differently are two fields.

[#e1017]
=== A construction is written where one may not be

[,text]
----
`Amount` is a type, so `Amount(...)` constructs one, and a construction cannot be written here.
----

Reported at the construction (<<a-construction-is-written-where-one-may-be>>). An `+invariant+` states a condition over a value it is given (<<invariant-expressions>>); building one inside it would run a check inside a check.

[#e1018]
=== A construction names something that is not a data

[,text]
----
I cannot construct `submit` here.
----

Reported at the name (<<a-construction-names-a-data>>).

[#e1019]
=== A binding takes a built-in's name

[,text]
----
`unreachable` is a built-in value and cannot be used as a binding name — it would shadow the
built-in; choose another name.
----

Reported at the binding (<<a-binding-does-not-shadow-a-builtin>>). Shadowing a model's own name is ordinary and harmless — the outer one is still there to read — and a built-in has no declaration to read instead.

[#e1020]
=== A sum names a case that is not declared

[,text]
----
Unknown case `Withdrawn` in sum `TravelRequest`.
----

Reported at the case (<<a-sum-case-is-a-declared-data>>).

[#e1021]
=== A sum contains itself

[,text]
----
Sum `TravelRequest` contains itself through `Submitted`. A sum's cases are the values it can be, so
one of them cannot be the sum itself.
----

Reported at the declaration (<<a-sum-does-not-contain-itself>>), naming the chain that closes. Self-reference through a *field* is how a tree is written and is unaffected (<<self-referential-data>>); this is about the cases.

[#e1022]
=== A value reaches itself

[,text]
----
Value `ceiling` reaches itself: ceiling -> limit -> ceiling.
----

Reported wherever the cycle closes (<<a-value-does-not-reach-itself>>). A value with no parameter list is substituted where it is named, so a cycle among them has nothing to substitute first.

[#e1023]
=== A name is not in scope

[,text]
----
I cannot find a value named `estimatdCost` in scope.
----

Reported at the name (<<a-name-is-in-scope>>), with the nearest name in scope suggested. A value, a type and a behavior each say which of the three was being looked for, because that is what narrows where to look.

[#e1024]
=== A name is held as a value and is not one

[,text]
----
`submit` is a behavior, and cannot be held as a value here.
----

Reported at the name (<<a-name-held-as-a-value-is-a-value>>). A behavior, a module qualifier and a type are names of other things; writing one where a value goes names the thing rather than a value of it.

[#e1025]
=== A standard-library function is written bare

[,text]
----
`map` is a standard-library function and must be called qualified, as `List.map`.
----

Reported at the name (<<a-standard-library-function-is-called-qualified>>). The qualifier is what reaches the library, and it is also what tells `+List.length+` from `+String.length+`.

[#e1101]
=== Invariant type mismatch

[,text]
----
An invariant expression must have type Bool, but this is String.
----

[#e1102]
=== An invariant is declared on a type with nothing to observe

[,text]
----
`Staff` is a unit data, so it has no field for this invariant to observe and nothing it could
reject. Drop the clause, or give `Staff` the field the constraint is about.
----

Reported where an `+invariant+` is written on a unit data (<<invariant-needs-a-value-to-constrain>>). A unit data has one value, so a clause over it is true of that value or true of nothing, and neither is a constraint.

[#e1103]
=== Two invariant clauses share a name

[,text]
----
`Amount` declares two invariant clauses named `inYen`. A name is what an attempted construction and
a boundary issue call one rule, so two rules cannot share it.
----

Reported at the second clause of a name (<<invariant-clause-names-are-distinct>>). An attempted construction answers a clause by name (<<attempt-departures>>) and a decode failure reports one by name; neither can say which of two was meant.

[#e1104]
=== An invariant clause is named `_`

[,text]
----
`_` cannot name an invariant clause of `Amount`.
Hint: Give the clause a name of its own, or leave it unnamed — an unnamed clause is enforced and
answered by `| _ -> ...`.
----

Reported where a clause is written `+invariant _ = …+` (<<invariant-clause-name-is-not-underscore>>). `+| _ -> …+` answers the clauses carrying no name, so a clause holding that name as its own could never be the one it answered.

[#e1105]
=== An invariant constructs a data

[,text]
----
The invariant of `QuoteLines` constructs `Line`, but an invariant may not construct a data — it
observes the value being built, it does not build one.
----

Reported at the construction (<<an-invariant-observes-and-does-not-build>>), naming the clause it is written in or reached from. Building inside a check would run that type's own check inside this one, so whether this value holds would depend on whether another one did.

[#e1106]
=== An invariant does not answer on every path

[,text]
----
The invariant of `Amount` reaches the `partial` helper `spin`, which carries no termination
guarantee; an invariant is checked on every construction.
----

Reported at the clause (<<an-invariant-answers-on-every-path>>). An invariant runs at every construction and at every boundary, so a path through it that answers nothing — an `+unreachable+`, or a `+partial+` helper that may not stop — is a check that may not come back.

[#e1107]
=== An invariant is declared where no construction would owe it

[,text]
----
`Stage` is a sum, and nothing constructs one — a value of `Stage` is written as one of its cases —
so this invariant would be owed by no construction and checked at no boundary.
----

Reported at the clause (<<an-invariant-is-declared-where-a-construction-owes-it>>). Write the rule on the case it is about, or on a newtype over the sum, which is built and carries its own check. Refused rather than kept as a clause with no effect: a sum is lowered into a declaration with no place to put one, so a clause allowed through here is dropped along with anything written inside it, and a reading that asks which rules a position has cannot tell it from a declaration nobody wrote a rule on.

[#e1201]
=== Non-exhaustive match

[,text]
----
This `match` on TravelRequest does not cover every case.
Hint: Add a case for: PreApproved
----

[#e1202]
=== `match` is written on something that is not a sum

[,text]
----
`match` needs a sum-typed value, but got Int.
----

Reported at the subject (<<a-match-subject-is-a-sum>>). A primitive is told apart by comparing it, not by naming its cases (<<primitive-arm>>).

[#e1203]
=== A `match` arm names a case the subject does not have

[,text]
----
`Drafting` is a case of `TravelRequest`, so this arm belongs to a match over `TravelRequest`.
----

Reported at the arm (<<a-match-arm-names-a-case-of-the-subject>>). Where the case belongs to another sum the message says which, since an arm in the wrong `+match+` reads exactly like a misspelling and is not one. Where a nested `+match+` has swallowed the arms that follow it on the line, it says that instead, that being how the arm arrived under the wrong subject.

[#e1204]
=== Two arms match one case

[,text]
----
`Submitted` is matched by more than one case.
----

Reported at the second arm (<<no-two-arms-match-one-case>>). The first one answers it, so this one cannot be reached.

[#e1205]
=== A `match` has no arms

[,text]
----
This `match` has no cases.
----

Reported at the `+match+` (<<a-match-has-arms>>).

[#e1206]
=== A pattern opens something that has nothing to open

[,text]
----
`Amount` wraps `Int`, not `String`.
----

Reported at the pattern (<<a-pattern-opens-what-the-value-is>>), in a `+match+` arm or in a binding. Three ways to be wrong about it: what is opened is not a newtype and holds no wrapped value, the type named inside is not what is wrapped, or the case has no payload to open at all.

[#e1207]
=== An or-pattern opens a value

[,text]
----
An or-pattern binds the sum type, so it cannot open a case's value with `(...)`.
----

Reported at the pattern (<<an-or-pattern-binds-the-sum-and-opens-nothing>>). What an or-pattern says is that these cases are handled the same way, and what they carry is what differs between them.

[#e1208]
=== The arms of a `match` disagree on a type

[,text]
----
The match branches disagree: Submitted vs Int.
----

Reported at the arm the arms stopped joining at (<<branches-agree-on-a-type>>). A `+match+` answers with one value, so its arms produce one type; a union of what they produce is written by declaring it.

An `+if+` names both of its branches, there being two of them and each with a type of its own. An `+else+` answering per clause (<<attempt-departures>>) has more, and joins them as a list joins its elements — so it is reported as a list is, at the arm the join was refused at, with the join of the branches before it stated rather than pointed at:

[,text]
----
This branch produces Int. The preceding branches produce Blue | Red.
----

[#e1301]
=== Use of null

[,text]
----
`null` is not part of the language. Use an optional field with `?`.
----

[#e1302-removed]
=== (retired)

E1302 is retired. `+.sou+` has no `+throw+` and no `+catch+` to reject: the rule that a failure is answered as a case of the output rather than raised is carried by the grammar, and a Java method named in an expression names nothing the language knows, so it is reported as a name that is not in scope (<<out-of-scope>>, <<e1023>>).

[#e1303]
=== Writing an option case

Reported where `+Some+` or `+None+` is written outside the one place an optional is made — a `+?+` field being given a value, where a plain value is wrapped and `+None+` is the empty one (<<optional>>). The two names fail for different reasons and are reported apart.

`+Some(...)+` is never a call, wherever it is written, because the wrap at the field is implicit:

[,text]
----
`Some(...)` is not a call: a value written into a `?` field is wrapped for you,
and that is the only place an optional is made.
Hint: Where a `?` field is being given a value, write the value on its own.
Elsewhere an optional is only read — a `?` field, `Map.get`, `List.find` — never made.
----

`+None+` is a value, and the error is that this position is not asking for one:

[,text]
----
`None` is the empty value of a `?` field, and nothing here is asking for one.
Hint: Where the model owns the absence, make it a case of its own sum. Absence of that
kind does not reach `List.filterMap`, whose step has to answer an optional: use
`List.flatMap` over a step answering a list of nought or one.
----

A pattern is unaffected by either: `+| Some v+` matches an optional the model was given.

[#e1307]
=== unreachable where the position states no type

[,text]
----
Nothing here says what this position holds, and `unreachable` answers no value to say it
with.
Hint: Write it where the type is stated — an arm beside one that answers a value, a
field, a declared return, an annotated binding.
----

Reported where an `+unreachable+` (<<unreachable>>) stands in a position that states no type and has no branch beside it that does — `+let n = unreachable "..."+`, or an arithmetic operand. No value is made there, but the code that would read one is still compiled, and it has no shape to read.

[#e1308]
=== `?` is written on a sum

[,text]
----
`?` marks a single type optional, but it follows a sum of 3 cases.
----

Reported where `+?+` follows a sum rather than one type (<<optional-marks-one-type>>). What a sum is missing is a case of its own; marking the whole optional says the absence belongs to whoever reads the field rather than to the model (<<optional>>).

[#e1310]
=== `unreachable` states no reason

[,text]
----
`unreachable` states why the point cannot be reached, as a string: `unreachable "twenty insured
years cannot precede the age of thirty"`.
----

Reported at an `+unreachable+` written with nothing after it (<<unreachable-states-its-reason>>). The reason is what a reader is shown when a row reaches the point (<<e1911>>), and it is the only thing the declaration says that the position does not.

[#e1311]
=== A value that cannot cross the boundary is written where one has to

[,text]
----
parameter `range` is a tuple; a tuple has no external representation and cannot cross the boundary,
so a behavior's input must be a named data.
----

Reported wherever a value with no external representation is written where one crosses (<<what-crosses-the-boundary-has-an-external-representation>>): a behavior's parameter or output, a data's field, or a type a codec would be derived from. Which value had none is what the message carries (<<what-has-no-external-representation>>), and the position it was written in is what the author changes.

[#e1312]
=== A behavior's parameter is written as an anonymous union

[,text]
----
parameter `outcome` has an anonymous union type `Allocated | OutOfStock`; a behavior's parameter must
name a single type — declare `data ... = Allocated | OutOfStock` and take that name.
----

Reported at the behavior's parameter (<<a-parameter-names-one-type>>). An anonymous union is the shape of one behavior's answer and introduces no name (<<union-member>>); an input is a type the caller names, so it takes a declared one. A helper's parameter is not a boundary and takes one (<<a-type-is-written-in-a-type-position>>).

[#e1313]
=== An optional stands in a behavior's boundary

[,text]
----
The output of `findMember` carries an optional (`Member?`).
----

Reported at the parameter, or at the behavior for its output (<<an-optional-does-not-stand-in-a-boundary>>). The message names the optional it found, which is the whole of the boundary when it is written as one and a part of it when a collection carries it — so what changes is the position that optional stands in, which is not always the behavior's own type. Absence at a boundary is part of the model's vocabulary: put it on the `+?+` field of the data that carries it, whose own decoder reads it, or name it as a case of a sum so the caller matches on a name rather than unwrapping. Where the whole output is the optional, that sum is written directly — `+-> Member | NotFound+`; a behavior's parameter takes a declared name instead (<<a-parameter-names-one-type>>).

[#e1314]
=== A Map that crosses the boundary has a key that cannot be written

[,text]
----
`inventory` crosses the boundary, so the Map it holds must be keyed by String, a temporal, an
enumeration, or a newtype over one of those.
----

Reported at the field, parameter or output holding the Map (<<a-map-that-crosses-is-keyed-by-a-text-key>>). The boundary writes a key as text and reads it back the same way, so a key with no text form is one nothing outside can address.

[#e1315]
=== A collection would have to compare something that cannot be compared

[,text]
----
This collection would have to compare (Int) -> Int as its key, and a function has no value to
compare.
----

Reported at the collection (<<a-collection-member-supports-equality>>). A `+Set+` asks whether two elements are the same and a `+Map+` whether two keys are; the message says which of the two asked, because that is what the author is holding.

[#e1316]
=== A collection type is written without its arguments

[,text]
----
`List` needs a type argument, e.g. `List<Int>`.
----

Reported at the type (<<a-generic-type-is-given-its-arguments>>). The bare qualifier names the module whose functions reach the collection (<<stdlib>>), which is why writing it where a type goes is not a shorthand for anything.

[#e1317]
=== A value does not have the type its position requires

[,text]
----
Field `estimatedCost` expects Amount, but got Int.
----

Reported at the value (<<a-value-has-the-type-its-position-requires>>). The rule most of the others in this range are a special case of, and what a diagnostic says where no narrower one applies; the positions it covers are the type positions (<<a-type-is-written-in-a-type-position>>).

[#e1318]
=== A list's elements do not all have one type

[,text]
----
This element has type Int. The preceding elements have type Amount.
Hint: Make every element the same type — widen them to a sum they are cases of, or convert the one that differs.
----

Reported at the element the elements stopped joining at (<<a-lists-elements-have-one-type>>). A list is `+List<T>+` for a single `+T+`; a sequence of several types is a list of a sum they are cases of. What that element conflicts with is the join of the elements before it. That is one element's type only where one element has been read, which is a fact about how long the list is and not about where the type came from — so it is stated rather than pointed at, and `+[1, "x", 2]+` and `+[1, 2, "x"]+` are reported the one way.

`{plus}{plus}` reaches the same rule from two written operands rather than from an accumulator, so each list is pointed at and the message names neither type:

[,text]
----
`++` joins two lists into one, so the elements of both must have one type.
----

[#e1319]
=== An operator is given types it is not defined for

[,text]
----
`++` joins two lists or two strings, but the operands are Int and String.
----

Reported at the operator or at the operand it is about (<<an-operator-takes-the-types-it-is-defined-for>>) — at the operator where the rule is about the pair, as comparing two unrelated types is.

[#e1320]
=== A tuple pattern does not match its tuple

[,text]
----
This pattern binds 2 name(s), but the tuple has 3 element(s).
----

Reported at the pattern (<<a-tuple-pattern-matches-its-tuple>>).

[#e1321]
=== A field is read off a value that does not have it

[,text]
----
I cannot read a field `submittedAt` on `TravelRequest`: a sum has no fields of its own, its cases
do, and which case this is is not known until it is opened.
----

Reported at the read (<<a-field-read-is-declared-by-the-value>>). A sum exposes a field only where every case spreads the data declaring it (<<sum-data>>), so the message says which of the two it is: no case has the field, or each case declares its own and nothing is shared.

[#e1322]
=== A temporal value is not written as a literal

[,text]
----
`2026-13-01` is not a Date. Write `Date("2026-07-01")` / `Time("09:00")` /
`DateTime("2026-07-01T09:00")` / `Instant("2026-07-01T09:00:00Z")`.

`2026-07-01T09:30:45.123` is finer than a second, and a `DateTime` holds no fraction of one. Write
it to the second, or use `Instant` where the sub-second reading is the point.

`2026-07-01T09:30:45+09:00` carries an offset. It names the same moment
`Instant("2026-07-01T00:30:45Z")` names, and that is how an `Instant` is written. A boundary reads
either.

`2026-06-30T23:59:60Z` names a leap second. The language has no moment for one, and reading it as
the second before would answer a different moment than the text says.
----

Reported at the value (<<a-temporal-value-is-written-as-a-literal>>). The text is spelled out so it can be read at compile time, which is what lets malformed text be refused here instead of at run time. The same code covers three more refusals, and they are not one kind of thing. A fraction of a second, and a leap second, are text saying something the type cannot hold — the first a reading finer than the type keeps (<<a-local-temporal-is-held-to-the-second>>), the second a moment the language does not have (<<a-leap-second-is-no-moment>>). A numeric offset is neither: it names exactly the moment the `+Z+` form of it names, and is refused only because a written value is written the way the value is written back (<<temporal-literal>>). A boundary reads the offset form and refuses the other two.

This code is about *writing* a temporal. Parts that name no moment are not this: they are values rather than text, so `+Date.fromParts+` answers `+NotADate+` at the position that built them (<<a-temporal-built-from-parts-is-refused-where-the-parts-name-none>>).

[#e1323]
=== A `String.matches` pattern is not a literal regular expression

[,text]
----
The pattern of `String.matches` must evaluate to a string at compile time, so it can be validated
there: write a string literal.
----

Reported at the pattern (<<a-matches-pattern-is-a-literal-regular-expression>>). A pattern computed at run time could not be checked at all, so writing it out is what makes the check possible; the same code says when what was written is not a valid expression.

[#e1324]
=== Newtype arithmetic does not follow the units

[,text]
----
`Amount` and `Rate` are newtypes over Int, and adding them would answer with neither.
----

Reported at the operator (<<newtype-arithmetic-follows-the-units>>). Which rule was broken — same-newtype addition, a product's units, a quotient's, a scalar, a reciprocal — is what an author needs and is not readable from the operand types, so the refusal names its own rule rather than reporting a type mismatch.

[#e1325]
=== A type the language declares stands in a behavior's boundary

[,text]
----
`divide` answers `DivisionByZero`, which the language declares rather than this model.
----

Reported at the parameter, or at the behavior for its output (<<a-boundary-carries-the-models-own-vocabulary>>). `+DivisionByZero+` and `+NotANumber+` say what `+Int.divide+` and `+String.toInt+` can answer, `+RoundingMode+` says what `+Decimal.round+` takes, and `+Raw+` is reserved; each is the language's account of one of its own operations. A behavior that has the same thing to report declares the case — `+data Undivided+`, answered as `+-> Int | Undivided+` — so that what the behavior publishes stays its own even when the language's account of itself changes. Reported wherever the name stands in what crosses, at any depth and as a member of an output union.

[#e1326]
=== An `unreachable` at a case the model admits

[,text]
----
`Off` can arrive at `f`, so this point is reachable.
Hint: `Off` is a case of `Flag`, and no rule refuses it here.
----

Reported at an `+unreachable+` in an arm of the first `+match+` a body reaches, on one of the behavior's own input positions, whose case the declarations reaching that position leave standing (<<unreachable-is-for-what-the-model-rules-out>>). Nothing stands between that fork and the caller, so the case is one a caller can supply and the point is one a row reaches — which is <<e1911>> for whoever writes it and an abort for whoever runs the model.

The diagnostic names three things, because acting on it takes all three: where the claim is written, which case at which position it is about, and the declaration that says the case can arrive. The answer is the one the rule above states — the arm answers a value, or that declaration changes.

Read from the body as it is emitted, so a claim written in a `+let+` this body called is judged here as well: what the claim says about this behavior's input is what it says wherever it was written.

[#e1327]
=== A branch the model's own rules make dead

[,text]
----
Nothing the model admits arrives at this branch, so no row can be written through it.
Hint: The conditions on the way here cannot all hold: line 11 holding, line 12 failing.
Hint: Take the branch out, or widen what reaches it.
----

Reported at the branch, as a warning (<<a-branch-the-rules-make-dead-is-said-so>>). What makes it dead is said with it: the conditions on the way and which way each went, the position whose values stop short of what the branch is written for, or the cases the rules refuse where the arm matches on them.

The same reading is what takes the branch's rows out of what the model is measured against, so a report never asks for a row through a branch it has already said nothing reaches. Where the reading cannot settle a branch, both halves leave it alone: the rows stay owed and nothing is said.

[#e1401-removed]
=== (retired)

E1401 is retired. It was raised where a name applied to arguments resolved to nothing, which is what E1023 says (<<e1023>>), and nothing established that such a name was on the JVM. `+java.time.LocalDate.now+`, which this section used as its example, is read from a root `+java+` that names no qualifier and resolves to nothing, so it was reported as an unknown name and not as this. That arbitrary Java calls are out of scope is a design decision and stays one (<<no-arbitrary-jvm-calls>>); it was never a thing a compile could find.

[#e1402]
=== A core form is written outside the core

[,text]
----
`intrinsic` is a core privilege: only a module in the reserved `souther` namespace declares one.
----

Reported where a module outside the reserved `+souther+` namespace writes `+intrinsic+`, `+private+`, a type variable, or an optional anywhere but on a data field (<<core-privileges-stay-in-the-core>>). Each is a way of writing the layer below the model, and one code says which one it was.

[#e1501]
=== Cyclic import

[,text]
----
Cyclic module dependency detected.
----

A module depends on every module it imports and on every module it names in a qualified reference (<<qualified-reference>>), so the cycle is reported whichever of the two closes it.

[#e1502]
=== A declaration takes a name the core has

[,text]
----
module `List` uses a name reserved for the standard-library qualifier `List` (as in `List.…` /
`import List ( … )`); pick another module name.
----

Reported where a module or a data takes a name the core holds (<<a-core-name-is-not-taken>>) — the reserved `+souther+` namespace, or a standard-library qualifier. The qualifier is the only spelling that reaches the library, so a declaration of that name does not shadow it in one place, it hides it everywhere.

[#e1503]
=== A module is declared in two places

[,text]
----
Module `example.employee` is compiled here and is also on the path.
----

Reported when two sources declare one module, or a compiled source declares one that is also on the path (<<a-module-is-declared-in-one-place>>). Which of the two an importer would read is not decided by the text, so neither is chosen.

[#e1504]
=== A module a compile reaches is not there

[,text]
----
Module `example.travel` needs `example.employee`, which is not on the path either.
----

Reported where a module a compile reaches is neither compiled here nor on the path (<<every-module-reached-is-on-the-path>>) — imported, written qualified, or needed by a module that is. A module the path does carry and this compiler cannot read is not this: it is on the path, this compile has heard of it, and what is wrong is the artifact (<<e1509>>).

[#e1505-removed]
=== A module on the path was compiled by another Souther

A module read from compiled output still has to have been built by a Souther that agrees with this one (<<a-published-module-agrees-with-this-compiler>>), and it is still refused where it was not. What changed is where the refusal is said. It is one of the ways an artifact cannot be read back, so it is reported as <<e1509>> with the disagreement said under it — a rule the publishing project broke, rather than the rule the importing author is shown as theirs.

[#e1506]
=== A name reached across a boundary is not declared there

[,text]
----
`Amount` is not defined in `example.employee`.
----

Reported where a name written on an `+import+` list or after a module qualifier is not one that module declares (<<a-reached-name-is-declared-by-its-module>>). The nearest name it does declare is suggested. Separate from <<e1507>>: this is a mistake about what is there, not about what is published.

[#e1507]
=== A name reached across a boundary is not exposed

[,text]
----
`Amount` is declared in `example.employee` but not exposed by it.
----

Reported where the name exists and the module keeps it (<<a-reached-name-is-exposed-by-its-module>>). What a module publishes is its own decision (<<exposed-surface>>), so the move is to expose it there rather than to reach past the decision from here.

[#e1508]
=== An imported name denotes more than one thing

[,text]
----
`Amount` is imported from both `example.employee` and `example.billing`.
----

Reported where a name an `+import+` brings in has more than one answer here (<<an-imported-name-denotes-one-thing>>): exposed by two modules, colliding with this module's own declaration, or an alias already in use. Reaching one of them qualified is what says which, and is why the answer is not to pick one for the author.

[#e1509]
=== A module on the path cannot be read back

[,text]
----
Module `example.employee` is on the class path, and this compiler cannot read what it published.
  It was built by Souther 0.4.0, which does not agree with this compiler about what an importing
  module may reach.
  Rebuild `example.employee` with this compiler, or compile against the version that built it.
----

Reported where a module the class path carries cannot be read back into the surface this compiler understands (<<a-published-module-can-be-read-back>>). What went wrong inside the artifact is said under the report: a class carrying metadata this compiler cannot read, a boundary revision that does not agree (<<a-published-module-agrees-with-this-compiler>>), declarations that name a different module from the one they were found under, a declaration whose class is not there (<<a-published-module-carries-what-it-says-it-declares>>), published text this compiler does not parse, an `+import+` line of the published surface it cannot read.

Said where the module was reached from — the `+import+` line in this project that arrives at it, and every other line that does, since a dependency two files import is reached by both. The artifact's own lines are not this compile's to quote, and a report carrying one sends an author to a place in a file they do not have.

One report and one code, whichever failure it was, because the author reading it has one thing to do about any of them. The rules broken are the publishing project's; naming one of them as the rule this author is in breach of hands them somebody else's obligation. A module refused here is still a module this compile has heard of, so an importer is not also told there is no such thing.

[#e1601-removed]
=== (retired)

E1601 is retired. A requirement is reconciled between the `+depends on+` clause and the body, so what it once covered is reported as the disagreement it is: a dependency the body uses and the clause omits is E1602, a clause naming what the body never calls is E1603, and a clause naming something that is no dependency at all is E1607.

[#e1801-removed]
=== (retired)

The `+error+` declaration and E1801 are retired. A failure is ordinary data, and whether it departs the mainline is decided by composition, so there is no distinction between a success position and a failure position (<<unmarked-sum>>, <<behavior>>, <<composition>>).

[#e1609]
=== `exposing` names something this module does not declare

[,text]
----
`exposing` names `Amount`, which is imported into this module, not defined here; `exposing` lists a
module's own definitions and does not re-export imported names.
----

Reported where `+exposing+` names something this module does not declare (<<exposing-lists-this-modules-own-definitions>>), whether that is nothing at all or a name it imported. Re-export is the second of those and is refused on purpose: one declaration behind two surfaces leaves a reader two modules to ask about it.

[#e1610]
=== `exposing` names a derived codec

[,text]
----
`exposing` is type-granular: a data's `decoder`/`encoder` are always public once the data is
exposed. Write `Amount`, not `Amount.decoder`.
----

Reported where `+exposing+` names a data's `+decoder+` or `+encoder+` (<<exposing-is-type-granular>>). They are how the type crosses a boundary, so a type that crosses one carries them; there is no state in which the data is published and they are not.

[#e1611]
=== An exposed signature names a type the module keeps

[,text]
----
Behavior `submit` is exposed and its input names `Draft`, which this module does not expose.
----

Reported where an exposed behavior's input or output, or an exposed data's field, names a type this module keeps to itself (<<an-exposed-signature-names-only-exposed-types>>). A caller would be handed a value of a type it cannot name. An injection target says it differently, because there the caller is Java.

[#e1612-removed]
=== (retired)

E1612 is retired. It refused a behavior with no `+let+` that wrote a `+depends on+` clause, on the reading that a behavior with no `+let+` is an injection target. That reading answered two questions with one fact: whether Java supplies the body, and whether the body has been written yet. The clause tells them apart — a behavior that declares `+depends on+` takes those dependencies as arguments of a `+let+`, so it is Souther's to implement, and with no `+let+` it is one nobody has written (<<unwritten-behavior>>). What is refused now is resting on such a behavior from something that is built here (<<e1627>>).

[#e1613]
=== A union member cannot be named in an arm

[,text]
----
`(Int, Int)` cannot be a union member: a member is a type a `match` arm can name — one of the
primitives, or a data type declared here or imported.
----

Reported at the member (<<a-union-member-is-nameable-in-an-arm>>). A union is read by matching on it, so a member an arm cannot write is a case nothing can handle. The same code says when one spelling stands for two members' types, which leaves an arm naming one of them and no way to say which.

[#e1614]
=== A `let` implements a composition

[,text]
----
`let placeOrder` cannot implement the composition `behavior placeOrder`, which is already its own
implementation.
----

Reported at the `+let+` (<<a-composition-has-no-let>>). A `+>->+` is the body: its stages are what runs, and a second body would be a second answer to what the behavior does.

[#e1615]
=== An implementing `let` disagrees with its behavior

[,text]
----
`let submit` parameter `now` should be `clock`: what `depends on` names becomes the trailing
parameters, in declared order.
----

Reported at the `+let+` (<<an-implementing-let-takes-its-shape-from-the-behavior>>). The behavior states the shape — how many parameters, what they are, the order the injected ones arrive in, what it answers with — so the `+let+` writes none of them again, and the same code says whichever of the four was written or was wrong.

[#e1616]
=== An `ensures` clause does not name the answer

Reported at the rule when it refers to the answer neither through an arm nor through `+value+` (<<an-ensures-clause-names-the-answer>>).

[#e1617]
=== An `ensures` clause does not name a parameter

Reported at the clause when an arm expression names no parameter (<<an-ensures-clause-names-a-parameter>>).

[#e1618]
=== A behavior with `ensures` has a parameter named `value`

Reported at the parameter (<<a-behavior-with-a-clause-has-no-parameter-named-value>>).

[#e1619]
=== An `ensures` arm is not an output case

Reported at the arm name (<<an-ensures-arm-is-an-output-case>>).

[#e1620]
=== An `ensures` clause over a sum names no arm

Reported at the clause (<<an-ensures-clause-over-a-sum-names-arms>>).

[#e1621]
=== An `ensures` clause over one type names an arm

Reported at the clause (<<an-ensures-clause-over-a-single-type-names-no-arm>>).

[#e1622]
=== Two `ensures` clauses share one name

Reported at the second name (<<ensures-clause-names-are-distinct>>).

[#e1623]
=== `_` names an `ensures` clause

Reported at the name (<<an-ensures-clause-name-is-not-underscore>>).

[#e1624]
=== A composition carries `ensures`

Reported at the clause (<<a-composition-carries-no-ensures>>).

[#e1625-removed]
=== (retired)

E1625 is retired. It refused a case named twice under one arrow, on the reading that a test selecting one branch would leave the second naming unread. What a clause states is a conjunction — every rule whose guard holds applies — so the second naming states what the first did, which is redundant and not ambiguous. The two collapse to one rule (<<ensures>>).

[#e1626]
=== The model names a value an attached file declares

[,text]
----
`floor` is declared in an attached `examples for` file, which holds the rows, the fakes
they run against and the values those rows name. A declaration of the model cannot name
one: the module would not compile without that file, and a clause carrying the name
travels to an importer that has no source for it.
----

Emitted where a declaration of the model reaches a value an attached file declares (<<an-attached-files-values-are-for-its-rows>>). Two things follow from naming one and either is enough: the module no longer compiles from its own source, because the name it writes is declared in a file of fixtures; and a `+data+`'s `+invariant+` or a `+behavior+`'s `+ensures+` is published as the text that was written (<<a-published-module-carries-what-it-says-it-declares>>), so an importer reads a clause spelling a name it has no source for.

Said where the name is written, once for each occurrence, and the module is not emitted. The `+exposing+` list is reported at the module header, because a name in it is written nowhere else.

What is not reported is the other direction. A row names such a value wherever the row is written — beside the values in the attached file, or inline in the model source — because the values join the module the rows join and a row is what the file is for. A `+fake+` names one as a row does, and one attached value names another. An attached value naming the model's own `+let+` is a fixture reading the model, which is what a fixture is.

The fix is to declare the value in the module's own source. A value the model is written against is part of the model, and the attached file goes on reaching it from there.

[#e1627]
=== Something built here rests on a behavior nobody has written

[,text]
----
`quote` is built here and rests on `price`, which declares what it depends on and has no
`let`, so Souther is the one to implement it and nobody has.
----

Reported where the name is written — in a `+depends on+` clause, or as a `+>->+` stage (<<unwritten-behavior>>). The behavior naming it has a body, so it is built here and holds the named one as an injected field, and nothing can supply that field: Java has no base to extend, because the model says the body is Souther's, and Souther has not written it. Declaring the unwritten behavior is not an error, and neither is resting on it from another behavior that is itself unwritten.

[#e1701]
=== behavior composition type mismatch

[,text]
----
These behaviors cannot be composed: the left behavior's output is not accepted by the
right behavior's input.
Hint: Make the left output a case the right behavior accepts, or handle the extra cases
before this stage.
----

[#e1602]
=== Missing depends on declaration

[,text]
----
`let preApprove` calls `now`, which has no implementation, but `behavior preApprove`
does not declare `depends on now`.
Hint: Add `depends on now` to `behavior preApprove`.
----

[#e1603]
=== Over-declaration of depends on

[,text]
----
`behavior preApprove` declares `depends on numbering`, but `let preApprove` never calls it.
Hint: Remove `numbering` from the `depends on` clause.
----

It requires declaration and implementation to match, and does not admit an over-declaration erring on the safe side (<<depends-on>>).

[#e1604]
=== Composition output declaration and inference mismatch

[,text]
----
behavior placeOrder declares its output as PlacedOrder | OutOfStock, but the pipeline
produces PlacedOrder | OutOfStock | BeingReturned.
Hint: Update the declared output, or handle the extra case before the output.
----

A composition with a written declaration requires exact agreement with the inferred output (<<declared-composition-output>>). An internal composition with none written stays inferred and this check does not run. A public composition requires the declaration, reconciling the signature placed in `+exposing+` with inference (<<declared-composition-output>>, E1605).

[#e1605]
=== Missing / misplaced output signature of a public composition

[,text]
----
Exposed composition `placeOrder` must declare its output in `exposing`.
Hint: Write `exposing ( placeOrder : PlacedOrder | OutOfStock )`.
----

A `+>->+` composition exposed with `+exposing+` requires an output signature (<<declared-composition-output>>). Its absence is this error. Writing a signature in `+exposing+` for a non-composition behavior is also E1605, since the type is fixed at the definition.

[#e1606]
=== A declared sum's case is imported

[,text]
----
`OutOfStock` is declared in `inventory`, so it cannot be a case of `ShipmentResult`.
Hint: A sum's cases are declared with it: the generated sum is a sealed interface, and
only its own module can implement it. Consume `OutOfStock` at the boundary, or re-express
it as a case of this module.
----

A `+data S = A | B+` declares a type this module owns and publishes. Its cases are its vocabulary, and they are written with it. A case from an imported module cannot be one of them: the generated sum is a sealed interface, the class the other module emitted cannot implement it, and the case would be permitted without being a member — no value satisfies the type and an exhaustive `+switch+` over it has no arms.

An anonymous output union takes an imported member (<<union-member>>), and the difference is not the JVM. A named sum introduces a type name: it is written in a parameter, in a field, in another sum. An anonymous union introduces none — it is the shape of one behavior's answer and nobody names it. Taking another context's type into a shape is answering with the value that context owns; taking it into a declared sum is making it a word of this module's own vocabulary, and that is what a bounded context does not do.

The move for a sum is to translate: match the imported result and return a case of this module. A module boundary is where another context's vocabulary is restated in this one's.

[,souther]
----
// allocate is imported and OutOfStock is its departure; CannotAllocate is this module's answer to it
behavior allocateAndShip : (number: PartNumber) -> ShipmentOrder | CannotShip | CannotAllocate
    depends on instructShipment
    constructs CannotAllocate

let allocateAndShip (number, instructShipment) =
    match allocate(number) with
        | Allocated as allocation -> instructShipment(allocation)
        | OutOfStock as shortfall -> CannotAllocate { number = shortfall.number }
----

`+instructShipment+` is a behavior of this module and has a requirement of its own, so it is named in `+depends on+` and arrives bound (<<fn-rules>>): a composition builds its stages and their requirements accumulate into it, while a caller receives what it calls. An imported behavior is reached either way: it is called by name when its requirement set is empty, and named in `+depends on+` when the caller must supply what it requires.

[#e1607]
=== depends on does not name a dependency

[,text]
----
`behavior ship` declares `depends on allocate`, but `allocate` depends on nothing, so
there is nothing to inject.
Hint: Remove `allocate` from `depends on` and call it directly; a behavior that depends
on nothing is reached by name.
----

`+depends on+` names a behavior whose requirement set is not empty, declared here or imported (<<depends-on>>). A behavior that depends on nothing is E1607 — call it instead (<<calling-a-behavior>>) — and so is a `+>->+` composition, whose requirements are inferred from its stages rather than written, and so is a name that is no behavior in scope. All three are reported at the clause, where the name was written.

[#e1608]
=== A behavior reaches itself

[,text]
----
`behavior ship` reaches itself: ship -> allocate -> ship.
Hint: A behavior does not recurse, through calls, `depends on` or `>->` stages. Write the
recursion as a helper `let`, where it is proven total.
----

The cycle is reported wherever it closes, and the edges may be calls, `+depends on+`, `+>->+` stages, or a mixture of them (<<calling-a-behavior>>). A `+depends on+` cycle leaves nothing to build first, and a call cycle does not terminate. Recursion belongs to a named module helper, where it is proven total (<<fn-rules>>).

[#e1305]
=== An injected behavior constructs an uncreatable data

[,text]
----
Injected behavior `findMember` declares `constructs StoredData`, but StoredData is
not exposed, so Java cannot build it.
Hint: Expose StoredData.
----

Emitted when an unexposed data is listed in the `+constructs+` of an injection-subject behavior (implemented from Java; <<java-base-class>>), because Java would have no way to create that value.

Exposing it is the whole of the hint. Making it a unit was the other way out while a unit could be named here; a unit is in no construction set now (<<constructs-excludes-unit-data>>), so taking that advice would answer this diagnostic with E1026 on the same entry. A unit case of the output needs neither: the base class carries its factory whatever `+exposing+` says.

[#e1702]
=== A pipeline stage takes more than one input

[,text]
----
`convert` takes 2 inputs, so it cannot follow `>->` in `placeOrder`. Every stage after the first
takes one input: call it inline, or give it a data holding both.
----

Reported at the stage (<<a-stage-after-the-first-takes-one-input>>). What reaches a stage is the one value the stage before answered with, so there is nothing to fill a second parameter from.

[#e1703]
=== A pipeline names a boundary edge as a stage

[,text]
----
decode/encode are boundary edges, not pipeline stages; `>->` composes behaviors only.
----

Reported at the stage (<<a-pipeline-composes-behaviors>>). The decoder and the encoder are where a pipeline starts and ends (<<decoder>>, <<encoder>>) and are not steps within it.

[#e1802]
=== Something is applied to the wrong number of arguments

[,text]
----
Helper `let depth` takes 2 argument(s), but is called with 1.
----

Reported at the application (<<an-application-gives-the-arguments-it-takes>>). It covers a helper, a newtype applied to more than the one value it wraps, a lambda, and a block whose parameter list does not match the position it was written in.

[#e1803]
=== Something that is not a function is applied

[,text]
----
`unreachable` is a name the language gives, not a function, so it cannot be applied to arguments.
----

Reported at the application (<<what-is-applied-is-a-function>>).

[#e1804]
=== A function is written where a value goes, or the other way round

[,text]
----
Argument 2 of `List.fold` is `seed`, which does not take a function. The function goes to argument 1
(`step`).
----

Reported at the argument (<<a-function-goes-where-a-function-is-taken>>). Which argument takes the function belongs to the function being called, so the message names the one that does rather than describing the shape.

[#e1805]
=== A block does not answer what its position takes

[,text]
----
A fold's block must return the accumulator type Amount, but got Int.
----

Reported at the expression that supplies the block's result (<<a-block-answers-the-type-its-position-takes>>): a `+fold+`'s accumulator, a predicate's `+Bool+`, the element type of what is being grown. A block's own position is where its parameters start, and the value being refused came from its body, so the report is at the body's answer — past any `+let+` written above it, which holds what it binds and not what the block answered. An `+if+` or a `+match+` the body ends in is that answer: its type is the one its arms join to, so no single arm supplied it.

[#e1806]
=== An argument does not have the type its parameter takes

[,text]
----
`List.map`'s element type Amount is not acceptable to the function, which takes Int.
----

Reported at the argument (<<an-argument-has-the-type-its-parameter-takes>>). Where the argument is a `+fold+`'s step and the step is written as a block, it is reported at the expression that supplies the step's result, as <<e1805>> is. Where the step is a function declared elsewhere, nothing at this call made the value it answered with, and the argument is as far as the source goes.

[#e1807]
=== A function binding would need two types

[,text]
----
The function `f` is applied with different argument types: Int vs String.
----

Reported at the binding (<<a-function-binding-has-one-type>>). Applying it at two argument types, or giving it from two branches that produce different function types, leaves the binding with no single type; Souther has no generics to give it one (<<type-variables>>).

[#e1808]
=== A function binding's type cannot be read

[,text]
----
Cannot infer the type of the function `f`: write its type (`let f: (Int) -> Bool = ...`), or apply
it in this scope so its parameter types follow.
----

Reported at the binding (<<a-function-bindings-type-is-known>>).

[#e1809]
=== A block is returned or stored

[,text]
----
A block is not a value: it may be passed as an argument or bound to a `let` and applied, but it
cannot be returned or stored in a field.
----

Reported where a block leaves the scope it was written in (<<a-block-is-not-a-value>>). A function has no external representation (<<external-representation>>), so a field holding one could not be decoded or encoded and an output holding one could not cross out.

[#e1810]
=== An annotation on a function binding is not a function type

[,text]
----
The binding `step` holds a function, and its annotation is not a function type.
----

Reported at the annotation (<<an-annotation-on-a-function-binding-is-a-function-type>>). A function type in that annotation is admitted, as it is in every type position a representation is not required at (<<a-type-is-written-in-a-type-position>>); what this reports is an annotation that describes something else.

[#e1811]
=== A helper parameter has no type

[,text]
----
Helper `let total` parameter `line` is only read through a field, which names no type; annotate it
with its type.
----

Reported at the parameter (<<a-helper-parameter-states-its-type>>). Reading a field off a parameter, or applying it, does not name a type — several types have a field of that name — so the annotation is what says which.

[#e1812]
=== A helper does not answer what it declares

[,text]
----
Helper `let total` declares it returns Amount, but its body is Int.
----

Reported at the helper (<<a-helper-answers-what-it-declares>>).

[#e1813]
=== A recursive helper declares no return type

[,text]
----
Recursive helper `let depth` must declare its return type (`let depth (...) : <type> = ...`),
because its result cannot be inferred from a body that names itself.
----

Reported at the helper (<<a-recursive-helper-declares-its-return-type>>).

[#e1814]
=== A recursive helper reaches an injected behavior

[,text]
----
Recursive helper `let depth` is pure and cannot call the injected behavior `now` — put the effect in
the behavior that calls this helper.
----

Reported at the call (<<a-recursive-helper-does-not-reach-an-injected-behavior>>). The helper is emitted as a pure static method (<<fn-rules>>), which is what lets its termination be proven and what keeps the effect where a reader of the behavior can see it. A function argument that reaches an effect is not this: the effect is the caller's and is declared there.

[#e1815]
=== An empty collection's type cannot be read where it is written

[,text]
----
This empty collection's element type cannot be inferred here.
Hint: Annotate the binding it feeds (`let x: <type> = ...`), place the fold directly in a typed
position — the field or behavior output it feeds — or seed it with a non-empty value.
----

Reported at the operation (<<an-empty-collections-element-type-is-determined>>). What an empty collection holds does not follow from the collection, so it follows from the position it is written in or from nothing at all.

[#e1816]
=== What is ordered by is not an ordered value

[,text]
----
`sort` needs an ordered element, but the element is Member.
----

Reported at the call (<<what-is-ordered-by-is-an-ordered-value>>). Not a type mismatch: `+sort+`, `+max+`, `+min+` declare `+List<'a>+` and the argument fits, and `+sortBy+`'s key function fits its declared signature too. What is missing is a capability the signature had no way to ask for, Souther having no `+comparable+` type class (<<stdlib>>), so the compiler asks it beside the signature. For `+sortBy+` it is asked of what the key answers with, that being the value being ordered by.

[#e1817]
=== What is summed is not a number

[,text]
----
`sum` needs an Int or a Decimal element, but the element is String.
----

Reported at the call (<<what-is-summed-is-a-number>>). The same shape as <<e1816>> and a different obligation: `+sum+` and `+product+` declare `+List<'a>+`, and what `+'a+` has to be is one of the two types arithmetic is defined for.

[#e1818]
=== A behavior is called from somewhere that does not reach one

[,text]
----
`allocate` is a behavior, and it cannot be called from here.
Hint: Call it from the behavior's own `let` and pass the result in, or name it in `depends on`.
----

Reported at the call (<<a-helper-does-not-call-a-behavior>>). A helper is expanded into whatever calls it and travels to an importing module as source, so what it may name is what travels with it; a `+>->+` composition is composed with a behavior rather than calling one. Not <<e1023>>, which is a name that resolves to nothing; this one resolves, to a behavior.

[#e1901]
=== Example target is not a behavior

[,text]
----
`noSuchThing` is not a behavior in this module, so this example has no target.
----

Emitted when an `+example+` names a target that is not a behavior of the module (<<examples>>).

[#e1902]
=== Example target is not evaluable

[,text]
----
`taxIncluded` cannot be evaluated as an example.
Hint: An example runs a behavior with a `let` body, or a `>->` composition of them; here
it is a helper `let`, and this module declares no behavior of that name.
----

Emitted when an `+example+` names something this module declares that is not a behavior — a helper `+let+` (<<fn-declaration>>). Being injected is not a reason: an injected behavior is a behavior, and its rows are recorded (<<example-pending>>). Neither is a `+depends on+` dependency, which is supplied by a fake (<<example-fakes>>), nor a `+>->+` composition, which is evaluated by running its stages.

[#e1903]
=== Example input cannot be built

[,text]
----
Input #1 of `submitTravelRequest` could not be built: value must be >= 0
----

Emitted when a fixture of the row itself did not produce a value when it ran: a wrong arity, a value that breaks the type's invariant, an operation that answers no value for what it was given. What a row writes is compiled as this module's code, so a value that does not state a value of its position at all is refused where the same value written in a body is (<<e1812>>, <<e1317>>, <<fixture-is-written-not-carried>>, <<fixture-names-what-it-supplies>>) and never reaches here. The expected result is built before the behavior is applied, so a row whose expectation does not produce a value is reported here rather than as a mismatch (<<e1905>>) against a value nothing built. A fixture standing in for a dependency is that dependency's, and is <<e1908>>.

[#e1904]
=== Expected arm is not a result case

[,text]
----
`Drafting` is not one of the result cases of `submitTravelRequest`.
Hint: Expected one of: Submitted, AwaitingPreApproval
----

Emitted when a row's expected arm is not one of the target behavior's output cases (<<example-expected>>). An expectation whose case is not written — a helper's answer — has no arm to hold against them and is compared as the value it is; a row expecting the wrong case is then <<e1905>>.

[#e1905]
=== Example does not hold

[,text]
----
This example does not hold.
found:    Submitted { submittedAt = "2026-07-20T09:00" }
expected: AwaitingPreApproval { submittedAt = "2026-07-20T09:00" }
----

Emitted when a row's evaluated result does not match the expected arm or value. Every failing row of a build is reported. Both sides are shown as values, encoded through the data's derived Encoder (<<encoder>>) — the arm name alone does not say what went wrong when a row fails inside one arm. A row that asserts only the arm (a bare case name, <<example-expected>>) shows just that name.

[#e1906]
=== An examples file contains non-example declarations

[,text]
----
An `examples for` file holds examples, the fakes they run against and the values they
name. A data, a behavior, an import, and a `let` with parameters belong in the module
itself.
----

Emitted when a file that begins with `+examples for+` holds something other than an example, a `+fake+` table, or a value its rows name — and when a value it declares carries a name the target module already declares (<<example-placement>>). The second is reported on the attached file, where the declaration is written.

[#e1907]
=== Examples file names an absent module

[,text]
----
`examples for example.nope` names a module that is not being compiled.
----

Emitted when an attached `+examples for <module>+` file names a target module that is not among the compiled modules (<<example-placement>>).

[#e1908]
=== A dependency has no fake

[,text]
----
`preApprove` depends on `now`, but no fake was supplied for it.
Hint: Add `with now = ...` on the row, or a `fake now` table.
----

Emitted when an example targets a behavior whose `+depends on+` dependency has neither a `+with+` on the row nor a `+fake+` declaration (<<example-fakes>>). A dependency that *is* faked but whose fake cannot be built — a `+with+` value, or a row of its `+fake+` table — is reported under the same code and says that instead, naming the dependency once: the fake is supplied, and what failed is the fixture.

Not being a value of what the dependency answers with is one of the ways it fails, and is said here rather than at the row: a stand-in is the dependency's answer while the row runs, so a value of another type is not an answer the row can disagree with (<<e1905>>). It is refused before the behavior is applied, so no row reaches the model holding one.

The two are quoted where each is written. A `+with+` is written on a row, so the row it is written on is where its value not building is said; a `+fake+` table is written on its own, so that is where a row of it not building is said — once for the table, whether or not any row reads it (<<example-fakes>>).

[#e1909]
=== A fake has no output for an input

[,text]
----
A fake had no output for an input: `findManager` has no output for EmployeeId("e-9")
----

Emitted when a `+fake+` table is asked for an input it does not list and has no `+_+` default row (<<example-fakes>>).

[#e1910]
=== An example exceeded its evaluation budget

[,text]
----
The evaluation of this row did not finish within its budget of 100000000 steps.
Hint: What the counting shows is that the evaluation did not finish, not that the model
does not: a `partial` recursion that never stops reaches this, and so does one that would
have stopped shortly after. If the model does finish and needs more, raise
`souther.example.step.limit`; otherwise bound the loop or make the recursion structural.
----

Emitted when a row's evaluation exceeds the budget the example runner holds it to: the number of counted points the evaluated code may pass, or the depth a recursion it reaches may go to. Both are counted by the generated code as it runs, so the reading is a property of what the row does and answers the same on every machine.

What this establishes is that the evaluation did not finish within the budget, and not that the model does not terminate. Those are different statements and a finite budget cannot reach the second: a recursion that never stops exceeds the budget, and so does one that would have stopped a hundred steps later. The usual cause is a `+partial+` declaration the row reaches, which publishes no termination guarantee (<<fn-rules>>) — but a model that does terminate and needs more than the budget allows reaches it too, and raising the setting is then the answer. A helper carrying the termination guarantee is not exempt: it is bounded, not free.

The guarantee is about this diagnostic and not about compilation as a whole. Whether a row is reported here does not depend on how fast the host is, how loaded it is, what the JVM has compiled, or what any row before it did. Whether the compiler manages to answer at all is a separate question: work that is not counted — loading a class, decoding a value through a derived decoder, comparing two of them — still takes time, and an evaluation that stops answering within the wait it is given is reported as <<e1923>>. That work is bounded by what the counted work produced, since a value only gets large by being built, but it is bounded rather than counted, so it is measured in time.

Two other things end an evaluation, and neither is decided by what the model says. Where it stops answering, that is <<e1923>>; where the JVM stack runs out before the counted depth limit is reached, <<e1924>>.

[#e1911]
=== An example reached a point declared unreachable

[,text]
----
This row reached a point the model declares cannot arise: twenty insured years cannot
precede the age of thirty (42:17)
Hint: Either the row states a combination the model rules out, or the reason written at
that `unreachable` does not hold.
----

Emitted when a row's evaluation reaches an `+unreachable+` (<<unreachable>>). The reason the model wrote is shown with the line and column it was written at, because the row and the declaration are usually in different rules; the diagnostic's own place is the row's. It is not a failed comparison: the row produced no answer to compare, and what it found is a premise that does not hold.

[#e1913]
=== No row expects a case the signature declares

[,text]
----
No row expects `Waiting`, so nothing says `submit` owes that answer.
Hint: Write a row whose expected value is a `Waiting`, or drop the case from the
signature.
----

A warning, off unless the build asks for it (<<example-adequacy>>). Emitted at the behavior's declaration when its output is a sum and no row expects one of its cases. Only where the measurement was complete: a row whose expected case could not be read leaves the question undecided, and telling an author to write a row they may already have written is worse than saying nothing.

[#e1915]
=== No row uses a case an input declares

[,text]
----
No row applies `submit` to a `Overseas`, so input #1 has a case nothing was tried with.
----

The same warning for an input position that is a sum. Covering every case an input can be is the mechanised half of asking whether the behavior is total (<<match>>).

Not warned about for a case the model's own rules refuse (<<a-case-the-rules-refuse-is-not-covered-or-uncovered>>): the row it asks for could not be built (<<e1903>>), so an author told to write one has been handed work their compiler refuses.

[#e1916]
=== No row is at a boundary

[,text]
----
No row is at the ON point submit/cost = 100 of the border the `guard` draws.
Hint: A row at the nearest value the border keeps in is what shows the border does not fall a step further in.
----

A warning, and one that needs the arms measured where the line was drawn by a fork of the body (<<example-branch-coverage>>): meeting such a line takes reaching the comparison, not writing the value. A line an invariant drew is decided without that. The rule that drew it is named, because the same value can be a border of several and reaching it through one leaves the others unmet.

Said of the two points against the line and of neither of the two away from it (<<a-border-owes-four-points>>). Those two are what simplified domain coverage asks for and what a row on the line shows that no other row does; the points inside and outside are the two items reliable domain coverage adds, and a build held to that criterion is told about them under <<e1917>>. Which of the two criteria a build is held to is what it asked for (<<example-adequacy>>), and the report still names neither as satisfied.

Which of the two it is, in the word the report gives it (<<a-border-says-which-point-it-owes>>). One finding is told on two surfaces, and a reader handed this vocabulary by the report was working it out again here. `+ON+` and `+OFF+` are the technique's terms rather than English words for it, so they are the same in every catalog and what a catalog holds is the sentence around them. The rule is named as the one that drew the border and not as one drawing its line here: only one of the two points carries the value the rule was written with, so the other would be told the line is where it is not. The hint follows the point and is said in the same words — what a row at the nearest value the border keeps in shows, and what one at the nearest value it keeps out shows. Not which side of the line the value falls on: which of the two points carries the line's own value turns on the border being closed or open (<<a-border-owes-four-points>>), so a hint keyed on that would be a second reading of one finding under a sentence that has just named the role. What tells `+<=+` from `+<+` is the border being closed or open, which the report says of the border.

The fork is named by the construct it was written as — an `+if+`, a `+guard+`, or a guard comprehension (<<stdlib-list>>) — and where it is written is pointed at rather than said, a place being something only a reader's own files give a name to. All three lower to one form before anything runs, so the word comes from where the source was read and not from the shape it was lowered to.

[#e1917]
=== No row is at a point away from a boundary

[,text]
----
No row is at an IN point of submit/cost, 50, away from the border the `guard` draws.
Hint: A row well inside is what shows the border, and not something else about the value, is
what divides the two.
----

[[every-border-has-a-row-away-from-its-line]]A warning, and only to a build held to reliable domain coverage. The same assessment of the same border answers this and <<e1916>>; what differs is which of the four points a build is asked for (<<a-border-owes-four-points>>). A build that asked for simplified domain coverage is not told this, and the points are still reported.

Told apart from <<e1916>> by a code of its own rather than by the role in the sentence. One code covering both would be a build asking to be held to the first criterion and refused over the second's items.

[#e1918]
=== No row goes through an arm

[,text]
----
No row goes through the `else` arm in `submit`.
Hint: Either a row is missing for what this arm answers, or nothing reaches it — which an
`unreachable` states.
----

A warning, needing the arms measured (<<example-branch-coverage>>). Quoted at the `+if+`, `+match+` or attempted construction the arm belongs to rather than at the arm's own body: the fork is what the author wrote, and a body is what lowering rewrites.

A behavior no row names is not warned about, whether or not another behavior's rows run through it. An arm that answers nothing is not an arm and is not warned about (<<an-arm-that-answers-nothing-is-not-an-arm>>); the hint names `+unreachable+` because stating that nothing reaches an arm is what answers this warning where no row can.

What the way through is called comes from the construct as well as from the way. An `+if+` has a `+then+` and an `+else+` to quote; a `+guard+` has the `+else+` the author wrote and, on the other side, the rest of the block, which is named as that rather than as a `+then+` nobody wrote; a guard comprehension has neither word, and its two ways are the element it yields and the empty list it yields instead.

[#e1919]
=== A stand-in and a recorded row answer one input differently

[,text]
----
This row and the `fake findMember` row state different answers for the same input.
Hint: The row says Found { id = "m-1" }; the fake says Missing { why = "no such member" }.
----

A warning, and not one the adequacy dial turns on (<<example-adequacy>>): nothing is counted and no behavior is applied, and what it costs is building the fixtures on both sides and comparing them. Emitted when a `+fake+` row, or a `+with+` for a dependency that takes no input, answers an input for which an `+example+` row records a different answer (<<example-pending>>). Which of the two the model is to be held to is not readable from the text, so neither is named as the right one.

One disagreement is one warning. It is quoted at the recorded row and carries a second region at the stand-in, which names its own file when the two are in different sources (<<example-placement>>). It is reported at both of those files, so an author reading either is told; where the caret sits is where a reader starts, and says nothing about which of the two the model is to be held to.

Where a recorded row names a case and nothing under it (<<example-expected>>), the comparison is on the case, and the case of the other side is the one its value turned out to be — read from the value, resolved to the type this module knows it under. Which case a value is comes from the value and not from the text that produced it, so a fake row answering through a helper states the case that helper returned.

A row whose fixture does not finish is read within its own budget and states nothing; the rows beside it are unaffected, and what happened is said where that row is evaluated (<<e1910>>).

A row whose arity is wrong, whose inputs or expectation will not build, or whose expectation is not a case the behavior answers with, states nothing to compare, and each is said as the error it is (<<e1903>>, <<e1904>>).

[#e1920]
=== A fake and the rows recorded for one behavior were not compared

[,text]
----
Could not compare this fake with the rows recorded for `find` — building the table spent
its budget of 100000000 steps.
----

A warning, quoted where the `+fake+` names the behavior it stands in for. Emitted when reading a `+fake+` table against the rows recorded for that behavior (<<example-pending>>) does not come to a value, so what the two state was never compared. Which of three things stopped it is said: the table spent its counted budget, it reached the recursion-depth limit, or the evaluation stopped answering. The first two are decided by what the table states and read the same on every machine; the third is not a statement about the model at all. Being told nothing was found and being told nothing was compared are different answers, and an empty list of disagreements is what the first one looks like.

[#e1921]
=== A fake's table was not built in time

[,text]
----
Building the `fake find` table spent its budget of 100000000 steps, so nothing it states
was checked.
----

A warning, quoted where the `+fake+` names the behavior it stands in for. Emitted when building the table where it is written (<<example-fakes>>) does not come to a value, with the same three reasons <<e1920>> distinguishes. A warning rather than an error: what stopped the build says the table was not read, not that it is wrong.

[#e1922]
=== An import is never used under the name it was given

[,text]
----
`Employee` is imported but never used under this name.
Hint: Take it off the import list. What another module declares is reachable qualified
whether or not it is imported, so an entry here earns its place only by being written bare
somewhere below.
----

A warning, at the entry on the list rather than at the import as a whole (<<imports-are-used>>). What an import list buys is the bare name; an entry no bare use reads buys nothing and still says the module depends on that name. A qualified reach does not keep an entry: it reads the same whether or not the name is listed (<<imports>>), so a name reached only that way is on the list for nothing. An editor is told the entry is unnecessary rather than merely told about it, since what to do with it is to delete it.

[#e1923]
=== An example evaluation did not answer

[,text]
----
The evaluation of this row did not answer within 60000ms and was given up on.
Hint: This is the compiler failing to decide, not the model failing to terminate: what did
not come back was not counted, so nothing here says the row would not have held. Code this
compile did not generate — a module read from a jar — is the usual reason.
----

Emitted when an evaluation stops answering and the wait the policy allows runs out. Not <<e1910>>: what did not come back was not held to a counted budget, so nothing was shown about whether the model terminates. Counted points are placed in the code this compile generates and in the classes it loads from the module path, so what this reaches is the work the compiler does around them — loading a class, decoding a value, comparing two of them — or the compiler itself.

[#e1924]
=== An example evaluation ran out of stack

[,text]
----
The evaluation of this row ran out of JVM stack (recursion overflowed the stack) before
reaching its recursion-depth limit of 10000.
Hint: The depth limit is what is meant to stop a recursion, and the stack got there first.
Lower the depth limit, give the evaluation a larger stack, or rewrite the helper as tail
recursion so it loops instead of adding frames.
----

Emitted when the JVM stack runs out before the counted recursion-depth limit is reached. Not <<e1910>> either: how many frames a stack holds depends on how large they are, which is decided by the helper and by whether the JVM has compiled it — so this says nothing about whether the recursion terminates. The depth limit and the stack an evaluation is given are set so the counted limit is reached first; this says they are set wrong for this model.

[#e1925]
=== A name is on more than one row of one behavior

[,text]
----
`over the ceiling` names more than one row of `submit`, so nothing outside this file can
say which row it means.
----

Emitted at every row of one behavior carrying a name another of its rows carries, in the file that row is written in (<<a-row-name-is-unique-within-its-behavior>>). Read over the module's own source and the `+examples for+` files attached to it together, since a behavior's rows may be written across them (<<example-placement>>).

Both rows are reported and neither is named as the duplicate. Which source a row is in comes from the order a compile was handed its files, which a build and a command line decide differently; a rule naming one of the two would say different things about one model depending on how it was compiled.

Rows written with no name are not compared with each other: a row without a name is not addressable from outside, so there is nothing for two of them to disagree about (<<example-row-name>>).

[#e1926]
=== A row of a fake's table can never answer

[,text]
----
An earlier row of `fake findMember` states these arguments, so that is what the table
answers with and this row answers nothing.
----

Emitted at a row of a `+fake+` table that the table's dispatch can never return, naming the row it returns instead (<<every-row-of-a-fake-table-can-answer>>). Two rows are written and never reached: one stating what an earlier row states, since the first match is what answers; and a `+_+` row with another `+_+` after it, since the last of them is what a table falls through to. The two point in opposite directions — the earlier explicit row answers, the later `+_+` does — so the row that answers is quoted rather than left to be read off the order.

An error, and not a measurement: which row answers is decided by the table's own rule rather than by anything a run finds out, so a row that cannot answer is refused the way an arm no `+match+` can reach is (<<e1204>>) rather than reported as something a build may carry.

A `+_+` row and an explicit row do not shadow each other, whichever order they are written in: an explicit row answers where it is asked for what it states, and the `+_+` row answers everywhere else. A second table written for one dependency is not this either — the first one written is what answers, and what the second states is said where that is decided (<<example-pending>>).

[#e1927]
=== What answers a behavior is not of the model being evaluated

[,text]
----
What answers `findTodo` was built against another revision of `todo`: `Title` is
declared differently there.
----

Emitted at an `+example+` whose behavior is answered by an implementation supplied from outside the compile, where nothing can establish that what answers it was built against this model (<<an-answer-is-of-the-module-being-evaluated>>). The rows are not run against it: what a row's values are is decided by this model and what reads them is decided by whatever that implementation was compiled against, so a row handed over would be decided by the two disagreeing rather than by the model — and that lands on a comparison, which is read as a statement about the model.

Said once for the behavior in each source its rows are written in, however many rows those are: one answer and one model disagreeing is one fact, and the rows did not stop for reasons of their own. Not once for the behavior outright, because a diagnostic is said where it can be quoted — a behavior whose rows are written in the module and in an attached `+examples for+` file has rows in two files, and a reader of the second one would otherwise be shown rows that stopped and nothing saying why. Each row is recorded as undecided, with its inputs and its expectation already built and held to their invariants.

Two things are reported under this code, and they are not the same finding. Where both sides were read and one declares something else, the declaration is named and the fix is to build again. Where the declarations could not be read at all — classes carrying none, or declarations this compiler cannot read back (<<a-published-module-can-be-read-back>>) — nothing has been established about either build, and saying a build was stale would be saying it on evidence nobody has.

[#e1928]
=== An example row states values the behavior's clause rules out

[,text]
----
The values this row states do not keep what `findTodo` declares of what it answers:
ensures not held on todo.findTodo: asked, answering Todo
----

Emitted at an `+example+` row whose input and stated answer do not keep an `+ensures+` of the behavior it is written for (<<a-row-keeps-what-the-behavior-states>>). The stated answer is the value the row wrote, or the case its arm names where it wrote no value. What decides it is the declaration's own check, applied to the values the row wrote (<<example-ensures>>), so what the message carries is what that check said — the clause that was not kept, the case that clause's rule was written for, and the case the answer turned out to be. An arm may name a case that has cases of its own, and an answer is one of those, so the two are said apart; where they are the same case it is said once.

A refusal and not a warning. A stand-in and a row disagreeing is two descriptions with no right side (<<e1919>>); this is a description held to what the model states, and a row that records an answer the model rules out is a record of something that will not happen.

Said before anything applies the behavior, so a row waiting for an implementation is told the same day it is written. A row whose expectation is a bare case name is held to what that case decides on its own (<<example-ensures>>).

[#e1929]
=== A fake row stands in with values the dependency's clause rules out

[,text]
----
The values this row stands in with do not keep what `lookup` declares of what it
answers: ensures not held on order.lookup: asked, answering Found
----

Emitted at a row of a `+fake+` table whose input and output do not keep an `+ensures+` of the behavior the table stands in for (<<a-fake-row-keeps-what-the-dependency-states>>). The same rule as <<e1928>> at the other place a model writes both sides of a relation down, and told apart from it because what an author does about the two is not the same: one is what a behavior owes, the other what stands in for it while some other behavior's row runs.

A `+_+` row states no input, so there is nothing here to relate its answer to; what it answers is held at the crossing into generated code, like any other answer arriving from outside.

A table with such a row is not one to stand in with, as a table that will not build is not (<<example-fakes>>). A row reaching it stops without a fake and says nothing of its own: running it would put the rest of the behavior in a state the model rules out, and what the row then reported would be about a run that cannot happen.

Neither is it compared with the rows recorded for the behavior (<<e1919>>). That comparison is written for two descriptions with nothing to choose between them, and this is a description held to a declaration — reported together they would say of one pair both that the fake is the side that is wrong and that neither side is.

[#e1930]
=== An implementation answers a row with a value the behavior's clause rules out

[,text]
----
The implementation supplied for `findTodo` answered this row's input with a value that
does not keep what `findTodo` declares of what it answers:
ensures not held on todo.findTodo: asked, answering Todo
----

Emitted at an `+example+` row of an injected behavior, where an implementation supplied to the run answered the row's input with a value that does not keep an `+ensures+` of that behavior (<<an-implementation-keeps-what-the-behavior-states>>). The check that decides it is the emitted one, the same one a crossing into generated code invokes.

Told apart from <<e1928>> by which text is wrong. That one is a row recording something the model rules out; this is code outside the model doing something the model says it will not, and what an author does about the two is not the same.

Only an injected behavior reaches it. A behavior with a body checks its own answer where it answers, so by the time a row sees that answer it has already been held to the declaration, and asking again would be a clause checked twice.

Said before the row's own comparison. What the answer disagrees with here is the model, and a row told only that it expected one value and saw another would send its author to look at the row.

[#e2001]
=== A helper does not carry the termination guarantee it claims

[,text]
----
Recursive helper `let depth` is not structurally recursive: it passes no argument that is
a strictly smaller part of a parameter. Recurse on a part obtained by `match` (a field or
a case), count with `fold`, or mark the helper `partial`.
----

Emitted when a helper's recursion is not shown to terminate by size-change analysis. A group of mutually recursive helpers is reported together, and a recursion too large for the analysis says so instead of guessing; `+partial+` is the opt-out in every case (<<fn-rules>>).

The same code says the two other ways the guarantee fails. One is reaching a declaration that does not carry it, reported at the helper's own name and naming the path — one report per helper, so a module needing the word in several places is told about all of them in one build.

[,text]
----
`let depth` does not carry the termination guarantee it claims: it reaches the `partial`
helper `spin` (depth -> measure -> spin). Mark `depth` `partial` as well, or stop
reaching it.
----

The other is writing a `+partial+` helper where a value goes, which takes it out of reach of the walk above.

[,text]
----
The `partial` helper `spin` is written where a value goes. A function type carries no
termination guarantee, so a `partial` helper may be applied but not handed over.
----

[#e2010]
=== A construction violates its invariant

[,text]
----
Constructing `Amount` here violates its invariant: the value being built is one the
invariant rejects. Fix the value.
----

Emitted when the check proves the invariant false at a construction (<<invariant-discharge>>). What is said of it depends on what proved it. The message above is for a violation the values reach on their own — a value written out, an arithmetic result, a name given one of those, or what an input's own type guarantees of it — which fails wherever the construction is written. An invariant is the conjunction of its clauses, so one clause failing that way is the whole invariant failing that way. Where the values alone do not settle it and what else is known where it stands does, the message says that instead:

[,text]
----
Constructing `Amount` here violates its invariant on a reachable path: under what is
assumed where it stands, the value being built is one the invariant rejects. Fix the
value, or guard the path so the violating case cannot reach here.
----

Neither names what decided it: the check knows the values alone did not, and not which of the things known there did.

The secondary regions identify only the invariant clauses shown to reject the value wherever it is built. A clause the check could settle neither way is a clause the guards did not establish, which is what <<e2011>> reports; it is not presented here as one the value fails.

Where the paths reaching a construction fail different clauses, every path violates the invariant and no clause is one every path fails. The message then names no clause — there is none to name — and the regions identify the clauses a path here fails, saying that of them and not that the value fails them.

[#e2011]
=== A construction is not shown to satisfy its invariant

[,text]
----
model.sou:7:5
7|     invariant charged <= limit
       ^^^^^^^^^^^^^^^^^^^^^^^^^^
The guards do not establish this clause.

Constructing `Amount` here may violate its invariant: the guards do not establish it. Guard
it with `guard`, or reify the relation into `Amount`'s (or an input's) invariant so it is
assumed here.
Hint: If a relation between inputs guarantees this, declare it as an `invariant` on the
input data so it is checked once at its boundary and assumed here.
----

The secondary regions identify the invariant clauses the guards did not establish, at the declarations that write them. Every one of them, in the order they are declared, and however many there are.

What a clause is called and where it is written are separate things a report may carry, and neither is the other's fallback. Of the clauses a report is about, the message names the ones an author wrote a name on (<<invariant-declaration>>), and the regions point at the ones this compile has the source of — which is every clause but those of a module read off the module path, whose declarations arrive as text reassembled from what the module carries. A clause with a name and no source to quote is named and not pointed at; a clause with a source and no name is pointed at and said to be "its invariant". The same holds of <<e2010>>.

Which clauses a report is about is a third thing, and the message and the regions need not be about the same ones: they say different things, and what may truthfully be said of a clause is what decides which of them says it. E2010 where the paths fail different clauses is that case.

A warning, and the one diagnostic in this chapter that says nothing about whether the model is wrong. E2010 is the check having proved the invariant false; this is the check having proved nothing either way, so what was always going to happen still happens — the invariant is tested when the value is built, and a violation aborts there (<<violation-destination>>). A model whose guards do establish the invariant by an argument the check cannot follow is told this too, and is not thereby wrong.

What to do about it is therefore a choice and not a repair. Guarding the construction puts the reason where the check can read it. Reifying the relation — declaring it as an `+invariant+` on an input's own data — moves it to that data's boundary, where it is tested once and assumed everywhere the value reaches afterwards (<<invariant-discharge>>). Leaving it is the third: the run-time test is the guarantee, and the warning says it is still the thing doing the work.

An attempted construction raises no warning. What this reports is a possible abort, and an attempt takes its `+else+` value instead of aborting (<<attempted-construction>>).

[#e2012]
=== An attempted construction that cannot be read

Two codes, for the two ways the form does not read at all (<<attempted-construction>>). E2012 is `+as+` naming something that does not build a value:

[,text]
----
`as` names the value a construction builds, and this is not a construction.
Hint: Write the construction whose invariant decides the branch, or drop the `as` and give
a condition.
----

E2013 is the form reading and having nothing to decide, since a type declaring no invariant refuses no value:

[,text]
----
`Amount` declares no invariant, so an attempted construction of it always succeeds and the
`else` value is never reached.
Hint: Construct `Amount` directly, or give it the invariant that decides the branch.
----

[#e2013]
=== An attempted type declares no invariant

[,text]
----
`Rank` declares no invariant, so an attempted construction of it always succeeds and the `else`
value is never reached.
Hint: Construct `Rank` directly, or give it the invariant that decides the branch.
----

Emitted when the type attempted declares no invariant, its own or one a spread brought in (<<attempt-subject-declares-an-invariant>>). The `+else+` is written to be reached, and here nothing can reach it: the branch is not one, so writing it is an error rather than dead code that reads as a decision.

[#e2014]
=== A per-clause departure and the clauses it answers for

Six codes, for the ways a departure answering one invariant clause at a time does not line up with the clauses the type declares (<<attempt-departures>>, <<invariant-clause-name>>). E2014 answers a clause by a name the type does not declare:

[,text]
----
`Amount` has no invariant clause named `nonNegative`.
Hint: The clauses that can be answered by name are: inYen. A clause is named where it is
declared (`invariant <name> = ...`); one declared without a name is answered by `| _ -> ...`.
----

E2015 leaves a clause that can fail with no arm to reach. A failure reaches one arm, so a clause with none is a failure with nowhere to go:

[,text]
----
These clauses of `Amount` have no arm: inYen. A failure reaches one arm, so every clause
that can fail needs one.
Hint: Answer each of them, or give the `else` one value, which answers any failure.
----

E2016 is the same for the clauses that have no name to be answered by:

[,text]
----
`Amount` has invariant clauses declared without a name, and nothing here answers them.
Hint: Add `| _ -> ...` for them, or name them where they are declared.
----

E2017 is the other direction — the arm that answers the unnamed clauses where there are none:

[,text]
----
Every invariant clause of `Amount` is named and answered, so `| _ -> ...` answers nothing.
Hint: Drop it. It would come back the moment `Amount` declares a clause without a name.
----

E2018 is arms with nothing to answer for, the departure having no attempted construction above it:

[,text]
----
These arms answer one invariant clause each, and there is no attempted construction here
for them to answer for.
Hint: Attempt the construction (`guard T(v) as x else ...`), or give the `else` one value.
----

E2019 is one clause answered twice:

[,text]
----
The clause `inYen` is answered twice. An arm is chosen by the clause that failed, so a
second arm for it could never be reached.
----

[#e2015]
=== A clause that can fail has no arm

[,text]
----
These clauses of `Amount` have no arm: inYen. A failure reaches one arm, so every clause that can
fail needs one.
Hint: Answer each of them, or give the `else` one value, which answers any failure.
----

Emitted when a named clause of the attempted type has no arm answering it (<<attempt-answers-every-failing-clause>>). All the unanswered clauses are named in one report: which of them a build meets first is not a property of the text, and telling an author about one at a time would make adding a clause a sequence of compiles.

[#e2016]
=== Unnamed clauses are left unanswered

[,text]
----
`Amount` has invariant clauses declared without a name, and nothing here answers them.
Hint: Add `| _ -> ...` for them, or name them where they are declared.
----

Emitted when the attempted type has a clause carrying no name and the arms do not write `+| _ -> …+` (<<attempt-answers-unnamed-clauses>>). A clause with no name cannot be answered by name, so `+| _+` is the only arm that can hold it; the other move is to name the clause where it is declared (<<invariant-clause-name>>).

[#e2017]
=== A catch-all arm has nothing to answer

[,text]
----
Every invariant clause of `Amount` is named and answered, so `| _ -> ...` answers nothing.
Hint: Drop it. It would come back the moment `Amount` declares a clause without a name.
----

Emitted when `+| _ -> …+` is written and the type has no clause carrying a name (<<attempt-catch-all-has-clauses-to-answer>>). It is worth saying rather than allowing as harmless: an arm that answers nothing reads as cover the mapping does not have, and the day a clause is added without a name it would silently start carrying it.

[#e2018]
=== Arms answer for no attempted construction

[,text]
----
These arms answer one invariant clause each, and there is no attempted construction here for them to
answer for.
Hint: Attempt the construction (`guard T(v) as x else ...`), or give the `else` one value.
----

Emitted when per-clause arms are written on a condition that is not an attempted construction (<<attempt-arms-answer-an-attempt>>). What an arm names is a clause of the type being built, and where nothing is being built there are no clauses for them to name.

[#e2019]
=== A clause is answered twice

[,text]
----
The clause `inYen` is answered twice. An arm is chosen by the clause that failed, so a second arm for
it could never be reached.
----

Emitted when two arms name the same clause (<<attempt-answers-each-clause-once>>). Which arm is taken is decided by the clause that failed and not by the order the arms are written in (<<attempt-departures>>), so the second one is unreachable rather than a fallback.

[#e2101]
=== A generated method exceeds the JVM parameter-slot limit

[,text]
----
`data Wide` needs 256 JVM parameter slots, but the constructor generated for it takes at
most 254. An `Int` field is carried as a `long` and takes two slots; every other field
takes one.
Hint: Split `Wide` into smaller data and hold them as fields.
----

A JVM method signature holds 255 argument slots, and an instance method spends one of them on the receiver. A declaration is emitted as methods that take what it declares — a `+data+`'s fields become the constructor's arguments, a `+behavior+`'s parameters become `+apply+`'s, its dependencies become its implementation constructor's, and a recursive helper's parameters become a static method's — so past a certain width the class cannot be loaded at all. This is decided before anything is emitted, at the declaration.

What the width is depends on which of them it is, because the emitted form is not the same. A `+data+`'s fields keep their runtime types, so an `+Int+` field takes two slots and every other field one, and the limit falls at 254 slots. A `+behavior+`'s parameters and dependencies are references whatever they were declared as, so each takes one and the limit falls at 254 of them. A recursive helper is emitted as a static method, which spends nothing on a receiver, so its limit is 255 parameters.

[#e2102]
=== A generated method exceeds the JVM code-size limit

[,text]
----
The `apply` method generated for `f` is 88025 bytes of code, but a JVM method holds at
most 65535.
Hint: Split the work into another behavior, which is emitted as a class of its own, or
move the table out of the source. Naming parts with `let` does not divide it: a
non-recursive helper is inlined before it is emitted.
----

How much code a definition becomes is not known until it is written, so unlike E2101 this is not decided at the declaration; it is reported as the definition that was being emitted when the class file writer refused. The message names the generated method, because one declaration becomes several — a `+data+` is emitted as a value class, a decoder and an encoder, and a `+behavior+` as an implementation and an interface — and which of them grew is not readable from the declaration.

[#e2103]
=== A generated class exceeds the JVM constant-pool limit

[,text]
----
The class generated for `f` refers to constant-pool entry 80031, and a class file
addresses at most 65535. The pool holds every name, type and literal the class refers to.
Hint: Move the table out of the source, or split the work into another behavior, whose
constants belong to a class of its own.
----

As E2102, for the other thing a class file runs out of. The pool belongs to the class rather than to any one method of it, so this is reported as the definition the class was generated for and names no method.

The pool is run out of two ways and the message says which: referring to an entry the class file cannot address, which names the entry that was wanted, and holding more entries than it can write out, which names how many there are. They are one code because the author's move is the same.

[#e2104]
=== A source nests deeper than the compiler reads

[,text]
----
This nests deeper than I read. Break it into parts that have names, so each one is read on its own
and says what it is: a binding for a value, a declaration for a type. What those parts add up to is
bounded too, and far above this.
----

Reported where reading a source descends past the depth the compiler holds itself to (<<source-nesting-is-bounded>>). It is reached while reading, so it is reported wherever the reading is done, including by the formatter, which reads sources it cannot compile, and at the token that reached it.

Naming the parts is the answer to this one, and what the parts add up to is a separate limit — <<e2107>>, which is far above this one and counted differently. What a pass builds from a source may be deeper than the source was; this limit says nothing about that.

[#e2107]
=== A definition is more complex than the compiler holds

[,text]
----
This block takes {steps} structural steps, and a definition holds at most {holds}. Everything a
statement is followed by is written inside what that statement introduced, so each step is a level —
a `guard` binds nothing and is one all the same.
----

[,text]
----
`let {definition}` is {costs} levels of structure, and a definition holds at most {holds}.
----

[,text]
----
Substituting `{value}` here takes this past {holds} levels of structure, which is what a definition
holds. A value is substituted where it is named, so what it is worth is added to what names it.
----

Reported where a definition says more than the compiler holds (<<source-structural-complexity-is-bounded>>), at whichever construct exceeded it: the block, the definition as written, or the value whose substitution composed it. The three are one limit counted one way, and which of them is reported is where the count first passed it.

Naming parts with `+let+` is not the answer to this one — it is what a block and a substitution are made of, and both are counted. What reduces it is writing less structure: a behavior of its own, which is a definition of its own and counted on its own.

[#e2108]
=== The compiler ran out of room

[,text]
----
I ran out of room working on this. What a source may nest and what a definition may say are both
bounded, and a source past either is refused with the place it happened, so this is not a report
about the source: on the stack this compiler is supported on, something it built went deeper than
anything it accepted should. Either the stack it was given is smaller than that, or this is a shape
the bounds do not hold yet.
----

Reported where a walk exhausted the stack (<<running-out-of-room-is-reported>>). It carries no position and asks for nothing: a source within <<source-nesting-is-bounded>> and <<source-structural-complexity-is-bounded>>, read on the runtime the compiler states, does not reach it.

[#e2105]
=== Two declarations become one class

[,text]
----
behaviors `bill` and `Bill` both capitalize to the same JVM class `Bill`; rename one so their class
names differ.
----

Reported at the second of the two (<<no-two-declarations-become-one-class>>). A declaration reaches a class name three ways — a behavior capitalizes (<<jvm-behavior>>), a union output becomes a sealed interface (<<jvm-sum>>), a foreign union member becomes a bridge case (<<bridge-case>>) — and a model may already have declared something of that name. Names are not reserved ahead of time: one nothing collides with stays available.

[#e2106]
=== A field is named after a method of `Object`

[,text]
----
`hashCode` cannot be a field of `Member`: the generated class reads its fields through accessors of
the same name, and `hashCode()` is already a method every class has.
----

Reported at the field (<<a-field-does-not-collide-with-an-object-method>>). `+equals+` is not among them: `+equals()+` and `+equals(Object)+` are different methods, and Java allows it as a component name.

[#e2201]
=== A written codec does not agree with its type

[,text]
----
The decoder for `Amount` must construct `Amount`, but constructs `Level`.
----

Reported where a hand-written codec is not a codec for the type it is declared on (<<a-custom-codec-agrees-with-its-type>>): a decoder constructing something else, an encoder missing a case of its sum, an element encoder against a different element type, a source-shaped encoder given another shape. A derived codec agrees because it was read off the type; a written one is held to the same agreement.

[#e2202]
=== A codec is reached for where there is none

[,text]
----
`Wide` has no decoder, so `Wide.decoder` cannot be called.
----

Reported at the reference (<<a-codec-reached-for-exists>>). Derivation covers what has an external representation (<<external-representation>>), and where it did not run and nothing was written by hand there is nothing to name.

[#e2301]
=== A declaration is not written in its form

[,text]
----
A behavior signature uses `:` before its parameters: `behavior submit : (...) -> ...`.
----

Reported where a declaration does not read (<<declaration-syntax>>): the module header, a top-level `+data+`, `+behavior+` or `+let+`, a behavior's signature, a `+depends on+` clause. The message names the form that was expected rather than the token that was found, since what a reader needs is the shape and not the position.

[#e2302]
=== An expression is not written in its form

[,text]
----
The right side of `|>` must be a function call or a function name.
----

Reported where an expression does not read (<<expression-syntax>>), including a block that ends in no expression: a block's value is its last expression, and one holding only `+let+` and `+guard+` lines states nothing.

[#e2303]
=== A pattern is not written in its form

[,text]
----
A case value is bound with `as`: write `| Submitted as s`, not `| Submitted s`. Only `Some` carries
a payload to bind positionally.
----

Reported where a pattern does not read (<<pattern-syntax>>). The forms differ by what is being destructured — a case binds with `+as+`, an optional binds its payload positionally, a record destructures its fields directly — so the message names the form for the thing at hand.

[#e2304]
=== An example is not written in its form

[,text]
----
An `example` needs at least one `|` row.
----

Reported where an `+example+`, a `+fake+` or an `+examples for+` file does not read (<<examples-syntax>>, <<examples>>).

[#e2305]
=== A literal is not written in its form

[,text]
----
A fractional literal is a Decimal and needs the `m` suffix (write `1.5m`); Souther has no
floating-point type.
----

Reported where a literal is not written in the form its type takes (<<literal-syntax>>). Most of it is answered while the text is read into tokens, before anything is parsed: a string not closed before the end of the line, a backslash written before something that is not an escape (<<string-literal>>), a fractional literal with no `+m+`. One is answered after, where a literal was expected and what stands there cannot be one.

[#e2306]
=== The text is not made of tokens

[,text]
----
Unexpected character `@`.
----

Reported while reading the text into tokens (<<the-source-is-made-of-tokens>>), before anything is parsed. Unlike the syntax codes above it says nothing about where in the language the text was, nothing having been read yet: a character that begins nothing, and a `+'+` with no name after it, are the two ways to reach it.

[#e2307]
=== An anonymous union is written in a narrow type position

[,text]
----
Field `ticket` is written with an anonymous union, which is not a form a field's type is written
in. Declare a `data` over the cases and give the field that name.
----

Reported where a `+|+` follows the type of a data field, a type argument or a tuple's member (<<an-anonymous-union-is-not-written-in-a-narrow-type-position>>). None of the three reads the form, so it is recognized at the `+|+` rather than left to the delimiter the reading stopped at — the author reached for a choice, and a report about a missing `+}+` is not about what they wrote. A behavior's parameter is the other position that refuses one, and it reaches <<e1312>>: it reads the form and the boundary refuses what was read (<<a-parameter-names-one-type>>). Everywhere else a whole type is read an anonymous union stands, a helper's signature included (<<union-intersection>>).

[#e2308]
=== An optional is written inside another type

[,text]
----
`?` marks a data field's own optional and is not written inside another type. Write `Option<T>`
for the type, or put the `?` on the field that holds it.
----

Reported where a `+?+` follows a type argument or a tuple's member (<<an-optional-is-not-written-inside-another-type>>). Distinct from <<e1402>>, which refuses a `+?+` a user model writes outside a field and which the core may write: this position takes none from anyone, so `+List<T?>+` is refused in the shipped core too.

[#example]
== Example: the business-trip application model

An example transcribing the spec DSL (`+chapter10/types-and-states.dsl+` and others) to Souther. Each definition is annotated with its corresponding spec DSL in a comment.

[,text]
----
module example.businesstrip exposing (
    TravelRequest,
    submitTravelRequest,
    preApprove
)

// --- value types (a simple value + a constraint comment in the spec DSL → invariant) ---
// data Amount = Integer // at least 0, in yen
data Amount = Int
    invariant value >= 0

// data EmployeeId = String // not empty
// newtype (the data X = Y form). The derived Decoder reads from bare text and checks the invariant.
// Normalization (trim, etc.), if needed, is done in a boundary-side custom Decoder.
data EmployeeId = String
    invariant String.length(value) > 0

// --- a nested sum with unit cases and a newtype ---
// data Rank = Manager OR Staff / data Manager = Level / data Level = Integer // 1..5
data Rank = Manager | Staff
data Level = Int
    invariant value >= 1 && value <= 5
data Manager = Level       // a newtype over Level
data Staff

// data CostBearer = OwnCost OR ClientCost / data OwnCost = Reimbursement OR Advance OR CorporateCard
data CostBearer = OwnCost | ClientCost
data OwnCost     = Reimbursement | Advance | CorporateCard
data Reimbursement
data Advance
data CorporateCard
data ClientCost

// --- product (role name ≠ type name) ---
// data Employee = EmployeeId AND Rank AND ManagerId
data Employee =
    { id: EmployeeId
    , rank: Rank
    , managerId: EmployeeId
    }

data TravelRequestCommon =
    { applicant: Employee
    , estimatedCost: Amount
    , destination: String
    , purpose: String
    , startsOn: Date
    , endsOn: Date
    }

// --- sum of states (case = named data, common items taken in flatly by a ... spread) ---
// data TravelRequest = Drafting OR Submitted OR AwaitingPreApproval OR PreApproved
// The derived Decoder reads the discriminator "type", dispatching by tag = case name ("Drafting", etc.).
// To read by a DB "status" or an English tag ("draft", etc.), use a boundary-side custom Decoder.
data TravelRequest =
    Drafting | Submitted | AwaitingPreApproval | PreApproved

data Drafting = { ...TravelRequestCommon }
data Submitted = { ...TravelRequestCommon, submittedAt: DateTime }

data PreApprovalReason = HighValueTravel | InsufficientAuthority | ClientCost
data HighValueTravel = { threshold: Amount }
// data InsufficientAuthority = Rank (a newtype)
data InsufficientAuthority = Rank
data ClientCost

data AwaitingPreApproval =
    { ...TravelRequestCommon
    , submittedAt: DateTime
    , preApprovalReasons: List<PreApprovalReason>
    }

data PreApproved =
    { ...TravelRequestCommon
    , submittedAt: DateTime
    , preApprovedAt: DateTime
    , preApprover: EmployeeId
    }

// NotAuthorized is just data (a failure is ordinary data; no error declaration)
data NotAuthorized

// --- behavior specifications (the spec DSL's behaviors line up as is) ---

// depends: clock → no implementation. Injected from Java
behavior now : () -> DateTime

// behavior decidePreApproval = EstimatedCost AND Applicant AND CostBearer -> PreApprovalReasons
behavior decidePreApproval : (
    estimatedCost: Amount,
    applicant:     Employee,
    costBearer:    CostBearer
) -> List<PreApprovalReason>
    constructs HighValueTravel, InsufficientAuthority

// behavior submitTravelRequest = Drafting -> Submitted OR AwaitingPreApproval (both success → bare sum)
behavior submitTravelRequest : (request: Drafting) -> Submitted | AwaitingPreApproval
    constructs Submitted, AwaitingPreApproval

// behavior preApprove = AwaitingPreApproval AND ApproverId -> PreApproved OR NotAuthorized (a failure is a case too)
// depends: clock → write it in depends on
behavior preApprove : (
    request:    AwaitingPreApproval,
    approverId: EmployeeId
) -> PreApproved | NotAuthorized
    depends on now
    constructs PreApproved

// --- implementations ---

let decidePreApproval (estimatedCost, applicant, costBearer) = { ... }

let submitTravelRequest (request) = { ... }

// precondition: approverId equals the applicant's managerId
// depends on now appears as the last argument
let preApprove (request, approverId, now) = {
    guard approverId == request.applicant.managerId
        else NotAuthorized

    PreApproved { ...request, preApprovedAt = now(), preApprover = approverId }
}
----

The type of `+preApprove+`:

[,text]
----
preApprove :
    Behavior<{now}, (AwaitingPreApproval, EmployeeId), PreApproved | NotAuthorized>
----

All three of `+Behavior+` come from the declaration: `+{now}+` is `+depends on+`, the input is the argument list, the output is the right of `+->+`. The `+let+` has 3 arguments because it is pre-injection; when `+PreApprove.bind(clock)+` fixes the last argument, it becomes the declared 2-argument type (<<depends-on>>).

`+...request+` copies the common items and `+submittedAt+` of `+AwaitingPreApproval+` by name, ignores `+preApprovalReasons+` (not in `+PreApproved+`), and adds `+preApprovedAt+` and `+preApprover+` to construct.

[#java-example]
== Java-side implementation example

[,java]
----
// extends the abstract base FindMember generated for a behavior with no implementation (not implements).
public final class JdbcFindMember extends FindMember {
    private final DataSource dataSource;

    public JdbcFindMember(DataSource dataSource) {
        this.dataSource = dataSource;
    }

    @Override
    public FindMemberResult apply(MemberId id) {   // the generated sealed union: Member | NoSuchMember | CorruptStoredData
        try (var connection = dataSource.getConnection();
             var statement = connection.prepareStatement(
                 "select id, email, display_name from member where id = ?")) {

            // value extraction goes through the encoder. MemberId is a newtype, so encode is a bare String.
            statement.setString(1, MemberId.encoder().encode(id));

            try (var resultSet = statement.executeQuery()) {
                if (!resultSet.next()) {
                    return NoSuchMember();                            // inherited protected factory
                }
                var raw = Map.<String, Object>of(
                    "id",   resultSet.getString("id"),
                    "Mail", resultSet.getString("email"),
                    "displayName", resultSet.getString("display_name"));

                // decode is a Raoh Result<Member>. An invariant violation maps to the CorruptStoredData case
                return switch (Member.decoder().decode(raw, Path.ROOT)) {
                    case Ok<Member> ok   -> ok.value();              // Member (mainline)
                    case Err<Member> err -> CorruptStoredData();          // inherited protected factory
                };
            }
        } catch (SQLException e) {
            // A platform failure is not folded into a case but thrown. Souther passes it through; the boundary handles it (503, etc.).
            // SQLException is checked, so wrap it in a non-checked exception of the DataAccessException family the boundary catches
            // (not IllegalStateException or a bare RuntimeException — those pass through the handler and make infrastructure failure look like a bug).
            throw new DataAccessResourceFailureException("member lookup failed", e);
        }
    }
}
----

Binding to a composition behavior:

[,java]
----
var handle = Handle.bind(new JdbcFindMember(dataSource));
var result = handle.apply(rawInput);
----

[#future-work]
== Future extensions

The language core is implemented. The following are future extensions — distinct from what is permanently absent (<<out-of-scope>>).

* A full formal grammar (BNF). The key delimiters and precedence are stated ahead in <<delimiters>>, and where each written form is given is <<grammar>>.
* Incremental compilation.

[#non-functional]
== Non-functional requirements

* compile errors are rendered from structured data — a code, a source region, a message, and hints; the human form quotes the source with a caret, a JSON form (`+--format json+`) is available for tools, and messages are localized, the language being selected as <<compile-errors>> states
* the generated `+.class+` passes standard verification and is usable straightforwardly from the Java version <<target-jdk>> names
* each compiler stage is independently testable
* nothing in souther-runtime calls Raoh; the dependency is the generated code's, and souther-runtime declares it so that a project holding generated code resolves it without naming it (<<codec-generation>>)
* the language core does not depend on the Java class model
* the requirement set is tracked statically; how it is reconciled and inferred is <<requirement-propagation>>
* generated code exposes no unvalidated value, on the terms <<jvm-construction-privacy>> states
* one spec DSL definition maps to one Souther definition with no intervening translation

[#core-semantics]
== Core semantics

Souther tracks two things about a data.

[,text]
----
which values are that data
which expressions have the authority to construct that data
----

It tracks three things about a behavior.

[,text]
----
input type   output type (sum of possible business results)   the set of required dependencies
----

A behavior conceptually has the type:

[,text]
----
Behavior<Requirements, Input, Output>
----

There is no fourth slot separating failure. A failure is one of the cases of Output, and whether it departs the mainline is decided by composition. `+>->+` simultaneously performs output-case routing (flowing cases the downstream consumes, propagating the rest) and requirement-set composition.

[,text]
----
f : Behavior<R1, A, O_f>
g : Behavior<R2, B, O_g>
f >-> g : Behavior<R1 ∪ R2, A, (O_g) | (sum of O_f minus the consumed part)>
----

This rule is Souther's central composition semantics. Output corresponds to the spec DSL's `+-> Out OR ...+`, and the requirement set to `+// depends+` / `+// side effect+`. What the author states explicitly is input, output (a bare sum), dependencies (`+depends on+`), and construction authority (`+constructs+`). On the same line as the spec DSL writing `+// depends:+` at a leaf but not on a composition, only a composition is left to inference (<<depends-on>>, <<composition-with-requirements>>).

`+Requirements+` is an argument fixed by injection, not a dynamically handled effect. So on the `+let+` side it appears as an ordinary argument, and `+bind+` partially applies it (<<depends-on>>). `+Behavior<R, I, O>+` can be read as `+(I, R) -> O+` closed over `+R+`. That is why no variable enters the requirement set, and that keeps the whole system simple.

Souther maps a specification model to an implementation model with minimal translation by promoting the value constraints, construction paths, and outside-world dependencies the spec DSL left in comments to invariants, confinement of construction paths, and `+depends on+` with injection.
