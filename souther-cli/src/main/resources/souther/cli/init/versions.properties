# Filled in at build time from this build's own properties. What a project `souther init` writes
# declares, so that the numbers it hands a reader and the numbers this compiler was built and tested
# against are one set rather than two that agree by hand.
junit.version=${junit.version}
surefire.version=${surefire.version}
